根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

本次审查（v3 诊断报告）共识别 11 项质量问题（4 严重 + 5 一般 + 2 轻微），不重复前序 19 项内部审议已闭环问题。审查定位侧重于内部审议未充分覆盖的维度：需求响应充分度、整体深度和完整性、事实准确性、逻辑一致性。审查方法为直接查阅仓颉 stdlib 文档、GLM 1.0.3 实际源码、阶段二实际实现和 docs/deviations.md，不依赖文档自证。

### 严重问题（事实错误/直接阻塞编码）

**问题 1（严重）**：`gtc/constants` 函数数量自相矛盾——文字计数（25 个）与 §3.12 函数名清单（28 个）及 GLM 1.0.3 实际声明数（28 个）不一致，文字与清单相差 3 个。
- 所在位置：§3.12 末段、§1 核心抽象表、§2 包组织、§7 D10、§8 产出物清单、§8.2 测试设计表、§10 覆盖矩阵等所有"25"出现位置
- 改进建议：将所有"25"修订为"28"（与 GLM 1.0.3 实际声明数一致），并核对 `gtc/constants_test.cj` 至少 28 个测试用例的覆盖目标。同步核验是否设计有意省略某些常量，若是，需明确说明省略原因。

**问题 2（严重）**：`axis` 函数 T(1) 获取方式描述与项目系统性约束矛盾——签名 `axis(x: Quat<T,Q>): Vec3<T,Q>` 无 `one: T` 参数，但实现公式需要 T(1) 两处（`tmp1 = T(1) - x.w*x.w` 与 `tmp2 = T(1) / sqrt(tmp1)`），且 §1 明确"在 `Number<T>` 约束上下文中不存在通用的演算路径"；设计给出的"通过 `x.w*x.w` 演算"解释错误（`x.w*x.w` 是 w² 而非 1）。
- 所在位置：§3.9 `axis` 函数描述（约第 443 行）
- 改进建议：二选一：
  - (A) 将 `axis` 签名修订为 `axis(x: Quat<T,Q>, one: T): Vec3<T,Q>`，与 `identity(one: T)` 风格一致，公式 `T(1)` 替换为 `one`；
  - (B) 显式说明 T(1) 的替代获取路径（如 `T(Float64(1))` 转换），并在 §8 编码启动前验证项新增对应验证项。
  - 同时删除/修正"通过 `x.w*x.w` 取得"的错误描述。

**问题 3（严重）**：`fromMat4` 降维策略未明确，与阶段二 `Mat3x3.fromMat4(m, one: T)` 模式不一致——设计给出"或"的选择（手动提取 vs GLM 策略转换），但两种方式语义不同（手动提取不需要 `one` 参数；Mat3x3 构造函数依赖阶段二未提供的 `Mat3x3<T,Q>(Mat4x4<T,Q>)` 构造重载），下游无法决策。
- 所在位置：§3.3 item 7（约第 308 行）
- 改进建议：明确选择一种策略。若选手动提取，需说明实现路径（参考 `cjglm/src/detail/matrix.cj:22` 的 `transpose` 模式）；若选 Mat3x3 构造，需先在阶段二 `Mat3x3` 扩展 `fromMat4x4(m: Mat4x4<T, SrcQ>): Mat3x3<T, Q>` 工厂函数。删除"或"的表述。同时核对 `fromMat3` 是否也需类似调整。

**问题 4（严重）**：`Mat2x2/FMat2x2` 双别名机制引用错误——阶段二 `cjglm/src/fwd.cj` 实际仅存在 `Mat2x2` (Float32) + `DMat2x2` (Float64) 双别名，不存在 `FMat*` 任何声明；`fwd.cj` 中唯一带 `F` 前缀的别名是 Vec 家族（`FVec1`~`FVec4`、`F32Vec1`~`F32Vec4`）。设计错误地套用 Vec 家族的 `FVec*` 模式到 Mat 家族作为先例。
- 所在位置：§3.14（约第 539 行 `FQuat` 段落）、§7 D27、§9 差异声明对应条目
- 改进建议：修订 §3.14 先例引用为 Vec 家族的三重模式（`Vec2` + `DVec2` + `FVec2`，见 `fwd.cj:106, 270, 275`）；修订 D27 决策依据；同步修订 §9 差异声明对应条目；明确阶段三 `Quat` 家族决策理由（是否需要 `FQuat` 双重别名）。

### 一般问题（关键遗漏/可读性不足/影响可操作性）

**问题 5（一般）**：测试文件命名约定与现有项目不一致——设计 13 个测试文件均使用 `xxx_test.cj` 命名，但阶段二 `cjglm/tests/glm/detail/` 25 个测试文件全部使用 `test_xxx.cj` 命名。
- 所在位置：§8.2 测试设计表
- 改进建议：将 13 个测试文件名统一调整为 `test_xxx.cj` 风格（`test_type_quat.cj`、`test_type_quat_cast.cj`、`test_vector_relational.cj` 等），与现有约定对齐。

**问题 6（一般）**：`trigonometric.cj` 函数清单未区分标量/向量重载——设计列出 15 个函数但未说明标量/向量重载形式，下游只声明向量版本会导致 `mix`/`slerp`/`roll`/`pitch`/`yaw`/`pow`/`log` 等的 `acos(T)`/`sin(T)`/`atan(T)` 标量调用编译失败。
- 所在位置：§3.13 表（约第 503-509 行）
- 改进建议：在 §3.13 表中明确 `trigonometric.cj` 应提供 30 个函数（15 标量 + 15 向量），并补充 `atan` 的双参数标量版本（`atan2(T, T): T`）。或新增 §3.13.1 子节明确"标量版本用于 `slerp`/`mix`/`pow` 等内部分量运算；向量版本用于后续阶段四的 vec 级运算"。

**问题 7（一般）**：`axis` 函数 Float32 sqrt 处理策略缺失——`std.math.sqrt` 仅支持 Float64 输入/输出，T 为 Float32 时 `sqrt(tmp1)` 编译失败；设计仅在 `length` 一处明确该问题，未将其作为一般性约束贯彻到所有 `std.math` 函数调用。
- 所在位置：§3.9 `axis` 函数描述
- 改进建议：在 §1「系统性设计约束」中新增"Float32 与 std.math 的交互约束"段落，统一说明 `std.math` 所有函数（sqrt/pow/log/exp/sin/cos/asin/acos/atan）仅支持 Float64，Float32 实例化时需显式 `T(Float64.xxx(Float64(...)))` 转换，或额外声明 Float32 重载；将 `axis`/`length`/`angleAxis`/`exp`/`log`/`pow`/`sqrt` 等所有使用 `std.math` 的函数均引用该一般性约束。

**问题 8（一般）**：`fromVec3` 工厂函数边界行为契约缺失——设计标记为 stub 占位但未说明 u, v 反平行等特殊场景的行为契约；GLM 1.0.3 实际有 180° 任意垂直轴退化路径（`type_quat.inl:196-217`）。
- 所在位置：§3.3 item 8、§5 错误处理策略、§5.3 边界条件表
- 改进建议：在 §3.3 item 8 补充"边界场景：u, v 反平行时返回 180° 任意轴四元数（GLM 退化路径）"契约说明；在 §5.3 边界条件表新增对应行；§11.5 函数可用性对照表新增对应条目。

**问题 9（一般）**：`mat3Cast`/`mat4Cast` 接受非旋转矩阵的边界行为契约未与 §3.3 工厂函数对齐——§3.3 item 6 已对 `Quat.fromMat3` 明确边界声明，但 §3.2.1 中 `mat3Cast`/`mat4Cast` 描述未同步；`mat3Cast` 接受非单位四元数时返回矩阵行为未定义。
- 所在位置：§3.2.1（type_quat_cast 函数签名规范段）、§5 错误处理策略
- 改进建议：在 §3.2.1 签名模板后新增"边界行为契约"段，明确 `mat3Cast`/`mat4Cast` 接受非单位四元数时返回矩阵不保证是旋转矩阵（缩放/剪切分量保留）；`quatCast` 接受非旋转矩阵时返回的四元数行为未定义。同步在 §5.3 边界条件表新增对应行。

### 轻微问题（表述/可读性）

**问题 10（轻微）**：测试文件 `gtc/quaternion_test.cj` 用例数与 gtc 命名空间 API 数量比例失衡——20 个用例分摊到 8 个完整实现函数仅 2.5 个/函数，低于其他测试文件。
- 所在位置：§8.2 测试设计表 `gtc/quaternion_test.cj` 行
- 改进建议：在 §8.2 测试设计补充"用例分配原则"段（完整实现函数每函数 ≥2 用例，stub 函数每函数 ≥1 用例，重导出函数每函数 ≥1 用例验证可达性），并按此原则重算 `gtc/quaternion_test.cj` 用例数（如 8×2 + 7×1 + 4×1 = 27 个）。

**问题 11（轻微）**：§11.5 函数可用性对照表 `isnan`/`isinf` 行未与 §3.11 `where` 子句约束保持一致——未明确具体约束的 where 子句形式，下游迁移者可能不理解为何整型 T 编译失败。
- 所在位置：§11.5 函数可用性对照表 `isnan`/`isinf` 行（约第 1121 行）、`mat3_cast`/`mat4_cast`/`quat_cast` 行（约第 1130-1131 行）
- 改进建议：在 §11.5 对应行末尾追加约束标注，如"约束：`where T <: FloatingPoint<T>`（D29/D32）"，便于下游快速理解。

### 未在本轮发现但建议下一轮关注的事项

1. §1 系统性约束中 T(0) 通过 `someValue - someValue` 演算在 `Bool` 实例化时 `-` 运算符不可用，`Quat<Bool, PackedHighp>` 实例化时依赖 T(0) 演算的函数将无法编译——`identity` 是否包含在排除范围未明确。
2. §3.11 `slerp` 4 参数版本 `k: Int64` 简化签名牺牲了泛型灵活性，但对阶段三 stub 状态无实际影响。
3. §4.1 单位四元数 `identity` 调用示例：`let q = Quat<Float32, PackedHighp>.identity(1.0f)` 在 extend 块中实现时不是 `const`，不能用于 `const val q = ...` 上下文。
4. §10 覆盖矩阵 `quaternion_exponential.hpp` 表 `sqrt(Quat)` 行描述"本阶段 stub"，但 §3.10 描述 `sqrt` 实现为 `pow(x, 0.5)`——若 `pow` 本身为 stub，则 `sqrt` 是否独立声明函数体未明确。

## 历史迭代回顾

本轮迭代已积累 3 轮历史反馈（迭代第 1 轮 13 项 + 迭代第 2 轮 14 项 + 迭代第 3 轮 11 项 = 38 项问题）。质询报告已确认本轮诊断结论为 LOCATED（已确认问题属实）。回顾历史：

### 已解决的问题（出现在历史反馈但当前反馈中不再提及）

- **迭代第 1 轮全部 13 项问题**：gtc/quaternion.cj 文件归属（已通过 v3 决策下沉到 type_quat_cast.cj）、Quat×Vec3 旋转公式错误（已修订为两次 Vec3 叉乘）、mat3_cast/mat4_cast/quat_cast 文件归属矛盾（已解决）、测试文件设计缺失（已新增 §8.2）、gtc/quaternion.cj 依赖图缺失（已补充）、eulerAngles/roll/pitch/yaw 状态矛盾（已统一为 stub）、pow 依赖不完整（已修订）、mix/slerp 依赖遗漏 epsilon<T>()（已补充）、slerp 4 参数 k 类型未定义（已固定为 Int64）、Quat×Vec3/Vec4 备注列空（已补充依赖说明）、cross 命名歧义（已新增约定说明）、边界条件表缺失（已新增 §5.3）、epsilon<T>() 约束（已收紧为 FloatingPoint<T>）。

- **迭代第 2 轮全部 14 项问题**：axis() 边界行为契约与 GLM 实现不符（已修订为"tmp1 <= 0 返回 Vec3(0,0,1)"并删除虚构 normalize 调用）、pow 依赖不完整（已补充 cos_one_over_two<T>()、asin、std.math.pow 递归等）、log 依赖遗漏（已补充 epsilon<T>()、pi<T>()、infinity 等价物）、exp 依赖遗漏 epsilon<T>()（已补充）、slerp 4 参数依赖遗漏 pi<T>() 与 mix（已补充）、axis() 描述自相矛盾（已统一）、路线图与 v3 设计不一致（已新增 §3.16 路线图同步段落与 §11.5 权威基准）、gtc/quaternion.cj 表格欧拉角函数矛盾（已统一为 stub）、conjugate 描述错误（已修订为 w 不变、x/y/z 取反）、type_quat_cast 签名未定义（已新增 §3.2.1）、lib.cj/fwd.cj 具体更新内容缺失（已明确 import/alias 清单）、pow 命名消歧（已补充）、mix 依赖重载版本未明确（已明确 acos/sin 单参数版本）、@Derive[Hashable] 实践依据缺失（已补充阶段二文件引用）。

### 持续存在的问题（在多轮反馈中反复出现，需重点解决）

- **`axis` 函数相关问题**（迭代第 2 轮问题 1、6 + 迭代第 3 轮问题 2、7）：第 2 轮聚焦"零四元数返回值与 GLM 不符"已修订；第 3 轮聚焦"T(1) 获取方式与 §1 系统性约束矛盾"+"Float32 sqrt 处理策略缺失"为新维度。需注意：本轮 §3.9 的修订不应回退第 2 轮已闭环的零四元数保护（`tmp1 <= T(0)` 返回 `Vec3(0, 0, T(1))`），需在解决 T(1) 获取问题的同时保留该行为。
- **`pow` 函数依赖完整性**（迭代第 1 轮问题 7 + 迭代第 2 轮问题 2 + 迭代第 3 轮未直接涉及）：前两轮已逐步补齐依赖，但每次审查都发现遗漏项。本轮应主动对照 GLM `ext/quaternion_exponential.inl:41-80` 完整实现，逐行核验所有 `cos`/`sin`/`acos`/`asin`/`sqrt`/`abs`/`clamp`/`epsilon<T>()`/`pi<T>()`/`cos_one_over_two<T>()`/`std.math.pow`/`std.math.log`/`std.math.exp` 等依赖是否齐全，避免下一轮再次发现遗漏。
- **测试设计细节**（迭代第 1 轮问题 4 + 迭代第 3 轮问题 5、10）：第 1 轮已新增 §8.2 测试设计表，第 3 轮又发现命名约定（test_xxx.cj vs xxx_test.cj）和用例分配比例两个新问题。需在修订 §8.2 时统一处理：先按 §8.2 已有覆盖维度规划，逐文件检查命名风格与用例数合理性。

### 新发现的问题（本轮新识别的问题）

- **问题 1（gtc/constants 25 vs 28）**：设计内部文本与清单数量自相矛盾，属"设计自身一致性"问题，前序审议未充分覆盖。建议全局搜索"25"并修订为"28"。
- **问题 3（fromMat4 降维策略两可）**：给出"或"的选择但两种方式实现语义不同，下游无法决策。属"设计决策明确性"问题，前序审议已闭环 fromMat3/fromMat4 工厂函数定义但未深入策略选择。
- **问题 4（FMat2x2 先例引用错误）**：直接通过查阅阶段二 `cjglm/src/fwd.cj` 全文即可发现无 `FMat*` 声明，属"与项目实际状态不一致"问题，前序审议主要依赖文档自证未交叉对照。
- **问题 5（测试文件命名 xxx_test.cj vs test_xxx.cj）**：属"项目命名约定对齐"问题，前序审议未充分对照阶段二现有约定。
- **问题 6（trigonometric 标量/向量重载区分）**：属"实现可操作性"问题，前序审议已闭环 mix/slerp/roll/pitch/yaw 等的 acos/sin/atan 依赖但未明确 trigonometric.cj 需提供哪些重载形式。
- **问题 7（Float32 sqrt 一般性约束）**：属"系统性约束"问题，前序审议仅在 §3.7 length 处明确该问题，未上升为一般性约束。
- **问题 8（fromVec3 边界行为契约）**：属"边界条件覆盖"问题，前序审议已新增 §5.3 边界条件表但未覆盖 fromVec3 反平行场景。
- **问题 9（mat3Cast/mat4Cast 边界行为）**：属"接口契约一致性"问题，§3.3 item 6 已明确 Quat.fromMat3 边界声明但 §3.2.1 mat3Cast 描述未同步。
- **问题 10（用例分配比例失衡）**：属"测试覆盖率合理性"问题，前序审议已闭环测试文件清单但未深入用例分配原则。
- **问题 11（§11.5 where 子句标注缺失）**：属"下游可操作性"问题，前序审议已新增 §11.5 权威基准表但未明确具体约束形式。

## 上一轮产出路径

C:/Develop/Software/cjglm_wp/harness/redeliberations/202606241929_stage3-ood/a_v3_design_v2.md

## 用户需求

C:/Develop/Software/cjglm_wp/harness/redeliberations/202606241929_stage3-ood/requirement.md
