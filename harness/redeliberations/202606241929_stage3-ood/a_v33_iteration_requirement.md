根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

### 问题 1（严重）：§9a 与 §11.5 合计行对 ⚠️ 计数不一致（3 vs 4）

§9a 覆盖矩阵合计行（line 2132）声明「87 ❌ + 3 ⚠️ = 90」，而 §11.5 函数可用性对照表合计行（line 2288）声明「⚠️ 4 个（Quat×Vec3 + Quat×Vec4 + Vec3×Quat + Vec4×Quat）」。两处同为「函数状态统计」核心表，⚠️ 计数差值为 1 且无任何注记说明差异来源。§11.5 的「计数口径映射注记」解释的是 ✅ 口径差异（18 vs 77），不覆盖 ⚠️ 计数差异。§8.3 H 节核验项 H1 要求覆盖矩阵合计行与逐行累加一致，但未要求 §9a 与 §11.5 的 ⚠️/❌ 计数交叉核对。下游消费者按 §9a 评估风险时会漏掉 1 个 ⚠️ 函数。

**所在位置**：§9a 覆盖矩阵合计行（line 2132）、§11.5 合计行（line 2288）、§8.3 H 节核验项 H2b（line 2030）

**改进建议**：
- 核实 ⚠️ 实际个数（应为 4：Quat×Vec3 + Quat×Vec4 + Vec3×Quat + Vec4×Quat），将 §9a 合计行从「3 ⚠️」修正为「4 ⚠️」，❌ 从 87 调整为 86，保持 ⚠️+❌ = 90 不变或调整 ❌ 使 ⚠️+❌ = 88（§11.5 口径下为 88）
- 扩展 H2b 核验项范围，要求 §9a 与 §11.5 的 ⚠️/❌/✅ 三项计数分别交叉核对，偏差为 0 方可发布

### 问题 2（严重）：v32 声称非决策性版本标注「已移除」，但正文仍存在多处 `（v{N} 修订）` 残留

文档头部（line 3-5）声称「正文中所有 `（v{N} 修订）`、`（v{N} 关键修订）`、`（v{N} 新增）`、`（Issue {N} 响应）` 等非决策性版本标注已被移除」，并称「§8.3 H7 核验门禁已将自动化 grep 检查纳入版本发布前置流程，实现版本标注零残留」。但正文中仍存在大量显式版本标注，例如 §3.3、§3.2.1、§3.9、§3.10、§3.11、§3.15 等章节存在大量 `（：v{N} ...）` 嵌套模式残留。H7 自动化 grep 门禁模式为 `grep -n "（v[0-9]\|（Issue [0-9]\|\\*\\*v[0-9]" *.md`，但残留的 `：v{N}` 模式（无外层 `（v{N}` 前缀，而是 `（：v{N}` 前缀）不被该模式匹配，形成绕道空间。

**所在位置**：全文散布，涉及 §3.2.1、§3.3、§3.9、§3.10、§3.11、§3.15 等章节

**改进建议**：
- 扩展 H7 grep 模式为 `grep -n "（v[0-9]\|（Issue [0-9]\|\\*\\*v[0-9]\|：v[0-9]" *.md` 以捕获 `：v{N}` 前缀残留
- 对已识别的残留标注执行第二轮清理，按「保留设计决策内容，删除版本前缀」原则处理
- 对 §3.3 item 6-8 中含量丰富的残留集中清理

### 问题 3（严重）：§1 对整数 T 的 `T(Float64(n))` 可行性描述存在自相矛盾

§1 「系统性设计约束」段（line 63-64）明确声明「对 `T = Int8~Int64/UInt8~UInt64` 等整数类型，`T(Float64(n))` 的编译可行性属于 **P0 假设 H1**（验证项 25），尚待编码启动前验证」。但同一段落的验证执行记录（line 138）声称「已验证 ✅ `Int8~Int64(Float64(0/1))`、`UInt8~UInt64(Float64(0/1))` 均编译通过，运行时值正确」。两处对同一事实的陈述直接矛盾——line 63-64 说「尚待验证」，line 138 说「已验证通过」。下游阅读者无法判断整数类型 `T(Float64(n))` 的实际验证状态。

**所在位置**：§1「系统性设计约束」段（line 63-64）与 §1「回退方案」节验证执行记录段（line 138）

**改进建议**：
- 统一两处陈述：若验证项 25 在 v25 已覆盖整数类型则删除 line 63-64 的「尚待」声明；若未覆盖则要求 line 138 的「已验证」降级为「仅验证了 Float32/Float64，整数类型仍待编码启动前验证」
- 在验证结果记录表中为整数类型设置单独的验证子项，明确标注验证状态

### 问题 4（一般）：§3.13 三角函数表 radians/degrees 行例外标注未与表头说明对齐

§3.13.1 表前说明（line 992）声明「本表「内部依赖」列标注的 std.math.sin/cos 等依赖为阶段四完整的实现后依赖」。但 radians/degrees 行的「内部依赖」列（line 1008-1009）额外标注「——硬编码 π 字面量（stdlib 不存在）」，暗示 radians/degrees 的实现存在特殊路径。审查 §3.13.1 表下段（line 1019）对 radians/degrees 有补充说明，但该说明在表行 15 行之后的表下位置，而非在表行中形成可读的例外标注。

**所在位置**：§3.13.1 三角函数表 radians/degrees 行（line 1008-1009）与表下说明段（line 1019）

**改进建议**：将表下段（line 1019）中关于 radians/degrees 的例外说明以脚注形式引用至表行，如添加脚注标记 `（*）（见本表下方「radians/degrees 实现策略」说明）`

### 问题 5（一般）：§5.3 边界条件表中 normalize 零四元数保护分支的返回值描述与 §1 的 T(0) 策略描述重叠且不一致

§5.3 边界条件表 normalize 零四元数行（line 1539）描述返回 `Quat(T(Float64(0)), ...)` 并注释「T(0) 原策略「通过 one - one 演算」在 normalize 签名（不含 one: T 形参）中不可用，统一修订为 T(Float64(0)) 字面量替代路径」。但 §3.7 normalize 函数描述（line 718）同样包含 T(0)/T(1) 获取路径的说明。两处对同一策略的说明文字不完全一致。此外，§5.3 行（line 1539）描述长度约 400 字，远超边界条件表其他行（通常 20-80 字），不成比例。

**所在位置**：§5.3 边界条件表「零四元数」行（line 1539）、§3.7 normalize 函数描述段（line 718）

**改进建议**：将 §5.3 边界条件表 normalize 零四元数行精简为「返回单位四元数（实现策略及 T(0)/T(1) 获取路径详见 §3.7 + §1）」，删除重复的 T(0) 路径讨论

### 问题 6（一般）：gtc/quaternion.cj 的 7 个 stub 函数签名模板缺失

§3.15 gtc/quaternion.cj 的 stub 函数段（line 1225-1232）仅列出函数名和「stub」状态，未提供完整的仓颉函数签名模板。具体缺失：eulerAngles(x)、roll(q)/pitch(q)/yaw(q)、quatLookAt 系列等共 7 个函数的完整签名（含 where 约束）。对比 §3.13 中 gtc/matrix_transform.cj 的 64 个 stub 函数已全部展开为可复制签名模板。

**所在位置**：§3.15「stub 函数（7 个）」段（line 1225-1232）

**改进建议**：在 §3.15「stub 函数（7 个）」段下方新增完整签名模板代码块，列出全部 7 个函数的完整仓颉签名（含 where 子句约束）

### 问题 7（一般）：test_matrix_transform_stubs.cj 的采样策略会导致编译期签名验证覆盖率不足

§8.2 测试文件清单表（line 1675）中 test_matrix_transform_stubs.cj 预计用例数为 ≥8，按 §8.2 第 9 类「编译期函数签名正确性验证」的要求（line 1751），每类至少 1 个代表性函数验证编译期签名。64 个函数覆盖 9 个子类，每类 1 个代表函数 = 9 个编译期引用测试用例，但设计表只给了 ≥8，略低于理论最低值 9。设计表小计（≥8）与抽样表（9 个代表性函数）直接冲突。

**所在位置**：§8.2 测试文件清单表（line 1675）与 stub 抽样策略表（line 1782-1793）

**改进建议**：将 test_matrix_transform_stubs.cj 的预计用例数从 ≥8 修订为 ≥9（与 9 个代表性函数一一对应），并同步更新 §8.2 合计行和分配表

### 问题 8（一般）：O-06「已实施」的声称与验证项待执行状态存在残留不一致

§1 验证时序审计声明段（line 266-277）声称「O-06（验证前置化流程制度化）已在 v27 从「待评估」升级为「下一轮必须实施」…当前版本（v32）确认 O-06 已落地实施」。但 §8.3 I 节已知未解决问题列表（line 2044-2052）中 O-06 的状态仍标注为「v32 已实施（O-06 可执行落地计划已于 v28 建立；v32 确认所有 P0 假设已在设计版本固化前完成编译验证）」。两处状态声明行文风格不同，且有继续用括号附带细节的情况。此外，验证结果记录表（line 1919-1949）中验证项 1、2、3、4、5 等非 P0 项全部留空，共 26 项留空。

**所在位置**：§1 验证时序审计声明段（line 266-277）、§8.3 I 节 O-06 行（line 2051）、验证结果记录表（line 1919-1949）

**改进建议**：
- 为验证结果记录表中所有非 P0 项统一标注「—（编码阶段执行）」或「N/A（当前轮次不适用）」标记，消除 26 项空白
- 将 §1 和 §8.3 I 节对 O-06 的描述措辞统一，删除多余括号细节，形成单一权威描述

### 问题 9（一般）：§11.6 四命名空间可达性矩阵的「方向不一致」未标注

§11.6 表（line 2301-2319）中，glm 顶层列标注 glm.Quat 的来源是「fwd.cj type alias」，glm.gtc.quaternion.Quat 的来源是「import detail」，glm.detail.Quat 的来源是「原始定义」。但 §3.14 明确说明 fwd.cj 的别名生成脚本在 scripts/gen_fwd_aliases.py，且别名通过 public import 机制由 lib.cj 重导出至 glm 顶层。§11.6 表缺少这个「通过 lib.cj 重导出」的中介说明。

**所在位置**：§11.6 表（line 2303）、§2 lib.cj 段落（line 304）、§3.14 别名清单（line 1177-1182）

**改进建议**：在 §11.6 表 type 行 glm 顶层列补充脚注标注「Quat 别名在 fwd.cj 中定义（package glm），lib.cj 的 public import 机制使其在 glm 命名空间下可直接访问」

### 问题 10（轻微）：§8 验证项 15 描述仍引用已废弃的「std.math.pow 仅 Float64」策略

验证项 15（line 1853）描述中写「因 std.math 的 pow/log/exp 签名仅支持 Float64…需先显式转换至 Float64 再降级」。但 §1 v11 修订（line 77）已明确 std.math.pow 提供 Float32/Float64 共 4 个重载，T=Float32 时可直接调用。验证项 15 的描述与 §1 的有效策略不一致。

**所在位置**：§8 编码启动前验证项 15（line 1853）与 §1「Float32 与 std.math 的交互约束」段（line 77）

**改进建议**：将验证项 15 描述中「仅支持 Float64」更新为「Float32 实例化时直接调用 std.math.pow(Float32, Float32) 重载，无需显式 Float64 转换」

## 历史迭代回顾

### 已解决的问题
- 版本号体系混乱（v13↔v14↔v15↔v16 多重矛盾）——在 v15~v16 轮次中被反复追踪，到 v17 轮基本统一，当前轮次（v32）未再报告版本号矛盾
- 包间循环依赖（glm.detail ↔ glm.gtc）——v3 识别后采用 type_quat_cast.cj 方案解决，后续轮次未再重现
- snake_case vs camelCase 命名冲突（mat3_cast vs mat3Cast）——v7 轮严重问题后采纳方案 C 统一为 camelCase，当前轮次不再涉及
- fromMat4 降维策略选择（手动提取 vs Mat3x3 构造）——v4~v5 轮反复讨论后明确路径，当前轮次未再出现
- fwd.cj 生成脚本位置问题（tools/gen_fwd.cj vs scripts/gen_fwd_aliases.py）——v8 轮修正后当前轮次未再报告
- gtc/matrix_transform.cj 函数数量偏差（35 vs 实际 64）——v8~v9 轮修正后当前轮次不再报告
- Trigonometric.cj 函数清单中 Float32 重载支持描述——v11~v12 轮修正后不再报告
- 修订说明与正文分离的「考古式阅读」问题——v10 轮后逐渐收敛，v32 未再报告该结构性问题

### 持续存在的问题（需重点解决）
- **版本标注残留清理**：从 v6 轮开始反复报告版本标注残留问题，经历多轮清理（手动清理→H7 门禁引入），至今仍未彻底消除。当前轮次问题 2 表明 H7 门禁的 grep 模式存在绕过漏洞（`：v{N}` 模式），是前序清理不彻底的延续。需要彻底根除。
- **核心统计表计数不一致**：从 v3 轮（28 vs 25）、v6 轮（8 vs 9）、v8 轮（35 vs 64）、v15 轮（18 vs 20）、v16 轮（194 vs 192）、v18 轮（80 vs 88）到当前轮次问题 1（3 vs 4）和问题 7（≥8 vs 9），计数交叉核验不足的问题持续贯穿各轮。每次修复仅针对单个孤立计数偏差而未建立系统性核验机制。
- **测试用例数与抽样策略冲突**：从 v8 轮 gtc/matrix_transform 64 个 stub 仅 ≥8 用例的问题，到当前轮次问题 7 的 ≥8 vs 9 冲突，测试计数与策略对齐始终保持不佳。
- **O-06 流程门禁的有效性**：从 v10 轮首次提出验证前置需求，到 v14~v15 轮实施回退方案，再到当前轮次问题 8 声称「已实施」但验证表 26 项留空，O-06 的落地状态始终存在措辞-事实缝隙。
- **stub 函数签名模板缺失**：从早期轮次开始，多个 stub 函数缺少完整签名模板。v28 轮修复了 gtc/matrix_transform 的 64 个签名，但当前轮次问题 6 表明 gtc/quaternion.cj 的 7 个 stub 仍缺少签名模板。

### 新发现的问题
- **问题 3（严重）**：§1 内部自相矛盾——line 63-64 称「尚待验证」而 line 138 称「已验证 ✅」。这一矛盾是新增审计声明的副作用，本轮次前未出现。
- **问题 4（一般）**：§3.13.1 三角函数表 radians/degrees 行例外标注与表头说明对齐不足——虽前述轮次已处理 radians/degrees 的依赖描述，但本轮次首次从「标注位置与读者查阅效率」视角发现问题。
- **问题 5（一般）**：§5.3 normalize 零四元数行描述篇幅失衡（400 字 vs 正常 20-80 字），本轮首次被识别为可读性问题。
- **问题 9（一般）**：§11.6 命名空间可达性矩阵的路径描述方向不一致，本轮首次被识别。
- **问题 10（轻微）**：验证项 15 引用已过时的「pow 仅 Float64」策略，与 §1 v11 修订脱节，是之前轮次修复验证项 15 时的遗漏。

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606241929_stage3-ood\a_v32_copy_from_v31.md

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606241929_stage3-ood\requirement.md
