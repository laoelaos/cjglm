根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

本轮（v7 迭代第 7 轮）组件 B 在 `b_v7_diag_v1.md` 中识别出 14 项质量问题（4 严重 + 5 一般 + 5 轻微），组件 B 在 `b_v7_challenge_v1.md` 中以 `LOCATED` 结论确认所有问题，并补充 5 个轻微质询要点。质询报告问题 A 建议将严重问题 3 的严重程度校准为「一般」（作为 v8 前修订前置项），但严重程度本身仍由组件 A 决定——本轮需求中保留其「严重」定位，待组件 A 在迭代中明确响应。

### 严重问题（4 项，必须修复，直接阻塞下游编码落地）

**严重问题 1**：`gtc/quaternion.cj` snake_case 函数名（`mat3_cast`/`mat4_cast`/`quat_cast`）与 `public import` 机制不兼容——依据 `cangjie-lang-features/package/README.md` 第 156-166 行规范，`public import a.b.f` 仅重导出 f 原始名，不进行命名转换；实测 `cjglm/src/lib.cj:8` 现有 `public import glm.detail.{transpose, matrixCompMult, outerProduct}` 在 glm 顶层仍以原始 camelCase 名暴露。下游按 §11.4 调用 `glm.gtc.quaternion.mat3_cast(q)` / `glm.gtc.quaternion.quat_cast(m)` 将编译失败。涉及位置：§2 lib.cj/fwd.cj 段（约第 132 行）、§3.2.1 函数签名定义（第 285-292 行）、§3.15 重导出函数段（第 708-712 行）、§10 覆盖矩阵 gtc/quaternion 表（第 1291-1294 行）、§11.4 迁移示例（第 1354-1355 行）。修复方案三选一：方案 A（在 gtc 端显式定义 wrapper 函数，保留 GLM snake_case 习惯 + gtc 端 camelCase 路径）/ 方案 B（detail 端采用 snake_case，与 GLM 完全对齐）/ 方案 C（gtc 端采用 camelCase，§11.4 / §10 同步修订）。

**严重问题 2**：`fwd.cj` 是自动生成文件（`cjglm/src/fwd.cj:1-2` 文件头注释明确标注），设计要求新增 9 个 type alias 但未说明实施路径——手动添加将随重新生成丢失，修改生成脚本的具体位置未说明。涉及位置：§2 lib.cj/fwd.cj 段（第 134-145 行）、§3.14 四元数别名文件（第 653-679 行）、§8 更新文件段（第 960 行）、§8.3 验收项 F（第 1119 行）。修复方案三选一：方案 A（推荐，修改 `tools/` 目录下生成脚本）/ 方案 B（移至 `lib.cj` 中作为 type alias 补充声明）/ 方案 C（新建独立手动维护文件 `cjglm/src/quaternion_aliases.cj`）。

**严重问题 3**：`§3.13.1 trigonometric.cj` 函数清单表行「内部依赖」列对 14 个单参数三角函数重复 14 次「std.math Float32 重载已支持」的相同说明；表头说「删除冗余 T 约束标注」但表行实际保留的是 Float32 重载说明（论述话题不同）；§3.13.1 表后「Float32 实例化影响」段与 §1 v11 修订形成三重重复。设计内部表述冗余 + 修订未闭环。涉及位置：§1 系统性设计约束段（第 54-69 行）、§3.13.1 表头（第 584 行）、§3.13.1 表行「内部依赖」列（第 586-597 行）、§3.13.1 表后「Float32 实例化影响」段（第 609 行）。

**严重问题 4**：`gtc/matrix_transform.cj`「仅函数签名空壳」的具体清单未明确——GLM 1.0.3 `glm/gtc/matrix_transform.hpp` 实际定义 20+ 函数，下游存在三种解读（空文件 / 完整 20+ 函数 / 仅阶段四核心函数），且 §2 lib.cj 第 131 行 `import glm.gtc.matrix_transform` 依赖该文件存在可被 import 的对象。涉及位置：§3.13 表格行（第 563 行）、§8 空桩占位段（第 946 行）、§8.3 验收项 A 覆盖矩阵（第 1065 行）、§2 lib.cj import 清单（第 131 行）。

### 一般问题（5 项，建议修复）

**一般问题 5**：§11.5 函数可用性对照表 gtc 重导出行的「约束继承自 detail 端实现，通过 public import ... 透明传递」表述与 §3.2.1 D32 决策存在归属歧义（与严重问题 1 同源）。涉及位置：§11.5 第 1383 行、§3.2.1 D32（第 912 行）。

**一般问题 6**：§3.7 normalize 函数末尾「实现策略（v11 补强）」段 T(0)/T(1) 获取路径描述与 §1 系统性约束 v9 扩展存在冗余，未引用 §1 形成单一权威来源。涉及位置：§3.7 第 459 行附近、§1 第 49-52 行。

**一般问题 7**：`conjugate` const func 适用性论断缺乏阶段二实践依据——未引用阶段一 `type_vec3.cj:54-80` 中 `negative`/`negate` 逐分量取反函数已成功声明为 const func 的具体行号。涉及位置：§3.11 第 517 行附近、§5.4 第 862 行附近、§8 编码启动前验证项。

**一般问题 8**：§8.2 测试文件清单中 `test_ext_quaternion_aliases.cj`「预计用例数 ≥4」与 9 个类型别名比例偏低，按 v8 修订「重导出函数每函数 ≥1 用例」原则应至少 9 个用例。涉及位置：§8.2 第 981 行。

**一般问题 9**：§3.4 Quat×Vec4 公式 `Vec4(q * Vec3(v), v.w)` 隐含要求 Vec4 主构造函数支持「`Vec4<T, Q>(Vec3<T, Q>, T)`」双参数版本，该签名在阶段二 Vec4 实现中是否存在未明确。涉及位置：§3.4 第 374 行附近。

### 轻微问题（5 项）

**轻微问题 10**：设计文档总行数 1764 行，§修订说明章节占比 37.4%，下游查阅效率受影响。质询报告问题 D 建议精简百分比统计描述，保留核心拆分建议即可。

**轻微问题 11**：§3.13.2 审计节与 §5.3 边界条件表存在内容重复，未形成双向闭环引用。涉及位置：§3.13.2 第 623-651 行、§5.3 第 838-856 行。

**轻微问题 12**：§11.5 函数可用性对照表 gtc 重导出行 snake_case 命名与顶层 glm 行 camelCase 命名存在视觉差异（与严重问题 1 同源）。涉及位置：§11.5 第 1382-1383 行。

**轻微问题 13**：§3.15 表格行 4 个比较函数 `where T <: Comparable<T>` 约束仅在顶部描述。质询报告问题 E 建议移除或合并。

**轻微问题 14**：§8.3 验收项 C 覆盖矩阵表 `gtc/matrix_transform.hpp` 行四列均为「全部」措辞，缺具体数字（与严重问题 4 同源）。涉及位置：§8.3 第 1095 行。

### 质询报告补充要点（轻微，不构成驳回）

- **问题 A**：严重问题 3 严重程度校准——可调整为「一般」（作为 v8 前修订前置项），或在描述中明确其作为「v8 前修订前置项」的定位
- **问题 B**：一般问题 8 论证循环——在改进建议中保留「若 v8 原则本身待调整，则需先重新审视该原则」的灵活性
- **问题 C（覆盖完备性补强）**：建议在 v8 中新增「四命名空间接口可达性矩阵」审查，对 `glm` / `glm.gtc` / `glm.detail` / `glm.ext` 四个命名空间下关键函数（含运算符重载、构造函数重载、主类型别名）的可达性做集中审计
- **问题 D**：轻微问题 10 属「统计行数」类型，建议精简或弱化描述
- **问题 E**：轻微问题 13 属表格格式偏好，建议移除或合并到一般问题 5

## 历史迭代回顾

历史迭代共 7 轮（v2~v11），累计 79 项审查意见。本轮（v8）针对 v11 修订稿提出的 14 项问题与历史反馈的关系如下：

### 已解决的问题（历史反馈中提出，已通过后续修订闭环）

- **第 1 轮 §2 包组织缺 `gtc/quaternion.cj` 条目**（严重）→ 已闭环：v3 修订后 §2 包组织图明确列出该文件
- **第 1 轮 `Quat×Vec3` 旋转公式错误**（严重）→ 已闭环：v3 修订后公式修正为 `v + (cross(QuatVector, v) * q.w + cross(QuatVector, cross(QuatVector, v))) * 2`，并通过 v9/v11 修订完善 T(2) 字面量替换
- **第 1 轮 `mat3_cast` 等文件归属矛盾**（严重）→ 已闭环：v3 关键决策下沉至 `detail/type_quat_cast.cj`
- **第 1 轮 测试设计不完整**（一般）→ 已闭环：v8/v10 多次修订后 §8.2 测试设计章节完善
- **第 1 轮 模块间依赖图缺 gtc/quaternion.cj**（一般）→ 已闭环：v10 修订后依赖图细分至函数组级别
- **第 2 轮 `pow` 依赖不完整、命名消歧、line 65/78 翻译**（严重/一般）→ 已闭环：v5-v7-v11 多轮修订后完善
- **第 2 轮 `axis()` 边界行为错误**（严重）→ 已闭环：v6 修订后修正
- **第 2 轮 `conjugate` 实现与 GLM 不一致**（一般）→ 已闭环：v6 修订后对齐 `ext/quaternion_common.inl:112-116`
- **第 3 轮 `gtc/constants` 25 vs 28 计数矛盾**（严重）→ 已闭环：v4-v5 修订后统一为 28
- **第 3 轮 `fromMat4` 降维策略未明确**（严重）→ 已闭环：v8/v9 修订后明确采用手动提取策略
- **第 3 轮 `FQuat` 先例引用错误**（严重）→ 已闭环：v9 修订后修订先例为 Vec 家族四重模式
- **第 3 轮 测试文件命名 `xxx_test.cj` vs `test_xxx.cj`**（一般）→ 已闭环：v8 修订后统一为 `test_xxx.cj`
- **第 3 轮 `trigonometric.cj` 标量/向量重载未区分**（一般）→ 已闭环：v9 修订后明确 15 标量 + 15 向量重载
- **第 4 轮 `fromMat4` 自相矛盾论据**（严重）→ 已闭环：v9 修订后删除错误代码引用
- **第 4 轮 `ext/vector_relational.cj` 文件引用错误**（一般）→ 已闭环：v9 修订后修正为 `gtc/epsilon.inl:32-41`
- **第 5 轮 `FloatingPoint<T>.getMin()`/`getInf()` 不存在 API**（严重）→ 已闭环：v10 修订后修订为类型分派 + 字面量 fallback
- **第 5 轮 `fwd.cj` 8 vs 9 别名计数矛盾**（严重）→ 已闭环：v10 修订后统一为 9
- **第 5 轮 `std.math.pow` 签名错误**（严重）→ 已闭环：v10 修订后明确为 `Float64` 输入/输出
- **第 5 轮 `epsilon<T>()` 与 `epsilonOf<T>(hint)` 关系**（一般）→ 已闭环：v10 修订后新增关系说明
- **第 5 轮 `trigonometric.cj` VecN 占位符展开**（一般）→ 已闭环：v10 修订后明确展开为 4 个独立重载
- **第 6 轮 `trigonometric.cj` 缺 `where T <: FloatingPoint<T>` 约束**（严重）→ 已闭环：v11 修订后表头新增「T 类型约束」段
- **第 6 轮 `normalize` 零四元数保护分支缺失**（严重）→ 已闭环：v11 修订后补充完整实现公式与「实现策略（v11 补强）」段
- **第 6 轮 `Quat×Vec3` 行 `QuatVector` 符号未定义**（一般）→ 已闭环：v11 修订后定义 `QuatVector = Vec3(q.x, q.y, q.z)`
- **第 6 轮 §4.4 行为契约示例命名空间不一致**（一般）→ 已闭环：v11 修订后统一为顶层 glm 命名空间

### 持续存在的问题（在多轮反馈中反复出现，需重点解决）

- **`gtc/quaternion.cj` 命名约定**（历史第 1 轮文件归属争议 → 本轮第 7 轮 snake_case vs public import 不兼容）：文件归属问题在 v3 决策已闭环，但 snake_case 命名与 public import 的兼容性在 v11 修订时未充分验证，形成新层面的阻塞问题
- **`fwd.cj` 自动生成机制与手动维护的冲突**（本轮第 7 轮新识别，但根因可追溯至 v5 决策将 9 个 Quat 别名归入 fwd.cj）：该问题是 v5 别名策略的直接后果，未在 v5-v11 期间审视生成机制
- **§3.7 normalize T(0)/T(1) 获取策略冗余**（本轮第 7 轮一般问题 6）：第 6 轮 v11 修订「实现策略（v11 补强）」段新增 T(0)/T(1) 说明时未引用 §1 单一权威来源，形成新的冗余
- **§11.5 gtc 重导出约束归属歧义**（第 4 轮轻微问题 6 → 第 7 轮一般问题 5）：v9 修订「约束继承自 detail 端实现，通过 public import 透明传递」措辞本身依赖命名一致，与 v11 的 snake_case 重导出设计存在内部矛盾
- **`conjugate` const func 实践依据**（第 5 轮一般问题 6 → 第 7 轮一般问题 7）：v10 修订已声明 const func 适用性但未引用阶段二具体行号
- **§3.13.2 审计节与 §5.3 边界条件表内容重复**（第 6 轮新增 §3.13.2 → 第 7 轮轻微问题 11）：v11 修订 §3.13.2 时未与 §5.3 形成双向引用闭环

### 新发现的问题（本轮 v8 新识别）

- **严重问题 1（snake_case vs public import 不兼容）**：v11 修订稿首次系统使用 snake_case 命名，但未验证 `public import` 的命名转换能力
- **严重问题 2（`fwd.cj` 自动生成机制冲突）**：v11 修订稿未说明 9 个 Quat 别名的实施路径（手动 vs 脚本）
- **严重问题 3（§3.13.1 表行冗余）**：v11 修订新增 Float32 重载说明时形成 14 行重复 + 三处表述重复
- **严重问题 4（`gtc/matrix_transform.cj` 函数签名清单缺失）**：v11 修订稿仅描述「函数签名空壳」但未列出具体清单
- **一般问题 8（测试用例数与别名数量比例）**：v8 修订引入「重导出函数每函数 ≥1 用例」原则，但未对所有重导出别名应用
- **一般问题 9（Vec4 主构造函数签名约束）**：v11 修订未明确 Vec4 主构造函数双参数版本的存在性
- **轻微问题 10（修订说明占比）**：v11 设计总行数 1764 行，修订说明占 37.4%
- **轻微问题 12（§11.5 命名视觉差异）**：v11 修订稿 §11.5 对照表存在 snake_case 与 camelCase 并存
- **轻微问题 13（§3.15 比较函数 where 子句表格标注）**：v11 修订稿完整实现段已标注但表格行未同步
- **轻微问题 14（§8.3 覆盖矩阵缺函数总数）**：v11 修订稿 `gtc/matrix_transform.hpp` 行缺具体数字

## 上一轮产出路径

C:/Develop/Software/cjglm_wp/harness/redeliberations/202606241929_stage3-ood/a_v7_copy_from_v6.md

## 用户需求

C:/Develop/Software/cjglm_wp/harness/redeliberations/202606241929_stage3-ood/requirement.md