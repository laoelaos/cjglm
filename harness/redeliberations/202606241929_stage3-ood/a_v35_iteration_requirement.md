根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

- **问题 1（严重）：Vec4(Vec3, T) 双参数构造函数在阶段一不存在，Quat×Vec4 实现路径中断**。§3.4 运算符体系表 `Quat×Vec4` 行声称 `Vec4(q * Vec3(v), v.w)` 实现公式依赖阶段一已实现的 `init(vec3: Vec3<T, Q>, w: T)` 双参数主构造函数，但经查阅 `cjglm/src/detail/type_vec4.cj` 全文（194 行），Vec4 结构体仅声明 `init(scalar: T)`、`const init(x: T, y: T, z: T, w: T)`、`init(v: Vec1<T, Q>)` 三个构造函数，不存在双参数 Vec3+w 版本。影响：直接阻塞 `Quat×Vec4` 运算符编码实现；`Vec4×Quat` 间接受影响；§11.5 中 ⚠️ 标注不准确（实际为 ❌ 编译失败）。改进建议：方案 A（推荐）在阶段三 Quat extend 块中手动逐分量构造 Vec4；方案 B 在阶段三 `type_vec4.cj` 补充该构造函数。同步修正 §11.5、§3.13.2、§8 等处状态标注。

- **问题 2（一般）：「阶段一 Vec3 已成功声明 27 个 const func」的实践依据不成立**。§3.11 `conjugate` 的 const 适用性论证引用「阶段一 `type_vec3.cj:54-80` 已成功声明 27 个 `const func`」，但经查阅 `type_vec3.cj` 全文（185 行），extend 块中所有运算符和函数均未带 `const` 关键字。`type_mat2x2.cj` 等其他阶段二文件也确认无 `const func`。项目实际模式是所有 extend 块函数均非 const。改进建议：删除 §3.11 中引用阶段一「27 个 const func」的段落，替换为诚实的声明；同步修订 §5.4 中对应引用；验证项 24 修订为独立验证 extend 块中 const func 声明的编译可行性。

- **问题 3（一般）：§9a 与 §11.5 的 ❌ 计数存在未解释的 2 函数差异**。§9a 覆盖矩阵合计行声明「86 ❌ + 4 ⚠️ = 90」，§11.5 合计行声明「⚠️ 4 个…❌ ~84 个」= 88，❌ 计数相差 2 个。§11.5 的计数口径映射注记解释了 ✅ 类别的差异（106 vs 165，差 59），但未解释 ❌ 类别的 2 函数差异。改进建议：在 §11.5 的计数口径映射注记中显式说明 ❌ 类别的口径差异来源。

- **问题 4（一般）：`fromMat4` 与 `Mat3x3(Vec3, Vec3, Vec3)` 主构造函数的可行性依赖未经验证**。§3.3 item 7 的 `fromMat4` 实现路径假设 `Mat3x3<T,Q>` 存在接受 3 个 `Vec3<T,Q>` 参数的主构造函数，但文档未验证或引用此构造函数的存在性和签名。改进建议：查阅阶段二 `type_mat3x3.cj` 确认三 Vec3 参数构造函数的存在性；或在 §3.3 item 7 中增加明确的引用行号；或改用逐元素提取 9 个标量直接构造 Quat（与 GLM `gtc/quaternion.inl:128` 一致）。

## 历史迭代回顾

- **已解决的问题**：版本标注混乱（H7 门禁已建立，v33 已执行清理但仍有残留，v34 进一步清理）；修订说明剥离至独立文件；计数口径映射（59 差异已解释）；P0 核心假设 H1/H2 已通过编译验证（v25）；路线图偏差决策 D35-D37 已闭环；`ltc/matrix_transform` 函数清单从 35 修订为 64；`gen_fwd_aliases.py` 路径已修正；版本号混乱已治理。

- **持续存在的问题**：
  - **Vec4(Vec3, T) 构造函数缺失（问题 1）**：首次检出于第 7 轮（issue 9），后续多轮修复但始终未通过实际代码交叉验证核验，本轮发现实际上该构造函数在阶段一代码中根本不存在。
  - **const func 实践依据虚构（问题 2）**：第 7 轮（issue 7）即指出 `conjugate` const func 适用性缺乏阶段二实践依据，当时应对方案是「补充阶段一 `type_vec3.cj` 中 27 个 const func 的引用」，但本轮验证发现该引用本身是虚构的——阶段一代码中根本不存在任何 const func。
  - **计数口径不一致（问题 3）**：§9a 与 §11.5 的计数差异问题在多轮（第 18/19/28/29/32 轮）反复出现，虽已建立 H2b 核验门禁，但本轮仍检出 ❌ 类别 2 函数差异。
  - **`fromMat4` 实现路径依赖未验证（问题 4）**：首次检出于第 3/4 轮，后续第 9/10/20 轮持续跟进，但 `Mat3x3(Vec3, Vec3, Vec3)` 构造函数的存在性始终未通过实际代码核验。

- **新发现的问题**：问题 2 是全新的发现——此前各轮认为 const func 实践依据是真实的（只是行号引用有误），本轮通过实际阅读 `type_vec3.cj` 全文发现该项目所有 extend 块函数均未使用 `const func`，因此问题性质从「引用不准确」升级为「实践依据不存在」。问题 1 的性质也从「构造函数签名不明确」升级为「构造函数根本不存在」。

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606241929_stage3-ood\a_v34_copy_from_v33.md

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606241929_stage3-ood\requirement.md
