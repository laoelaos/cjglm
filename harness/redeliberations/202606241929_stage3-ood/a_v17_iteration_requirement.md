根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

### 问题 1（严重）：§11.5 权威对照表遗漏 4 个「真完整实现」函数
§3.7、§3.9、§11.5 等多处声明「§11.5 函数可用性对照表是阶段三验证的**单一权威来源**」，但核查 §11.5 表（行 1534-1559）的 22 行条目，发现以下 4 个已在 §3.13.2 审计结论（行 716）明确列为「真完整实现」的函数**未出现在 §11.5 表中**：`axis(q)`、`cross(Quat, Quat)`、`equal(Quat, Quat)` / `equal(Quat, Quat, epsilon)`、`notEqual(Quat, Quat)` / `notEqual(Quat, Quat, epsilon)`。

**改进建议**：在 §11.5 表中补充上述 4 行，同步核查 §8.3 验收项 D 对 §11.5 的引用。

### 问题 2（严重）：§8.2 测试用例合计值与实际逐项累加结果不一致
§8.2 测试文件清单（行 1072）声明「合计 ≥194」，但逐项累加 40+8+8+12+13+4+4+2+16+13+9+28+27+8 = 192，差 2。随后用例分配依据表（行 1131）中 `test_type_quat.cj` 列 42（非文件清单的 40），使分配表累加结果为 194——即**文件清单与分配表使用不同的基准值**（40 vs 42），两者未对齐。

**改进建议**：二选一——(a) 将文件清单中 `test_type_quat.cj` 的 ≥40 修正为 ≥42，使合计为 ≥196；(b) 将分配表中 `test_type_quat.cj` 的 42 修正为 40，使分配表合计为 192，并相应调整下行「增补 5 个边界用例可达 ≥199」的措辞。

### 问题 3（严重→经质询降为一般）：§修订说明(v13) 标题与内容存在版本号自相矛盾
§修订说明(v13) 章节标题为「## 修订说明（v13）」，但内容第一行写「修订定位：v14 设计的迭代修订（v13，对应本轮审查 a_v13_iteration_requirement.md）」——标题指 v13，内容第一句说 v14。该矛盾已在第 14/15 轮迭代中被反复指出，但 v16 仅清理了正文中的「v16 修订」标记，未触及 §修订说明(v13) 的章节内头部描述。

**改进建议**：将「v14 设计的迭代修订」修正为「v13 设计的迭代修订」，或在章节标题中增加歧义消除说明。同步检查 §修订说明(v12)（行 1649）首行描述——「v11 设计的迭代修订（v12）」也需统一为「v12 设计的迭代修订」。

### 问题 4（一般→经质询降为轻微）：§修订说明(v12/v13) 未按注释声明剥离至独立文件
行 1596 的 HTML 注释明确声明「修订说明(v2)~(v14) 已剥离至独立文件」，但 §修订说明(v12)/§修订说明(v13) 均在 v2~v14 范围内，却仍保留在正文中，与注释自相矛盾。

**改进建议**：二选一——(a) 将注释范围缩小为「修订说明(v2)~(v11) 已剥离」；(b) 将 v12/v13 也剥离至独立文件；(c) 删除过时注释，统一说明。

### 问题 5（一般）：gtc/matrix_transform 64 个 stub 函数的测试覆盖原则与实施不一致
§8.2 用例分配原则规定「stub 函数：每函数 ≥1 个用例」，但 `test_matrix_transform_stubs.cj` 仅分配 ≥8 个测试用例（每 8 个函数 1 个用例），抽样策略未在任何原则段落中声明过，也未曾说明抽样覆盖率充分性依据。

**改进建议**：在 §8.2 新增「同类 stub 函数抽样策略」说明，明确按类别分组后的抽样覆盖条件；或修订分配原则以匹配实际策略；同步更新 §8.3 验收项 B 的测试用例数参考。

### 问题 6（一般）：§11.5 不同入口函数的分组粒度不一致，影响「权威来源」的可查阅性
§11.5 表将一些函数合并为一行（如 `conjugate`/`inverse`、`dot`/`length`/`normalize`），而另一些函数独占一行（如 `identity`、`lerp`、`mix`）。分组粒度不统一导致用户难以快速确认某函数是否在表中。

**改进建议**：将分组行改为显式枚举（如 `dot/length/normalize/cross`），并注明每个分组包含的函数数；或拆分为逐函数行的完整表。

## 历史迭代回顾

### 已解决的问题（出现在历史反馈但当前反馈中不再提及）
- **迭代15-问题1（版本号体系未彻底修复）**：v16 已清理正文中 12 处「v16 修订」标记为「v15 修订」，正文版本号体系已修复；但 §修订说明(v13) 首行残留版本号矛盾（见当前问题 3），此为残余而非完全重复。
- **迭代15-问题2（§3.13.2 声明18个 vs §8 声明20个真完整实现计数口径不一致）**：已修复，当前审查未再提及。
- **迭代14-问题1（§8 产出物分类说明声称「不抛Exception("stub")」与 Quat×Vec3/Quat×Vec4 矛盾）**：已修复，当前审查未再提及。
- **迭代14-问题6（P0 核心假设未提供回退方案）**：v15 已新增 §1 回退方案子节（决策树 + 5 种证伪情形 + 5 条回退路径），已修复。
- **迭代14-问题7（D21 决策体量巨大重复）**：已处理，当前审查未再提及。
- **迭代13-问题1（设计擅自修改路线图）**：已修复为「⚠️ 待确认」标注，当前审查未再提及。
- **迭代13-问题3（实施顺序缺失）**：已新增 §8.4 实施批次建议，当前审查未再提及。
- **迭代12-问题1（§11.5 缺失 scalar_quat_ops/rotate 等 3 组函数）**：已修复，当前审查未再提及。
- **迭代11-问题1~3（length 依赖矛盾/axis 依赖矛盾/length 自相矛盾）**：均已修复，当前审查未再提及。
- **迭代10-问题1（考古式阅读模式）**：已执行修订说明剥离，当前问题 4 是剥离后的残余清理问题。
- **迭代10-问题2~3（T(Float64(n)) 核心假设未验证/Mat4x4[] 依赖未验证）**：已新增验证项，当前审查未再提及。
- **迭代9-问题1（欧拉函数实现状态矛盾）**：已修复，当前审查未再提及。
- **迭代8-问题1~4（lib.cj 第131行引用错误/gtc/matrix_transform 函数数量偏差/生成脚本路径错误/fwd.cj 文件头引用错误）**：均已修复，当前审查未再提及。
- 早期迭代（1-7轮）中的函数依赖错误、公式错误、签名缺失等问题均已陆续修复。

### 持续存在的问题（在多轮反馈中反复出现，需重点解决）
- **§11.5 对照表完整性**：迭代14-问题3（审计表缺失4行）、迭代16-问题1（§11.5 遗漏4函数）、当前问题1（相同函数持续缺失）。§11.5 作为「单一权威来源」始终未能做到与实际审计结论完全同步。**重点解决**：本次须确保 §11.5 表行数与 §3.13.2 审计结论完全一致，两者之间建立双向校验引用。
- **§8.2 测试用例计数**：迭代14-问题5（≥199 与差值矛盾）、迭代16-问题2（192 vs 194）、当前问题2（相同数据正确性问题）。测试计数在 v14~v16 三轮中持续出现算术错误。**重点解决**：本次须完成完整的数值核验，文件清单、分配表、合计行三者统一。
- **版本号体系**：迭代10-问题9（文档自身版本号混乱）、迭代14-问题2（v13↔v14↔v15↔v16 四重矛盾）、迭代15-问题1（正文标记未统一）、当前问题3（§修订说明(v13) 首行残留矛盾）。经过多轮修复版本号体系仍未彻底干净。**重点解决**：全文档执行 `grep -n "v[0-9]"` 系统性核验，包括修订说明章节头部和文件头。
- **测试覆盖原则一致性**：迭代16-问题3、当前问题5。stub 函数的「≥1 用例/函数」原则与实际的抽样实施不一致，原则层面缺少抽样策略声明。

### 新发现的问题（本轮新识别的问题）
- **当前问题4（修订说明剥离注释与正文不一致）**：行 1596 的 HTML 注释声明「修订说明(v2)~(v14) 已剥离」，但 v12/v13 仍在正文中。此问题在 v15/v16 迭代中未被专门的审查识别，为 v17 新发现。

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606241929_stage3-ood\a_v16_copy_from_v15.md

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606241929_stage3-ood\requirement.md
