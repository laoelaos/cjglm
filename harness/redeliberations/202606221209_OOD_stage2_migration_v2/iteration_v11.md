# 再审议判定报告（v11）

## 判定结果

RETRY

## 判定理由

组件B诊断报告经质询确认（LOCATED），共识别11个问题，其中：
- **严重问题2个**：fromParts签名表Mat2xR和Mat3xR家族参数缺失（问题1）；B类方向一致性分析误判（问题2）
- **中等/一般问题6个**：参数命名差异未说明（问题3）；fromMat跨Qualifier限制（问题4）；复合赋值运算符表述不清（问题5）；63个方向验证任务无优先级（问题6）；compile order方案二/三区别不清晰（问题7）；scalar_mat_ops.cj未标注@OverflowWrapping（问题8）
- **轻微问题3个**：filled()示例命名不一致（问题9）；length()调用形式不一致（问题10）；fwd.cj别名Lowp家族未覆盖（问题11）

根据判定标准，审查报告包含严重及一般等级问题，须重新运行组件A。

## 需要解决的问题

- **问题描述**：fromParts签名表中Mat2x2/Mat2x3/Mat2x4/Mat3x2/Mat3x3/Mat3x4的参数数量与C×R不匹配
- **所在位置**：§3.3 item 4 fromParts签名表格
- **严重程度**：严重
- **改进建议**：补齐所有缺失参数，使每个签名参数数量等于C×R

- **问题描述**：Mat3x3←Mat3x2和Mat3x4←Mat3x2被标记为"一致"，但GLM在(2,2)位置填入1，与主规则"新增行全填T(0)"相悖
- **所在位置**：§3.3 B类方向对照分析表
- **严重程度**：严重
- **改进建议**：①修正主规则表述，明确对角线位置填入one；②重新标记一致性

- **问题描述**：fromParts参数命名`a{列}{行}`与GLM的`{x,y,z,w}{列}`映射关系未说明
- **所在位置**：§3.3 item 4参数命名约定
- **严重程度**：一般
- **改进建议**：补充命名映射关系说明或转换示例

- **问题描述**：fromMat 6a签名缺失SrcQ参数，跨Qualifier转换被迫降级到6b
- **所在位置**：§3.3 item 6a签名与§9 fromMat条目
- **严重程度**：一般
- **改进建议**：评估是否恢复SrcQ泛型参数，或强化§9中的影响说明

- **问题描述**：复合赋值运算符回退方案注释将"非方阵不定义*=(Mat)和/=(Mat)"可能被误读为标量`*=`也被排除
- **所在位置**：§3.5 手动定义回退方案注释
- **严重程度**：一般
- **改进建议**：明确区分矩阵乘除与标量乘除的排除范围

- **问题描述**：63个非B类方向验证任务无优先级排序
- **所在位置**：§3.3编码阶段验证要求（L292-293）
- **严重程度**：一般
- **改进建议**：按偏差风险分组（高/中/低），优先标注高风险方向

- **问题描述**：compile order方案二分散定义导致Vec扩展代码重复，维护成本未评估
- **所在位置**：§8 fallback方案（L542-543）
- **严重程度**：一般
- **改进建议**：评估代码重复量，倾向采用方案三独立文件

- **问题描述**：§2包组织图中scalar_mat_ops.cj条目未标注@OverflowWrapping要求
- **所在位置**：§2包组织图（L59-61）
- **严重程度**：一般
- **改进建议**：补充@OverflowWrapping标注要求
