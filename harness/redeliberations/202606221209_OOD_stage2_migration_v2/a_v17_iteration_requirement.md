根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

### P1（严重）— matrixCompMult 和 outerProduct 的泛型约束错误

§3.7 将 transpose、matrixCompMult、outerProduct 共 27 个重载的泛型约束统一从 `where T <: Number<T>, Q <: Qualifier` 放松为 `where Q <: Qualifier`，声称"这三种运算仅涉及元素索引重排或逐元素按位运算，不涉及算术运算，因此无需 Number<T> 约束"。该论述存在事实错误：

- **matrixCompMult**：实现方式为 `dst.cj[i] = x.cj[i] * y.cj[i]`，元素级乘法（`*`）属于算术运算，需要 `Number<T>` 接口。
- **outerProduct**：实现方式为"列向量 c 的每个分量乘以标量 r[j]"，同样需要 `*` 运算符和 `Number<T>` 接口。
- **transpose**：仅涉及元素索引重排，确实无需算术运算，约束放松是正确的。

按当前签名实施，这两个函数体中的 `*` 运算在缺少 `Number<T>` 约束时将无法通过编译。

**所在位置**：§3.7，L540-542（约束放松说明段）；L519-538（matrixCompMult/outerProduct 签名）

**改进建议**：将 matrixCompMult 和 outerProduct 的泛型约束恢复为 `where T <: Number<T>, Q <: Qualifier`（与 determinant/inverse 一致）。transpose 可保留 `where Q <: Qualifier`。约束放松说明段需同步修正：仅声明 transpose 的约束已放松，matrixCompMult 和 outerProduct 因涉及算术运算仍需 Number<T>。

---

### P2（严重）— fromMat 9×8 转换矩阵表 Mat4x3→Mat4x4 标签错误

在 fromMat 9×8 转换矩阵表中，源矩阵 Mat4x3（4×3, R=3）到目标矩阵 Mat4x4（4×4, R=4）的方向被标注为"EQL"（同尺寸）。但 Mat4x3 的行数 R=3，Mat4x4 的行数 R=4，两者尺寸不同。C_src=C_dst=4（列数相同），R_src=3 < R_dst=4（行数增加），正确标签应为"B: rowExt(4)"。

**所在位置**：§3.3，L369-378，Mat4x3 行、→Mat4x4 列

**改进建议**：将 Mat4x3→Mat4x4 的标签从"EQL"修正为"B: rowExt(4)"。同时建议逐格复核表中所有非对角线单元格的标签准确性（上轮迭代已发现并修正了 11 处同类错误，本轮新增此遗漏）。

---

### P3（中等）— 缺少对 GLM 1.0.3 头文件的系统性 API 覆盖清单

用户需求明确指出参考依据包括"原始 GLM 1.0.3 头文件位于 references/glm-1.0.3/"，但当前设计文档未提供任何形式的 API 覆盖度对照表。具体问题和后果包括：

- 无法从设计文档判断阶段二是否完整覆盖了 GLM 1.0.3 矩阵相关的全部 API。
- 实施阶段无法按图索骥地对照 GLM 头文件逐项验证实现完整性。
- 后续阶段（三、四）的规划者无法基于本文档准确识别剩余工作边界。

**所在位置**：全篇（§1-§9），尤其应在 §1 概述或 §8 产出物清单中添加独立章节

**改进建议**：新增独立章节"GLM 1.0.3 API 阶段覆盖矩阵"，按 GLM 头文件路径组织，每行标注函数名、阶段覆盖状态（本阶段实现/本阶段 stub/后续阶段）、对应设计方案章节。

---

### P4（中等）— scalar_mat_ops.cj 全局函数签名完全缺失

§2 包组织图和 §3.5 算术运算符中均提及 `scalar_mat_ops.cj` 提供 T+Mat 等标量左侧运算的全局具名函数（add、sub、mul、div），但全文未提供任何一个函数的完整签名模板。实现者需要明确的签名信息包括：泛型约束（是否需要 Number<T>）、参数顺序（scalar 在左、matrix 在右）、返回值类型、@OverflowWrapping 标注等。

**所在位置**：§2 L74-76（仅一句话描述）；§3.5 L407-408（仅提及文件职责）

**改进建议**：在 §3.5 或新增子节中，提供至少一个代表性签名模板（如 Mat2x2 版本），并说明其余 8 个矩阵类型的签名模式。明确标量-矩阵运算需要 `Number<T>` 约束（因涉及 `*` 和 `/`）。

---

### P5（一般）— col() 越界错误处理未明确声明

§3.4 声明"下标越界抛出 Exception"覆盖 `[](i: Int64)`，但 `col(i: Int64)` 被描述为"与 [] 完全等价"。虽然等价性暗示了相同的错误处理行为，但设计文档中未明确声明 `col()` 的越界行为。对于正式的设计文档，等价性声明应包含行为契约的一致性确认。

**所在位置**：§3.4，L400-403

**改进建议**：在 `col()` 描述后补充"下标越界时抛出 Exception，与 `[]` 行为一致"。

## 历史迭代回顾

### 已解决的问题（出现在历史反馈但当前反馈中不再提及的问题）

以下 v15 及之前轮次发现的问题已在 v16 得到系统修复，当前反馈不再涉及：
- fromMat 9×8 转换矩阵表 11 处标签错误（v15 F1）→ 已修正，但 P2 新增 1 处遗漏
- fromMat 执行顺序规定与标注不一致（v15 F2）→ 已统一为先列后行
- transpose 函数不必要的 Number<T> 约束（v15 F3）→ 已放松，但过度应用导致 P1
- C→R→→ 标注对行收缩方向的误导（v15 F4）→ 已改为 rowExt/rowSh 区分符号
- common.cj mod 签名约束不匹配（v15 F5）→ 已补充暂定注释
- fromMat 6a one: T 语义未明确（v15 F6）→ 已补充说明
- 矩阵乘法 @OverflowWrapping 未声明（v15 F7）→ 已标注
- 144 个签名构造量风险（v15 F8）→ 已标注
- diagonal() Bool 缺失标注（v15 F9）→ 已补充
- v*m 退化迁移影响（v15 F10）→ 已评估
- 其他涉及 fromParts/fromColumns 签名、Mat×Vec 运算符位置、复合赋值运算符、transpose 实现说明等更早轮次的问题已在之前迭代中修复

### 持续存在的问题（本轮仍存在的全部问题均为 v16 首次发现，均需本轮解决）

以下 5 个问题在 v16 被首次识别，v16→v17 的迭代中未被修复，仍在当前版本中持续存在：

1. **P1**：matrixCompMult 和 outerProduct 泛型约束错误（v16 第 1 条）— 因 v15 F3 修复时过度放松导致
2. **P2**：Mat4x3→Mat4x4 标签错误（v16 第 2 条）— v15 修正 11 处标签后遗留的 1 处
3. **P3**：API 覆盖度对照表缺失（v16 第 3 条）— 首次提出的新需求
4. **P4**：scalar_mat_ops.cj 签名缺失（v16 第 4 条）— 首次提出的新需求
5. **P5**：col() 越界处理未明确（v16 第 5 条）— 首次提出的新需求

### 新发现的问题

本轮未发现全新问题。全部 5 个问题均与 v16 历史反馈一致，属于持续存在但未修复的问题。

## 上一轮产出路径
harness/redeliberations/202606221209_OOD_stage2_migration_v2/a_v16_design_v1.md

## 用户需求
harness/redeliberations/202606221209_OOD_stage2_migration_v2/requirement.md
