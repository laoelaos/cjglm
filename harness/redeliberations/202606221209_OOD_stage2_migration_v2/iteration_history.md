## 迭代第 1 轮

1. **问题描述**：fromMat 6a/6b 工厂函数的类型参数 SrcQ 缺少 `<: Qualifier` 约束，将导致编译失败
   - 所在位置：§3.3 第6a条（第253行）、第6b条（第264行）
   - 严重程度：严重
   - 改进建议：补充 `where SrcQ <: Qualifier` 约束
   - **修改结果**：**已采纳**。§3.3 6a/6b 签名增加 `where SrcQ <: Qualifier` 约束，完整泛型参数展开示例同步更新（a_v2_design_v1.md 修订说明 v14）

2. **问题描述**：行向量 × 矩阵运算符 `Vec{R} * Mat{C}x{R}` 的定义归属未指定，可能导致编码阶段大量返工
   - 所在位置：§3.5 二元运算符（矩阵-向量）段落（第416-417行）
   - 严重程度：严重
   - 改进建议：明确选择方案A（Vec extend块成员运算符）、方案B（包级自由函数）或方案C（降级为具名函数）
   - **修改结果**：**已采纳**。选择方案A（Vec 类型 extend 块成员运算符），§3.5 补充定义归属说明，新增 D32 设计决策（a_v2_design_v1.md 修订说明 v14）

3. **问题描述**：fromColumns 中 Q2 缺少 `<: Qualifier` 约束，将导致编译错误
   - 所在位置：§3.3 第5条（第243行）
   - 严重程度：严重
   - 改进建议：补充 `where Q2 <: Qualifier` 约束
   - **修改结果**：**已采纳**。§3.3 第5条 fromColumns 签名增加 `where Q2 <: Qualifier` 约束（a_v2_design_v1.md 修订说明 v14）

4. **问题描述**：fromParts 工厂函数包含未使用的类型参数 Q2，编译器会报类型参数未使用警告或错误
   - 所在位置：§3.3 第4条（第236行）
   - 严重程度：一般
   - 改进建议：删除 `<U, Q2>` 中的 Q2，或在签名/实现中实际使用 Q2
   - **修改结果**：**已采纳**。§3.3 第4条 fromParts 签名删除 `Q2` 类型参数，仅保留 `U`（a_v2_design_v1.md 修订说明 v14）

5. **问题描述**：@Derive[Hashable] 对泛型 T = Bool 的可行性未评估，若 Bool 未实现 Hashable 将导致编译错误
   - 所在位置：§3.1（第181行）
   - 严重程度：一般
   - 改进建议：验证 Bool 是否实现 Hashable，若否则添加 `T <: Hashable` 约束或在差异声明中注明
   - **修改结果**：**已采纳**。§3.1 补充 T=Bool 可行性验证，确认 Bool <: Hashable，D17 补充结论（a_v2_design_v1.md 修订说明 v14）

## 迭代第 2 轮

1. **问题描述**：复合赋值运算符自动生成假设未经验证，设计基于"仓颉编译器自动生成复合赋值运算符"的假设推导全套可用性表，但此假设在阶段一Vec代码中无实证支持，且参考的C++ GLM中所有复合赋值运算符均为显式定义
   - 所在位置：§3.5 复合赋值运算符段落（第423-432行）
   - 严重程度：严重
   - 改进建议：在编码启动前通过最小化测试验证仓颉编译器是否自动生成复合赋值运算符；若自动生成不成立，需在设计中显式列出全部复合赋值运算符
   - **修改结果**：**已采纳**。§3.5 补充仓颉语言文档引用和编码启动前验证项；若验证不成立则须显式列出全部复合赋值运算符（a_v3_design_v1.md 修订说明 v15）

2. **问题描述**：fromMat第7条与第6条参数顺序不一致——第6b条签名为`fromMat<SrcT, SrcQ>(conv, m, one)`（conv在前），第7条签名为`fromMat<U, P>(m, conv)`（源矩阵在前）
   - 所在位置：§3.3 第6b条（第266行）vs 第7条（第292行）
   - 严重程度：一般
   - 改进建议：统一参数顺序，建议统一为`fromMat(conv, m, ...)`模式（conv在前，源矩阵在后）
   - **修改结果**：**已采纳**。§3.3 第7条签名统一为 `fromMat<U, P>(conv: (U) -> T, m: Mat4x4<U, P>)`，与第6b条参数顺序一致（a_v3_design_v1.md 修订说明 v15）

3. **问题描述**：fromParts和fromColumns定义在带Number\<T\>约束的extend块中，但实现逻辑仅涉及conv闭包调用和赋值操作，不依赖Number\<T\>提供的任何运算，导致对非Number类型不可用
   - 所在位置：§3.3 第4条fromParts（第238行）、第5条fromColumns（第245行）
   - 严重程度：一般
   - 改进建议：将fromParts和fromColumns移出Number\<T\>约束的extend块，放入单独的无约束extend块或struct体内
   - **修改结果**：**已采纳**。§3.3 第4/5条补充"定义位置"说明，应定义在仅带 Qualifier 约束的 extend 块中（a_v3_design_v1.md 修订说明 v15）

4. **问题描述**：非方阵上identity(one)等价于filled(one)，但设计未明确编码阶段如何避免重复实现——若各自独立实现则6种非方阵类型产生12个内部逻辑完全相同的函数，浪费编码工作量且产生维护隐患
   - 所在位置：§3.3 第8条（第305行）、D31（第768行）
   - 严重程度：一般
   - 改进建议：在设计中明确非方阵identity的实现策略——推荐方案为非方阵的identity(one)内部委托filled(one)，或统一为非方阵仅提供filled(one)而不提供identity
   - **修改结果**：**已采纳**。非方阵 identity(one) 内部委托 filled(one)，方阵保持独立实现（a_v3_design_v1.md 修订说明 v15）

## 迭代第 3 轮

1. **问题描述**：§3.3第7条跨类型同尺寸转换工厂函数签名定义为 `fromMat<U, P>(conv: (U) -> T, m: Mat4x4<U, P>)`（conv为首个位置参数），但§4.5行为契约中调用方式为 `Mat4x4<Float32, PackedHighp>.fromMat(mInt, conv: { x => Float32(x) })`，实参位置与签名不匹配，导致编译错误。
   - 所在位置：§3.3第7条（签名声明） vs §4.5（行为契约使用示例）
   - 严重程度：一般
   - 改进建议：将§4.5的使用示例修正为与签名一致，即 `fromMat({ x => Float32(x) }, mInt)`，并检查全文中其他使用fromMat第7条的调用示例是否也存在类似问题。
   - **修改结果**：**已采纳**。§4.5 调用示例修正为 `fromMat({ x => Float32(x) }, mInt)`，全文检查无其他同类问题（a_v4_design_v1.md 修订说明 v16）

## 迭代第 4 轮

1. **问题描述**：D08 及 §9 中 ++/-- 替代方案 `m += T(1)` 与 T(1) 约束矛盾，identity() 修复后同步遗漏
   - 所在位置：第 753 行（D08）、第 837 行（§9 差异声明）
   - 严重程度：一般
   - 改进建议：将 `m += T(1)` 修正为具体类型上下文使用字面量（如 `m += 1.0f`），或泛型上下文通过参数传递 `one: T` 后使用 `m += one`
   - **修改结果**：**已采纳**。将 `m += T(1)` 修正为具体类型上下文使用字面量方案，泛型上下文通过参数传递代替。

2. **问题描述**：§9 差异声明缺失模板赋值运算符 `operator=` 的差异条目，跨类型同尺寸矩阵赋值不可用
   - 所在位置：§9 差异声明（第 831-858 行）、§3.5 运算符清单
   - 严重程度：一般
   - 改进建议：在 §9 中新增条目，说明跨类型同尺寸矩阵赋值不可用，推荐通过 `fromMat(conv, m)` 或显式逐元素转换替代
   - **修改结果**：**已采纳**。§9 新增模板赋值运算符差异条目，说明跨类型同尺寸赋值不可用及推荐替代方案。

3. **问题描述**：fromMat 工厂函数签名使用伪代码 `Mat{C_src}x{R_src}`，缺少具体的重载清单及填充规则分类
   - 所在位置：§3.3 第 6a 条（第 257-280 行）、第 6b 条（第 268-274 行）
   - 严重程度：一般
   - 改进建议：提供完整的目标矩阵类型 → 8 个源矩阵类型映射表，标注每个方向的填充规则类别
   - **修改结果**：**已采纳**。补充目标→源矩阵映射表，按方向类别标注填充规则。

## 迭代第 5 轮

1. **问题描述**：cjpm子包构建风险缺乏充分应对方案——将ext/文件置于src/根目录并声明package glm的备选方案未做冲突风险评估（矩阵别名与detail包泛型类型可能产生名称冲突），且该方案可行性未经验证
   - 所在位置：§2 "cjpm子包构建预验证"（第160-167行）
   - 严重程度：一般
   - 改进建议：编码前用小规模原型验证cjpm的子包发现机制；备选方案需提前做冲突风险评估；若备选方案也不可行，需有第三方案
   - **修改结果**：**已采纳**。预验证段落扩展为三方案递进体系（package glm.ext / src/根目录 / 独立子模块），补充冲突风险分析表和原型验证步骤。

2. **问题描述**：B类fromMat转换方向的行为契约不完整——9个B类方向中仅Mat4x4←Mat4x2给出了具体的GLM偏差描述，其余8个仅标注"需核对GLM .inl"，未提供行为描述或偏差分析
   - 所在位置：§3.3第6条 "fromMat目标→源矩阵类型映射表"（第292-310行）
   - 严重程度：一般
   - 改进建议：为每个B类方向补充GLM .inl的实际行为摘要或文件行号范围；或至少说明B类方向是否与Mat4x4←Mat4x2的偏差模式一致
   - **修改结果**：**已采纳**。逐一查阅 GLM 1.0.3 全部 9 个 .inl 转换构造源码，形成逐项对照分析表；发现 9 个 B 类方向中 8 个与主规则一致，仅 Mat4x4←Mat4x2 存在偏差。

## 迭代第 6 轮

1. **问题描述**：fwd.cj 矩阵类型别名新增的具体内容未定义，缺少按元素类型家族和精度变体的矩阵别名完整清单
   - 所在位置：§2 包组织（fwd.cj ★ 条目）、§8 产出物清单
   - 严重程度：一般
   - 改进建议：明确列出 fwd.cj 中需要新增的所有矩阵类型别名，格式参考现有向量别名模式
   - **修改结果**：**已采纳**。§8 新增"fwd.cj 矩阵类型别名新增清单"子节，按 8 个元素类型家族分组枚举全部 108 个新增别名。

2. **问题描述**：stub 文件缺少函数签名清单，编码者无法确定每个 stub 应包含哪些函数
   - 所在位置：§3.7 stub 函数库
   - 严重程度：一般
   - 改进建议：在每个 stub 文件描述后附上完整函数签名清单，可参考 docs/04_ood_phase2.md §3.7
   - **修改结果**：**已采纳**。§3.7 扩展为完整函数签名清单：common.cj 12 个标量函数、matrix.cj 33 个签名、geometric.cj 20 个签名。

3. **问题描述**：Vec 类型 extend 块中行向量×矩阵乘法运算符的具体签名未定义，每个 Vec{R} 类型实际需要与哪些 Mat{C}x{R} 类型配对不明确
   - 所在位置：§2 包组织（type_vecN.cj 条目）、§3.5 算术运算符
   - 严重程度：一般
   - 改进建议：按 Vec1/Vec2/Vec3/Vec4 逐一列出每个类型需要新增的 operator * 签名
   - **修改结果**：**已采纳**。§2 包组织中 type_vecN.cj 条目扩展为按 Vec1~Vec4 逐一列出每个类型的 operator * 签名。

## 迭代第 7 轮

1. **问题描述**：矩阵-列向量乘法 Mat*Vec 运算符签名完全缺失
   - 所在位置：§3.5 算术运算符（矩阵-向量 *）、§2 包组织（type_vecN.cj 条目）
   - 严重程度：严重
   - 改进建议：补充 9 个矩阵类型的 `operator*(v: VecR<T,Q>): VecC<T,Q>` 成员运算符签名
   - **修改结果**：**已采纳**。§2 包组织中新增"矩阵-列向量乘法 Mat×Vec"子节，列出全部 9 个矩阵类型的成员运算符签名（a_v8_design_v1.md 相关修订）。

2. **问题描述**：矩阵-矩阵乘法重载数量声称 29 个与实际 27 个不符
   - 所在位置：§3.5 算术运算符
   - 严重程度：中等
   - 改进建议：修正数量为 27（3+24），或提供 29 个的具体枚举
   - **修改结果**：**已采纳**。§3.5 修正为"含 27 个乘法重载：3 同尺寸 + 24 跨尺寸"。

3. **问题描述**：手动定义数量推导中出现算术描述错误（6 标量运算符应为 4，54 应为 36）
   - 所在位置：§3.5 算术运算符，复合赋值运算符手动定义回退方案文件分布表之后
   - 严重程度：中等
   - 改进建议：将"6 标量运算运算符"修正为"4 标量运算运算符"，修正相关算术推导过程
   - **修改结果**：**已采纳**。§3.5 手动定义总计说明修正为"含 4 标量运算运算符"，相关推导过程同步修正。

## 迭代第 8 轮

1. **问题描述**：§8 编译顺序分析依赖未经验证的编译器行为假设（"延迟解析"声明无文档引用或实验验证）
   - 所在位置：§8 "compile order 说明"（约 L448-459）
   - 严重程度：一般
   - 改进建议：标注为"待验证"，编码首日进行最小原型验证；若失败则提供 fallback 方案
   - **修改结果**：**已采纳**。将"延迟解析"声明改为"【待验证】"标注；补充三档递进 fallback 方案：方案一（全局自由函数）、方案二（矩阵文件末尾追加 Vec extend 块）、方案三（独立文件 + 编译顺序保证）。

2. **问题描述**：§3.3 fromParts/fromColumns 工厂函数签名完全缺失（无参数个数、类型、conv 闭包作用范围、返回值等）
   - 所在位置：§3.3 items 4, 5
   - 严重程度：一般
   - 改进建议：为每个矩阵类型（或至少按列数分类）提供 fromParts 和 fromColumns 的完整函数签名示例
   - **修改结果**：**已采纳**。§3.3 第 4 条（fromParts）扩展为按矩阵列数分族的签名示例表；第 5 条（fromColumns）扩展为全部 9 种矩阵类型的完整签名表。

3. **问题描述**：fromMat 6a/6b/7 工厂函数缺少完整泛型约束声明（无 where 约束子句）
   - 所在位置：§3.3 items 6a, 6b, 7；§4.3-4.5 仅给出使用示例
   - 严重程度：一般
   - 改进建议：为 fromMat 6a、6b、7 各提供一个完整的泛型函数签名示例，包含 where 约束子句
   - **修改结果**：**已采纳**。§3.3 第 6 条拆分为 6a/6b 两个子条，各提供完整泛型签名示例（含 where 子句）；第 7 条提供完整签名示例。

## 迭代第 9 轮

1. **问题描述**：填充规则描述"扩展列/行按单位矩阵填充"未区分列扩展与行扩展的差异，行扩展应填充T(0)而非单位矩阵值
   - 所在位置：§3.3 item 6 (L225)
   - 严重程度：一般
   - 改进建议：将填充规则拆分为列扩展（单位矩阵填充）和行扩展（填充T(0)）两个明确表述
   - **修改结果**：**已采纳**。§3.3 item 6 填充规则拆分为"列扩展规则""行扩展规则""列收缩/行收缩规则"三个明确子规则。

2. **问题描述**：非方阵identity(one)的语义未定义，非方阵上"单位矩阵"非数学标准概念
   - 所在位置：§3.3 item 8 (L239)、§9 (L552)
   - 严重程度：一般
   - 改进建议：补充非方阵identity semantics说明段，明确对角矩阵行为
   - **修改结果**：**已采纳**。§3.3 item 8 扩充为方阵/非方阵两段行为定义，明确非方阵主对角线填入 one、其余 T(0)，附 Mat2x4 示例。

3. **问题描述**：fromMat非B类方向（C和R同时变化）的行为未与GLM对照验证，已验证的Mat4x4←Mat4x2偏差表明GLM并非统一规则
   - 所在位置：§3.3 B类方向对照分析表 (L241-259)
   - 严重程度：一般
   - 改进建议：补充至少3组代表性验证方向（C和R同时扩展、C收缩R扩展、C扩展R收缩）
   - **修改结果**：**已采纳**。新增"非 B 类方向对照分析"表格，选取 3 组代表性方向进行 GLM 对照验证。

4. **问题描述**：§2中matrix.cj的"stub"标签与§3.7中附带提供的33个函数签名（其中27个可直接实现）自相矛盾
   - 所在位置：§2包组织matrix.cj条目 (L57) vs §3.7函数签名清单 (L322-364)
   - 严重程度：一般
   - 改进建议：在§2中区分直接实现的函数（transpose、matrixCompMult、outerProduct）与stub空壳（determinant、inverse）
   - **修改结果**：**已采纳**。§2 中 matrix.cj 条目拆分为"直接实现"和"stub 空壳"两类标注。

## 迭代第 10 轮

1. **问题描述**：`fromParts`家族签名模板展示16个分量参数，但注释说明不同R值需要不同参数数量（R=2需8个、R=3需12个、R=4需16个），仓颉不支持条件编译参数数量，该模板信息矛盾无法直接编码。Mat2xR族签名模板使用了非法的条件简写语法。
   - 所在位置：§3.3 item 4，Mat4xR 族签名示例
   - 严重程度：一般
   - 改进建议：参考§3.3 item 5 `fromColumns`的做法，逐一列出9个矩阵类型的独立签名，确保参数数量与实际列数×行数一致；或至少在家族模板后补充说明"每个矩阵类型需要独立定义签名，参数数量为 C×R"。
   - **修改结果**：**已采纳**。§3.3 item 4 将 3 族家族模板替换为 9 个矩阵类型的独立签名表，逐一列出列主序排列的完整参数序列（a_v11_design_v1.md 修订说明）。

## 迭代第 11 轮

1. **问题描述**：fromParts签名表中Mat2x2/Mat2x3/Mat2x4/Mat3x2/Mat3x3/Mat3x4的参数数量与C×R不匹配
   - 所在位置：§3.3 item 4 fromParts签名表格
   - 严重程度：严重
   - 改进建议：补齐所有缺失参数，使每个签名参数数量等于C×R
   - **修改结果**：**已采纳**。补齐缺失参数，使每个签名参数数量等于 C×R（§3.3 item 4 签名表格已重写）。

2. **问题描述**：Mat3x3←Mat3x2和Mat3x4←Mat3x2被标记为"一致"，但GLM在(2,2)位置填入1，与主规则"新增行全填T(0)"相悖
   - 所在位置：§3.3 B类方向对照分析表
   - 严重程度：严重
   - 改进建议：①修正主规则表述，明确对角线位置填入one；②重新标记一致性
   - **修改结果**：**已采纳**。修正主规则表述，明确对角线位置填入 one；重新标记一致性。

3. **问题描述**：fromParts参数命名`a{列}{行}`与GLM的`{x,y,z,w}{列}`映射关系未说明
   - 所在位置：§3.3 item 4参数命名约定
   - 严重程度：一般
   - 改进建议：补充命名映射关系说明或转换示例
   - **修改结果**：**已采纳**。补充命名映射关系说明。

4. **问题描述**：fromMat 6a签名缺失SrcQ参数，跨Qualifier转换被迫降级到6b
   - 所在位置：§3.3 item 6a签名与§9 fromMat条目
   - 严重程度：一般
   - 改进建议：评估是否恢复SrcQ泛型参数，或强化§9中的影响说明
   - **修改结果**：**已采纳**。评估后维持当前设计，强化 §9 中影响说明。

5. **问题描述**：复合赋值运算符回退方案注释将"非方阵不定义*=(Mat)和/=(Mat)"可能被误读为标量`*=`也被排除
   - 所在位置：§3.5 手动定义回退方案注释
   - 严重程度：一般
   - 改进建议：明确区分矩阵乘除与标量乘除的排除范围
   - **修改结果**：**已采纳**。明确区分矩阵乘除与标量乘除的排除范围。

6. **问题描述**：63个非B类方向验证任务无优先级排序
   - 所在位置：§3.3编码阶段验证要求（L292-293）
   - 严重程度：一般
   - 改进建议：按偏差风险分组（高/中/低），优先标注高风险方向
   - **修改结果**：**已采纳**。按偏差风险分组并优先标注高风险方向。

7. **问题描述**：compile order方案二分散定义导致Vec扩展代码重复，维护成本未评估
   - 所在位置：§8 fallback方案（L542-543）
   - 严重程度：一般
   - 改进建议：评估代码重复量，倾向采用方案三独立文件
   - **修改结果**：**已采纳**。评估代码重复量，倾向采用方案三独立文件。

8. **问题描述**：§2包组织图中scalar_mat_ops.cj条目未标注@OverflowWrapping要求
   - 所在位置：§2包组织图（L59-61）
   - 严重程度：一般
   - 改进建议：补充@OverflowWrapping标注要求
   - **修改结果**：**已采纳**。补充 @OverflowWrapping 标注要求。

## 迭代第 12 轮

1. **问题描述**：行向量×矩阵乘法重载数量声称与实际不符。§8 方案一和方案三中声称有"12 个重载"，但根据 §2 包组织 type_vecN.cj 条目中列出的完整签名清单，实际应为 9 个（Vec4 3 个、Vec3 3 个、Vec2 3 个、Vec1 0 个）。
   - 所在位置：§8 "编译顺序说明"子节，方案一描述（第 556 行）、方案三描述（第 561 行）
   - 严重程度：一般
   - 改进建议：将"12 个重载"修正为"9 个重载"。同时检查方案三中"全部 12 个 Vec extend 块"是否应同步修正为"全部 9 个 Vec extend 块"。
   - **修改结果**：**已采纳**。将"12 个重载"修正为"9 个重载"，方案三中同步修正。

## 迭代第 13 轮

1. **问题描述**：§2 包组织图中Mat×Vec运算符签名表被放置在`type_vecN.cj`条目下，与实际矩阵类型成员运算符定义矛盾
   - 所在位置：§2 包组织，L62-88
   - 严重程度：严重
   - 改进建议：将Mat×Vec签名表移出`type_vecN.cj`条目，归入`type_mat{N}x{M}.cj`家族条目或新增独立条目说明
   - **修改结果**：**已采纳**。将 Mat×Vec 签名表移出 type_vecN.cj 条目，归入独立说明。

2. **问题描述**：§3.3 items 1-2（逐分量构造和列向量构造）仅有一句话描述，未提供函数签名、参数数量/顺序/类型等核心信息
   - 所在位置：§3.3，L196-199
   - 严重程度：严重
   - 改进建议：参照items 4-5的表格式，为9个矩阵类型逐一列出完整签名
   - **修改结果**：**已采纳**。参照 items 4-5 的表格式，为 9 个矩阵类型逐一列出完整签名。

3. **问题描述**：§5 错误处理策略仅标注复合赋值运算符和全局函数需`@OverflowWrapping`，遗漏常规算术运算符
   - 所在位置：§5 错误处理策略，L499-506
   - 严重程度：一般
   - 改进建议：补充常规算术运算符溢出策略声明，或确认有意保持默认行为后在§9说明
   - **修改结果**：**已采纳**。补充常规算术运算符溢出策略声明。

4. **问题描述**：§8 fallback方案标签优先级（一>二>三）与文本描述（推荐方案三优于方案二）矛盾
   - 所在位置：§8 "compile order 说明" fallback子节，L554-561
   - 严重程度：一般
   - 改进建议：统一标签优先级与文本描述，选择一个清晰一致的优先级排序
   - **修改结果**：**已采纳**。统一标签优先级与文本描述。

## 迭代第 14 轮

1. **问题描述**：fromMat 72 个转换方向的实现策略不充分，编码者无法直接落地。60个方向仅按风险等级分组，既无通用算法描述也无具体行为规范。该问题已在多轮审查中反复提出，至今未被充分解决。
   - 所在位置：§3.3 item 6a/6b（L272-341）
   - 严重程度：严重
   - 改进建议：提供至少一种完备性方案：(a) 给出完整的列扩展/行扩展/列收缩/行收缩的数学定义，证明该规则可机械地应用于所有72个方向；(b) 在OOD中补充一张9×8的fromMat转换矩阵表，标注每个方向的填充规则和GLM偏差情况；(c) 明确说明采用match运行时分支的实现策略并给出原型验证计划。
   - **修改结果**：**已采纳**。采纳方案(b)：补充 9×8 fromMat 转换矩阵表，标注每个方向的填充规则和 GLM 偏差情况。

2. **问题描述**：矩阵-矩阵跨尺寸乘法（24个重载）签名完全缺失。仅给出了Mat×Vec和Vec×Mat的完整签名表，完全未给出Mat×Mat跨尺寸乘法的函数签名。同样缺失跨尺寸乘法的结果类型推导规则。该问题在v7、v13审查中已提出，尚未被解决。
   - 所在位置：§3.5 算术运算符（L355-358）
   - 严重程度：严重
   - 改进建议：提供完整的Mat×Mat乘法重载签名表（至少以列表形式覆盖全部24+3=27个重载），明确标注结果类型的推导规则（左矩阵列数=右矩阵行数）。
   - **修改结果**：**已采纳**。提供完整的 Mat×Mat 乘法重载签名表，覆盖全部 27 个重载，标注结果类型推导规则。

3. **问题描述**：§3.7 transpose直接实现说明中示例方向写反，代码示例与文本描述不一致。文本声称Mat2x3→Mat3x2，但代码示例实际演示的是Mat3x2→Mat2x3的行为。编码者若按此实现将产生编译错误。
   - 所在位置：§3.7 matrix.cj 直接实现说明（L456）
   - 严重程度：严重
   - 改进建议：修正方向标签或代码示例，使二者一致。两种改法择一即可。
   - **修改结果**：**已采纳**。修正方向标签与代码示例一致。

4. **问题描述**：`filled(scalar)`名称强烈暗示全填充矩阵，但实际行为是对角矩阵。使用`filled`名称会造成编码者的认知偏差。
   - 所在位置：§3.3 item 3（L231-236）
   - 严重程度：中等
   - 改进建议：改名为`diagonal(scalar)`、`scale(scalar)`或`fromScalar(scalar)`以消除歧义。若坚持保留`filled`名称，应在其文档注释中显式标注警告。
   - **修改结果**：**已采纳**。保留 `filled` 名称，在文档注释中显式标注警告。

5. **问题描述**：§8将方案一（API破坏性最大）标为"首选"，但方案二（独立文件，保留运算符语法，不破坏API）明显更优。"首选"标签与其API破坏性特征矛盾。
   - 所在位置：§8 compile order fallback 方案（L594-601），修订说明第4项（L683）
   - 严重程度：中等
   - 改进建议：将方案一降级为"第三备选"，方案二升级为"首选"。
   - **修改结果**：**已采纳**。将方案一降级为"第三备选"，方案二升级为"首选"。

6. **问题描述**：方案一建议的自由函数名`mul_row_vec_mat`采用snake_case命名，与现有代码库中所有函数的camelCase风格不一致。
   - 所在位置：§8 方案一描述（L594）
   - 严重程度：中等
   - 改进建议：将函数名改为`mulRowVecMat`以匹配项目camelCase风格，或采用`matRowMul`/`rowMul`等更简洁的命名。
   - **修改结果**：**已采纳**。函数名改为 `mulRowVecMat`。

## 迭代第 15 轮

1. **问题描述**：fromMat 9×8 转换矩阵表存在多处标签错误（11个单元格），包括误标 rowShrink、C: 前缀错误、操作顺序颠倒、Mat2x2→Mat4x4 ⛔ 误标等
   - 所在位置：§3.3，L350-360
   - 严重程度：严重
   - 改进建议：逐方向修正标签，统一遵循先列后行顺序，区分行扩展与行收缩符号
   - **修改结果**：**已采纳**。重写整个 9×8 转换矩阵表，逐一核验 72 个方向的维度变化（a_v16_design_v1.md 修订说明 v27）。

2. **问题描述**：fromMat 执行顺序规定（先列后行）与实际表格标注不一致，多处方向被标注为 rowSh→colSh
   - 所在位置：§3.3 L341-344 vs L350-360
   - 严重程度：中等
   - 改进建议：统一所有方向标注均遵循先列后行顺序
   - **修改结果**：**已采纳**。重写转换矩阵表，所有方向标注遵循"先列后行"顺序（a_v16_design_v1.md 修订说明 v27）。

3. **问题描述**：transpose 函数使用了不必要的 Number<T> 约束，排除了 Bool 矩阵转置
   - 所在位置：§3.7，L483-491
   - 严重程度：中等
   - 改进建议：将约束放松为 where Q <: Qualifier
   - **修改结果**：**已采纳**。§3.7 transpose/matrixCompMult/outerProduct 约束从 `T <: Number<T>, Q <: Qualifier` 统一放松为 `Q <: Qualifier`（a_v16_design_v1.md 修订说明 v27）。

4. **问题描述**：C→R→→ 标注对行收缩方向有误导性，图例 C→R→→ 解释为行扩展
   - 所在位置：§3.3 转换矩阵表 L352-358 及图例 L362-363
   - 严重程度：中等
   - 改进建议：图例中补充行收缩符号，表中区分行扩展与行收缩
   - **修改结果**：**已采纳**。改用精确符号 `rowExt`（行扩展）、`rowSh`（行收缩）替代原统一符号（a_v16_design_v1.md 修订说明 v27）。

5. **问题描述**：common.cj stub mod 签名 Number<T> 约束与实际实现需求不匹配
   - 所在位置：§3.7 L471
   - 严重程度：一般
   - 改进建议：添加注释说明签名为暂定，最终需拆分为 Integer<T> 和浮点重载
   - **修改结果**：**已采纳**。§3.7 common.cj mod 签名下方补充注释说明为暂定（a_v16_design_v1.md 修订说明 v27）。

6. **问题描述**：fromMat 6a one: T 参数在跨 Qualifier 方向上的语义未明确
   - 所在位置：§3.3 item 6a L272-276
   - 严重程度：一般
   - 改进建议：补充说明 one 为目标元素类型 T
   - **修改结果**：**已采纳**。§3.3 item 6a 新增 one: T 参数语义说明段落（a_v16_design_v1.md 修订说明 v27）。

7. **问题描述**：矩阵乘法 operator * 未声明 @OverflowWrapping，需对照 §5 才能确认
   - 所在位置：§3.5 L390-422；§5 L598-610
   - 严重程度：一般
   - 改进建议：在签名表上方或图例中明确标注
   - **修改结果**：**已采纳**。§3.5 乘法签名表上方增加标注"所有乘法运算符标注 @OverflowWrapping"（a_v16_design_v1.md 修订说明 v27）。

8. **问题描述**：144 个 fromMat 6a/6b 签名构造量未作为实施风险明确标注
   - 所在位置：§3.3 item 6（L267-370）；§8 L650
   - 严重程度：一般
   - 改进建议：统计签名总数并建议代码生成工具
   - **修改结果**：**已采纳**。§3.3 item 6 末尾补充签名数量统计并建议代码生成工具（a_v16_design_v1.md 修订说明 v27）。

9. **问题描述**：diagonal() 工厂函数缺少 Bool 矩阵不支持的标注，D33 遗漏
   - 所在位置：§3.3 L236；§9 D33 L633
   - 严重程度：一般
   - 改进建议：D33 中显式补充 diagonal() 和 identity()
   - **修改结果**：**已采纳**。D33 扩展 Bool 矩阵不支持列表，包含 diagonal() 和 identity()（a_v16_design_v1.md 修订说明 v27）。

10. **问题描述**：v * m 退化为 mulRowVecMat(v, m) 的迁移影响未评估
   - 所在位置：§8 fallback 方案说明 L658-673
   - 严重程度：一般
   - 改进建议：补充方案升降级判定标准和迁移路径说明
   - **修改结果**：**已采纳**。§8 fallback 方案一的 API 破坏性说明中补充迁移影响评估（a_v16_design_v1.md 修订说明 v27）。

## 迭代第 16 轮

1. **问题描述**：matrixCompMult和outerProduct的泛型约束从`where T <: Number<T>, Q <: Qualifier`错误地放松为`where Q <: Qualifier`，但这两个函数实现中涉及乘法运算，需要Number<T>约束
   - 所在位置：§3.7，L540-542（约束放松说明段）；L519-538（matrixCompMult/outerProduct签名）
   - 严重程度：严重
   - 改进建议：将matrixCompMult和outerProduct的泛型约束恢复为`where T <: Number<T>, Q <: Qualifier`，transpose可保留`where Q <: Qualifier`，约束放松说明段同步修正
   - **修改结果**：**已采纳**。恢复 matrixCompMult（9 个重载）和 outerProduct（9 个重载）的约束为 `where T <: Number<T>, Q <: Qualifier`，transpose 保留 `where Q <: Qualifier`（a_v17_design_v1.md 修订说明 v28）。

2. **问题描述**：fromMat 9×8转换矩阵表中Mat4x3→Mat4x4被错误标注为"EQL"，实际R_src=3≠R_dst=4，正确标签应为"B: rowExt(4)"
   - 所在位置：§3.3，L369-378，Mat4x3行、→Mat4x4列
   - 严重程度：严重
   - 改进建议：将Mat4x3→Mat4x4标签从"EQL"修正为"B: rowExt(4)"，并逐格复核所有非对角线单元格
   - **修改结果**：**已采纳**。将 Mat4x3→Mat4x4 标签从"EQL"修正为"B: rowExt(4)"（a_v17_design_v1.md 修订说明 v28）。

3. **问题描述**：设计文档未提供按GLM 1.0.3头文件组织的API覆盖度对照表，无法判断阶段二API覆盖完整性
   - 所在位置：全篇（§1-§9）
   - 严重程度：中等
   - 改进建议：新增独立章节"GLM 1.0.3 API阶段覆盖矩阵"，按GLM头文件路径组织，标注函数名、阶段覆盖状态、对应设计方案章节
   - **修改结果**：**已采纳**。新增 §10 "GLM 1.0.3 API 阶段覆盖矩阵"（a_v17_design_v1.md 修订说明 v28）。

4. **问题描述**：scalar_mat_ops.cj全局函数（add/sub/mul/div）的完整签名模板完全缺失，实现者无法获得泛型约束、参数顺序、返回值类型等关键信息
   - 所在位置：§2 L74-76；§3.5 L407-408
   - 严重程度：中等
   - 改进建议：在§3.5中提供至少一个代表性签名模板，明确泛型约束（需要Number<T>）、参数顺序（scalar左、matrix右）、返回值类型、@OverflowWrapping标注
   - **修改结果**：**已采纳**。§3.5 末尾新增"scalar_mat_ops.cj 全局函数签名模板"段落，提供 add 的代表性签名模板（a_v17_design_v1.md 修订说明 v28）。

5. **问题描述**：col()的越界行为未明确声明，虽声明与`[]`等价但未包含行为契约的一致性确认
   - 所在位置：§3.4，L400-403
   - 严重程度：一般
   - 改进建议：在col()描述后补充"下标越界时抛出Exception，与`[]`行为一致"
   - **修改结果**：**已采纳**。§3.4 col() 描述后补充"下标越界时抛出 Exception，与 [] 行为一致"（a_v17_design_v1.md 修订说明 v28）。

## 迭代第 17 轮

1. **问题描述**：NaN比较语义描述存在事实错误——文档声称"任何涉及NaN的比较结果为false"，但IEEE 754标准下`NaN != NaN`返回true而非false。阶段一现有实现已验证`!=`正确返回true。
   - 所在位置：§3.6，L483
   - 严重程度：严重
   - 改进建议：将L483的"任何涉及NaN的比较结果为false"修正为精确表述，区分`==`/`equalExact`（涉及NaN时返回false）与`!=`（涉及NaN时返回true），或直接引用IEEE 754标准语义。
   - **修改结果**：**已采纳**。将 L483 NaN 比较行为描述修正为精确表述，区分 ==/equalExact 与 != 的 NaN 行为（a_v18_design_v1.md 相关修订）。

2. **问题描述**：identity()适用范围描述与约束说明自相矛盾——L290声称"对所有9种矩阵类型均提供"，但L303注释指出"Bool矩阵不提供此操作"。
   - 所在位置：§3.3 item 8，L290与L303
   - 严重程度：一般
   - 改进建议：将L290的"对所有9种矩阵类型均提供"明确限定范围，排除Bool元素类型，消除与L303的显式矛盾。
   - **修改结果**：**已采纳**。L290 正文修正为"对所有 9 种尺寸的矩阵类型均提供（Bool 元素类型除外，见 D33）"（a_v18_design_v1.md 相关修订）。

3. **问题描述**：复合赋值`/=(Mat)`在本阶段的运行时行为未澄清——复合赋值回退方案表（L449-457）中将方阵的`/=(Mat)`列为"手动定义"运算符之一，但§5（L636）明确指出mat/mat除法依赖inverse stub且本阶段stub异常，实现者可能误认为`/=(Mat)`可在此阶段正常使用。
   - 所在位置：§3.5复合赋值回退方案表（L449-457）、§5错误处理策略（L636）
   - 严重程度：一般
   - 改进建议：在复合赋值回退方案表下方补充注释，明确方阵的`/=(Mat)`和`*=(Mat)`在此阶段会抛出stub异常，在相关条目旁标注"（依赖inverse stub，本阶段不可用）"。
   - **修改结果**：**已采纳**。复合赋值回退方案表下方新增注释，明确方阵 `*=(Mat)` 和 `/=(Mat)` 依赖 inverse stub，本阶段不可用（a_v18_design_v1.md 相关修订）。

## 迭代第 18 轮

1. **问题描述**：fromMat 9×8 转换矩阵表缺少 Mat4x3 目标列，导致 9 个方向的填充模式标注完全遗漏
   - 所在位置：§3.3 fromMat 6a 9×8 转换矩阵表（L368-378）
   - 严重程度：严重
   - 改进建议：将 9×8 表扩展为完整的 9×9 表（含 →Mat4x3 目标列），逐行标注 9 个源矩阵到 Mat4x3 的填充模式；同步修正 item 6 正文中的算式
   - **修改结果**：**已采纳**。将 9×8 表扩展为完整的 9×9 表，新增 →Mat4x3 列（a_v19_design_v1.md 相关修订）。

2. **问题描述**：fromColumns 函数签名均省略了 where 约束子句，与其他条目的编写方式不一致
   - 所在位置：§3.3 item 5（L253-265）
   - 严重程度：中等
   - 改进建议：在 9 个签名示例中至少写出第一个（如 Mat2x2）的完整签名含 where 子句，其余 8 个可复用"（约束同前）"标注
   - **修改结果**：**已采纳**。首行 Mat2x2 签名写出完整 where 子句，其余 8 个标注"（约束同前）"（a_v19_design_v1.md 相关修订）。

3. **问题描述**：复合赋值手动定义表格未区分"可用手动定义"与"抛异常手动定义"，总计数字 60 未拆分
   - 所在位置：§3.5 复合赋值手动定义回退方案文件分布表（L444-460）
   - 严重程度：中等
   - 改进建议：在表格中对 *=(Mat) 和 /=(Mat) 条目添加标注"† 依赖 inverse stub，本阶段不可用"；总计数字拆分为"54 个可用 + 6 个 stub"
   - **修改结果**：**已采纳**。表格新增"可用数"列，对方阵条目追加 † 标注，总计数字拆分为"54 个可用 + 6 个 stub"（a_v19_design_v1.md 相关修订）。

4. **问题描述**：§9 差异声明 D18 未同步标注 Bool 矩阵排除
   - 所在位置：§9 差异声明 D18（L766）
   - 严重程度：一般
   - 改进建议：在 D18 条目末尾追加"(Bool 矩阵不提供此操作，见 D33)"
   - **修改结果**：**已采纳**。D18 条目末尾追加"Bool 矩阵不提供此操作（见 D33）"（a_v19_design_v1.md 相关修订）。

5. **问题描述**：§8 compile order fallback 方案中 stub 文件被归入"辅助文件"最后编译，与 §2 模块依赖存在时序冲突
   - 所在位置：§8 文件组织顺序建议（L706-711）、§2 模块间依赖（L130）
   - 严重程度：一般
   - 改进建议：将 stub 文件细化为"stub 文件（需在其他引用者之前编译）"并调整至矩阵类型文件之前或之间
   - **修改结果**：**已采纳**。编译顺序调整为：基础类型文件 → stub 文件 → 矩阵类型文件 → Vec extend 块 → 其余辅助文件（a_v19_design_v1.md 相关修订）。

6. **问题描述**：fromMat 7 的模板化编码策略未说明 9 个重载的签名生成方式
   - 所在位置：§3.3 item 7（L286-288）
   - 严重程度：一般
   - 改进建议：补充说明 fromMat 7 共 9 个重载（每个矩阵类型一版），或像 item 4/5 那样以表格式逐一列出
   - **修改结果**：**已采纳**。补充说明 fromMat 7 共 9 个重载，以 Mat4x4 为完整示例（a_v19_design_v1.md 相关修订）。

7. **问题描述**：fromMat 规则化编码策略缺少 R_src 的获取方式说明
   - 所在位置：§3.3 fromMat 6a 规则化的编码策略段（L382-387）
   - 严重程度：一般
   - 改进建议：补充说明 R_src 和 R_dst 需在每个 fromMat 实现中硬编码（编译期已知常量），或增加静态 rows() 函数
   - **修改结果**：**已采纳**。新增"维度信息获取方式"段落，明确 R_src/R_dst 为编译期已知常量（a_v19_design_v1.md 相关修订）。

8. **问题描述**：fromMat 6a/6b 的 one: T 参数在纯收缩方向存在语义冗余
   - 所在位置：§3.3 fromMat 6a/6b（L267-284）
   - 严重程度：一般
   - 改进建议：拆分为含扩展方向和纯收缩方向两个 API，或在签名注释中明确标注"纯收缩方向下 one 参数被忽略"
   - **修改结果**：**已采纳**。纯收缩方向下 one 参数被忽略，但签名中保留以保持 API 统一性（a_v19_design_v1.md 相关修订）。

9. **问题描述**：§10 API 覆盖矩阵中 trunc/round/roundEven 的覆盖状态标注与 §3.7 不一致
   - 所在位置：§10 GLM 1.0.3 API 阶段覆盖矩阵（L802）
   - 严重程度：一般
   - 改进建议：将 trunc/round/roundEven 从"本阶段 stub（标量部分）"行中移出，单独列为"后续阶段"条目
   - **修改结果**：**已采纳**。将 trunc/round/roundEven 从"本阶段 stub"行移出，单独列为"后续阶段"条目（a_v19_design_v1.md 相关修订）。

## 迭代第 19 轮

1. **问题描述**：矩阵乘法运算符签名缺少泛型约束声明，§3.5 的 27 个运算符签名仅以类型映射表呈现，未提供 `where T <: Number<T>, Q <: Qualifier` 约束，与其他章节（§3.7、§5）形成对比，实现者无法直接获知应使用什么约束
   - 所在位置：§3.5，L418-448
   - 严重程度：严重
   - 改进建议：在签名表上方或图例中补充泛型签名模板，明确列出 `where T <: Number<T>, Q <: Qualifier`
   - **修改结果**：**已采纳**。在 §3.5 乘法签名表上方补充泛型声明："所有乘法重载的泛型约束统一为 `where T <: Number<T>, Q <: Qualifier`"（a_v20_design_v1.md 相关修订）。

2. **问题描述**：B 类方向计数错误，§3.3 称"8 个 B 类方向"和"其余 7 个"，实际根据 9×9 转换矩阵表统计 B 类方向共 9 个（8 个遵循主规则，1 个偏差）
   - 所在位置：§3.3，L333
   - 严重程度：一般
   - 改进建议：将"8 个 B 类方向"修正为"9 个 B 类方向"，将"其余 7 个"修正为"其余 8 个"
   - **修改结果**：**已采纳**。将 L333 计数修正为"9 个 B 类方向"和"其余 8 个"（a_v20_design_v1.md 相关修订）。

3. **问题描述**：fromMat 转换规则的四项基本操作缺少对 Bool 矩阵的兼容性评估，未明确 Bool 矩阵是否支持缩减转换（colShrink/rowShrink 等纯截断路径）
   - 所在位置：§3.3 fromMat 6a/6b（L267-284, L339-402）与 §9 D33（L788）
   - 严重程度：一般
   - 改进建议：在 D33 中补充说明 Bool 矩阵是否支持缩减转换，若支持则提供无 `Number<T>` 约束的专门重载，若不支持则说明理由
   - **修改结果**：**已采纳**。D33 补充说明 fromMat 6a/6b 对 Bool 矩阵均不可用，Bool 矩阵仅支持 fromMat 7（a_v20_design_v1.md 相关修订）。

4. **问题描述**：转换矩阵表中 Mat4x2→Mat4x3 标签缺少 "B:" 前缀，9 个 B 类方向中仅此 1 个缺少，测试覆盖验证时造成混淆
   - 所在位置：§3.3，L379，源行 Mat4x2、目标列 →Mat4x3 的单元格
   - 严重程度：一般
   - 改进建议：将 `rowExt(3)` 修正为 `B: rowExt(3)`
   - **修改结果**：**已采纳**。将 `rowExt(3)` 修正为 `B: rowExt(3)`（a_v20_design_v1.md 相关修订）。

5. **问题描述**：equalEpsilon 的 NaN 行为未在 §5 错误处理策略中显式记载，下游消费者可能产生误判
   - 所在位置：§3.6 L497；§5 L647-658
   - 严重程度：一般
   - 改进建议：在 §5 中补充注明"equalEpsilon 在含 NaN 分量时返回 false"或在 §3.6 明确 equalEpsilon 对 NaN 的行为
   - **修改结果**：**已采纳**。§5 新增"equalEpsilon NaN 行为"条目；§3.6 显式声明"当任意对应分量存在 NaN 时返回 false"（a_v20_design_v1.md 相关修订）。

6. **问题描述**：§10 API 覆盖矩阵中 `dot` 行仅标注"本阶段 stub"未列出覆盖维度，`normalize`、`length`、`distance` 等同样未标注维度信息
   - 所在位置：§10，L821-831
   - 严重程度：一般
   - 改进建议：在 `dot` 行补充"本阶段 stub（Vec1~Vec4）"，同步为其他 stub 函数补充维度信息
   - **修改结果**：**已采纳**。§10 为 stub 函数补充维度信息：`dot` Vec1~Vec4、`cross` Vec3、`normalize`/`length`/`distance` 等 Vec2~Vec4（a_v20_design_v1.md 相关修订）。

## 迭代第 20 轮

1. **问题描述**：矩阵-列向量乘法（Mat×Vec）运算符签名与GLM行为完全相反
   - 所在位置：§2 包组织（第57-70行）、§3.5 算术运算符（第417-418行）
   - 严重程度：严重
   - 改进建议：将Mat×Vec成员运算符签名修正为`operator*(v: VecC<T,Q>): VecR<T,Q>`，匹配GLM的`mat<C,R> * vec<C> → vec<R>`语义，同步更新所有相关章节
   - **修改结果**：**已采纳**。§2 包组织图中 Mat×Vec 签名表全部重写，9 个运算符签名统一从 `*(VecR)→VecC` 修正为 `*(VecC)→VecR`（a_v21_design_v1.md 修订说明 v32）。

2. **问题描述**：矩阵-矩阵乘法结果类型推导规则错误，导致24/27个乘法重载不匹配GLM
   - 所在位置：§3.5 算术运算符（第418行规则定义 + 第422-449行乘法结果表）
   - 严重程度：严重
   - 改进建议：修正维度匹配规则为合法当且仅当`C1 = R2`，结果类型为`Mat(C2×R1)`；按修正后规则重新生成全部27个乘法重载的结果类型
   - **修改结果**：**已采纳**。矩阵乘法规则从 `R1=C2` 修正为 `C1=R2`，结果类型从 `Mat(C1×R2)` 修正为 `Mat(C2×R1)`（a_v21_design_v1.md 修订说明 v32）。

3. **问题描述**：F1+F2的涟漪效应导致§2/§4/§8/§9等多个章节一致性失效
   - 所在位置：全篇，以§3.5为核心向外辐射
   - 严重程度：严重
   - 改进建议：先行修正F1和F2，修正后重新审查所有引用Mat×Vec和Mat×Mat的章节，追加对GLM源码逐行对照的验证步骤
   - **修改结果**：**已采纳**。逐章审阅确认 §4/§8 无需修改，§9 补充 Mat×Vec 标准列向量乘法已对齐 GLM（a_v21_design_v1.md 修订说明 v32）。

4. **问题描述**：标准列向量乘法（matrix × column_vector）完全缺失
   - 所在位置：§2 包组织图（第57-70行Mat×Vec签名表）、§3.5 算术运算符
   - 严重程度：严重
   - 改进建议：在修正F1后，矩阵类型成员运算符自动变为正确的列向量乘法；同时审查Vec extend块中的行向量乘法是否仍然需要
   - **修改结果**：**已采纳**。F1 修正后即自动解决——修正后的 Mat×Vec 运算符即为标准列向量乘法（a_v21_design_v1.md 修订说明 v32）。

5. **问题描述**：§10 API覆盖矩阵中缺少对Mat×Vec和Mat×Mat具体覆盖维度的显式标注
   - 所在位置：§10 第843行
   - 严重程度：中等
   - 改进建议：将运算符条目拆分为子项，标注每个重载族各自的覆盖状态和重载数量
   - **修改结果**：**已采纳**。§10 "成员运算符"行拆分为 5 个子行：一元负号（9）、标量 ±*/（36）、矩阵 ± 同尺寸（9）、矩阵 * 跨尺寸（27）、Mat×Vec（9）（a_v21_design_v1.md 修订说明 v32）。

6. **问题描述**：Mat×Vec和Vec×Mat的NaN行为对比测试策略缺失
   - 所在位置：§3.5、§5
   - 严重程度：中等
   - 改进建议：在§5或§9中补充声明浮点矩阵乘法中NaN的传播遵循IEEE 754标准
   - **修改结果**：**已采纳**。§5 新增"Mat×Vec / Vec×Mat NaN 传播策略"段落（a_v21_design_v1.md 修订说明 v32）。

7. **问题描述**：scalar_mat_ops.cj全局函数未列出完整36个函数的签名
   - 所在位置：§3.5（第476-486行）
   - 严重程度：一般
   - 改进建议：可保持当前模板模式，但建议增加交叉引用标注
   - **修改结果**：**已采纳**。§3.5 scalar_mat_ops.cj 段落改写为完整 9 重载签名表格（a_v21_design_v1.md 修订说明 v32）。

8. **问题描述**：transpose实现说明中的示例与签名方向描述不一致
   - 所在位置：§3.7 第571行
   - 严重程度：一般
   - 改进建议：将"以Mat3x2→Mat2x3为例"改为"以Mat2x3→Mat3x2为例"
   - **修改结果**：**已采纳**。§3.7 transpose 实现说明中补充方向映射关系标注（a_v21_design_v1.md 修订说明 v32）。

9. **问题描述**：identity()的编码实现路径未明确
   - 所在位置：§3.3 item 8（第293-317行）
   - 严重程度：一般
   - 改进建议：补充编码策略说明，推荐复用diagonal的零值演算机制或直接使用diagonal(one)
   - **修改结果**：**已采纳**。§3.3 item 8 新增"编码路径"段落（a_v21_design_v1.md 修订说明 v32）。

10. **问题描述**：D16（非方阵 *= 不可用）措辞未限定范围
   - 所在位置：§9 D16（第777行）
   - 严重程度：一般
   - 改进建议：将D16修正为"非方阵`*=(Mat)`和`/=(Mat)`不可用，标量`*=(T)`和`/=(T)`仍可用"
   - **修改结果**：**已采纳**。D16 条目从"非方阵 *= 不可用"修正为"非方阵 *=(Mat) 不可用"，补充说明"标量 *=(T) 仍可用"（a_v21_design_v1.md 修订说明 v32）。

## 迭代第 21 轮

1. **问题描述**：排除别名族的覆盖说明缺乏精确性，统计数字与GLM实际偏差过大（"约81"实际144，"约108"实际480），覆盖矩阵精度等级与实际不符（"3精度"实际4精度）
   - 所在位置：§8（L771-773）、§10（L876-881）
   - 严重程度：一般
   - 改进建议：将排除统计替换为精确数字或定性描述；覆盖矩阵精度等级修正为"4精度"
   - **修改结果**：**已采纳**。§8 排除统计替换为定性描述；§10 精度等级从"3精度 × 9 尺寸 = 27"修正为"4精度 × 9 尺寸 = 36"（a_v23_copy_from_v22.md 修订说明 v33）。

2. **问题描述**：col()函数的mut赋值版本签名未在设计中显式列出，与声明的"与[]完全等价"不一致
   - 所在位置：§3.4（L411-415）
   - 严重程度：一般
   - 改进建议：补充col()的赋值版本签名，或明确声明col()仅提供取值版本
   - **修改结果**：**已采纳**。§3.4 补充 col() 赋值版本签名说明：mut 版本签名模式为 `mut func col(i: Int64): VecR<T,Q>`（a_v23_copy_from_v22.md 修订说明 v33）。

3. **问题描述**：§10 API覆盖矩阵中mod函数覆盖状态标注不精确，未反映签名暂定及编码阶段拆分需求
   - 所在位置：§10（L835）
   - 严重程度：一般
   - 改进建议：同步更新覆盖状态说明，与§3.7注释一致
   - **修改结果**：**已采纳**。§10 覆盖矩阵中 `mod` 行更新为"本阶段 stub（标量），`mod` 签名暂定，编码阶段需拆分为整数和浮点重载"（a_v23_copy_from_v22.md 修订说明 v33）。

4. **问题描述**：equalExact函数的NaN语义在§3.6与§5之间存在文档缺口，§5未为equalExact做NaN行为显式声明
   - 所在位置：§3.6（L503-504）、§5（L664）
   - 严重程度：一般
   - 改进建议：在§5中补充equalExact NaN行为条目，与equalEpsilon保持对称
   - **修改结果**：**已采纳**。§5 新增"equalExact NaN 行为"条目，声明 equalExact 对含 NaN 分量的矩阵返回 false（a_v23_copy_from_v22.md 修订说明 v33）。

## 迭代第 22 轮

1. **问题描述**：`col()` 赋值版本签名设计为 `mut func col(i: Int64): VecR<T,Q>`，在 struct 值语义下该签名返回列向量副本而非引用，导致 `m.col(i) = newVec` 实际修改的是临时副本而非矩阵内部状态
   - 所在位置：§3.4（第 412 行）
   - 严重程度：一般
   - 改进建议：澄清 `col()` 赋值版本的实际签名形态——要么提供独立的 `mut func setCol(i: Int64, v: VecR<T,Q>): Unit`，要么声明 `col()` 仅提供取值版本；若 `[]` 下标运算符已提供赋值版本，则 `col()` 无需重复提供赋值语义
   - **修改结果**：**已采纳**。§3.4 重新设计 `col()` 仅提供取值版本，不提供赋值语义；列向量替换通过 `[]` 下标运算符赋值版本实现（a_v23_copy_from_v22.md 修订说明 v34）。

2. **问题描述**：fromMat 6a/6b 的 144 个重载签名与四项基本操作的对应关系缺少完整的编码模板示例，当前仅有伪代码层次的描述而非仓颉语法正确的函数体
   - 所在位置：§3.3（第 387-399 行规则化编码策略段落）
   - 严重程度：一般
   - 改进建议：在 §3.3 中增加一个方向的完整仓颉函数体示例，展示四项组合规则如何映射为具体语法；同时提供偏差方向的 if-else 分支结构示意
   - **修改结果**：**已采纳**。§3.3 新增"编码模板示例"子节，提供两个方向的完整仓颉函数体：Mat2x3←Mat2x2（B 类简洁方向）和 Mat4x4←Mat4x2（DEVIATION 偏差方向）（a_v23_copy_from_v22.md 修订说明 v34）。

## 迭代第 23 轮

1. **问题描述**：`operator[]` 赋值版本签名缺少 `!` 后缀
   - 所在位置：§3.4
   - 严重程度：轻微
   - 改进建议：补充 `!` 后缀
   - **修改结果**：**已采纳**。补充 `operator[]` 赋值版本签名的 `!` 后缀。
