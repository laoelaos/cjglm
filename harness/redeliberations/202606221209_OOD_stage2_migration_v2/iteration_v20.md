# 再审议判定报告（v20）

## 判定结果

RETRY

## 判定理由

组件B诊断报告经质询确认（LOCATED），包含多项严重问题：F1（Mat×Vec运算符签名与GLM行为完全相反）、F2（矩阵乘法结果类型推导规则错误导致24/27个重载不匹配）、I1（F1+F2的涟漪效应波及多章节）、C1（标准列向量乘法完全缺失）。质询报告验证了证据充分性、逻辑完整性、覆盖完备性和报告必要性，确认问题定位准确。根据判定标准，组件B审查报告包含严重等级问题，应判定为RETRY。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：矩阵-列向量乘法（Mat×Vec）运算符签名与GLM行为完全相反
- **所在位置**：§2 包组织（第57-70行）、§3.5 算术运算符（第417-418行）
- **严重程度**：严重
- **改进建议**：将Mat×Vec成员运算符签名修正为`operator*(v: VecC<T,Q>): VecR<T,Q>`，匹配GLM的`mat<C,R> * vec<C> → vec<R>`语义，同步更新所有相关章节

- **问题描述**：矩阵-矩阵乘法结果类型推导规则错误，导致24/27个乘法重载不匹配GLM
- **所在位置**：§3.5 算术运算符（第418行规则定义 + 第422-449行乘法结果表）
- **严重程度**：严重
- **改进建议**：修正维度匹配规则为合法当且仅当`C1 = R2`，结果类型为`Mat(C2×R1)`；按修正后规则重新生成全部27个乘法重载的结果类型

- **问题描述**：F1+F2的涟漪效应导致§2/§4/§8/§9等多个章节一致性失效
- **所在位置**：全篇，以§3.5为核心向外辐射
- **严重程度**：严重
- **改进建议**：先行修正F1和F2，修正后重新审查所有引用Mat×Vec和Mat×Mat的章节，追加对GLM源码逐行对照的验证步骤

- **问题描述**：标准列向量乘法（matrix × column_vector）完全缺失
- **所在位置**：§2 包组织图（第57-70行Mat×Vec签名表）、§3.5 算术运算符
- **严重程度**：严重
- **改进建议**：在修正F1后，矩阵类型成员运算符自动变为正确的列向量乘法；同时审查Vec extend块中的行向量乘法是否仍然需要

- **问题描述**：§10 API覆盖矩阵中缺少对Mat×Vec和Mat×Mat具体覆盖维度的显式标注
- **所在位置**：§10 第843行
- **严重程度**：中等
- **改进建议**：将运算符条目拆分为子项，标注每个重载族各自的覆盖状态和重载数量

- **问题描述**：Mat×Vec和Vec×Mat的NaN行为对比测试策略缺失
- **所在位置**：§3.5、§5
- **严重程度**：中等
- **改进建议**：在§5或§9中补充声明浮点矩阵乘法中NaN的传播遵循IEEE 754标准

- **问题描述**：scalar_mat_ops.cj全局函数未列出完整36个函数的签名
- **所在位置**：§3.5（第476-486行）
- **严重程度**：一般
- **改进建议**：可保持当前模板模式，但建议增加交叉引用标注

- **问题描述**：transpose实现说明中的示例与签名方向描述不一致
- **所在位置**：§3.7 第571行
- **严重程度**：一般
- **改进建议**：将"以Mat3x2→Mat2x3为例"改为"以Mat2x3→Mat3x2为例"

- **问题描述**：identity()的编码实现路径未明确
- **所在位置**：§3.3 item 8（第293-317行）
- **严重程度**：一般
- **改进建议**：补充编码策略说明，推荐复用diagonal的零值演算机制或直接使用diagonal(one)

- **问题描述**：D16（非方阵 *= 不可用）措辞未限定范围
- **所在位置**：§9 D16（第777行）
- **严重程度**：一般
- **改进建议**：将D16修正为"非方阵`*=(Mat)`和`/=(Mat)`不可用，标量`*=(T)`和`/=(T)`仍可用"
