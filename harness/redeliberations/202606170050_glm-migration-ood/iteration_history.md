## 迭代第 1 轮

1. **问题描述**：Vec 结构体数据成员未指定 `let`/`var`，无法指导编码
   - 所在位置：Section 3.2（Vec 结构体系）
   - 严重程度：严重
   - 改进建议：明确指定 Vec 结构体的数据成员为 `var`，并在 Section 4.3 中补充说明 `var` 成员对 `let` 绑定变量的影响
   - a组件迭代的修改措施：§3.2 明确 Vec1~Vec4 数据成员声明为 `var`，§7 D8 补充对 `let` 绑定变量的影响说明

2. **问题描述**：`@OverflowWrapping` 标注策略与仓颉复合赋值自动生成机制存在矛盾
   - 所在位置：Section 4.6 + D3
   - 严重程度：严重
   - 改进建议：明确分析仓颉复合赋值自动生成对设计方案的影响；可能的替代路径：① 直接在二元运算符上标注 `@OverflowWrapping`；② 验证显式 `mut operator func +=`（带注解）的优先级高于自动生成版本
   - a组件迭代的修改措施：§4.6 重写标注策略，复合赋值与二元运算符均直接标注 `@OverflowWrapping`，分析自动生成与显式定义的优先级

3. **问题描述**：缺少 `length()` 成员函数定义
   - 所在位置：Section 3.2 + Section 4（行为契约）
   - 严重程度：严重
   - 改进建议：在 Section 4 中为每个 Vec 类型补充 `length()` 静态成员函数，统一签名 `public static func length(): Int64`
   - a组件迭代的修改措施：§3.2 补充 `public static func length(): Int64` 声明，Vec1~Vec4 分别返回 1/2/3/4

4. **问题描述**：`!=` 运算符语义描述使用了仓颉中不合法的 `!==` 概念
   - 所在位置：Section 4.5（比较运算）
   - 严重程度：一般
   - 改进建议：将 `!==` 语义改为 `!(a == b)` 或直接描述为"对 `==` 的结果取反"
   - a组件迭代的修改措施：§4.5 将 `!=` 定义改为 `!(a == b)`，§7 D9 记录决策理由

5. **问题描述**：构造函数体系缺少完整枚举，不足以支撑编码
   - 所在位置：Section 4.1（向量构造）
   - 严重程度：一般
   - 改进建议：在 Section 4.1 中为每个 Vec 类型列出完整的构造函数重载签名清单
   - a组件迭代的修改措施：§4.1 补充 Vec2 完整构造函数签名清单（8 个重载），并说明 Vec3/Vec4 以 C++ 源为准

6. **问题描述**：缺少 `operator` 重载所属位置的精确描述
   - 所在位置：Section 3.2（Vec 结构体系，运算符位置部分）
   - 严重程度：一般
   - 改进建议：将此处改为"复合赋值运算符在 Vec 结构体内定义为 `mut operator func`；二元运算符通过 `extend VecN<T,Q>` 块定义为扩展成员函数"
   - a组件迭代的修改措施：§3.2 明确复合赋值在结构体内定义为 `mut operator func`，二元运算符通过 `extend` 块定义

7. **问题描述**：`scalar + vec` 不可行的结论未在设计文本中体现
   - 所在位置：Section 4.3（算术运算）
   - 严重程度：一般
   - 改进建议：明确标注 `scalar + vec` 方向不支持运算符重载；若需要保留该语义，提供具名函数替代方案
   - a组件迭代的修改措施：§4.3 明确 `scalar + vec` 不可重载，提供 `add` 具名函数替代，§7 D10 记录决策

8. **问题描述**：文件迁移表中 `setup.cj` 的依赖声明与 Shim 职责重叠
   - 所在位置：Section 8（迁移文件清单与依赖拓扑），文件 1 `setup.cj`
   - 严重程度：一般
   - 改进建议：明确 `LengthT`/`SizeT` 的归属，建议归入 `shim_cstddef.cj` 且 `setup.cj` 通过 `import` 引用
   - a组件迭代的修改措施：§2 与 §8 修正依赖拓扑，`LengthT`/`SizeT` 归入 `shim_cstddef.cj`，`setup.cj` 仅保留配置常量

9. **问题描述**：缺少 `countof` 辅助函数的说明
   - 所在位置：Section 8 文件 1 `setup.cj` 的说明列 / Section 3.6 Shim 层
   - 严重程度：一般
   - 改进建议：在 Section 8 和 Section 3.6 中补充 `countof` 函数
   - a组件迭代的修改措施：§3.6 在 `shim_cstddef.cj` 说明中补充 `countof(arr)` 函数的作用与典型签名

10. **问题描述**：`ComputeEqual` 的 `IsFloat` 编译期判定方案存在不确定性
    - 所在位置：Section 3.5（ComputeEqual）
    - 严重程度：一般
    - 改进建议：明确 `ComputeEqual` 的最终实现路径：① 依靠 `shim_limits.cj` 提供 `isFloat<T>` 布尔常量配合 `const if`；② 若不支持编译期区分，则统一使用精确比较
    - a组件迭代的修改措施：§3.5 明确两条判定路径：首选 `isIec559 + const if`，备选统一精确比较

11. **问题描述**：缺少 `Vec1` 数据成员命名
    - 所在位置：Section 3.2（Vec 结构体系）
    - 严重程度：一般
    - 改进建议：在 Section 3.2 中明确说明 Vec1 的数据成员名为 `x`
    - a组件迭代的修改措施：§3.2 明确 Vec1 数据成员名为 `x`

12. **问题描述**：`const` 配置常量的作用域限制未在设计决策中提及
    - 所在位置：Section 7 D6
    - 严重程度：轻微
    - 改进建议：在 D6 中补充一条限制说明：`const + if` 仅适用于函数体内的内联条件代码
    - a组件迭代的修改措施：§7 D6 补充作用域限制，`const + if` 仅适用于函数体内的内联条件代码

13. **问题描述**：256 个别名的真正实用性未做分析
    - 所在位置：Section 3.8（向量类型别名）
    - 严重程度：轻微
    - 改进建议：在 Section 3.8 或设计决策中增加一节，按使用频率对别名进行分级
    - a组件迭代的修改措施：§3.8 增加别名实用性分级表（必备 ~32 / 常用 ~64 / 可选 ~160）

## 迭代第 2 轮

1. **问题描述**：`&&` / `||` 运算符不可重载
   - 所在位置：§3.2（布尔逻辑运算符）、§4.5（比较运算）
   - 严重程度：严重
   - 改进建议：移除 `&&` 和 `||` 的运算符重载设计，改用具名函数（如 `logicalAnd`/`logicalOr`）或位运算符（`&`/`|`）替代
   - a组件迭代的修改措施：§3.2/§4.5 移除 `&&`/`||` 运算符重载，改为 `logicalAnd()`/`logicalOr()` 具名函数，新增 D12

2. **问题描述**：`~` 运算符不可重载
   - 所在位置：§3.2（位运算符）、§4.4（位运算）
   - 严重程度：严重
   - 改进建议：移除 `~` 运算符重载，按位取反应通过具名函数（如 `bitwiseNot()`）实现
   - a组件迭代的修改措施：§3.2/§4.4 移除 `~` 运算符重载，改为 `bitwiseNot()` 具名函数，D12 统一记录

3. **问题描述**：跨 Vec 转换构造的泛型约束 `where T2 <: T` 不适用于数值类型（仓颉数值类型间无子类型关系）
   - 所在位置：§4.1（构造函数签名清单）
   - 严重程度：严重
   - 改进建议：重新设计约束机制——不做约束通过 `T(v)` 显式转换，或定义 `ConvertibleTo<T>` 接口
   - a组件迭代的修改措施：§4.1 移除 `where T2 <: T, Q2 <: Q` 约束，改用无约束泛型+分量级 `T(v)` 显式转换，新增 D14

4. **问题描述**：`const if` 在非 `const` 函数中不可用，`==` 运算符需 `const init` 前提
   - 所在位置：§3.5（ComputeEqual）、§3.6（shim_limits）
   - 严重程度：严重
   - 改进建议：为 Vec 添加 `const init` 构造函数使比较运算符可声明为 `const` 函数，或改用运行时 `if` 分支
   - a组件迭代的修改措施：§3.2/§4.1 新增 `const init` 构造函数，§3.5 更新 ComputeEqual 路径一前提，§4.5 将 `==` 声明为 `const`，新增 D11

5. **问题描述**：`@OverflowWrapping` 标注策略存在三处内部矛盾表述
   - 所在位置：§4.6（`@OverflowWrapping` 标注策略）
   - 严重程度：严重
   - 改进建议：做出明确唯一的标注决策——若显式定义优先则二元运算符不标注（与 roadmap 一致），若自动生成优先则均需标注
   - a组件迭代的修改措施：§4.6 完全重写为唯一确定策略——仅复合赋值标注 `@OverflowWrapping`，二元运算符委托继承 wrapping 语义

6. **问题描述**：`scalar / vec` 行为描述与不存在运算符矛盾
   - 所在位置：§9.2（除零行为）与 §4.3（算术运算）
   - 严重程度：一般
   - 改进建议：统一表述——若通过具名函数实现则改为 `div(0, vec)` 语法，若不支持则删除该行
   - a组件迭代的修改措施：§4.3 标量在左运算替代方案增加 `scalar / vec` 说明，§9.2 除零表对应行改为"具名函数"描述

7. **问题描述**：缺少 GLM_CONFIG_* 常量的完整清单与值
   - 所在位置：§7 D6（GLM_CONFIG_* 配置常量）
   - 严重程度：一般
   - 改进建议：在 D6 或单独一节中列出 18 个常量的完整清单（常量名、仓颉等效名、首轮值、简要说明）
   - a组件迭代的修改措施：§7 D6 补充 18 个 `GLM_CONFIG_*` 常量完整清单（仓颉等效名、首轮值、用途说明）

8. **问题描述**：`%` 运算符在浮点 Vec 上不可用（仓颉浮点类型无 `%` 运算符）
   - 所在位置：§3.2（运算符列表）、§4.3（算术运算）
   - 严重程度：一般
   - 改进建议：明确 `%` 仅在 `T` 为整数类型时可用，添加 `where T <: Integer<T>` 约束
   - a组件迭代的修改措施：§3.2/§4.3 明确 `%` 仅整数类型可用，建议添加 `where T <: Integer<T>` 约束或编译期检查，新增 D13

9. **问题描述**：`countof<T>(arr: T): Int64` 签名无法实现编译期长度计算
   - 所在位置：§3.6（shim_cstddef.cj）
   - 严重程度：一般
   - 改进建议：修正签名为 `func countof<T, N>(arr: VArray<T, N>): Int64`，通过类型参数 `N` 获取编译期长度
   - a组件迭代的修改措施：§3.6 修正 `countof` 签名为 `func countof<T, N>(arr: VArray<T, N>): Int64`，§8 文件说明同步更新

## 迭代第 3 轮

1. **问题描述**：`mut operator func +=` 的显式定义在仓颉语言中不可实现（复合赋值由二元运算符自动生成），导致 §4.6 溢出标注策略和"copy + op="委托模式产生自递归，编码阻塞。
   - 所在位置：§4.6、§3.2
   - 严重程度：严重
   - 改进建议：重构溢出标注策略：在二元运算符（+、-、*、/）上直接标注 @OverflowWrapping，逐分量实现运算；复合赋值由编译器自动生成，方向改为 += 自动继承自 + 而非 + 委托给 +=。
   - a组件迭代的修改措施：§4.6 重写，二元运算符直接标注 @OverflowWrapping 并逐分量实现，复合赋值由编译器自动生成

2. **问题描述**：设计文档引用了未定义的类型 `Vec<Bool, Q>`，但实际设计中只有 `Vec1<T,Q>`、`Vec2<T,Q>`、`Vec3<T,Q>`、`Vec4<T,Q>` 四个独立结构体，编码阶段无法根据此签名生成代码。
   - 所在位置：§4.5
   - 严重程度：严重
   - 改进建议：将所有 `Vec<Bool, Q>` 引用替换为每个 VecN 类型的具体版本，或统一表述为"各 VecN 类型分别定义对应版本"。
   - a组件迭代的修改措施：§3.2/§4.5 将 `Vec<Bool,Q>` 替换为各 `VecN<Bool,Q>` 分别定义对应版本

3. **问题描述**：泛型 Vec 的 `public init()` 默认构造函数无法编译——成员 `var x: T` 无初始值导致仓颉编译器不会自动生成无参构造函数，而显式定义的 `init() { this.x = T(0) }` 对非数值类型（如 Bool）不合法。
   - 所在位置：§3.2、§4.1
   - 严重程度：高
   - 改进建议：统一 §3.2 成员声明与 §4.1 构造函数清单：为成员添加泛型默认值并自定义 Default 接口（需评估与 D5 冲突），或移除 `public init()` 默认构造，或仅在明确支持的类型族上通过约束提供。
   - a组件迭代的修改措施：§3.2/§4.1 成员改为 `var x: T` 无初值、删除 `public init()`，用标量填充构造 `Vec(T(0))` 替代默认

4. **问题描述**：`logicalAnd`/`logicalOr` 从 C++ 运算符（`&&`/`||`）改为具名函数，未评估语法变更对下游 GLM 代码迁移成本的影响。
   - 所在位置：§4.5
   - 严重程度：高
   - 改进建议：增加一节评估将 `&&`/`||` 改为具名函数对迁移工作量的影响，给出代码迁移模式（搜索-替换规则或 wrapper 函数）。
   - a组件迭代的修改措施：新增 §11 评估 `&&`/`||`→`logicalAnd`/`Or` 的迁移成本，给出 C++→仓颉正则搜索替换规则

5. **问题描述**：P4（跨类型复合赋值未明确承认）、P6（Functor/ComputeVec 无消费者）、P7（`operator[]` 签名未完整）、P8（§3.6 与 §5.1 断言行为矛盾）、P9（`%` 运算符浮点类型编译错误）、P10（`ComputeEqual` 的 `IsFloat` 参数映射问题）、N2（`countof` 签名 `$` 前缀缺失）等中等级别问题需一并处理。
   - 所在位置：§3.2~§5.1 多处
   - 严重程度：一般
   - 改进建议：逐条按诊断报告中的改进建议进行修复。
   - a组件迭代的修改措施：D15 跨类型不支持、D16 预留、operator[] 补全、断言统一、ComputeEqual 简化、countof 加 `$N` 前缀

## 迭代第 4 轮

1. **问题描述**：`++`/`--` 自增自减运算符完全缺失，设计文档未覆盖C++ GLM中的前缀/后缀自增自减运算符
   - 所在位置：§4.3（算术运算）、§4.6（`@OverflowWrapping` 标注策略）
   - 严重程度：严重
   - 改进建议：在 §3.2 或 §4.3 中明确 `++`/`--` 不可作为仓颉运算符重载实现；决定替代方案（具名函数 `increment()`/`decrement()` 或直接移除）；更新 §4.6 标注策略；参照 §11 增加迁移成本评估
   - a组件迭代的修改措施：§3.2/§4.3 新增 `increment()`/`decrement()` 替代 `++`/`--`，§4.6 增加 8 处标注，§11.2 新增迁移评估

2. **问题描述**：`NumericLimits<T>` 泛型约束与 D5 宽松约束冲突，导致 `VecN<Bool, Q>` 的 `==` 运算符可能编译失败
   - 所在位置：§3.5（ComputeEqual）、§3.6（shim_limits.cj）、§7 D5
   - 严重程度：严重
   - 改进建议：重新设计 `NumericLimits<T>` 使其对任意 `T` 可用，或分两个版本提供，或为 `Vec<Bool, Q>` 单独特化 `==` 运算符
   - a组件迭代的修改措施：§3.5/§3.6 将 NumericLimits 拆分为保留+新增无约束 `isIec559Of<T>()`，§7 新增 D18 决策

3. **问题描述**：`bitwiseNot()` 对整数类型的实现路径不明确，描述存在歧义且不完整
   - 所在位置：§4.4（位运算）
   - 严重程度：严重
   - 改进建议：明确指定整数分量的 `bitwiseNot()` 实现策略（如 `T(-1) ^ x` 或特化实现），补充定义位置说明
   - a组件迭代的修改措施：§4.4 重写 `bitwiseNot()` 描述，明确对每个分量调用 `!` 即可（整数取反/Bool 逻辑非）

4. **问题描述**：构造函数签名清单缺少类型参数声明，无法直接指导编码
   - 所在位置：§4.1（向量构造）
   - 严重程度：严重
   - 改进建议：修正 §4.1 的签名清单，为每个跨类型构造函数补充完整的类型参数声明和约束
   - a组件迭代的修改措施：§4.1 构造函数签名补全 `<T2, Q2>` 类型参数与 where 约束，标题改为"示例性签名"

5. **问题描述**：事实错误——"仓颉位运算无溢出异常"不适用于 `<<` 运算符
   - 所在位置：§4.4（位运算）
   - 严重程度：严重
   - 改进建议：修正错误描述，明确 `<<` 需标注 `@OverflowWrapping`；更新 §4.6 标注策略；在 §5.1 中补充位运算溢出场景的处理策略
   - a组件迭代的修改措施：§4.4 删除位运算无溢出描述，§4.6 将 `<<` 纳入 `@OverflowWrapping`（新增 4 处），§5.1 新增行

6. **问题描述**：`scalar op vec` 模式的具名函数替代不完整，遗漏 `mod`
   - 所在位置：§4.3（算术运算）
   - 严重程度：一般
   - 改进建议：在 §4.3 的具名函数替代列表中明确补上 `mod(vec, scalar)` 和 `mod(scalar, vec)` 双向重载
   - a组件迭代的修改措施：§4.3 标量在左运算替代列表补上 `mod(vec, scalar)` 与 `mod(scalar, vec)` 双向重载

7. **问题描述**：跨类型赋值语义未定义，C++ GLM 允许的模板化 `operator=` 在仓颉中无对应处理策略
   - 所在位置：§4（关键行为契约）
   - 严重程度：一般
   - 改进建议：在 §4 或 §7 中增加设计决策，明确跨类型赋值的处理路径（显式转换、定义泛型 `=` 或记录为已知差异）
   - a组件迭代的修改措施：§7 新增 D17 决策（显式转换后赋值），§11.3 新增跨类型赋值差异迁移评估

## 迭代第 5 轮

1. **问题描述**：§9 标题下仅保留小节标题，正文写"（与 v3 一致，略去具体内容）"，编码人员无法独立获取零向量行为、除零、NaN/Infinity 传播、类型转换边界等关键边界约定
   - 所在位置：§9（异常场景和边界条件补充），全文第 534-543 行
   - 严重程度：严重
   - 改进建议：恢复 §9 的全部具体内容，或以条文形式直接将关键边界约定写回本节，确保 v5 自洽完整
   - a组件迭代的修改措施：§9 完全恢复，展开为 9.1~9.4 四节并以表格列出零向量、除零、NaN/Infinity 传播、类型转换边界的具体约定

2. **问题描述**：设计多处依赖 const if (isIec559Of<T>()) 实现编译期分支，但仓颉泛型函数不允许按类型参数约束进行重载，亦不支持 C++ 式泛型函数全特化，主设计路径的可行前提尚未验证
   - 所在位置：§3.5（ComputeEqual）、§3.6（shim_limits.cj 实现策略）
   - 严重程度：严重
   - 改进建议：明确 isIec559Of<T>() 的仓颉实现方案。可选路径包括：① 在函数体内使用 const if 配合类型比较（需验证仓颉是否支持 const 上下文的 T == Float32 语法）；② 放弃编译期分支，统一使用 == 精确比较；③ 采用反射或注解方式获取运行期类型信息
   - a组件迭代的修改措施：§3.5/§3.6/§10 重写实现描述，放弃不可行的重载特化路径，改用 const if 配合 is 运算符值级类型检测，并新增 D19 决策记录

3. **问题描述**：构造函数签名中多处引用 Vec1<T2> 而非 Vec1<T2, Q2>，缺少 Qualifier 参数；文档未明确 Vec1~Vec4 的 Q 参数是否具有默认值
   - 所在位置：§4.1（向量构造，第 261-263 行）
   - 严重程度：一般
   - 改进建议：在 §3.2 或 §3.1 中明确声明默认 Qualifier 策略，同步修正 §4.1 中所有 Vec1<T2> 引用为 Vec1<T2, Q2>
   - a组件迭代的修改措施：§4.1 修正所有 `Vec1<T2>` 引用为 `Vec1<T2, Q2>`（含 `Q2 <: Qualifier` 约束），`Vec1<T>` 改为 `Vec1<T, Q>`；§3.1 补充默认 Qualifier 策略（Defaultp = PackedHighp）

4. **问题描述**：bitwiseNot() 描述未说明浮点类型 Vec 实例化时 ! 运算符不可用将产生编译错误
   - 所在位置：§4.4（位运算，bitwiseNot 段落）
   - 严重程度：一般
   - 改进建议：补充说明 bitwiseNot() 对浮点类型 T 的 Vec 实例产生编译错误，与 D5（宽松泛型约束，延迟检查）一致
   - a组件迭代的修改措施：§4.4 位运算末尾新增「浮点类型约束」段落，说明 bitwiseNot() 对 Float32/Float64 Vec 实例产生编译错误并关联 D5 延迟检查语义

## 迭代第 6 轮

1. **问题描述**：<< 运算符重载右操作数类型限制描述不准确，声称仅接受 T 类型右操作数，但仓颉允许具体类型如 Int64 作为右操作数
   - 所在位置：§4.3（第 311 行）、§4.4（第 323 行）
   - 严重程度：严重
   - 改进建议：修正为"不能引入新的类型参数，但可使用具体类型如 Int64"，将移位量类型决策从"必须为 T"改为评估 Int64 方案
   - a组件迭代的修改措施：§4.3/§4.4 修正 `<<` 描述为"运算符不能为泛型"，§4.4 新增方案 A（取 T）与方案 B（取 Int64）对比评估

2. **问题描述**：@OverflowWrapping 标注策略声明"与 roadmap §0.5.1 完全对齐"但实际策略完全相反（设计标注二元运算符，roadmap 标注复合赋值）
   - 所在位置：§4.6（第 336 行）
   - 严重程度：严重
   - 改进建议：修正文字声明，说明实际策略与 roadmap 的差异及设计迭代中论证的理由
   - a组件迭代的修改措施：§4.6 新增与 roadmap §0.5.1 的策略差异说明段落，解释二元标注与复合赋值自动生成的等价性

3. **问题描述**：构造函数体系声明为"示例性"并要求编码以 C++ #define 展开为准，设计文档不具备独立完整性
   - 所在位置：§4.1（第 287 行）
   - 严重程度：严重
   - 改进建议：在 §4.1 中提供 Vec1~Vec4 各类型的完整构造函数签名清单（仓颉语法），替代"示例性 + 以 C++ 展开为准"的表述
   - a组件迭代的修改措施：§4.1 完全重写，列出 Vec1~Vec4 各类型完整构造函数签名清单（含类型参数与 where 约束）

4. **问题描述**：别名分级表格中的数值与实际计算不一致（"常用"级声称 ~64 实为 24，"可选"级声称 ~160 实为 72）
   - 所在位置：§3.8（第 247-253 行）
   - 严重程度：一般
   - 改进建议：修正表格数值使其与实际计算一致
   - a组件迭代的修改措施：§3.8 别名分级表格修正为必备 64 / 常用 48 / 可选 144，合计 256

5. **问题描述**：缺失 </<=/>/>= 逐分量比较运算符的设计覆盖
   - 所在位置：§4.5（比较运算）
   - 严重程度：一般
   - 改进建议：补充这四组运算符的设计，或记录首轮不覆盖的理由
   - a组件迭代的修改措施：§4.5 新增 `</<=/>/>=` 子节说明首轮不定义，§7 新增 D22 决策记录

6. **问题描述**：<< 移位量类型设计未评估 Int64（固定整数类型）替代方案
   - 所在位置：§4.4（第 323-324 行）
   - 严重程度：一般
   - 改进建议：评估并选择 Int64 或 T 作为移位量类型，记录决策理由
   - a组件迭代的修改措施：§4.4 新增方案 B（移位量取 Int64）评估，决策采用方案 A，方案 B 作为备选

7. **问题描述**：C++ GLM 的 storage<L, T, Q> 模板结构体未映射也未解释省略理由
   - 所在位置：§3.1（qualifier 体系）、§8（迁移文件清单）
   - 严重程度：一般
   - 改进建议：在 §3.1 末尾或新增 D20 中说明 storage 的处理策略（省略理由或等效方案）
   - a组件迭代的修改措施：§3.1 末尾新增 storage<L,T,Q> 模板结构体省略说明段落，§7 新增 D23 决策

8. **问题描述**：genTypeTrait、find_best_type 等辅助设施在设计文档中未覆盖
   - 所在位置：§3.1（qualifier 体系）、§8（迁移文件清单）
   - 严重程度：一般
   - 改进建议：审查并在 §3.1 或 D20 中记录首轮哪些 trait 不需要及理由，哪些需要等效方案
   - a组件迭代的修改措施：§3.9 新增「C++ 辅助设施省略说明」子节，列出 genTypeTrait/init_gentype/find_best_type 及省略理由，§7 新增 D24

9. **问题描述**：Vec 结构体的 Q 参数未指定是否有默认类型参数
   - 所在位置：§3.2（Vec 结构体系）、§7 D2（第 398 行）
   - 严重程度：一般
   - 改进建议：在 §3.2 中明确声明 Q 参数是否具有默认值（= PackedHighp），并在 §4.1 签名清单中统一体现
   - a组件迭代的修改措施：§3.2「类型参数默认值」段落明确声明 `where Q <: Qualifier = PackedHighp`，说明 `Vec2<Float32>` 合法

## 迭代第 7 轮

1. **问题描述**：bitwiseNot() 在 VecN<Bool, Q> 上使用 ! 运算符的语义与 C++ GLM 的 ~bvec 行为相反（C++ ~bool 因整数提升始终返回 true，仓颉 !bool 执行逻辑否定）
   - 所在位置：§3.2（第 134 行）、§4.4（第 425-426 行）
   - 严重程度：严重
   - 改进建议：修正声明，明确标注为已知行为差异，或排除 VecN<Bool, Q> 的 bitwiseNot() 使其编译时报错
   - a组件迭代的修改措施：§3.2/§4.4 修正 bitwiseNot() 对 Bool 行为声明，标为「已知行为差异」，新增 D25 决策

2. **问题描述**：GLM_CONFIG_CLIP_CONTROL 首轮推荐值仍为 0x02，正确值应为 0x0A（RH_BIT(8) | NO_BIT(2)），缺失了 RH_BIT
   - 所在位置：§7 D6 配置常量表格（第 560 行）
   - 严重程度：严重
   - 改进建议：将值更正为 0x0A（十进制 10），标注语义为 RH_BIT | NO_BIT，同时反馈至 roadmap 文档阻断误差传播
   - a组件迭代的修改措施：§7 D6 表格中 GLM_CONFIG_CLIP_CONTROL 值由 0x02 更正为 0x0A（RH_BIT(8) | NO_BIT(2)）

3. **问题描述**：问题 1 和 2 作为事实错误跨越轮次未被纠正，设计文档不具备独立完整性，编码人员按文档实现将产生与 C++ GLM 不一致的代码
   - 所在位置：全文
   - 严重程度：严重
   - 改进建议：在编码启动前优先修复问题 1 和 2，由独立审查人逐项核对上一轮诊断问题清单全部关闭后，标记为"可编码状态"
   - a组件迭代的修改措施：v8 修订说明注明通过修复问题 1 和 2 间接解决（标记为已解决）

4. **问题描述**：跨类型 Vec 构造函数中标量参数使用了 T2（源分量类型）而非 T（目标分量类型），与 C++ GLM 的隐式转换行为不一致，增加调用方复杂度
   - 所在位置：§4.1 Vec3/Vec4 跨类型构造函数签名
   - 严重程度：严重
   - 改进建议：将所有跨类型构造函数中的标量参数类型改为 T（目标分量类型），构造体内通过 T(scalar) 转换
   - a组件迭代的修改措施：§4.1 跨类型构造函数所有标量参数由 T2 改为 T（Vec2×2、Vec3×5、Vec4×30 处），新增 D26

5. **问题描述**：需求响应充分度的系统评估仅在修订说明中呈现，报告主体缺少独立章节，读者仅阅读 Issues 主体将看不到需求覆盖的系统性确认
   - 所在位置：报告主体（Issues 1-6）
   - 严重程度：一般
   - 改进建议：在报告主体中增加独立"需求覆盖验证"小节，简要陈述 roadmap §3E 对照结论
   - a组件迭代的修改措施：v8 未明确修复（修订说明表仅含问题 1/2/3/4/6，未增加独立「需求覆盖验证」小节）


## 迭代第 8 轮

1. **问题描述**：别名目标数量三处不一致（256/48/≥64），编码人员无法确定首轮实际应实现的别名数量
   - 所在位置：§3.8（第 266-267 行）、§12.2 验收标准表（第 900 行）
   - 严重程度：严重
   - 改进建议：明确首轮别名数量的唯一目标值并设为强制交付标准；将"最简可行范围"段落改为可选降级策略并标注触发范围变更评审；将 §12.2 验收标准中的验证数量与 §3.8 目标值对齐
   - a组件迭代的修改措施：§3.8 重写为 256 个别名强制交付标准，48 降级为可选评审触发策略，§12.2 验收项改为"256 全量不可降级"

2. **问题描述**：设计未利用 `public import` 定义公共 API 面，下游消费者导入路径不明
   - 所在位置：§2（模块划分，第 34-51 行）、§3.8（第 244-271 行）
   - 严重程度：严重
   - 改进建议：在 `package glm` 中新增公共 API 入口文件（如 `lib.cj`/`mod.cj`），使用 `public import` 重导出 `glm.detail` 中的 Vec 类型和 Qualifier 类型；在 §2 模块划分中明确公共 API 面设计；在 §12 验收标准中增加公共 API 导入可用性验收项
   - a组件迭代的修改措施：§2 新增 `lib.cj` 通过 `public import` 重导出 Vec/Qualifier 类型，§8 清单加 lib.cj，§12.2 新增公共 API 导入验收项

3. **问题描述**：缺少与 roadmap 首轮范围的可追溯性映射，编码阶段无法独立验证范围完整性
   - 所在位置：§1（概述，第 9 行）、§8（迁移文件清单与依赖拓扑，第 689-715 行）
   - 严重程度：严重
   - 改进建议：在 §8 迁移文件清单中为每个文件添加 roadmap 子范围编号和对应源文件路径；增加"范围可追溯性对照表"逐项对照 roadmap §3E/3G 核心文件清单；对与 roadmap 推荐范围不一致的决策明确标注偏离项、理由及条件
   - a组件迭代的修改措施：§1 新增范围可追溯性段落，§8 完全重写为 §8.1/8.2/8.3 含 roadmap 子范围编号映射与可追溯性对照表

4. **问题描述**：D6 配置常量表引用未定义的子常量，编码人员无法从设计文档独立获取完整常量值
   - 所在位置：§7 D6（第 544-565 行）
   - 严重程度：一般
   - 改进建议：明确声明子常量的编码策略——统一采用字面量替换或命名常量映射；根据所选方案清理或补充子常量定义
   - a组件迭代的修改措施：§7/D6 配置表所有 C++ 宏子常量统一替换为字面量，并新增"子常量编码策略"段落说明

5. **问题描述**：设计对 `Qualifier` 的运行时行为约定未涉及——不同 Qualifier 在当前设计下功能等价但未明确约定
   - 所在位置：§3.1（Qualifier 体系，第 82-98 行）、§3.2（Vec 结构体系，第 117-118 行）
   - 严重程度：一般
   - 改进建议：在 §7 中新增设计决策约定首轮中不同 Qualifier 类型之间是否可相互赋值/转换；补充未来轮次 Qualifier 差异引入路径的预期说明；在验收标准中增加跨 Qualifier 赋值/转换行为的测试项
   - a组件迭代的修改措施：§3.1 末尾新增 Qualifier 跨 Q 转换约定+差异引入路径，§7 新增 D27 决策，§12.2 新增跨 Q 转换验收项

## 迭代第 9 轮

1. **问题描述**：`operator[]` 赋值形式签名缺少 `mut` 修饰符，struct 普通实例成员函数无法修改成员变量，直接阻断编译
   - 所在位置：§4.2（分量访问），第 424 行
   - 严重程度：严重
   - 改进建议：在 `operator[]` 赋值形式签名中添加 `mut` 修饰符
   - a组件迭代的修改措施：§4.2 中 `operator[]` 赋值形式签名添加 `mut` 修饰符，§7 新增 D28 决策记录 `mut` 必要性

2. **问题描述**：`const operator ==` 函数中 `abs()`、`epsilonOf<T>()`、`T(0)` 等构成的表达式链未系统性验证 const 兼容性，部分环节在 const 上下文中合法性未知
   - 所在位置：§3.5、§3.6、§4.5
   - 严重程度：一般（中高）
   - 改进建议：在 §3.5 或 §7 中新增设计决策，系统性罗列 const 表达式链的兼容性要求并标注验证状态
   - a组件迭代的修改措施：§3.5 末尾新增引用段、§7 新增 D29 决策表，系统罗列表达式链各环节 const 兼容性要求及回退方案

3. **问题描述**：`@OverflowWrapping` 标注在编译器自动生成的复合赋值版本（`+=` 等）上的继承性未经验证，是 §4.6 溢出策略的基础假设
   - 所在位置：§4.6，第 485 行
   - 严重程度：一般（中高）
   - 改进建议：编写原型测试代码验证标注继承性，或在 §4.6 中补充备选方案
   - a组件迭代的修改措施：§4.6 总结后新增验证要求段，§7 新增 D30 决策明确原型验证计划及 extend 具名函数备选方案

4. **问题描述**：`Vec<Bool, Q>` 抽象类型引用在 §3.2 中仍存在，实际设计中只有 Vec1~Vec4 四个独立结构体，跨多轮迭代未彻底修复
   - 所在位置：§3.2，第 161-167 行
   - 严重程度：一般（中等）
   - 改进建议：将 `Vec<Bool, Q>` 替换为 `VecN<Bool, Q>` 或明确说明"各 VecN 类型分别定义对应版本"
   - a组件迭代的修改措施：全文主章节中所有 `Vec<Bool, Q>` 抽象引用替换为 `VecN<Bool, Q>`（涉及 §3.2/§4.3/§4.4/§7 D20/§11.1/§12.2）

5. **问题描述**：跨类型 fill-from-Vec1 构造器不可与同类型版本共存的原因（仓颉函数重载规则限制）未记录为设计决策，编码人员无法理解约束来源
   - 所在位置：§4.1、§7
   - 严重程度：一般（中等）
   - 改进建议：在 §7 中新增设计决策记录此约束及理由，并在 §4.1 签名旁添加注释引用
   - a组件迭代的修改措施：§4.1 fill-from-Vec1 构造器注释追加引用标记，§7 新增 D31 决策记录重载冲突约束及理由

## 迭代第 10 轮

1. **问题描述**：缺少 `Hashable` 实现，Vec 类型无法作为哈希键正常使用，且浮点 Epsilon 容差 `==` 与按位哈希存在契约不一致
   - 所在位置：§3.2（Vec 结构体系）、§4（关键行为契约）、§12（验收标准）
   - 严重程度：严重
   - 改进建议：明确 Vec 类型的 `Hashable` 实现策略，对整数/Bool Vec 使用 `@Derive[Hashable]`，对浮点 Vec 记录为已知限制；在 §4 补充 `hashCode()` 签名和行为约定；在 §12.2 新增哈希集合可用性验收项
   - a组件迭代的修改措施：§3.2 新增「Hashable 实现策略」统一用 `@Derive[Hashable]`，浮点容差与按位哈希不一致标注为已知限制；新增 §4.7 哈希契约子节；§12.2 新增哈希集合可用性验收项

2. **问题描述**：Vec2 缺少 `Vec1+Vec1` 构造函数，与 C++ GLM `type_vec2.inl:73-78` 行为不一致
   - 所在位置：§4.1（向量构造 — Vec2 构造函数清单）
   - 严重程度：严重
   - 改进建议：补充 `public init<T2, Q2>(a: Vec1<T2, Q2>, b: Vec1<T2, Q2>) where Q2 <: Qualifier`
   - a组件迭代的修改措施：§4.1 Vec2 构造函数清单中新增 `public init<T2, Q2>(a: Vec1<T2, Q2>, b: Vec1<T2, Q2>) where Q2 <: Qualifier`

3. **问题描述**：Vec3 缺少 4 个 `Vec1` 参与的组合构造函数，与 C++ GLM `type_vec3.inl` 行为不一致
   - 所在位置：§4.1（向量构造 — Vec3 构造函数清单）
   - 严重程度：严重
   - 改进建议：补充 `Vec1+Vec1+T`、`Vec1+T+Vec1`、`T+Vec1+Vec1`、`Vec1+Vec1+Vec1` 四个构造函数
   - a组件迭代的修改措施：§4.1 Vec3 构造函数清单中补充 `Vec1+Vec1+T`、`Vec1+T+Vec1`、`T+Vec1+Vec1`、`Vec1+Vec1+Vec1` 共 4 个构造函数

4. **问题描述**：`ComputeEqual` 被 `public import` 暴露为公共 API，泄漏实现细节
   - 所在位置：§2（公共 API 面设计），`lib.cj` 的 `public import` 列表
   - 严重程度：一般
   - 改进建议：从 `lib.cj` 的 `public import` 列表中移除 `ComputeEqual`，降级为 `internal` 包级可见类型
   - a组件迭代的修改措施：§2 `lib.cj` 的 `public import` 列表移除 `ComputeEqual`，仅保留 Vec1~Vec4、Qualifier 及实现类型的导出

5. **问题描述**：`isIec559Of<T>()` 实现中包含 `Float16` 类型增加不必要的验证风险
   - 所在位置：§10，`isIec559Of<T>()` 实现代码（第 893-896 行）
   - 严重程度：一般
   - 改进建议：简化实现为仅检查 `Float32`/`Float64`，将 `Float16` 分支推迟至后续轮次
   - a组件迭代的修改措施：§3.6 与 §10 中 `isIec559Of<T>()` 实现移除 `T(0) is Float16` 分支，仅保留 Float64/Float32 检测；§10 对照表 Float16 行改为 false 并标注简化策略排除

## 迭代第 11 轮

1. **问题描述**：256个别名的命名约定未明确定义，文档中两套命名风格（全小写vs大写驼峰）存在冲突，编码人员无法确定最终type别名的书写格式
   - 所在位置：§3.8（向量类型别名），第291-303行
   - 严重程度：严重
   - 改进建议：在§3.8中增加命名约定子节，统一选定一种命名风格（GLM原生小写或大写驼峰），明确精度变体前缀的拼接方式（下划线或驼峰），并提供至少覆盖4个精度变体的示例表
   - a组件迭代的修改措施：v12 未明确修复（v11 设计 v2 已含完整约定：大写驼峰 + 驼峰拼接 + 4 变体示例表）

2. **问题描述**：`scalar op vec`具名函数的定义位置（包/文件）、形态（扩展成员函数/独立函数）和导入方式未指定
   - 所在位置：§4.3（算术运算），第455-460行
   - 严重程度：一般
   - 改进建议：在§4.3或§2中补充文件归属、定义形态选择及调用方导入方式
   - a组件迭代的修改措施：v12 未明确修复（v11 设计 v2 已明确定义位置：extend 块 + `compute_vector_decl.cj`）

3. **问题描述**：Aligned* Qualifier类型的可见性策略未明确，影响下游API边界
   - 所在位置：§3.1（Qualifier体系）、§2（模块划分）
   - 严重程度：一般
   - 改进建议：在§3.1或§7中明确Aligned系列首轮可见性，建议设为internal并在后续轮次切换配置时修改
   - a组件迭代的修改措施：v12 未明确修复（v11 设计 v2 已明确首轮 internal 及后续变更步骤）

4. **问题描述**：`compute_vector_relational.cj`的依赖声明包含不必要的`shim_cstddef.cj`，是历史残留
   - 所在位置：§2第85行、§8.1迁移文件清单文件6
   - 严重程度：一般
   - 改进建议：将依赖修正为仅`shim_limits.cj`
   - a组件迭代的修改措施：v12 未明确修复（v11 设计 v2 已修正 `compute_vector_relational.cj` 仅依赖 `shim_limits.cj`）

5. **问题描述**：`LengthT`类型别名在Vec类型中未被使用，抽象一致性不足
   - 所在位置：§3.6第277行、§3.2第145行、§4.2第442-449行
   - 严重程度：一般
   - 改进建议：将Vec的`length()`返回类型和`operator[]`索引类型改为`LengthT`，或在§3.6中说明其用途
   - a组件迭代的修改措施：v12 未明确修复（v11 设计 v2 已说明 LengthT 供下游使用、Vec 内部保持 Int64）

6. **问题描述**：浮点Vec的IEEE 754精确相等比较无可用入口，`==`使用容差比较导致Infinity == Infinity返回false
   - 所在位置：§4.5（比较运算）、§9.3（NaN/Infinity传播）
   - 严重程度：一般
   - 改进建议：在§4增加`equalExact`/`equalIEEE`具名函数作为`==`的补充
   - a组件迭代的修改措施：v12 未明确修复（v11 设计 v2 §4.5 已定义 `equalExact` 扩展成员函数提供精确比较）

7. **问题描述**：默认字符串表示的行为约定使用推测性措辞（"可能""预期"），API契约不精确
   - 所在位置：§3.2第173-176行
   - 严重程度：轻微
   - 改进建议：替换为精确声明或实现自定义`toString()`
   - a组件迭代的修改措施：v12 未明确修复（v11 设计 v2 已使用精确声明措辞、无推测性表述）

## 迭代第 12 轮

1. **问题描述**：D16声明`compute_vector_decl.cj`和`vectorize.cj`的类型"首轮仅定义不消费"，但§4.3将`scalar-op-vec`方向具名函数放置于`compute_vector_decl.cj`中，首轮即被消费，全文件笼统陈述会使编码人员产生误判
   - 所在位置：§7 D16（第726-731行）与§4.3（第493-497行）
   - 严重程度：一般
   - 改进建议：在D16中明确区分"`compute_vec_*`策略结构体首轮不消费"和"方向辅助函数首轮被消费"两个子句；或新增例外说明
   - a组件迭代的修改措施：v13 将 D16 标题改为"含方向辅助函数例外"，正文分两条子句区分策略结构体首轮不消费与方向辅助函数首轮被消费

2. **问题描述**：§4.5新增了`equalExact(other: VecN<T,Q>): Bool`具名函数用于浮点IEEE 754精确相等比较，但§12.2验收标准表中未包含任何对应验证项，编码阶段无法确认测试要求和期望行为
   - 所在位置：§4.5（第522行）—`equalExact`定义；§12.2验收标准表（第1109-1123行）—缺少对应项
   - 严重程度：一般
   - 改进建议：在§12.2中新增`equalExact`验收项，包括NaN/Infinity边界行为和整数/Bool Vec上的行为等价性
   - a组件迭代的修改措施：v13 在 §12.2 验收标准表中新增 equalExact 正确性项，覆盖 Inf/NaN 边界与整数 Vec 等价性验证

## 迭代第 13 轮

1. **问题描述**：`equalExact` 声明为非 `const` 但与 `==` 的 const 策略不一致，限制了编译期计算场景可用性
   - 所在位置：§4.5，第 522 行
   - 严重程度：一般（中等）
   - 改进建议：将 `equalExact` 改为 `const` 扩展成员函数，与 `==` 的 const 策略保持一致
   - a组件迭代的修改措施：§4.5 中将 `equalExact` 声明从"非 const 的扩展成员函数"改为"const 的扩展成员函数"

2. **问题描述**：`epsilonOf<T>()` 在 §3.5 代码示例中被引用但未在文档任何位置显式定义签名、所属文件及实现策略
   - 所在位置：§3.5，第 237 行
   - 严重程度：一般（中等）
   - 改进建议：在 §3.5 或 §3.6 中补充 `epsilonOf<T>()` 的完整签名、所属文件和实现策略
   - a组件迭代的修改措施：§3.5 新增「`epsilonOf<T>()` 定义」子节，给出完整签名、所属文件 `shim_limits.cj` 及委托实现

3. **问题描述**：`equalExact` 验收标准在 §12.2 中存在但 §12.1 验证层次中无对应测试覆盖
   - 所在位置：§12.1 vs §12.2，第 1133 行
   - 严重程度：一般（中等）
   - 改进建议：在 §12.1 层次三或层次四中补充 `equalExact` 验证子项
   - a组件迭代的修改措施：§12.1 层次三新增 `equalExact` 验证子项，覆盖 Infinity/NaN 精确比较和整数/Bool Vec 等价性

## 迭代第 14 轮

1. **问题描述**：Vec1 缺失从 Vec2/Vec3/Vec4 的截断构造函数，Vec2 缺失从 Vec3/Vec4 的截断构造函数，Vec3 缺失从 Vec4 的截断构造函数，共 6 个构造函数缺失
   - 所在位置：§4.1（向量构造 — Vec1/Vec2/Vec3 构造函数清单）
   - 严重程度：严重
   - 改进建议：在 §4.1 中补充全部 6 个跨分量数截断构造函数，均标为 `public` 并使用 `where Q2 <: Qualifier` 约束
   - a组件迭代的修改措施：§4.1 补全 6 个跨分量数截断构造函数（Vec1+3、Vec2+2、Vec3+1）

2. **问题描述**：Vec3 构造函数包含 `Vec2+T` 与 `T+Vec2`，但不包含 `Vec2+Vec1` 与 `Vec1+Vec2`，与 GLSL 5.4.1 规范不一致
   - 所在位置：§4.1（向量构造 — Vec3 构造函数清单）
   - 严重程度：严重
   - 改进建议：在 §4.1 Vec3 构造函数清单中补充 `init(a: Vec2<T2,Q2>, b: Vec1<T2,Q2>)` 与 `init(a: Vec1<T2,Q2>, b: Vec2<T2,Q2>)`，验证泛型重载解析无冲突
   - a组件迭代的修改措施：§4.1 Vec3 构造函数清单新增 `Vec2+Vec1` 与 `Vec1+Vec2` 两个组合构造

3. **问题描述**：§4.3 第 500 行将 `mod` 列于首轮具名函数替代清单中，第 506 行却声称 `mod` 不属于首轮范围，编码人员无法判断是否需在首轮中实现
   - 所在位置：§4.3（算术运算）
   - 严重程度：一般
   - 改进建议：做出明确的唯一决策并修正矛盾：若 `mod` 属于首轮则修正第 506 行措辞，若不属首轮则从第 500 行清单移除
   - a组件迭代的修改措施：§4.3 `%` 整数约束段落将"不属于首轮范围"改为"属于首轮范围"

4. **问题描述**：§4.3 中 `scalar-op-vec` 方向辅助函数定义为包级独立函数置于 `compute_vector_decl.cj`，但 §2 中 `lib.cj` 的 `public import` 列表未重导出这些函数，违反"下游消费者只需 `import glm.*` 即可使用所有公共 API"的封装原则
   - 所在位置：§2（公共 API 面设计）+ §4.3（具名函数的定义位置）
   - 严重程度：一般
   - 改进建议：在 §2 `lib.cj` 的 `public import` 列表中补充 `{ add, sub, mul, div, mod }` 的导出行，同步更新 §8 迁移文件清单
   - a组件迭代的修改措施：§2 `lib.cj` 的 `public import` 列表新增 `add/sub/mul/div/mod` 导出行，同步更新 §8.1 与 §8.2
