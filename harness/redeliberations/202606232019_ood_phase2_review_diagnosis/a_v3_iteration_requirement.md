根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

### 2.1 缺乏全局修复执行优先级排序（严重度：一般）
§6 给出了依赖图与串行约束分析，但没有给出明确的"先做什么后做什么"的全局执行优先级建议。T5/T6/T7 被归类为"阻塞合并"，但报告未将这些阻塞项置于一个统合的 fix sequence 中。执行者拿到报告后需要自行从依赖分析中推导执行顺序。
**改进建议**：在 §6 开头或末尾追加明确的推荐执行顺序（例如：Phase 1=修复 T5 解除 CI 阻塞 → Phase 2=补充 T6/T7 严重覆盖缺口 → Phase 3=T1/T19 偏差文档 → Phase 4=其余一般/轻微项），并标注各阶段是否可以并行。

### 2.2 T15 未显式限定 NaN 测试作用于浮点类型的范围约束（严重度：一般）
T15 讨论了 OOD §5 要求的 NaN 传播行为测试，但未在自身段落中说明 NaN 测试仅适用于 Float32/Float64 类型矩阵。当前 9 个矩阵测试文件几乎全部使用 Int64 作为底层类型（T16 已确认这一事实），而 Int64 等整数类型不存在 NaN。若执行者先读 T15 着手编写测试，可能会尝试在 Int64 矩阵上构造 NaN 场景。
**改进建议**：在 T15 段落末尾追加显式约束说明，例如："注意：NaN 行为测试仅适用于 Float32/Float64 浮点类型矩阵；Int64 等整数类型不存在 NaN 概念。应在 T16（浮点类型基础覆盖）完成后执行本项。"

### 2.3 T13 缺乏具体可操作的验证方法指南（严重度：一般）
T13 准确识别了原型验证计划 5 个步骤均未执行，也区分了"测试域内 alias 验证"与"外部导入验证"两个层面。但报告只指出了"什么没做"，没有给出"具体怎么做"的操作指南。例如如何配置 cjpm.toml、如何创建外部验证项目、回退方案的具体实现方式。
**改进建议**：补充一个"验证步骤"子章节，至少给出三个最常见的 cjpm 子包配置模式以及验证命令，或明确标注"需先查阅 cjpm 文档确认子包路由机制"。

### 2.4 T5 未建议先验证 OOD DEVIATION 本身的正确性（严重度：一般）
T5 的根因结论为"集成测试的预期值是错误的"，直接要求将预期值修改为符合 OOD DEVIATION 的结果。但报告未要求执行者在修改前独立验证 OOD DEVIATION（Mat4x4←Mat4x2 丢弃源列 2/3 数据）是否与 GLM 1.0.3 真实行为一致。
**改进建议**：在"影响范围"后追加验证步骤要求，例如："修复前应打开 references/glm-1.0.3/glm/glm/detail/type_mat4x4.inl 第 246 行附近确认 GLM mat4x4(colon, mat4x2 const& m) 实际行为，确认 OOD DEVIATION 的描述正确后再修改预期值。"

### 2.5 T18 给出多个平行选项降低可操作性（严重度：轻微）
T18 的结论为"建议保留 ZeroVec 测试，但可将 9 个合并为 1~2 个代表性场景，或选择保持现状"。三个选项并列为执行者提供了多种选择，将决策责任推给了执行者。
**改进建议**：选定其中一个方案并给出理由，例如"建议保留全部 9 个 ZeroVec 测试——它们的维护成本极低（模板化代码），且作为零向量×任意矩阵=零矩阵的数学恒等式验证，每个矩阵类型均有独立的验证价值。"

### 2.6 T16 覆盖标准估算未讨论测试模式共享优化（严重度：轻微）
T16 提出的最小覆盖标准为"9 类型 × 3 场景 × 2 精度 = 54 个新增测试"，但未评估不同类型的测试模式是否可共享。54 的估算可能高估实际需要的新增测试数量，执行者可能因"54 个太多"而推迟行动。
**改进建议**：补充说明可利用仓颉的 @Types[T in <Float32, Float64>] 类型参数化测试机制减少测试代码重复，或将覆盖标准简化为"每个矩阵族系（按行数分 3 组：C=2/3/4）至少一组场景 × 2 精度"，约需 3×3×2=18 个新增测试。

## 历史迭代回顾

### 已解决的问题（在第 1 轮反馈中提出、第 2 轮已修正）
- T16 可操作性严重不足 → v2 已补充可量化最小覆盖标准
- T13 事实表述偏差及计数错误 → v2 已修正文件存在性描述和计数
- T18 无明确结论 → v2 已追加明确结论（保留 ZeroVec）
- T8 编译器警告声明依赖未经验证 → v2 已标注为"未验证"
- T10 未确认测试框架对异常消息断言的支持 → v2 已补充 try-catch 示例模式
- 缺失跨问题依赖分析 → v2 已新增 §6 依赖分析

上述 6 项均为第 1 轮反馈提出，在第 2 轮诊断报告（a_v2_diag_v1）中均已妥善处理，不再作为本轮问题。

### 持续存在的问题（无）
第 2 轮反馈的 4 项问题（全局优先级、T15 范围约束、T13 操作指南、T5 偏差验证）均为本轮新识别，尚未进入修正周期。

### 新发现的问题（本轮组件 B 诊断报告识别的 6 项）
1. **全局修复执行优先级排序缺失**（§2.1） — 第 2 轮反馈新增，需在 §6 追加分阶段执行顺序建议
2. **T15 未限定浮点类型范围**（§2.2） — 第 2 轮反馈新增，需在 T15 段落追加 NaN 仅适用于浮点类型的显式约束
3. **T13 缺乏操作指南**（§2.3） — 第 2 轮反馈新增，需补充 cjpm 子包配置验证步骤
4. **T5 未要求先验证 OOD DEVIATION**（§2.4） — 第 2 轮反馈新增，需追加偏差验证步骤
5. **T18 保留多个平行选项**（§2.5） — 本轮新识别，需选定唯一推荐方案
6. **T16 未讨论测试模式共享优化**（§2.6） — 本轮新识别，需补充类型参数化测试或少覆盖量的优化建议

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606232019_ood_phase2_review_diagnosis\a_v2_diag_v1.md

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606232019_ood_phase2_review_diagnosis\requirement.md
