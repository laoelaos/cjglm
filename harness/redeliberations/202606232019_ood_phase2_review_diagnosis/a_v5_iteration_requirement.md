根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

### 事实错误

**T16 族系分组策略表述矛盾**（一般）
- §4 T16 中"按行数分 3 组：C=2/3/4"——"行数"与"C=2/3/4（列数）"自相矛盾
- 族系内共享模板可行性论证不足，18 个新增测试的工作量估算缺少模板机制论证，执行者可能低估工作量
- **改进建议**：修正"行数→列数"的表述错误；对族系内共享模板的可行性给出具体示例或标注"需进一步评估泛型化测试模板的复杂度"

**outerProduct 缺失重载方向描述不精确**（轻微）
- §1 T7 中 `Vec2×Vec3→Mat3x2` 等标注存在行/列来源歧义——行数=3 但 v1.len()=2，来源关系未明确
- **改进建议**：统一标注为 `outerProduct(Vec{rows}, Vec{cols})` 格式，消除行/列来源歧义

### 深度与完整性不足

**T9/T10 共享文件冲突未识别**（一般）
- T9 操作 `test_common.cj`、`test_geometric.cj`、`test_geometric_refract.cj` 三个文件
- T10 同样操作这三个文件
- §6 依赖图及分组将二者均标记为可并行，但并行执行时会产生合并冲突
- **改进建议**：将 T9 和 T10 改为"必须串行"或"建议合并为一次提交"，标注共享文件清单

**T7/T10 在 test_matrix.cj 的共享文件冲突未识别**（一般）
- T7 操作 `test_matrix.cj:163-255`（新增 12 个测试重载）
- T10 操作 `test_matrix.cj:258-290`（修改 6 个 stub 测试）
- T7 新增测试超出第 255 行边界将导致 T10 所依赖的行号偏移，产生合并冲突
- **改进建议**：在依赖图中标注共享文件冲突，建议串行执行或合并为同一次提交

**T10 可复用性考虑缺失**（一般）
- §4 T10 提供了 try-catch 验异常消息的模式和代码示例，但未讨论此模式在 4 个文件、涉及数十个 stub 测试函数中的重复使用问题
- **改进建议**：将 try-catch 模式抽取为辅助函数（如 `expectStubMessage(func)`），并给出函数签名示例；标注涉及的全部 stub 测试函数数量估算

**T8 缺乏决策路径指引**（一般）
- §3 T8 正确标注了编译器警告行为为"未验证"，但未给出从"未验证"到具体动作的决策路径
- **改进建议**：补充决策树：1) 编译验证 cjc 1.1.0 是否产生 unused variable 警告 → 2a) 无警告→无需操作；2b) 有警告→移除收缩方向 `let zero` 声明或在纯收缩重载中改用 `_` 前缀命名；2c) 编译错误→标注加版本号

**T6/T7 与 T16 合并后范围膨胀未被量化**（一般）
- §1 多处建议"Phase 2 直接按 T16 标准同时完成 Float 变体"，但未给出范围膨胀预估
- T6 从 24 个测试膨胀至最多 72 个（24×3 类型），T7 从 12 个测试膨胀至最多 36 个（12×3 类型），合计新增 72 个测试
- **改进建议**：在"前向依赖标注"中给出量化的范围膨胀预估，帮助执行者评估"一次性完成 vs 分阶段完成"的成本权衡

### 可操作性问题

**T6/T7 严重度与实际阻塞性质不匹配**（一般）
- requirement.md 将 T5/T6/T7 统一列入"阻塞合并"类别，T5 正确标注了"严重（CI 阻塞）"，T6/T7 仅标注为"严重"而未标注阻塞性质
- **改进建议**：在 T6/T7 严重度后追加注释说明其阻塞性质，例如"严重（覆盖率缺口 > 66%，与 T5 同为阻塞合并项）"

**T6/T7 Float 变体建议未明确属性**（一般）
- "建议 Phase 2 直接按 T16 的覆盖标准同时完成 Float 变体"——该建议改变了 T6/T7 的原定范围（仅 Int64），但未明确是可选建议还是强制要求
- **改进建议**：明确标注"此为可选优化建议，非强制性；若选择不实施，保留 T6→T16/T7→T16 前向依赖标注"

**T7-cs 和 T14 的推测性内容**（轻微）
- §3 T7-cs 包含"**可能原因**：Mat2x2 是最先编写的文件..."
- §4 T14 包含"缺失的原因可能是编写测试时认为 `col()` 已委托 `[]`..."
- 推测性内容对执行者无实际帮助
- **改进建议**：删除"可能原因"段落，或将推测性内容转移至注释/脚注并明确标注"推测"

## 历史迭代回顾

### 已解决的问题
- **T16 覆盖标准量化**（迭代 1 问题 1）：已增加覆盖标准（3 场景 × 2 精度的推荐方案）——本轮新增的族系表述矛盾为修正方向而非重新提出
- **T13 事实偏差**（迭代 1 问题 2）：已修正文件存在性描述和计数
- **T18 无明确结论**（迭代 1 问题 3）：已选定唯一推荐方案（保留全部 9 个）
- **T8 未经验证推测**（迭代 1 问题 4）：已标注为"未验证"
- **T10 框架确认**（迭代 1 问题 5）：已加入仓颉测试框架适配分析
- **缺失依赖分析**（迭代 1 问题 6）：已新增 §6 跨问题依赖分析
- **全局执行优先级**（迭代 2 问题 1）：已追加 Phase 1~4 分阶段执行顺序
- **T15 NaN 浮点约束**（迭代 2 问题 2）：已追加浮点类型约束说明
- **T13 操作指南**（迭代 2 问题 3）：已补充验证步骤指南子章节
- **T5 验证要求**（迭代 2 问题 4）：已追加修复前验证要求
- **T17 rowSh 方向推荐**（迭代 3 问题 1）：已推荐 Mat4x4→Mat4x3
- **T6/T7 前向依赖**（迭代 3 问题 2）：已在依赖图中追加 T6→T16、T7→T16
- **T2 严重度调整**（迭代 3 问题 3）：已从"轻微"改为"一般"
- **T15 GLM 参考位置**（迭代 3 问题 4）：已补充具体函数名和行号范围

### 持续存在的问题
（无——迭代 4 的 9 个问题均为本轮新识别的问题）

### 本轮新发现的问题
1. T16 族系分组策略表述矛盾（"行数" vs "C=2/3/4（列数）"自相矛盾）
2. outerProduct 缺失重载方向描述不精确（标注格式歧义）
3. T9/T10 共享文件冲突未识别（均操作 test_common/geometric/geometric_refract.cj）
4. T7/T10 在 test_matrix.cj 共享文件冲突未识别
5. T10 可复用性考虑缺失（try-catch 模式重复使用问题）
6. T8 缺乏决策路径指引（"未验证"到具体动作无路径）
7. T6/T7 严重度与实际阻塞性质不匹配（未标注阻塞合并性质）
8. T6/T7 Float 变体建议属性不明确（可选 vs 强制未明确；范围膨胀未量化）
9. T7-cs/T14 推测性内容（"可能原因"对执行者无实际帮助）

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606232019_ood_phase2_review_diagnosis\a_v4_diag_v1.md

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606232019_ood_phase2_review_diagnosis\requirement.md
