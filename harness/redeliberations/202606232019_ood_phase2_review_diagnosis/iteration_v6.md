# 再审议判定报告（v6）

## 判定结果

RETRY

## 判定理由

诊断报告（b_v6_diag_v1）共检出 1 项严重、7 项一般、1 项轻微问题。质询报告（b_v6_challenge_v1）确认诊断结论 LOCATED，但指出 §2.1 T8 计数偏差的严重度标记不当——该偏差不影响核心判断也不影响修复路径，应降为[一般]。即使采纳此调整，报告仍包含 8 项一般等级问题，满足 RETRY 条件。需要组件 A 重新运行以修正全部一般及以上等级问题。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：T8 纯收缩方向计数为 60 个重载，实际枚举仅 54 个，存在数字偏差
- **所在位置**：§3 T8，第 226 行
- **严重程度**：一般
- **改进建议**：修正计数为 27+27=54，或附上枚举清单以证明计数准确性

- **问题描述**：T6/T7 同为阻塞合并项却放入 Phase 2，Phase 1 命名"解除 CI 阻塞"隐含地将二者排除，未说明"CI 阻塞"与"评审阻塞"的区分逻辑
- **所在位置**：§1 严重度标注与 §6 阶段划分
- **严重程度**：一般
- **改进建议**：在 Phase 1 描述中明确区分"CI 运行时失败"与"覆盖率评审阻塞"两种阻塞类型，或调整 Phase 1 命名使其包容二者

- **问题描述**：T10 try-catch 示例中 `@Fail` 与 `catch (e: Exception)` 存在互斥风险，可能导致误导性测试失败消息
- **所在位置**：§4 T10，第 302-312 行示例代码
- **严重程度**：一般
- **改进建议**：在示例代码下方追加注释警告此风险，或改用 `@ExpectThrows` 与独立消息验证函数分离的方式

- **问题描述**：T13 模式 C 建议将 `src/ext/` 下文件移至 `src/` 并改 `package glm`，但未分析此操作对 `tests/glm/test_ext.cj` 中 16 个测试用例的编译影响
- **所在位置**：§5 T13 模式 C 描述（第 540-544 行）
- **严重程度**：一般
- **改进建议**：在模式 C 中追加副作用分析，标注受影响的测试文件及应对措施

- **问题描述**：T16 提出按族系共享测试模板将 54 个测试压缩至 18 个，但未提供 CangJie 测试框架层面参数化模板的可行性验证
- **所在位置**：§4 T16，第 415-417 行
- **严重程度**：一般
- **改进建议**：补充 CangJie 测试框架中实现参数化测试模板的具体机制或约束说明；或明确标注"18 个测试仅为理论最大值"并给出 54 个方案的独立成本评估

- **问题描述**：§6 推荐 Phase 2 直接覆盖 Float 变体以消除前向依赖，但未评估与后续 Phase 4 T16 标准不一致导致返工的风险
- **所在位置**：§6 推荐执行顺序表格（第 618-650 行）
- **严重程度**：一般
- **改进建议**：在 Phase 2 建议后追加约束条件——仅当执行者一致或 T16 标准已冻结时此方案才有效

- **问题描述**：T2 Hashable 测试指出了 9 个矩阵类型的测试缺失，但未给出测试用例编写指南（如应测试 hash 一致性、冲突概率、还是仅编译通过）
- **所在位置**：§4 T2（第 246-266 行）
- **严重程度**：一般
- **改进建议**：补充 Hashable 测试的最小覆盖标准说明

- **问题描述**：T14 指出 Mat3x3/Mat4x4 缺失 ColOutOfBounds/ColNegative 测试，但未说明越界时的预期行为
- **所在位置**：§4 T14（第 338-355 行）
- **严重程度**：一般
- **改进建议**：补充预期行为说明，例如"测试预期 `col(3)` 越界时抛出 Exception，与 `[3]` 行为一致"

- **问题描述**：T15/T16 的依赖方向标注与 Phase 4 分组不一致，T16 先于 T15 的串行要求不易直观识别
- **所在位置**：§6 组 B（第 639 行）
- **严重程度**：轻微
- **改进建议**：在 Phase 4 分组中将 T15/T16 标注为"须串行：T16 先于 T15"，或将两者放入同一子组
