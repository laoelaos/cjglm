根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

1. **T17：推荐的 `rowSh` 方向缺失，可操作性不足**（一般）
   - 诊断报告列出了 9 个满足 `rowSh` 模式的 fromMat 方向，但未向执行者推荐具体选择哪一个方向来实现集成测试。执行者需自行从 9 个中挑选，可能选出非典型方向或导致测试数量膨胀到 9 个。
   - 改进建议：推荐一个代表性的 `rowSh` 方向并说明选择理由，例如 `Mat4x4→Mat4x3` 或 `Mat3x4→Mat3x3`。

2. **跨问题依赖分析：T6/T7 与 T16 的前向依赖未标注**（一般）
   - T6（非方阵标量-矩阵测试）和 T7（matrixCompMult/outerProduct 测试）在依赖图中标注为"独立"，且 Phase 2 中被标记为"可并行"。但 T6 和 T7 当前仅在 Int64 类型上做测试。T16（Float 类型覆盖）在 Phase 4 实施时需回头为 T6/T7 新增的测试追加 Float 变体——造成不必要的重新打开。
   - 改进建议：在依赖图及 Phase 2 说明中追加注释，或建议 Phase 2 直接按 T16 的覆盖标准同时完成以消除返工。

3. **T2：严重度评定与 OOD 要求存在偏差**（一般）
   - 报告将 T2（补充 Hashable 测试）评定为"轻微"。但 OOD §3.1 明确要求 9 个矩阵类型通过 `@Derive[Hashable]` 自动派生哈希支持——这是必须满足的设计要求。`@Derive` 宏展开正确但生成错误的哈希逻辑（如所有矩阵哈希到相同值）在编译期无法检测，必须由运行时测试暴露。
   - 改进建议：将 T2 严重度从"轻微"改为"一般"，并说明理由。

4. **T15：未指明 NaN 测试应参考的具体 GLM 乘法函数位置**（轻微）
   - 报告提到 GLM 参考代码位于 `func_matrix.inl` 及 `type_mat4x4.inl`，但未给出具体的函数名或行号范围。执行者需要自行搜索目标函数。对比 T5 的 GLM 引用（给出了具体构造函数名称和行号范围），T15 的引用不够精确。
   - 改进建议：补充具体的 GLM 函数引用信息，如 Mat×Vec 乘法的模板函数名及所在文件行号范围。

## 历史迭代回顾

分析历史反馈与当前反馈的关系：

- **已解决的问题**（出现在历史反馈但当前反馈中不再提及）：
  - T16可操作性不足（第1轮问题1）— v3已给出量化覆盖标准，当前反馈未提及
  - T13事实偏差/计数错误（第1轮问题2）— v3已修正，当前反馈未提及
  - T18无结论（第1轮问题3）— v3已给出明确结论，当前反馈未提及
  - T8编译器警告未经确认（第1轮问题4）— v3已标注"未验证"，当前反馈未提及
  - T10框架支持能力（第1轮问题5）— v3已提供try-catch方案，当前反馈未提及
  - 缺失跨问题依赖分析（第1轮问题6）— v3已添加§6依赖分析，当前反馈未提及
  - 无优先级排序（第2轮问题1）— v3已添加Phase 1-4，当前反馈未提及
  - T15未限定NaN类型（第2轮问题2）— v3已追加约束说明，当前反馈未提及
  - T13无操作指南（第2轮问题3）— v3已补充验证步骤，当前反馈未提及
  - T5验证步骤缺失（第2轮问题4）— v3已追加验证要求，当前反馈未提及

- **持续存在的问题**（本轮与上一轮反馈共同提及，需重点解决）：
  - T17推荐方向缺失（第3轮问题1）— 本轮仍被指"未推荐方向"，建议在 v4 中明确选择一个代表性方向并给出理由
  - T6/T7→T16前向依赖未标注（第3轮问题2）— 本轮仍被指"依赖图缺边"，建议在 v4 中追加依赖标注或调整Phase策略
  - T2严重度评定偏差（第3轮问题3）— 本轮仍被指"轻微不当"，建议在 v4 中改为"一般"
  - T15 GLM引用精度不足（第3轮问题4）— 本轮仍被指"函数位置不精确"，建议在 v4 中补充具体函数名和行号

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606232019_ood_phase2_review_diagnosis\a_v3_diag_v1.md

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606232019_ood_phase2_review_diagnosis\requirement.md
