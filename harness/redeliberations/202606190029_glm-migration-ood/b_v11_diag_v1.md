# 质量审查诊断报告（v11 — 第 11 轮）

**审查对象**：`a_v11_copy_from_v10.md`（GLM 1.0.3 仓颉迁移首轮 OOD 设计方案）
**审查维度**：需求响应充分度、事实准确性、逻辑一致性、深度与完整性、落地可操作性
**审查范围**：侧重于内部审议（设计-验证循环）未充分覆盖的维度，避免重复验证已确认的维度

---

## 一、总体评价

产出整体质量较高，对上一轮（v10）的 4 项审查反馈已逐一落实，且附录中可见完整的 11 轮迭代演进痕迹，说明文档维护严谨。设计方案在架构合理性、接口完整性和边界场景覆盖方面均达到较高水平，可直接指导编码实现落地。

但仍存在以下待改进的质量问题，集中在**残留事实错误**、**结构和依赖描述不一致**两个方面。

---

## 二、具体质量问题

### P1（中等）— §4.3 残留导入路径错误

**问题描述**：§4.3 "具名函数的定义位置与形态"子节中，`scalar-op-vec` 方向函数的导入方式仍表述为"通过 `import glm.detail.scalar_vec_ops.*` 或通过 `glm` 包统一引入"。`scalar_vec_ops.cj` 声明了 `package glm.detail`，其函数属于 `glm.detail` 包命名空间，正确的导入路径应为 `import glm.detail.{ add, sub, ... }` 或 `import glm.detail.*`。`glm.detail.scalar_vec_ops` 会被编译器解释为不存在的子包 `glm.detail.scalar_vec_ops`。

**所在位置**：§4.3，第 617 行（"调用方通过 `import glm.detail.scalar_vec_ops.*`..."）

**严重程度**：中等

**背景说明**：此错误在 v10 P1 中已被发现并修复了 `lib.cj` 代码块中的对应表述（§2 公共 API 面设计），但 §4.3 中的此处引用未被同步修正，属于修复遗漏。

**改进建议**：将 `import glm.detail.scalar_vec_ops.*` 改为 `import glm.detail.{ add, sub, mul, div, mod }` 或 `import glm.detail.*`。同步全文搜索确认无其他残余的 `glm.detail.scalar_vec_ops` 路径引用。

---

### P2（中等）— 测试目录结构与仓颉包路径解析规则不匹配

**问题描述**：§2 初始目录树形图（第 139 行）显示测试目录为 `tests/glm.detail/`（以英文句点分隔）。仓颉包声明规则要求目录路径匹配包名——`package glm.detail` 应映射到 `glm/detail/` 目录（以路径分隔符组织子目录），而非 `glm.detail/`（单目录名包含句点）。此外，§12.3（第 1714 行）同时引用了 `tests/glm.detail/` 和 `tests/glm/` 两个测试包路径，但目录树形图仅展示了 `tests/glm.detail/`，缺少 `tests/glm/`。

**所在位置**：§2 初始目录树形图（第 139 行）、§12.3 测试包组织结构（第 1714-1716 行）

**严重程度**：中等

**改进建议**：
1. 将目录树形图和第 1714-1715 行中的 `tests/glm.detail/` 改为 `tests/glm/detail/`，使目录结构与 `package glm.detail` 的路径解析规则一致。
2. 在目录树形图中补充 `tests/glm/` 目录条目，使其与 §12.3 的文本描述一致。

---

### P3（一般）— §1 出现重复的 "### 设计目标" 标题

**问题描述**：§1 概述中连续出现两个 `### 设计目标` 三级标题（第 9 行和第 13 行）。第一个标题后的内容（范围可追溯性说明）本应属于独立的子节或 §8 范围追溯的指引性声明，却被置于与第二个"设计目标"相同的标题层次下，造成结构歧义。

**所在位置**：§1 概述，第 9-15 行

**严重程度**：一般

**改进建议**：二者择一修改：
- 方案 A：将第一个标题改为 `### 范围可追溯性承诺`，与第二个"设计目标"区分。
- 方案 B：删除第一个 `### 设计目标` 标题，将"范围可追溯性"段落移入第二个"设计目标"段落后作为补充说明，或移入 §8 范围追溯章节。

---

### P4（一般）— §3.5/§3.6 残留 "`const if`" 术语未按 D34 修正

**问题描述**：v11 P1 的修复承诺"全文将 `const if (...)` 改写为 `if (...)`"（文档摘要第 3 行、修订说明第 1886 行），并在 §7 新增 D34 设计决策记录此约定。但 §3.5（第 272 行）和 §3.6（第 339 行）仍存在 `const if` 的表述（`使用 const if 在编译期分支`、`函数体内使用 const if 配合 is 运算符`）。虽然这些是自然语言中的指代性描述而非关键字代码示例，但 D34 的"统一改写"决策应涵盖所有文本。

**所在位置**：§3.5 第 272 行、§3.6 第 339 行

**严重程度**：一般

**改进建议**：将上述两处 `const if` 改写为"`const` 函数中的 `if` 表达式"或"编译期 `if` 分支"，与 D34 的约定一致。

---

### P5（一般）— §2 模块间依赖表 scalar_vec_ops 的依赖描述不完整

**问题描述**：§2 模块依赖图（第 92 行）声明 `scalar_vec_ops → qualifier（无包内其他依赖，仅使用 Qualifier 约束）`，但 `scalar_vec_ops.cj` 中定义的函数签名引用了 `VecN<T, Q>` 类型（如 `add(s: T, v: Vec2<T,Q>): Vec2<T,Q>`），在依赖拓扑上应依赖于 `type_vecN.cj`。虽然同一包内所有文件在编译时互见、且在 Cangjie 包编译模型下 this 不影响编译，但依赖表描述的"无包内其他依赖"在语义上不准确，可能误导后续轮次维护者。

**所在位置**：§2 模块间依赖图，第 92 行

**严重程度**：一般

**改进建议**：将依赖从 `scalar_vec_ops → qualifier` 修改为 `scalar_vec_ops → qualifier + type_vec1~4`，或在说明中补充"同包内函数签名引用 Vec 类型，但不构成独立编译依赖（Cangjie 包级编译模型自动包含同包所有定义）"的澄清。

---

## 三、整体评价

- **需求响应充分度**：设计方案完整响应了"将 GLM 1.0.3 用仓颉重新实现，做首轮迁移的 OOD 设计"的需求，覆盖了基础设施层（shim/配置）、向量类型系统（Vec1~Vec4）、类型别名体系（256 个）、运算行为约定（算术/位/比较/bool 逻辑）、构造函数体系（GLSL 5.4.1 规范）以及迁移成本评估。
- **事实准确性**：上述 P1、P2、P4、P5 涉及的事实错误均为残留问题/修复遗漏，非本次引入的新错误。整体质量可靠。
- **深度与完整性**：文档在异常场景（§9 零向量/除零/NaN-Infinity/类型转换边界）、并发设计（§6 多线程场景表）、验证策略（§10 设计阶段验证 + §12 验收标准）等维度覆盖充分，可直接指导编码实施。
- **落地可操作性**：§2 给出了 cjpm.toml 模板和初始目录结构，§8.1 给出了迁移文件清单及增量验证节点，§4.1 给出了完整构造函数签名清单（仓颉语法），整体可直接作为编码实现指南。

以上 5 个问题中，P1-P2 为中等严重程度（影响导入路径和包目录结构的正确性），P3-P5 为一般严重程度（影响结构清晰度和文档准确性）。建议在下轮迭代中集中修复。
