## 迭代第 1 轮

1. **问题描述**：`equalExact` 的 `const` 声明与 extend 块中 `const` 函数的编译器支持性未经验证
   - 所在位置：§4.5（第543行），§3.2（第156行）
   - 严重程度：严重
   - 改进建议：补充 extend 块中定义 `const` 函数的语法示例，标注验证状态；若不受支持则降级为非 `const`
   - 解决结果：已在 a_v2 中解决（§4.5 新增 extend 块定义 const 函数的完整语法示例，标注验证状态并提供降级路径）

2. **问题描述**：`Inf == Inf` 在容差路径下返 false 的行为差异未纳入迁移成本评估
   - 所在位置：§9.3（第952行），§11
   - 严重程度：严重
   - 改进建议：在 §11 新增子节评估该行为差异影响，补充搜索模式和替换规则
   - 解决结果：已在 a_v2 中解决（§9.3 重写移除推测性"改进方案"，新增 §11.5 子节系统评估差异机理、受影响模式、搜索规则和工作量）

3. **问题描述**：`const if (T(0) is Float64)` 方案的三项核心依赖均未验证
   - 所在位置：§3.5（第234-268行），§10（第991-999行），§7 D29（第825-846行）
   - 严重程度：严重
   - 改进建议：将"编码前验证"升级为设计阶段验证，编写原型测试代码确认可行性；无法验证时增加风险标注并明确回退路径
   - 解决结果：已在 a_v2 中解决（§10 升级为"设计阶段验证要求"，以表格形式列出三项核心依赖及验证方法）

4. **问题描述**：`@OverflowWrapping` 自动继承假设未经验证
   - 所在位置：§4.6（第572-578行），§7 D30（第848-856行）
   - 严重程度：严重
   - 改进建议：明确标注策略取决于编译器的未文档化行为，将备选方案工作量纳入 §11 迁移成本评估
   - 解决结果：已在 a_v2 中解决（§4.6/§7 D30 标注依赖编译器未文档化行为，§11.6 新增备选方案工作量评估）

5. **问题描述**：`epsilonOf<T>()` 的 where 约束在非执行路径上的编译器行为不确定性
   - 所在位置：§3.5（第246-252行），§7 D5（第653-655行）
   - 严重程度：一般
   - 改进建议：增加兼容性验证项，必要时移除 where 约束或内联 epsilon 逻辑
   - 解决结果：已在 a_v2 中解决（§3.5 扩展为兼容性验证段落，提供方案 A 移除 where 约束 + 方案 B NumericLimits<Bool> 空壳特化两条路径）

6. **问题描述**：`scalar-op-vec` 方向具名函数的命名空间污染风险未评估
   - 所在位置：§4.3（第514-517行），§2（第67行）
   - 严重程度：一般
   - 改进建议：新增设计决策评估命名冲突风险，给出调用方最佳实践
   - 解决结果：已在 a_v2 中解决（§4.3 新增"命名空间污染风险评估"段落，评估风险等级为低-中并给出三条最佳实践）

7. **问题描述**：§9.3 "改进方案"段落混入设计文档主体，造成编码人员混淆
   - 所在位置：§9.3（第952行）
   - 严重程度：一般
   - 改进建议：将实现约束合并到行为表格，移除推测性方案描述
   - 解决结果：已在 a_v2 中解决（§9.3 Infinity 比较行重写，移除"改进方案"推测性描述，约束合并入行为表格）

8. **问题描述**：D29 验证树过于复杂，将设计不确定性实质性地推至编码阶段
   - 所在位置：§7 D29（第820-846行）
   - 严重程度：一般
   - 改进建议：简化验证策略为二值决策，移除细分的逐级回退方案
   - 解决结果：已在 a_v2 中解决（D29 完全重写为二值决策——单一原型测试验证完整表达式链，失败则整体回退到精确比较路径）

9. **问题描述**：256 个别名的首轮生成策略未明确
   - 所在位置：§3.8（第352-356行）
   - 严重程度：一般
   - 改进建议：补充外部脚本生成策略的建议
   - 解决结果：已在 a_v2 中解决（§3.8 首轮实现策略新增"外部脚本生成策略"段落，推荐 Python/Node.js 脚本按命名约定模板枚举 256 种组合）

10. **问题描述**：`add(v,s)` 扩展成员函数与 `v + s` 运算符的功能重叠未明确
    - 所在位置：§4.3（第505-513行，第514-518行）
    - 严重程度：一般
    - 改进建议：说明二元运算符和具名函数的关系及选择指南
    - 解决结果：已在 a_v2 中解决（§4.3 新增"二元运算符与具名函数的关系及选择指南"段落，说明三种形式关系和编码建议）

11. **问题描述**：§7 D29 的回退路径在文档其他章节中未体现
    - 所在位置：§7 D29 vs §3.5、§4.5、§9.3
    - 严重程度：一般
    - 改进建议：在 §3.5、§4.5、§9.3 增加回退路径标注
    - 解决结果：已在 a_v2 中解决（§3.5 备选路径段落末尾新增"回退路径一致性"标注，§4.5 新增"=="回退路径标注子段落，§9.3 Infinity 比较行新增回退路径影响说明）

12. **问题描述**：`extend` 块中定义二元运算符的语法细节不足
    - 所在位置：§3.2（第162-163行）、§4.4（第525行）
    - 严重程度：轻微
    - 改进建议：提供至少一个 `extend` 块的完整语法示例
    - 解决结果：已在 a_v2 中解决（§4.4 位运算章节开头新增完整 extend 块语法示例，含 `<<` / `>>` / `&` / `|` / `^` / `bitwiseNot` 完整定义）

## 迭代第 2 轮

1. **问题描述**：§8.1 与 §8.2 范围追溯编号不一致，`setup.cj` 在 §8.1 的 §3E 序号为 "S1"，但在 §8.2 中对应项序号为 "1"，破坏追溯可信度
   - 所在位置：§8.1（line 905）vs §8.2（line 926）
   - 严重程度：严重
   - 改进建议：统一两表编号体系，确保交叉引用时每一个文件的编号在两表中完全一致
   - 解决结果：已在 a_v3 中解决（§8.1 文件清单 setup.cj 的 §3E 序号从 "S1" 修正为 "1"，与 §8.2 对照表一致）

2. **问题描述**：D25-D27 三条设计决策仅存在于文档末尾的修订说明中，未移入 §7「设计决策」主节，编码人员若仅查阅 §7 将无法获取完整决策记录
   - 所在位置：§7（lines 664-897 缺失 D25-D27）；修订说明（lines 1303-1321）
   - 严重程度：严重
   - 改进建议：将 D25-D27 插入 §7 中 D24 之后、D28 之前，与现有编号顺序一致
   - 解决结果：已在 a_v3 中解决（将 D25（VecN<Bool,Q>.bitwiseNot() 行为差异）、D26（跨类型构造函数标量参数使用目标类型）、D27（Qualifier 跨 Q 赋值/转换行为约定）从修订说明移入 §7，插入 D24 之后、D28 之前）

3. **问题描述**：`mod` 具名函数在浮点 Vec 上的实现方案未定义 — 浮点类型不支持 `%` 运算符，且仓颉标准库不含 `fmod`/`remainder`，但文档声明 `mod` 属于首轮范围，编码人员无法直接实现
   - 所在位置：§4.3（line 518, 527）、§4.3（line 166）
   - 严重程度：中等
   - 改进建议：明确选择实现策略（方案A：算术恒等式 `x - y * trunc(x/y)`；方案B：限制 `mod` 仅支持整数 Vec；方案C：引入外部依赖），并在 §12 补充对应验收测试
   - 解决结果：已在 a_v3 中解决（§4.3 补充浮点 mod 实现策略——整数 T 用 `%` 运算符；浮点 T 用算术恒等式 `x - y * trunc(x/y)`，Float32 提升至 Float64 截断再降级，双路径通过 `const if (isIec559Of<T>())` 编译期分支选择）

4. **问题描述**：Vec1 的 `const init(x: T)` 同时承担 const 构造、运行时构造和标量填充构造三重角色，但构造函数清单未显式标注其为标量填充构造的等效入口
   - 所在位置：§4.1 Vec1 构造函数清单（lines 385-395）
   - 严重程度：中等
   - 改进建议：在 Vec1 构造函数清单中为 `const init(x: T)` 增加注释说明其承担标量填充构造角色
   - 解决结果：已在 a_v3 中解决（§4.1 Vec1 构造函数清单为 `const init(x: T)` 增加注释："同时承担标量填充构造角色（等效于其他 VecN 的 `init(scalar: T)`）"）

5. **问题描述**：`isIec559Of<Float16>()` 的说明文字 "非 IEC 60559 浮点类型" 可能引起语义混淆，使人误以为 Float16 本身不符合标准规范
   - 所在位置：§10（line 1015）
   - 严重程度：一般
   - 改进建议：改写为更精确表述，如 "`is` 类型检测因 `Float16` 不匹配 `Float64`/`Float32` 而隐式返回 `false`"
   - 解决结果：已在 a_v3 中解决（§10 对照表 Float16 行附注改写为 "`isIec559Of<Float16>()` 因 Float16 不匹配 Float64/Float32 而隐式返回 false，无需显式排除"）

6. **问题描述**：§12.3 未约定测试包与源码包的映射关系，以及测试代码如何访问 `glm.detail` 中的 `internal` 可见性类型
   - 所在位置：§12.3（line 1257）
   - 严重程度：一般
   - 改进建议：补充测试包组织约定，明确命名空间映射关系及测试范围的边界
   - 解决结果：已在 a_v3 中解决（§12.3 补充测试包组织结构约定——测试包命名空间与源码包一一对应（`tests/glm.detail/` ↔ `glm.detail`），internal 类型通过同名包直接访问，公共 API 测试通过 `import glm.*` 遵循下游消费者路径，文件级映射约定）

## 迭代第 3 轮

1. **问题描述**：`bitwiseNot()`对`VecN<Bool, Q>`的排除策略在仓颉中不可实现
   - 所在位置：§3.2 `VecN<Bool, Q>` 行为约定（第175行）、§7 D25（第858-862行）
   - 严重程度：严重
   - 改进建议：移除"可通过排除实现严格兼容"的不可行建议，改为明确声明此为不可消除的已知行为差异；迁移`~bvec`时需要人工审查。
   - 解决结果：已在 a_v4 中解决（§3.2 移除"若编码阶段需要严格一致，应为 `VecN<Bool, Q>` 排除 `bitwiseNot()`"的不可行建议，改为明确声明此为"不可消除的已知行为差异"，并补充技术原因；§7 D25 同步更新增加不可消除性论证）

2. **问题描述**：别名命名约定（PascalCase）偏离C++ GLM API（小写），迁移成本未纳入§11评估
   - 所在位置：§3.8命名约定（第317-319行）、§11迁移成本评估（全文）
   - 严重程度：中等
   - 改进建议：在§11中新增子节评估别名重命名的迁移成本，包括受影响范围、重命名映射表、机械替换可行性和迁移工具策略。
   - 解决结果：已在 a_v4 中解决（§11 新增 §11.7 "别名命名约定偏离的迁移成本" 子节，包含别名映射表、机械替换可行性分析、迁移工具策略和工作量评估约 0.5-1 人天）

3. **问题描述**：跨Q赋值迁移模式未纳入§11
   - 所在位置：§11（全文）
   - 严重程度：中等
   - 改进建议：在§11中补充跨Q赋值的迁移模式说明，明确哪些场景需要补全Q、哪些可通过别名避免，以及自动化工具的可行性评估。
   - 解决结果：已在 a_v4 中解决（§11 新增 §11.8 "跨 Q 赋值迁移模式" 子节，含场景对照表、自动化工具分级和工作量评估约 2-3 人天）

4. **问题描述**：`epsilonOf<T>()` Option B兼容性策略（为`NumericLimits<Bool>`添加空壳特化）不可行
   - 所在位置：§3.5兼容性策略Option B（第256-257行）
   - 严重程度：一般
   - 改进建议：从§3.5中完全移除Option B，仅保留Option A（内联固定容差）作为唯一的兼容性策略。
   - 解决结果：已在 a_v4 中解决（§3.5 完全移除 Option B，改为"采用以下唯一策略"，仅保留 Option A 内联固定容差 `T(1e-6)`）

5. **问题描述**：`operator[]` const取值形式未提供
   - 所在位置：§4.2分量访问（第500-502行）、§7 D28（第876-880行）
   - 严重程度：一般
   - 改进建议：验证仓颉是否支持仅通过`const`修饰符区分运算符版本；若不支持，通过具名const访问函数`componentAt(i: Int64): T`提供const上下文中的分量访问能力。
   - 解决结果：已在 a_v4 中解决（§4.2 新增 "const 上下文中的分量访问" 段落，说明仓颉不支持 const 区分运算符版本，提供具名 const 函数 `componentAt(i: Int64): T`；§7 D28 扩展覆盖 const 取值与赋值形式）

6. **问题描述**：`mod`浮点实现的外部依赖（`std.math.trunc`）未显式验证
   - 所在位置：§4.3 `%`运算的整数约束（第527行）
   - 严重程度：一般
   - 改进建议：在§10设计阶段验证要求表中新增一项，验证`Float32`浮点`mod`通过算术恒等式实现的正确性，覆盖边界值（`Float32.Max`、`Float32.MinPositive`、`0.0`、`±Inf`、`NaN`）下的行为。
   - 解决结果：已在 a_v4 中解决（§10 末尾新增"浮点 mod 实现正确性验证"子节，含 4 项验证依赖——`std.math.trunc` 可用性、Float32 提升→截断→降级、负操作数符号、整数/浮点 `const if` 分支，覆盖 `Float32.Max`、`Float32.MinPositive`、`0.0`、`±Inf`、`NaN` 边界值）

## 迭代第 4 轮

1. **问题描述**：`mod` 具名函数使用 `const if` 但未声明为 `const` 函数，在 Cangjie 中 `const if` 要求所在函数为 `const` 函数，当前签名将导致编译错误
   - 所在位置：§4.3 `mod` 具名函数描述及浮点取模实现策略段落；§10 浮点 `mod` 实现正确性验证表中的依赖项④
   - 严重程度：一般
   - 改进建议：将 `vec-op-scalar` 和 `scalar-op-vec` 方向的 `mod` 均声明为 `const` 函数；同步评估 `add`/`sub`/`mul`/`div` 的 const 化必要性；在 §7 新增设计决策记录
   - 解决结果：已在 a_v5 中解决（`mod` 在 vec-op-scalar 和 scalar-op-vec 两种方向均声明为 const，新增 §7 D32 设计决策记录理由）

2. **问题描述**：`length()` 静态函数未声明为 `const`，无法在编译期上下文中使用，与设计文档声称的编译期角色自相矛盾
   - 所在位置：§3.2 Vec 结构体系，`public static func length(): Int64` 声明描述
   - 严重程度：一般
   - 改进建议：将签名改为 `const public static func length(): Int64`
   - 解决结果：已在 a_v5 中解决（签名修正为 `const public static func length(): Int64`）

## 迭代第 5 轮

1. **问题描述**：§3.2 `length()` 函数声明缺少 `const` 修饰符，与 v5 修订说明矛盾
   - 所在位置：§3.2，第 154 行
   - 严重程度：严重
   - 改进建议：将 `public static func length(): Int64` 修正为 `const public static func length(): Int64`
   - 解决结果：已在 a_v5 中修复，a_v6 中确认仍为 `const public static func length(): Int64`

2. **问题描述**：§7 设计决策节缺少 D32（add/sub/mul/div/mod 统一声明为 const 函数）
   - 所在位置：§7，D31 之后
   - 严重程度：严重
   - 改进建议：将 v5 修订说明中的 D32 设计决策文本移入 §7 正文，紧跟 D31 之后
   - 解决结果：已在 a_v6 中解决（D32 存在于 §7 正文 D31 之后）

3. **问题描述**：§7 D13 声明"浮点取模具名函数方案不属于首轮范围"，但 §4.3 已将 `mod` 列为首轮具名函数并给出实现策略
   - 所在位置：§7 D13（第 779 行）vs §4.3（第 527 行）
   - 严重程度：严重
   - 改进建议：将 D13 中"不属于首轮范围"改写为"属于首轮范围"，消除自相矛盾
   - 解决结果：已在 a_v6 中解决（D13 末句修改为 mod "属于首轮范围" 并引用 §4.3）

4. **问题描述**：§7 D29（const 路径验证）和 D30（@OverflowWrapping 继承性验证）未考虑两者同时失败的组合场景
   - 所在位置：§7 D29（第 885-908 行）、§7 D30（第 901-909 行）
   - 严重程度：中等
   - 改进建议：新增组合回退分析段落，评估两种回退同时触发时的交互影响
   - 解决结果：已在 a_v6 中解决（D30 之后新增"组合回退场景分析"子节，以状态表形式评估三项组合的交互影响）

5. **问题描述**：`increment()`/`decrement()`/`bitwiseNot()` 未被声明为 `const` 函数，但与已声明 `const` 的 `add()`/`sub()` 等同样为纯函数，缺少对称性理由
   - 所在位置：§4.3、§4.4
   - 严重程度：中等
   - 改进建议：在 §7 中补充设计决策，统一约定上述函数是否声明为 `const`
   - 解决结果：已在 a_v6 中解决（新增 §7 D33 设计决策，统一声明三者为 const 函数并给出理由：纯函数、API 一致、const 上下文可用）

6. **问题描述**：§3.8 大写驼峰别名（`BVec2`、`IVec3`）未评估对下游新代码开发的长期认知负担
   - 所在位置：§3.8（第 315-323 行）、§11.7（第 1239-1280 行）
   - 严重程度：中等
   - 改进建议：在 §3.8 新增命名差异长期影响说明，或在 §11 中新增"持续使用成本"子节
   - 解决结果：已在 a_v6 中解决（§3.8 末尾新增"命名差异的长期影响"段落，评估过渡期认知映射成本及缓解措施）

7. **问题描述**：§3.5 `epsilonOf<T>()` 约束条件仍使用"采取以下方案之一"，但仅列出方案 A（唯一策略）
   - 所在位置：§3.5（第 254-255 行）
   - 严重程度：一般
   - 改进建议：将"采取以下方案之一"改为"采用以下唯一策略"，移除"方案 A"编号
   - 解决结果：已在 a_v6 中解决（表述改为"采用以下唯一策略"，移除"方案 A"编号）

8. **问题描述**：`@OverflowWrapping` 与 `const` 函数修饰符共存于同一函数的交互行为未经验证
   - 所在位置：§10（第 1060-1064 行）、§7 D30（第 901-909 行）
   - 严重程度：一般
   - 改进建议：在 §10 中新增验证依赖，测试 `const` + `@OverflowWrapping` 共存兼容性
   - 解决结果：已在 a_v6 中解决（§10 新增"@OverflowWrapping 与 const 函数共存验证"子节，含三项验证依赖和合并原型测试方法）

9. **问题描述**：修订说明中的 D32 设计决策未纳入 §7 正文（与 P2 实质相同）
   - 所在位置：§7 D31 之后
   - 严重程度：一般
   - 改进建议：同 P2——将 D32 移入 §7 正文
   - 解决结果：已在 a_v6 中确认 D32 纳入 §7 正文（与 P2 同一处理）

## 迭代第 6 轮

1. **问题描述**：文档版本标识自相矛盾（标题 v5、文件名 v6、修订说明 v6），修订说明序列混乱，实施者无法确定当前版本
   - 所在位置：文档标题（第 1 行）、文件路径名、修订说明序列（第 1435-1665 行）
   - 严重程度：严重
   - 改进建议：统一标题为 v6；整理修订说明按时间正序排列；增加版本变更摘要行
   - 解决结果：已在 a_v7 中解决（统一标题为 v7、新增版本变更摘要行、历史修订说明移入附录按时间顺序排列）

2. **问题描述**：缺少 cjpm.toml 内容、编译入口和构建命令、项目初始化脚手架步骤、测试运行器配置，无法直接指导编码启动
   - 所在位置：§2 模块划分、§8 迁移文件清单、全文
   - 严重程度：严重
   - 改进建议：补充前置准备工作子节，提供最小 cjpm.toml 模板和初始目录结构示例
   - 解决结果：已在 a_v7 中解决（§2 新增"项目初始化与构建基础设施"子节，含 cjpm init 命令、cjpm.toml 模板、初始目录结构、cjpm build/test 命令、首次编码前原型验证要求）

3. **问题描述**：D32 将 40 个算术函数声明为 const，但 D29 的 const 表达式链验证仅覆盖 == 运算符，不覆盖算术运算的 const 兼容性
   - 所在位置：§7 D32（第 935-939 行）、§7 D29（第 887-901 行）、§10 验证要求表
   - 严重程度：严重
   - 改进建议：在 D32 中补充 const 兼容性验证要求；在 §10 新增算术运算 const 兼容性验证；定义 D32 独立回退方案
   - 解决结果：已在 a_v7 中解决（§7 D32 新增"const 兼容性验证要求"子节，含核心类型验证、mod `const if` 验证、非核心类型延迟审查三张验证表和对应回退方案）

4. **问题描述**：mod 函数 const if 分支假设能抑制非选择分支编译检查，但该行为未在目标编译器上通过原型测试确认
   - 所在位置：§4.3 mod 函数实现描述（第 529 行）、§10 验证表依赖项④（第 1098 行）
   - 严重程度：严重
   - 改进建议：新增原型测试分别以 Int32 和 Float32 实例化验证 const if 编译抑制行为；定义回退方案为独立具名函数
   - 解决结果：已在 a_v7 中解决（§10 新增"const if 编译抑制行为验证"子节，含 3 项验证依赖，验证项①为编码前原型验证（与 D29/D30 同级），回退方案定义为拆分 `modInt`/`modFloat`）

## 迭代第 7 轮

1. **问题描述**：extend块中多处声明const实例成员函数，但仓颉语言不允许extend块中定义const成员函数
   - 所在位置：§4.4（第624-643行）、§4.5（第668-672行）、§4.5（第683-684行）、§7 D33（第1043-1048行）
   - 严重程度：严重
   - 改进建议：移除extend块中的const修饰符，将bitwiseNot()、equalExact()、logicalAnd/logicalOr降级为非const扩展成员函数，同步更新D32/D33的const统一决策
   - 解决结果：已在 a_v8 中解决（§3.2 删除"extend 块中也可定义 const 实例成员函数"声明；§4.3-§4.5 中 extend 块的 `bitwiseNot`、`equalExact`、`logicalAnd`/`logicalOr`、vec-op-scalar 方向算术具名函数均改为非 const；§7 D32/D33 重写声明策略区分扩展成员函数与包级独立函数）

2. **问题描述**：`const if (T2 is Bool)`语法中类型参数级is运算符和`is not`语法可行性未经验证，未纳入§10验证计划
   - 所在位置：§9.4（第1159行）
   - 严重程度：一般
   - 改进建议：将此项验证纳入§10验证要求表；若验证失败则默认启用方案B
   - 解决结果：已在 a_v8 中解决（§9.4 重写——明确说明 `T2 is Bool` 不可行，改为分量值检测 `const if (v.x is Bool)`，并优先采用方案 B（运行时 if 分支））

3. **问题描述**：§8.1迁移文件清单未覆盖测试文件，编码启动指引存在缺口
   - 所在位置：§8.1（第1055-1071行）、§12.3（第1553-1557行）
   - 严重程度：一般
   - 改进建议：在§8.1文件清单中新增"对应测试文件"列或新增§8.1.1测试文件清单子节
   - 解决结果：已在 a_v8 中解决（§8.1 新增测试文件清单 T1~T14，每个源文件对应 `test_*.cj` 测试文件，§12.3 补充测试包命名空间组织说明）

4. **问题描述**：`<<`默认溢出策略描述为已确认事实而非待验证假设，实际编译器行为未经确认
   - 所在位置：§4.4（第647行）、§4.6（第699行）
   - 严重程度：一般
   - 改进建议：将确定性陈述改写为待验证假设，在§10中新增溢出行为验证项
   - 解决结果：已在 a_v8 中解决（§4.4 重写——将"仓颉 `<<` 默认使用 `@OverflowThrowing` 策略"改为"`<<` 默认溢出策略属于编译器实现定义行为，`ThrowingOp<T>` 不包含 `throwingShl`"，标注为编码阶段待验证假设）

5. **问题描述**：构造函数体系对Q参数的约束充分性存疑，跨Q类型构造的重载解析风险未充分验证
   - 所在位置：§4.1（第529-575行）、§7 D11（第859-863行）、§7 D31（第1021-1027行）
   - 严重程度：一般
   - 改进建议：在§10中新增重载解析验证项
   - 解决结果：已在 a_v8 中解决（§4.1 构造函数清单后新增"跨 Q 构造函数重载解析风险与约束验证"段落，分析已知安全场景和风险领域，定义编码阶段原型验证要求及回退策略）

6. **问题描述**：`const if`编译期分支抑制依赖的综合评估未反映在验证计划中，多个依赖点未覆盖
   - 所在位置：§3.5（第286-294行）、§3.6（第339-344行）、§4.3（第613行）、§9.4（第1159行）、§10（第1195-1201行）
   - 严重程度：一般
   - 改进建议：在§10中新增const if非选择分支抑制的独立验证依赖项
   - 解决结果：已在 a_v8 中解决（§10 扩展为"const if 编译抑制行为综合验证"子节，以统一清单形式列出 5 个依赖点 A~E，提供统一原型测试方法，为每个依赖点定义独立回退方案）

7. **问题描述**：对参考实现（references/glm-1.0.3/）的使用策略未作说明
   - 所在位置：全文
   - 严重程度：一般
   - 改进建议：在§1中新增"参考实现的使用策略"子节，或在§12中补充交叉验证方法
   - 解决结果：已在 a_v8 中解决（§8.3 后新增"§8.4 参考实现使用策略"，定义 6 条使用策略：行为确认、构造函数清单验证、运算符行为对照、配置宏确认、命名映射、非首轮内容边界）

## 迭代第 8 轮

1. **问题描述**：扩展成员函数 add/sub/mul/div/mod（vec-op-scalar 方向）缺少 @OverflowWrapping 标注策略，导致整数分量 Vec 在溢出时抛异常而非 wrapping，与二元运算符行为不一致
   - 所在位置：§4.3 具名函数定义段落（第 605-621 行）、§4.6 @OverflowWrapping 标注分布表（第 693-704 行）
   - 严重程度：严重
   - 改进建议：选择以下之一并在文档中明确：A. 在 §4.6 标注分布表中将20个扩展成员函数纳入 @OverflowWrapping 范围；B. 在 §4.3 中明确实现模式——委托给已标注 @OverflowWrapping 的二元运算符
   - 解决结果：已在 a_v9 中解决（§4.3 第 619 行通过委托策略说明解决——二元运算符标注 `@OverflowWrapping` 后扩展成员函数隐式继承 wrapping 语义）

2. **问题描述**：Vec1 构造函数接口的不对称性缺少说明——Vec1 因 `const init(x: T)` 与 `init(scalar: T)` 参数列表相同而无法共存，但文档未解释此限制
   - 所在位置：§4.1 Vec1 构造函数清单（第 466-477 行）、§4.1 Vec2 构造函数清单（第 479-497 行）、§7 D11（第 856-860 行）
   - 严重程度：一般
   - 改进建议：在 §4.1 注释或 §7 D11 中补充 Vec1 构造接口不对称性的原因说明
   - 解决结果：已在 a_v9 中解决（§4.1 第 478 行新增 Vec1 构造函数接口不对称性说明段落，解释 const 修饰符不构成重载区分依据）

3. **问题描述**：`countof` 辅助函数的范围跟踪遗漏——roadmap 要求项在 §8.2 范围追溯表中未标注偏离
   - 所在位置：§8.2 范围可追溯性对照表第 1 行（第 1105 行）、§3.6 shim_cstddef.cj 描述（第 347 行）
   - 严重程度：一般
   - 改进建议：在 §8.2 序号 1 的偏离说明中补充 countof 的排除理由及替代方案
   - 解决结果：已在 a_v9 中解决（§8.2 第 1123 行 S3 行补充偏离说明，记录 `VArray` size 属性替代方案）

4. **问题描述**：跨类型构造函数 `init<T2, Q2>(v: VecN<T2, Q2>)` 的 T2 有效性范围未集中归档，实施者需交叉查阅 §4.1 和 §9.4 才能了解各 T/T2 组合行为
   - 所在位置：§4.1 跨类型构造函数签名（第 472、486、506、536 行等）、§9.4 类型转换边界（第 1196 行）
   - 严重程度：一般
   - 改进建议：在 §4.1 跨类型构造函数描述末尾添加 T2 约束矩阵，明确各 T/T2 组合的编译时行为
   - 解决结果：已在 a_v9 中解决（§4.1 第 580 行末尾新增跨类型构造函数 T2 有效性范围段落并交叉引用 §9.4）

## 迭代第 9 轮

1. **问题描述**：lib.cj 的 public import 语句使用了错误的包路径 glm.detail.scalar_vec_ops，应改为 glm.detail
   - 所在位置：§2 公共 API 面设计，lib.cj 代码块第4行
   - 严重程度：严重
   - 改进建议：将 public import glm.detail.scalar_vec_ops.{ add, sub, mul, div, mod } 改为 public import glm.detail.{ add, sub, mul, div, mod }，同步检查其他引用
   - 解决结果：已在 a_v10 中解决（§2 第 70 行修正为 `public import glm.detail.{ add, sub, mul, div, mod }`）

2. **问题描述**：§7 D6 声称 GLM_CONFIG_CLIP_CONTROL 默认值通过 GLM_CLIP_CONTROL_DISABLE 验证，但 references/glm-1.0.3/glm/glm/detail/setup.hpp 中不存在该宏定义
   - 所在位置：§7 D6，GLM_CONFIG_CLIP_CONTROL 配置行
   - 严重程度：严重
   - 改进建议：删除错误描述，替换为准确的条件编译逻辑说明（依赖 GLM_FORCE_DEPTH_ZERO_TO_ONE/GLM_FORCE_LEFT_HANDED 平台检测宏）
   - 解决结果：已在 a_v10 中解决（§7 D6 第 815 行重写默认值来源说明，改为 `GLM_FORCE_DEPTH_ZERO_TO_ONE`/`GLM_FORCE_LEFT_HANDED` 两层条件编译逻辑）

3. **问题描述**：equalExact 在验证计划中未覆盖 NaN 返回 false、整数/Bool Vec 边界组合及回退路径下行为等价断言
   - 所在位置：§4.5 equalExact 段落、§9.3、§12.1 层次三测试列表
   - 严重程度：中等
   - 改进建议：补充 NaN 测试项；显式声明 equalExact 在 == 容差路径下行为不同（精确比较 vs 容差比较）；将 NaN 测试加入层次四
   - 解决结果：已在 a_v10 中解决（§12.1 第 1607 行扩展 `equalExact` 验证项覆盖回退路径等价性测试）

4. **问题描述**：测试文件通过声明 package glm.detail 访问 internal 类型的假设未经验证，依赖编译器包解析策略
   - 所在位置：§12.3 测试工具小节
   - 严重程度：中等
   - 改进建议：在 §10 新增验证项确认测试文件能否声明同包访问 internal 类型；定义备选方案（提升可见性或提供测试 helper）
   - 解决结果：已在 a_v10 中解决（§12.3 第 1645 行标注未经验证并补充原型验证步骤和回退方案）

5. **问题描述**：初始目录树形图仍显示两个同级的 src/glm/ 条目，文件系统中不可能实现
   - 所在位置：§2 项目初始化与构建基础设施，初始目录结构树形图
   - 严重程度：一般
   - 改进建议：修正为正确的嵌套结构（src/glm/ 同时包含lib.cj、wd.cj 和 detail/ 子目录）
   - 解决结果：已在 a_v10 中解决（§2 第 117-141 行修正为正确嵌套结构）

6. **问题描述**：countof 偏离理由解释为"VArray 的 $N 必须为固定字面量"，与其实际功能（计算 C 数组长度）无关
   - 所在位置：§8.2 范围可追溯性对照表 S3 行、§3.6 shim_cstddef.cj 描述
   - 严重程度：一般
   - 改进建议：修正偏离理由为 Cangjie 无 sizeof 等效设施且无原生数组类型，VArray<T, >.size 可覆盖 countof 的角色
   - 解决结果：已在 a_v10 中解决（§8.2 第 1122 行修正为 countof 实际功能说明 `sizeof(array)/sizeof(array[0])` 及仓颉无 C 数组语义的实际情况）

## 迭代第 10 轮

1. **问题描述**：`const if` 为文档简写而非语法关键字，`if` 在 `const` 函数中的分支抑制行为未经验证
   - 所在位置：§3.5 ComputeEqual.callConst、§3.6 isIec559Of 实现、§4.3 mod 双路径、§10 const if 抑制验证
   - 严重程度：中等
   - 改进建议：将 `const if (...)` 改写为 `if (...)`；在 §7 新增 D34 记录此依赖的验证状态；§10 原型测试中的代码与实际可编译代码保持一致
   - 解决结果：已在 a_v11 中解决（全文 `replaceAll` 将 `const if (...)` 改写为 `if (...)`，§7 D34 第 1065 行记录简写约定理由）

2. **问题描述**：`public import glm.detail.{ add, sub, mul, div, mod }` 存在潜在的命名冲突——`glm.detail` 包的其他文件未来也可能导出同名函数
   - 所在位置：§2 lib.cj、§4.3 具名函数定义位置
   - 严重程度：中等
   - 改进建议：补充命名空间占用声明或在 §7 新增设计决策约定 `scalar_vec_ops.cj` 对 `add`/`sub`/`mul`/`div`/`mod` 名称的独占权
   - 解决结果：已在 a_v11 中解决（§2 第 79 行补充命名空间占用声明，§7 D35 第 1073 行正式记录 `scalar_vec_ops.cj` 对五个函数名的独占权约定）

3. **问题描述**：§11 迁移成本评估缺少 `equalExact` 迁移模式的系统性覆盖
   - 所在位置：§11（全文）
   - 严重程度：中等
   - 改进建议：在 §11 中新增子节"§11.9 `equalExact` 迁移模式"，覆盖 C++ GLM `==` 精确语义与仓颉容差 `==` 的行为差异、替换场景、搜索规则和工作量评估
   - 解决结果：已在 a_v11 中解决（§11.9 第 1614 行新增 `equalExact` 迁移模式子节，覆盖行为差异对照、替换场景清单、搜索替换规则和迁移工作量评估）

4. **问题描述**：对 `references/glm-1.0.3/` 的引用基于假设其存在，但未验证关键路径的实现准确性
   - 所在位置：§7 D6、§8.4
   - 严重程度：一般
   - 改进建议：在 §1 或 §8.4 补充前置验证步骤确认条件编译逻辑一致；增加兜底条款，若参考实现分支逻辑与描述不符则以参考实现为准更新 D6
   - 解决结果：已在 a_v11 中解决（§8.4 第 1167 行新增前置验证步骤策略 0 和第 1171 行配置宏确认的兜底条款）

## 迭代第 11 轮

1. **问题描述**：§4.3 残留导入路径错误，`scalar-ops-vec` 方向函数仍表述为 `import glm.detail.scalar_vec_ops.*`，正确的导入路径应为 `import glm.detail.{ add, sub, ... }` 或 `import glm.detail.*`
   - 所在位置：§4.3，第 617 行
   - 严重程度：中等（对应判定框架"一般"）
   - 改进建议：将 `import glm.detail.scalar_vec_ops.*` 改为 `import glm.detail.{ add, sub, mul, div, mod }` 或 `import glm.detail.*`；全文搜索确认无其他残余的 `glm.detail.scalar_vec_ops` 路径引用
   - 解决结果：已在 a_v12 中解决（§4.3 第 620 行修正为 `import glm.detail.{ add, sub, mul, div, mod }`）

2. **问题描述**：测试目录结构 `tests/glm.detail/` 与仓颉包路径解析规则不匹配（应用 `tests/glm/detail/`）；目录树形图缺少 `tests/glm/` 条目
   - 所在位置：§2 初始目录树形图（第 139 行）、§12.3 测试包组织结构（第 1714-1716 行）
   - 严重程度：中等（对应判定框架"一般"）
   - 改进建议：将 `tests/glm.detail/` 改为 `tests/glm/detail/`；在目录树形图中补充 `tests/glm/` 目录条目
   - 解决结果：已在 a_v12 中解决（§2 第 138-144 行修正为 `tests/glm/detail/` 并补充 `tests/glm/` 目录条目，§12.3 第 1717 行同步修正）

3. **问题描述**：§1 出现重复的 `### 设计目标` 标题，造成结构歧义
   - 所在位置：§1 概述，第 9-15 行
   - 严重程度：一般
   - 改进建议：将第一个标题改为 `### 范围可追溯性承诺`，或删除第一个标题并将段落移入第二个"设计目标"段落后或 §8
   - 解决结果：已在 a_v12 中解决（§1 第 9 行将第一个标题改为 `### 范围可追溯性承诺`）

4. **问题描述**：§3.5/§3.6 残留 `const if` 术语，未按 D34 设计决策统一改写
   - 所在位置：§3.5 第 272 行、§3.6 第 339 行
   - 严重程度：一般
   - 改进建议：将 `const if` 改写为"`const` 函数中的 `if` 表达式"或"编译期 `if` 分支"，与 D34 约定一致
   - 解决结果：已在 a_v12 中解决（§3.4 第 275 行改为"使用编译期 if 分支"，§3.6 等处同步修正为"编译期 if 配合 is 运算符"等表述）

5. **问题描述**：§2 模块依赖表 `scalar_vec_ops` 声明"无包内其他依赖"，但其函数签名引用 `VecN<T, Q>` 类型，依赖描述不完整
   - 所在位置：§2 模块间依赖图，第 92 行
   - 严重程度：一般
   - 改进建议：将依赖修改为 `scalar_vec_ops → qualifier + type_vec1~4`，或补充说明同包内函数签名引用 Vec 类型但不构成独立编译依赖
   - 解决结果：已在 a_v12 中解决（§2 第 92 行修正为 `scalar_vec_ops → qualifier` 并补充说明：函数签名引用 Vec 类型，同包内由 Cangjie 包级编译模型自动包含，不构成独立编译依赖）

## 迭代第 12 轮

1. **问题描述**：`const init` 构造函数缺少显式 `public` 可见性声明，Vec1 可能无法从外部构造
   - 所在位置：§4.1 构造函数清单
   - 严重程度：严重
   - 改进建议：在 §4.1 所有 `const init` 签名前补充 `public` 修饰符，或在 §7 记录语言特性假设并编码验证
   - 解决结果：已在 a_v13 中解决（§4.1 所有 `const init` 签名补充 `public` 修饰符，第 488/503/523/553 行；§7 D11 决策声明末尾补充"所有 const init 构造函数均显式声明为 public"；§12.2 新增 public const init 外部可构造验收项）

2. **问题描述**：编码前验证任务分散在多个章节，缺少集中检查清单
   - 所在位置：§2、§8.4、§10
   - 严重程度：一般（中等）
   - 改进建议：在 §2 末尾新增"编码前验证检查清单"子节，集中列出所有验证任务
   - 解决结果：已在 a_v13 中解决（§2 末尾新增 §2.1 "编码前验证检查清单"，以表格形式集中列出 7 项验证任务及对应回退方案）

3. **问题描述**：§10 多处残留"const if"独立术语，与 D34 约定不一致
   - 所在位置：§10 标题、首段、表格等 7 处
   - 严重程度：一般
   - 改进建议：全文 grep 替换为 D34 约定的表述
   - 解决结果：已在 a_v13 中解决（§10 全文按 D34 约定统一替换为"编译期 if"或"if"表述）

4. **问题描述**：§12.3 测试目录路径描述存在残余不一致（`tests/glm.detail/` 混合表示法）
   - 所在位置：§12.3 第 1718 行
   - 严重程度：一般
   - 改进建议：改为 `tests/glm/detail/` 目录路径或仅使用包名称 `glm.detail`
   - 解决结果：已在 a_v13 中解决（将 `tests/glm.detail/` 改为 `tests/glm/detail/` 目录路径）

5. **问题描述**：§10 Float16 T(0) const 构造验证超出首轮范围
   - 所在位置：§10 设计阶段验证表依赖①
   - 严重程度：一般
   - 改进建议：从验证类型列表中移除 Float16，保留注释行
   - 解决结果：已在 a_v13 中解决（从验证表依赖①的验证类型列表中移除 Float16，保留 `isIec559Of` 取值对照表中对 Float16 的注释行）

6. **问题描述**：§4.3 mod 浮点实现将精度假设陈述为确定性断言，与 §10 验证要求矛盾
   - 所在位置：§4.3
   - 严重程度：一般
   - 改进建议：添加交叉引用标注或将确定性断言改为待验证表述
   - 解决结果：已在 a_v13 中解决（§4.3 改为"预期精度提升无损取模语义的正确性（需验证，详见 §10 浮点 mod 实现正确性验证依赖项②）"）

## 迭代第 13 轮

1. **问题描述**：修订历史附录结构混乱，版本追溯困难，混用两套编号体系且排列顺序不一致
   - 所在位置：§附录 历史修订说明（第 1740-1933 行）
   - 严重程度：一般
   - 改进建议：在附录首段增加版本体系说明，两段之间插入分隔线并加注释标题，或将旧审议轮次历史移至独立文件
   - 解决结果：已在 a_v14 中解决（附录首段新增版本体系说明区分两套编号，附录内插入 `---` 分隔线并加注释标题（新审议早期/旧审议/新审议后续）将三段清晰分割）

2. **问题描述**：`isIec559Of<Float16>` 的值呈现为语义正确，实为实现限制——Float16 符合 IEEE 754 标准但当前实现模式仅覆盖 Float32/Float64
   - 所在位置：§10 `isIec559Of<T>()` 取值对照表（第 1257 行）
   - 严重程度：一般
   - 改进建议：改写说明文字，明确此为检测模式局限性而非 Float16 的语义判定，后续引入 Float16 支持时应扩展检测条件
   - 解决结果：已在 a_v14 中解决（§10 取值对照表附注改写为"因 `is` 运算符检测模式仅覆盖 Float32/Float64，`isIec559Of<Float16>()` 受限于此模式返回 false。若后续轮次引入 Float16 支持，应扩展检测条件或改用其他方案以正确返回 true"）

3. **问题描述**：`componentAt` const 访问函数未纳入验证计划，缺少 const 上下文编译通过性、越界断言行为、返回值正确性的显式验证项
   - 所在位置：§4.2 `componentAt` 定义（第 610-614 行）、§10 验证要求表、§12 验证层次
   - 严重程度：一般
   - 改进建议：在 §12.1 层次二中新增 `componentAt` const 访问验证项
   - 解决结果：已在 a_v14 中解决（§12.1 层次二新增 `componentAt` const 访问验证项："在 const 函数体内调用 `componentAt(i)` 验证正确性，与 `operator[]` 取值形式行为一致"）

## 迭代第 14 轮

1. **问题描述**：`componentAt` 在 §12.1 层次二已有验证项，但 §12.2 验收标准表未同步增加对应的强制验收项，验证未升格为验收标准层级
   - 所在位置：§12.2 验收标准表
   - 严重程度：中等
   - 改进建议：在 §12.2 验收标准表中新增 componentAt const 访问正确性验收项
   - 解决结果：已在 a_v15 中解决（§12.2 验收标准表新增"componentAt const 访问正确性"行，标准为"const 函数体内调用 `componentAt(i)` 返回值与 `operator[]` 取值形式一致；越界时触发断言"）

2. **问题描述**：§11 迁移成本评估未涵盖"标量在左运算的具名函数替换"（D10/§4.3 已分析但缺少迁移评估章节）
   - 所在位置：§11（全文）
   - 严重程度：中等
   - 改进建议：在 §11 中新增子节 §11.10 评估 scalar-on-left 运算符替换的迁移模式和工作量
   - 解决结果：已在 a_v15 中解决（§11 末尾新增 §11.10 "标量在左运算的具名函数替换"，覆盖受影响表达式模式表、与 vec-op-scalar 方向关系和选择指南、搜索替换规则、工作量评估约 1.5-2.5 人天）

3. **问题描述**：`@Derive[Hashable]` 在泛型 struct 上对类型参数 T 的约束传递性未经验证；原始诊断中关于"仅有 const init 无默认构造时编译器行为"的假设无语言规范依据，经质询后应将焦点调整为约束传递性验证
   - 所在位置：§3.2、§4.1、§10 验证计划
   - 严重程度：一般
   - 改进建议：在 §10 中新增验证依赖，验证 `@Derive[Hashable]` 在泛型 struct 上对类型参数 T 的 Hashable 约束传递性，保留浮点 Vec 实例化编译通过性验证
   - 解决结果：已在 a_v15 中解决（§3.2 Hashable 实现策略新增"泛型类型参数 T 的 Hashable 约束传递性"独立条目；§10 新增"@Derive[Hashable] 在泛型 struct 上的约束传递性验证"子节含 3 项验证依赖：编译通过性、HashSet 可用性、非 Hashable 负向测试）

4. **问题描述**：§2 模块间依赖表 `scalar_vec_ops` 行未标注对 `std.math` 的标准库依赖，实施者首次编码时需注意额外 import
   - 所在位置：§2 模块间依赖表 `scalar_vec_ops` 行
   - 严重程度：一般
   - 改进建议：在 §2 脚注或 §8.1 文件清单"说明"列中标注 `scalar_vec_ops.cj` 依赖 `std.math.trunc`
   - 解决结果：已在 a_v15 中解决（§2 依赖拓扑澄清末尾新增"标准库依赖声明"段落，说明 `scalar_vec_ops.cj` 浮点 mod 路径依赖 `std.math.trunc`，编码阶段需添加 `import std.math.{ trunc }`；§8.1 文件清单 `scalar_vec_ops` 行"依赖"列补充 `std.math`）

## 迭代第 15 轮

1. **问题描述**：`equalEpsilon` 回退函数在 §3.5 和 §7 D29 中提及，但未在 §4 关键行为契约中正式定义，且之前第7轮已将其替换为 `equalExact`，这两处残留未同步修复
   - 所在位置：§3.5 第345行、§7 D29 第1018行
   - 严重程度：一般
   - 改进建议：在 §4.5 新增 `equalEpsilon` 完整定义，或将残留引用改为 `equalExact`，或删除引用
   - 解决结果：已在 a_v16 中解决（§4.5 第 699-712 行新增 `equalEpsilon` 完整定义——含非 const 扩展成员函数签名、行为约定、Inf/NaN 边界行为、与 `equalExact` 的关系；§3.5 和 §7 D29 引用指向更新为 §4.5）

2. **问题描述**：Bool→数值转换方案B假设非 `const` 函数中运行时 `if` 的 else 分支（`T(v.x)` 当 `T2=Bool` 时不合法）不会被编译检查，但仅 `const if` 具有编译期分支抑制行为，该假设未经论证
   - 所在位置：§9.4 Bool→数值转换行（第1241行）
   - 严重程度：一般
   - 改进建议：将假设标注为"待验证"并加入 §10 验证计划，定义明确回退方案（编译期方案或具名转换函数 `fromBool`）
   - 解决结果：已在 a_v16 中解决（§9.4 重写 Bool→数值行——移除"方案 B 为首选推荐路径"确定性表述，将方案 B 标注为"待验证"，新增路径 A（具名工厂函数 `fromBoolVec` 零编译依赖）为首选推荐；§10 新增 3 项依赖验证）

3. **问题描述**：附录历史修订版本序列混乱，两套版本编号体系混用，新读者难以追溯当前版本状态
   - 所在位置：附录 历史修订说明（第1797-2020行）
   - 严重程度：一般
   - 改进建议：增加版本关系图，将当前版本修订说明移至章节顶部
   - 解决结果：已在 a_v16 中解决（附录结构重构——新增版本关系时间线图（旧 v7~v15 → 新 v2~v16），当前版本 v16 修订说明移至附录章节顶部，后续版本按倒序排列）

4. **问题描述**：§9.4 方案 B 仅以 Vec1 为例描述 `v.x is Bool` 运行时类型检测，未讨论 Vec2~Vec4 的适用性
   - 所在位置：§9.4 Bool→数值转换行（第1241行）
   - 严重程度：一般
   - 改进建议：明确说明 Vec2~Vec4 的 Bool 检测模式（逐分量检测或委托辅助函数）
   - 解决结果：已在 a_v16 中解决（§9.4 Bool→数值行新增"Vec2~Vec4 Bool 检测模式"子节，明确给出 Vec2 的逐分量 `as` 检测示例和 `fromBoolVec` 工厂函数在 Vec2~Vec4 上的逐分量构造模式）

## 迭代第 16 轮

1. **问题描述**：`epsilonOf<T>()` 固定容差实现与 §9.3 缩放容差描述矛盾
   - 所在位置：§3.5 `epsilonOf<T>()`（第322-325行） vs §9.3 `NumericLimits<T>.epsilon()`（第1243-1245行）
   - 严重程度：一般
   - 改进建议：在全文中统一 `epsilonOf<T>()` 的语义。若为固定容差策略，修正 §9.3 描述；若为缩放容差策略，在 §3.5 中修改实现为 `NumericLimits<T>.epsilon() * abs(a)` 并处理零值边界
   - 解决结果：已在 a_v17 中解决（§9.3 已将描述统一为"固定容差 `NumericLimits<T>.epsilon()`（非缩放容差）"，与 §3.5 实现一致）

2. **问题描述**：`equalEpsilon` 对 VecN\<Bool, Q\> 的编译行为描述错误，声称"行为等价于 exact"但实际应产生编译错误（Bool不支持 `-` 运算符，`epsilonOf<Bool>()` 无法编译）
   - 所在位置：§4.5 `equalEpsilon` 定义（第699-711行）
   - 严重程度：一般
   - 改进建议：将描述修正为整数T上行为等价于exact，Bool T上因运算符不可用而产生编译错误（与 §4.3 中 `increment()`/`decrement()` 在Bool Vec上的行为一致）
   - 解决结果：已在 a_v17 中解决（§4.5 已将注释修正为"整数 T（Int32 等）上 `-` 和 `abs` 合法，行为等价于 exact；Bool T 上因 `-` 运算符不可用而在实例化时产生编译错误"）

3. **问题描述**：`equalEpsilon` 代码示例中的未使用变量 `comp` 及注释与代码矛盾
   - 所在位置：§4.5 `equalEpsilon` 定义（第705-707行）
   - 严重程度：轻微
   - 改进建议：统一实现：删除 `comp` 变量和内联比较，或将比较逻辑委托给 `comp.callConst(a, b)`
   - 解决结果：已在 a_v17 中解决（§4.5 已移除 `let comp = ComputeEqual<T>()` 变量及"委托给 `comp.callConst`"矛盾注释，保留内联比较实现）

4. **问题描述**：`equalEpsilon` 在 §10/§12 中缺少系统性验证计划覆盖
   - 所在位置：§10 设计阶段验证、§12 验证层次
   - 严重程度：轻微
   - 改进建议：在 §10 新增 `equalEpsilon` 验证依赖项（`abs()` 可用性、`epsilonOf<T>()` 编译通过性、Inf/NaN边界）；在 §12.1 层次三和 §12.2 验收标准中新增验证项
   - 解决结果：已在 a_v17 中解决（§10 新增"equalEpsilon 设计阶段验证"子节——覆盖 `abs()` 可用性、`epsilonOf<T>()` 编译通过性、Inf/NaN 边界；§12.1 层次三与 §12.2 验收标准表同步新增 `equalEpsilon` 验证项）

## 迭代第 17 轮

1. **问题描述**：equalEpsilon 与 epsilonOf<T>() 回退路径的隐式依赖链未归档，当 epsilonOf<T>() 回退方案被触发后 equalEpsilon 体内的引用失效
   - 所在位置：§3.5 epsilonOf<T>() 兼容性策略、§4.5 equalEpsilon 函数体、§10 equalEpsilon 设计阶段验证子节
   - 严重程度：一般
   - 改进建议：在 §12.1 层次三和 §10 中新增交叉引用注解和依赖验证项
   - 解决结果：已在 a_v18 中解决（§10"equalEpsilon 设计阶段验证"新增依赖项④，显式归档回退路径联动修改策略——约束保留 vs 移除两路径）

2. **问题描述**：fromBoolVec 工厂函数未评估 Vec1 构造函数不对称性对开发体验的影响
   - 所在位置：§9.4 Bool→数值转换路径 A、§4.1 Vec1 构造函数不对称性说明
   - 严重程度：一般
   - 改进建议：在 §9.4 路径 A 和 §4.1 中添加 Vec1 特殊说明
   - 解决结果：已在 a_v18 中解决（§4.1 新增"Vec1 构造函数接口不对称性说明"段落，§9.4 路径 A 新增"Vec1 特殊说明"及具体实现模式代码）

3. **问题描述**：文档未说明 Vec 结构体是否实现 Display/ToString/Format 接口，且测试策略未覆盖此决定的验证
   - 所在位置：§3.2 字符串表示策略段落、§12 验证层次
   - 严重程度：一般
   - 改进建议：在 §3.2 补充选择理由，在 §12.1 补充编译验证项
   - 解决结果：已在 a_v18 中解决（§3.2 补充三点选择理由——编译器默认表示足够、无消费方、策略待后续统一设计；§12.1 层次一新增"Display/ToString/Format 未实现验证"编译验证项）

## 迭代第 18 轮

1. **问题描述**：§11.5 "Inf == Inf 容差比较路径行为差异" 与 §11.9 "equalExact 迁移模式" 描述同一组行为差异，覆盖范围重叠的工作量评估未说明是累加还是互补，存在重复计算风险
   - 所在位置：§11.5（第 1541-1573 行）、§11.9（第 1689-1725 行）
   - 严重程度：一般
   - 改进建议：将 §11.5 合并为 §11.9 的子节（如 §11.9.1），统一迁移指引和工作量评估；或在 §11.9 开头注明包含关系
   - 解决结果：已在 a_v19 中解决（§11.5 开头新增覆盖声明"本节为 §11.9 子集无需重复审查"，§11.5 末尾补充"两份工作量评估非累加关系"说明）

2. **问题描述**：§4.3 仍使用 "const if" 作为单独术语，与 D34 要求（统一改写为 "if" 或 "编译期 if 分支"）不一致，可能误导实施者编写不存在的 const if 关键字
   - 所在位置：§4.3（第 646 行）
   - 严重程度：一般
   - 改进建议：将 "仓颉中 const if 要求" 改为 "仓颉中编译期分支 if 要求" 或 "仓颉中 const 函数体内的 if 表达式要求"
   - 解决结果：已在 a_v19 中解决（§4.3 第 646 行已改为"编译期分支 `if`"，与 D34 统一改写约定一致）

3. **问题描述**：§11.4 迁移影响表给出 ~bvec 的替换选项 "按需使用 logicalNot"，但 §4.5 中未定义 logicalNot 具名函数，存在接口引用无法使用的问题
   - 所在位置：§11.4（第 1529-1530 行）
   - 严重程度：一般
   - 改进建议：将 "按需使用 logicalNot" 改为 "直接使用 ! 运算符（已重载）"，或在 §4.5 中补充 logicalNot 定义
   - 解决结果：已在 a_v19 中解决（§11.4 迁移影响表已改为"直接使用 `!` 运算符（已重载）"，消除无效引用）

4. **问题描述**：D31 声明 Vec2~Vec4 的 fill-from-Vec1 仅提供同类型版本，但未解释 Vec1 为何可以保留跨类型版本，实施者需自行推断原因
   - 所在位置：§7 D31（第 1073-1079 行）
   - 严重程度：一般
   - 改进建议：在 D31 末尾追加说明：Vec1 跨类型 fill-from-Vec1 实质是分量级类型转换而非广播填充，与 const init(x: T) 不构成重载冲突
   - 解决结果：已在 a_v19 中解决（D31 末尾追加说明"Vec1 跨类型版本实质是分量级类型转换而非广播填充，与 `const init(x: T)` 参数列表类型结构不同，不构成重载冲突"）

## 迭代第 19 轮

1. **问题描述**：D34 "const if" 术语清理不彻底，全文25处残留，实施者面临误导风险
   - 所在位置：§3.2/§3.5/§3.6/§5/§7 D11~D32/§9/§9.4 共25处，详见诊断报告发现1
   - 严重程度：一般
   - 改进建议：对全文档执行系统性搜索替换，将 `const if` 统一改写为 `编译期 if` 或 D34 推荐的表述方式；D32 第1087行等重点修复
   - 解决结果：已在 a_v20 中解决（版本变更摘要明确对全文档 25 处执行系统性搜索替换并按上下文改写为"编译期 `if`"/"`const` 函数体内的 `if` 表达式"等 D34 推荐表述，修订记录标记总计修复 25+ 处）

2. **问题描述**：§12.3 同一行内混用 `tests/glm/detail/` 和 `tests/glm.detail/` 两种目录路径表示法
   - 所在位置：§12.3 第1843行
   - 严重程度：一般
   - 改进建议：将 `tests/glm.detail/` 改写为 `tests/glm/detail/`，全文 grep 搜索 `glm\.detail/` 模式确保无遗漏
   - 解决结果：已在 a_v20 中解决（§12.3 已统一为 `tests/glm/detail/`，修订记录标记全文 grep `glm\.detail/` 模式一次性清理确认仅此一处）

## 迭代第 20 轮

1. **问题描述**：equalEpsilon 与 epsilonOf<T>() 回退路径联动修改未在 §12.2 验收标准中覆盖，若回退触发但联动修改遗漏，会导致编译错误
   - 所在位置：§3.5 epsilonOf<T>() + §4.5 equalEpsilon + §10 依赖④ + §12.2 验收标准表
   - 严重程度：一般（中等）
   - 改进建议：在 §12.2 验收标准表末尾新增联动修改验收项
   - 解决结果：已在 a_v21 中解决（§12.2 已新增 "equalEpsilon-epsilonOf 回退联动验收" 项）

2. **问题描述**：public const init 在 Vec2~Vec4 上的 const 可达性依赖未在 §12.2 验收标准中独立列项
   - 所在位置：§4.1 构造函数签名 + §7 D11
   - 严重程度：一般
   - 改进建议：在 §12.2 验收标准中新增 const init 泛型构造可用性验证项
   - 解决结果：已在 a_v21 中解决（§12.2 已新增 "const init 泛型构造可用性" 验收项）

3. **问题描述**：mod 扩展成员函数浮点路径不可用的 API 不对称性缺少显式文档标注
   - 所在位置：§4.3 mod 函数描述 + §4.6 @OverflowWrapping 标注分布表
   - 严重程度：一般
   - 改进建议：在 §4.3 末尾补充浮点路径不可用标注，或纳入 §12.2 验收标准
   - 解决结果：已在 a_v21 中解决（§4.3 已补充 API 影响标注框 + §12.2 新增 "mod 扩展成员函数浮点路径不可用约束" 验收项）

## 迭代第 21 轮

1. **问题描述**：发现 1 — `@OverflowWrapping` 标注策略未覆盖 scalar-op-vec 方向包级独立函数，导致 `add(s,v)` 等 20 个函数的溢出行为（`@OverflowThrowing` 默认）与 `v+s`/`v.add(s)`（`@OverflowWrapping`）不一致
   - 所在位置：§4.3 具名函数定义（第 631-646 行）、§4.6 `@OverflowWrapping` 标注分布表（第 736-748 行）
   - 严重程度：中等（一般）
   - 改进建议：在 §4.6 标注分布表中新增 scalar-op-vec 方向包级独立函数条目，在 `scalar_vec_ops.cj` 的 20 个函数定义上标注 `@OverflowWrapping`
   - 解决结果：已在 a_v22 中解决（§4.6 标注分布表已补充 scalar-op-vec 方向 20 个函数条目）

2. **问题描述**：发现 2 — §8.2 范围可追溯性表缺少 `scalar_vec_ops.cj` 的显式映射行
   - 所在位置：§8.2 范围可追溯性对照表（第 1195-1211 行）
   - 严重程度：一般
   - 改进建议：在 §8.2 末尾新增 `scalar_vec_ops.cj` 的 roadmap 映射行
   - 解决结果：已在 a_v22 中解决（§8.2 已新增 `scalar_vec_ops.cj` 的显式映射行）

## 迭代第 22 轮

1. **问题描述**：`fwd.cj` 256 个别名缺乏可执行的生成脚手架，实施者面临命名规则理解偏差、脚本开发工时未纳入成本评估、生成后难以手动核对等实操风险。
   - 所在位置：§3.8 向量类型别名（第 439-444 行）、完整 §8 迁移文件清单
   - 严重程度：一般
   - 改进建议：在 §3.8 末尾新增最小生成脚本示例（30-50 行 Python/Node.js 脚本模板）；在 §11 迁移成本评估中新增"脚本开发与调试"条目；建议生成脚本在 §2 项目初始目录结构中预先占位。
   - 解决结果：已在 a_v23 中解决（§3.8 末尾新增最小生成脚本示例子节含 40 行 Python 模板，并在 §2 初始目录结构中预占位 `scripts/` 目录；§11 新增 §11.11 "fwd.cj 脚本开发与验证工作量"子节）

2. **问题描述**：§4.5 `equalEpsilon` 代码示例仅展示了路径 A（调用 `epsilonOf<T>()`）的写法，未显式指向 §3.5 双路径备选策略，实施者可能仅按示例编写代码而忽略路径 B 的联动修改。
   - 所在位置：§3.5 `epsilonOf<T>()` 双路径策略（第 328-335 行）vs §4.5 `equalEpsilon` 代码示例（第 708-718 行）
   - 严重程度：一般
   - 改进建议：在 §4.5 代码示例下方添加交叉引用标注，说明 `epsilonOf<T>()` 引用受 §3.5 双路径策略约束；或在 §3.5 末尾增加指向 §4.5 的交叉引用，形成双向可见。
   - 解决结果：已在 a_v23 中解决（§3.5 双路径策略与 §4.5 `equalEpsilon` 的交叉引用标注已补充，形成双向可见）

3. **问题描述**：`internal` 类型测试访问的备选方案仅简化为"将可见性从 `internal` 提升为 `public`"，缺少影响范围评估、条件判断逻辑和替代方案（如 `protected` 可见性或测试 helper 文件间接访问）。
   - 所在位置：§12.3 测试工具（第 1886 行）
   - 严重程度：一般
   - 改进建议：在 §12.3 末尾新增"`internal` 类型测试访问回退方案"子节，明确首选方案（提升为 `public` 并同步更新 §3.1 和 §8.2）及替代方案评估；将此验证作为 §2.1 编码前验证检查清单的补充验证项。
   - 解决结果：已在 a_v23 中解决（§12.3 已新增 `internal` 类型访问三级回退方案，并补充影响范围评估和条件判断逻辑）

## 迭代第 23 轮

1. **问题描述**：`fwd.cj`中`type`别名缺少`public`可见性修饰符，仓颉中顶层声明默认可见性为`internal`，`public import`仅重新导出标为`public`的声明，导致下游消费者无法访问256个别名
   - 所在位置：§3.8 脚本模板（第484-514行）及预期文件结构示例（第448-469行）
   - 严重程度：严重
   - 改进建议：为所有`type`别名添加`public`修饰符；同步更新§3.8示例；在§12.2验收标准中新增"别名对外可见性"验证项
   - 解决结果：已在 a_v24 中解决（§3.8 脚本模板将 `type` 改为 `public type`，同步更新 `fwd.cj` 示例；§12.2 新增"别名对外可见性"验收项）

2. **问题描述**：§2 声称别名可通过`public import`暴露给下游，但§3.8代码示例使用`type`（默认`internal`）而非`public type`，设计意图与代码示例的可见性不一致
   - 所在位置：§2 vs §3.8 vs §7 D1
   - 严重程度：一般
   - 改进建议：统一全文所有`type`别名示例为`public type`形式；在§3.8末尾注明"`type`别名默认可见性为`internal`，对外暴露须添加`public`修饰符"
   - 解决结果：已在 a_v24 中解决（统一全文 `type` 别名示例为 `public type` 形式；§3.8 末尾新增"仓颉 `type` 别名可见性提醒"段落）

3. **问题描述**：§8.2可追溯性对照表仅映射§3E内容，未显式映射§3G的关键结论（依赖闭合性、独立可编译性、最简可行范围定义）
   - 所在位置：§8.2
   - 严重程度：一般
   - 改进建议：在§8.2表格末尾新增映射行，或增加说明标注"§3G结论已在§2、§8.3、§3.8中覆盖"
   - 解决结果：已在 a_v24 中解决（§8.2 前新增 §3G 范围确认结论追溯说明段，覆盖依赖闭合性、独立可编译性、最小可行范围定义三项）

4. **问题描述**：§2.1编码前验证检查清单和§10验证要求表均未包含对生成脚本本身的验证项；未定义脚本输出不一致时的检测机制；未约定手动修改`fwd.cj`与脚本同步的规则
   - 所在位置：§2.1 + §10 + §3.8
   - 严重程度：一般
   - 改进建议：在§2.1中新增生成脚本验证项；在§10中新增脚本验证依赖；在§3.8中补充手动修改须同步更新脚本的警告
   - 解决结果：已在 a_v24 中解决（§2.1 新增验证项⑪脚本映射表正确性验证；§10 新增"生成脚本验证依赖"子节；§3.8 新增"生成脚本生命周期警告"）

## 迭代第 24 轮

1. **问题描述**：脚本模板命名错误，48/256 个别名将产生错误名称（如 `VecVec2` 而非 `Vec2`）
   - 所在位置：§3.8 第 507 行
   - 严重程度：严重
   - 改进建议：修复脚本中名称生成逻辑，对 `Vec`/`DVec`/`FVec` 三个家族使用条件格式避免重复 "Vec"
   - 解决结果：已在 a_v25 中解决（§3.8 已修复命名逻辑，Vec/DVec/FVec 三家族改用条件格式；§2.1 验证项⑪增加抽查与回退说明）

2. **问题描述**：const/extend 不对称导致 11 个日常操作在 const 上下文中不可用，缺少消费者告知
   - 所在位置：§3.2 Vec 结构体系、§4.3、§4.5、§7 D32/D33
   - 严重程度：中等
   - 改进建议：新增 const 上下文 API 可用性速查表，补充迁移成本评估
   - 解决结果：已在 a_v25 中解决（§3.2 末尾新增 const 上下文 API 可用性速查表，覆盖 11 个日常操作；§11.12 合计表追加审查工作量）

3. **问题描述**：迁移成本评估缺少合计汇总
   - 所在位置：§11
   - 严重程度：中等
   - 改进建议：在 §11 末尾新增合计评估子节，按乐观/最可能/悲观三档汇总
   - 解决结果：已在 a_v25 中解决（§11 末尾新增 §11.12 合计评估子节，按乐观/最可能/悲观三档汇总各子节成本）

4. **问题描述**：参考实现行为声明缺少具体文件行号引用
   - 所在位置：§7 D6、§8.4
   - 严重程度：一般
   - 改进建议：在 §8.4 策略 0 前置验证项中增加参考实现文件路径和行号范围
   - 解决结果：已在 a_v25 中解决（§8.4 策略 0 前置验证项补充 `setup.hpp`/`qualifier.hpp`/`type_vec*.inl` 路径与行号范围）

5. **问题描述**：跨文件依赖未显式声明
   - 所在位置：§附录
   - 严重程度：一般
   - 改进建议：在 §附录首段增加跨文件依赖声明
   - 解决结果：已在 a_v25 中解决（§附录首段新增跨文件依赖声明，标注依赖路径 `a_v18_revision_history.md`）

6. **问题描述**：编译器行为依赖聚合风险未系统性评估
   - 所在位置：§10、§2.1
   - 严重程度：一般
   - 改进建议：新增统一编译器依赖验证测试，编码启动前一次性运行；补充回退叠加成本评估
   - 解决结果：已在 a_v25 中解决（§2.1 新增验证项⑫编译器依赖统一验证测试；§11.13 新增回退成本评估）

## 迭代第 25 轮

1. **问题描述**：§11.12 合计评估表中 §11.5 与 §11.9 存在重复计算风险，实施者可能对两行求和产生约 1.0~2.0 人天的重复估算
   - 所在位置：§11.12 合计评估表（第 1947-1973 行），§11.5 最末段（第 1688-1691 行）
   - 严重程度：一般
   - 改进建议：在合计评估表 §11.5 行末尾添加脚注"与 §11.9 非累加，实际以 §11.9 为准"，或将 §11.5 行从合计表中移除并将其成本归入 §11.9 的子评估中
   - 解决结果：已在 a_v26 中解决（§11.5 行末添加脚注 [^1]，§11.9 段首明确非累加关系，合计表说明段落补充脚注内容）

2. **问题描述**：`fromBoolVec` 工厂函数未在 §4 关键行为契约中正式定义，也未出现在 §12.1/§12.2 验证与验收项中
   - 所在位置：§9.4 类型转换边界（第 1393 行路径 A），§4 全文
   - 严重程度：一般
   - 改进建议：在 §4 中新增子节正式给出 `fromBoolVec` 的 Vec1~Vec4 完整签名清单，并在 §12.1/§12.2 中补充对应验证与验收项
   - 解决结果：已在 a_v26 中解决（§4 新增 §4.8 给出 Vec1~Vec4 完整签名清单，§9.4 改为交叉引用，§12.1/§12.2 补充验证与验收项）

## 迭代第 26 轮

1. **问题描述**：`fromBoolVec` 定义为扩展成员函数导致调用方需持有无关 dummy 实例作为 receiver，与本设计其他工厂函数风格不一致
   - 所在位置：§4.8 `fromBoolVec` 签名清单
   - 严重程度：一般
   - 改进建议：改为包级独立函数或 VecN 静态函数，消除 dummy instance 依赖
   - 解决结果：已在 a_v27 中解决（§4.8 改为包级独立函数，定义于 `type_fromBoolVec.cj`，§4.1/§9.4/§12 同步更新，调用方无需 dummy receiver）

2. **问题描述**：Vec4 多重重载构造函数在重载解析中存在歧义风险，影响范围约 30 个构造函数签名，重构成本高；§10 未新增重载解析验证项，未定义回退方案
   - 所在位置：§4.1 "跨 Q 构造函数重载解析风险与约束验证"段落末
   - 严重程度：一般
   - 改进建议：在 §10 新增重载解析验证项；定义具体回退方案（移除歧义分支并转为具名工厂函数）；前置到 §2.1 编码前验证清单
   - 解决结果：已在 a_v27 中解决（§4.1 风险等级升级为中等并附具体回退方案——移除 4-6 个歧义分支改为具名工厂函数；§10 新增 Vec4 重载解析验证子节；§2.1 清单新增第⑬项）

3. **问题描述**：`equalEpsilon` 定义于 `extend` 块中（非 const），在 D29 回退路径描述段落中未显式标注 const 限制，编码实施者可能遗漏
   - 所在位置：§4.5 D29 回退描述段落
   - 严重程度：一般（中等）
   - 改进建议：在 D29 回退描述段落添加显式 const 限制警示；在 §12.2 验收标准中新增 D29 回退下 const 上下文容差比较的备选方案验证项
   - 解决结果：已在 a_v27 中解决（§4.5 D29 回退段落新增 ⚠ const 真空警示并给出两项缓解方案；§12.2 验收表新增 `equalEpsilon` D29 回退路径 const 真空验证项）

## 迭代第 27 轮

1. **问题描述**：`fromBoolVec`/`fromBoolVecQ2` 的迁移文件 `type_fromBoolVec.cj` 未纳入§8.1迁移文件清单，§2目录结构树形图和§8.3迁移顺序也未同步更新，实施者面临文件归属不明确的困境
   - 所在位置：§4.8（第876-936行）、§8.1迁移文件清单（第1335-1366行）、§2目录结构（第63-81行）
   - 严重程度：一般
   - 改进建议：在§8.1迁移文件清单中新增`type_fromBoolVec.cj`条目（或确认最终文件名），同步更新§2目录结构树形图和§8.3迁移顺序的阶段5；或显式指定函数归入现有文件（如`scalar_vec_ops.cj`）并同步更新相关章节
   - 解决结果：已在 a_v28 中解决（§8.1 新增 12b 条目 `type_fromBoolVec.cj` 及 T12b 测试条目；§2 目录结构树形图新增文件行；§8.3 迁移顺序新增阶段 5b "Bool→Numeric 转换"）

## 迭代第 28 轮

1. **问题描述**：版本标识不统一——文档标题标注"v27"，文件名为 `a_v28_copy_from_v27.md`，版本摘要描述 v26→v27 变更，读者无法确定基线版本
   - 所在位置：文档标题第 1 行、文件名、版本变更摘要
   - 严重程度：中等
   - 改进建议：在文档标题标注为"v28（基于 v27 迭代审查基线）"或在文件头增加版本声明块
   - 解决结果：已在 a_v29 中解决（标题同步为 v29，新增版本声明块（第 3 行），统一以文件名为准）

2. **问题描述**：`equalExact` const 化备选方案与 D32/D33 统一决策未协同，未评估架构影响
   - 所在位置：§4.5、§7 D32/D33
   - 严重程度：一般
   - 改进建议：在 D33 末尾补充 `equalExact` const 化备选方案说明，或在 §4.5 移除"移至 struct 体内"建议
   - 解决结果：已在 a_v29 中解决（D33 末尾补充 `equalExact` const 化备选方案说明；§4.5 引用 D33 统一约定而非独立声明）

3. **问题描述**：`scalar_vec_ops.cj` 的 `std.math` 外部依赖未在模块依赖表中显式标注（仅自然段补充说明）
   - 所在位置：§2 模块间依赖表 `scalar_vec_ops` 行
   - 严重程度：一般
   - 改进建议：在依赖表 `scalar_vec_ops` 行添加脚注或修改依赖标注为 `→ qualifier + std.math`
   - 解决结果：已在 a_v29 中解决（§2 模块依赖表 `scalar_vec_ops` 行显式标注 `+ std.math`（浮点 mod 路径依赖 `std.math.trunc`），并引用 §10）

4. **问题描述**：T2 跨类型约束矩阵未集中归档为结构化表格，实施者需交叉查阅两处
   - 所在位置：§4.1 跨类型构造函数描述末尾
   - 严重程度：一般
   - 改进建议：在 §4.1 新增结构化矩阵表，行标题为 T2 类型，列标题为 T 类型，单元格填写编译行为描述
   - 解决结果：已在 a_v29 中解决（§4.1 末尾新增 T2 跨类型约束矩阵结构化表——行=目标类型 T，列=源类型 T2，单元格=编译/行为描述）

5. **问题描述**：const 真空备选方案（`equalEpsilon` 迁至 struct）的架构影响和成本未在 §11 中评估
   - 所在位置：§4.5 const 真空警示、§11 迁移成本评估
   - 严重程度：一般
   - 改进建议：在 §11 末尾新增"`equalEpsilon` const 化备选方案成本评估"子节，或显式声明此方案的触发条件
   - 解决结果：已在 a_v29 中解决（新增 §11.14 `equalEpsilon` const 化备选方案成本评估，覆盖方案①（约 0.5 人天）与方案②（约 1.0-1.5 人天））

## 迭代第 29 轮

1. **问题描述**：const 真空缓解方案①（将 equalEpsilon 移至 struct 体）与 D29 回退触发条件存在逻辑矛盾——方案①不改变函数体内容，在 D29 已验证失败的条件下必然同样编译失败；§11.14 成本评估（0.5 人天）基于不可行前提
   - 所在位置：§4.5（第 881-884 行）、§11.14（第 2174-2204 行）、§7 D29（第 1257-1271 行）
   - 严重程度：严重
   - 改进建议：修正方案①为"理论路径"并标注前提条件；§11.14 可行性评级降级并标注前提条件依赖；补充第三条缓解方案（const 上下文使用精确比较）
   - 解决结果：已在 a_v30 中解决（方案①降级为"理论路径"并标注前提条件——仅在 D29 由 extend 语法限制单独导致时可行；可行性评级降为"极低"；新增路径②（const 上下文用精确比较 ==）作为实际可行推荐；§11.14 方案①成本标注为理论值并声明前提条件）

2. **问题描述**：修订说明版本序列存在非连续跳变（v29→v27 缺少 v28），影响文档可追溯性和版本审计能力
   - 所在位置：§附录 修订说明（第 2310-2395 行）
   - 严重程度：一般
   - 改进建议：确认 a_v28_copy_from_v27.md 中是否存在修订说明，将其移至 v29 附录正确位置；若 v28 为确认性审查则新增"版本 v28"条目
   - 解决结果：已在 a_v30 中解决（附录新增"版本 v28 — 确认性审查轮次"条目，标注为内部确认性审查、无实质设计变更、修订说明为空以保持序列连续性）

3. **问题描述**：protected 回退方案声称"仅允许同包和子包访问"但仓颉规范中 protected 可见域为当前模块，跨模块可用性未在目标编译器上验证
   - 所在位置：§12.3（第 2297 行）
   - 严重程度：一般
   - 改进建议：标注前提条件（需验证测试包与生产包是否属同一模块）；调整回退层级顺序
   - 解决结果：已在 a_v30 中解决（§12.3 回退方案层级表标注 protected 前提条件——需验证测试包与生产包是否同模块；新增判断条件说明；可行时降为 2nd 方案）

4. **问题描述**：cjpm.toml 骨架缺少 test-dir 或 [test] 相关配置，14 个测试文件的自动发现机制未说明，@Test 在 static 项目中的可用性未作为独立依赖项验证
   - 所在位置：§2（第 106-157 行）
   - 严重程度：一般
   - 改进建议：补充完整配置示例（含测试目录设置）；在 §10 新增测试可用性验证项
   - 解决结果：已在 a_v30 中解决（§2 补充完整可直接拷贝的 `cjpm.toml` 配置——含 `[test] src-dir = "tests"`；§10 验证项⑩明确 `cjpm test` 可发现 `@Test` 标注用例）

5. **问题描述**：equalEpsilon 在整数 Vec 上的编译通过性声称（Int32 支持 - 和 abs）缺少对 epsilonOf<T>() 的 Number<Int32> 约束是否满足的显式验证追踪链
   - 所在位置：§4.5（第 871-873 行）、§10（第 1654 行）
   - 严重程度：一般
   - 改进建议：在 §4.5 注释中显式指向 §3.5 双路径策略；在 §10 依赖② 补充验证步骤与 §3.5 的联动关系
   - 解决结果：已在 a_v30 中解决（§4.5 `equalEpsilon` 代码示例注释显式指向 §3.5 兼容性策略路径 A/B；§10 依赖④补充与 §3.5 双路径策略的联动关系验证步骤）

6. **问题描述**：需求响应充分度补审结论整体良好，但 §8.4 策略 0 缺少验证失败后的具体回退操作步骤
   - 所在位置：§1, §8.2, §8.4
   - 严重程度：一般
   - 改进建议：在 §8.4 策略 0 中补充验证失败场景的具体执行路径（如 SIMD 路径非预期启用时的影响范围分析与依赖调整）
   - 解决结果：已在 a_v30 中解决（§8.4 策略 0 末尾补充三项验证项各自的失败回退路径及文档更新流程）

7. **问题描述**：接口定义充分性方面，fromBoolVec 的 T 参数无约束但接口层面未说明合法取值范围；256 个别名缺少消费者快速选择指南
   - 所在位置：§2, §3.8, §4.8
   - 严重程度：一般
   - 改进建议：在 §4.8 补充 T 参数类型范围说明文本；在 §3.8 新增 2-3 行快速选择示例
   - 解决结果：已在 a_v30 中解决（§4.8 补充 T 参数类型范围说明——所有数值类型 + Bool；§3.8 别名实用性分级表后新增消费者快速选择指引段落）

8. **问题描述**：异常场景与边界条件系统性评估发现两个缺口：A) mod 的 NaN/Infinity 边界未在 §9.3 显式声明；B) 有符号整数 @OverflowWrapping 与 C++ UB 的迁移注意事项缺少说明
   - 所在位置：§5.2, §9, §12.1
   - 严重程度：一般
   - 改进建议：在 §9.3 新增 mod 运算的 NaN/Infinity 说明行；在 §5.1 或 §11 补充有符号整数 @OverflowWrapping 迁移注意事项
   - 解决结果：已在 a_v30 中解决（§9.3 新增 mod 运算 NaN/Infinity 边界说明行——含 `Inf % finite`、`finite % 0`、`NaN % finite` 等传播规则；§5.1 补充有符号整数 `@OverflowWrapping` 与 C++ UB 迁移注意事项——依赖/不依赖 UB 的三种迁移场景）

9. **问题描述**：编码直接指导性方面，阶段 5 粒度较粗（Vec1~Vec4 合并）、运算符签名使用自然语言描述缺少完整语法模板、equalEpsilon 跨 5 章节依赖缺少集中参考框、§10 依赖④ 缺少对 §12.2 对应验收项的引用
   - 所在位置：§4.3, §4.5, §8.3, §10
   - 严重程度：一般
   - 改进建议：拆分阶段 5 为 5a/5b；补充完整运算符语法模板；添加 equalEpsilon 集中参考框；补充 §10→§12 交叉引用
   - 解决结果：已在 a_v30 中解决（§8.3 拆分阶段 5 为 5a/5b/5c；§4.3 补充 Vec2 算术运算符完整语法模板；§4.5 `equalEpsilon` 定义前新增集中参考框；§10 依赖④补充 §12.2 验收项引用）

## 迭代第 30 轮

1. **问题描述**：§8.4 和 §7 D6 中引用的参考实现行号与实际代码位置存在严重偏差（如 setup.hpp 声称 450~570 行，实际在 558~585 行）
   - 所在位置：§8.4 策略 0 验证项①②③、§7 D6
   - 严重程度：一般
   - 改进建议：核对 references/glm-1.0.3/ 中各目标文件的实际行号，更新为准确值并标注具体内容范围
   - 解决结果：**未解决**（a_v30 仍保留 450~570 等错误行号，line 1573）；当前无 a_v31，需要在下一轮迭代中按 b_v30_diag_v1.md 改进建议核对 `references/glm-1.0.3/` 实际行号并更新

2. **问题描述**：编译器行为假设（12+ 项）的验证依赖关系、方法、回退方案、成本分析分散在 §2.1、§7、§10、§11.13 四个相隔较远的章节，缺乏集中可视化图
   - 所在位置：§2.1、§7 D29/D30、§10、§11.13
   - 严重程度：一般
   - 改进建议：在 §2 末尾新增编译器行为依赖全景图（Mermaid 流程图或结构化表格），集中展示验证依赖关系和回退路径
   - 解决结果：**未解决**（a_v30 仍分散在 §2.1/§7/§10/§11.13，grep 无 Mermaid/全景图/依赖全景图匹配）；当前无 a_v31，需要在 §2 末尾新增编译器行为依赖全景图（Mermaid 或结构化表格）并补充失败回退章节编号

3. **问题描述**：缺少完整的"非首轮内容排除清单"，实施者需自行从 §8.2 追溯表、§3.9、参考实现目录推断排除范围
   - 所在位置：§8.4 策略 6、§8.2、§3.9、§7 D24
   - 严重程度：一般
   - 改进建议：在 §8.4 末尾新增"首轮内容边界声明"表格，按文件级别列出首轮包含/排除/仅定义不消费的内容
   - 解决结果：**未解决**（a_v30 §8.4 策略 6 line 1585 仅一段简短说明，无结构化清单）；当前无 a_v31，需要在 §8.4 末尾新增"首轮内容边界声明"表格（按文件级别列出包含/排除/仅定义不消费）

4. **问题描述**：跨 Q 赋值等价假设的隐含影响面（泛型构造函数、算术运算符、比较运算符等约 64 处代码位置）未被标记，后续轮次引入 Qualifier 差异化时难以评估影响范围
   - 所在位置：§3.1 Qualifier 运行时行为约定
   - 严重程度：一般
   - 改进建议：在 §3.1 中新增跨 Q 等价假设影响范围表，列出受影响的泛型代码类别和位置
   - 解决结果：**未解决**（a_v30 §3.1 无跨 Q 等价假设影响范围表）；当前无 a_v31，需要在 §3.1 或 §7 D27 新增影响范围表（覆盖 8 类受影响的代码类别与位置）并对 `type_vecN.cj` 中的相关位置添加注释标记