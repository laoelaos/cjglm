# 再审议判定报告（v3）

## 判定结果

RETRY

## 判定理由

组件B诊断报告（b_v3_diag_v1）包含1个严重等级问题（问题1：`bitwiseNot()`对`VecN<Bool, Q>`的排除策略在仓颉中不可实现）、2个中等等级问题（问题2：别名命名约定偏离C++ GLM API导致迁移成本未被评估；问题3：跨Q赋值迁移模式未纳入§11）以及3个一般等级问题（问题4~6）。质询报告结果为LOCATED，确认了诊断的有效性且未实质性削弱任何问题的严重性等级。根据判定标准，审查报告包含严重或一般等级的问题，应判定为RETRY重新运行组件A。

组件B内部循环最大轮次12，实际轮次1（提前终止），质询结果LOCATED说明审查结论已被确认。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：`bitwiseNot()`对`VecN<Bool, Q>`的排除策略在仓颉中不可实现
- **所在位置**：§3.2 `VecN<Bool, Q>` 行为约定（第175行）、§7 D25（第858-862行）
- **严重程度**：严重
- **改进建议**：移除"可通过排除实现严格兼容"的不可行建议，改为明确声明此为不可消除的已知行为差异；迁移`~bvec`时需要人工审查。

- **问题描述**：别名命名约定（PascalCase）偏离C++ GLM API（小写），迁移成本未纳入§11评估
- **所在位置**：§3.8命名约定（第317-319行）、§11迁移成本评估（全文）
- **严重程度**：中等
- **改进建议**：在§11中新增子节评估别名重命名的迁移成本，包括受影响范围、重命名映射表、机械替换可行性和迁移工具策略。

- **问题描述**：跨Q赋值迁移模式未纳入§11
- **所在位置**：§11（全文）
- **严重程度**：中等
- **改进建议**：在§11中补充跨Q赋值的迁移模式说明，明确哪些场景需要补全Q、哪些可通过别名避免，以及自动化工具的可行性评估。

- **问题描述**：`epsilonOf<T>()` Option B兼容性策略（为`NumericLimits<Bool>`添加空壳特化）不可行
- **所在位置**：§3.5兼容性策略Option B（第256-257行）
- **严重程度**：一般
- **改进建议**：从§3.5中完全移除Option B，仅保留Option A（内联固定容差）作为唯一的兼容性策略。

- **问题描述**：`operator[]` const取值形式未提供
- **所在位置**：§4.2分量访问（第500-502行）、§7 D28（第876-880行）
- **严重程度**：一般
- **改进建议**：验证仓颉是否支持仅通过`const`修饰符区分运算符版本；若不支持，通过具名const访问函数`componentAt(i: Int64): T`提供const上下文中的分量访问能力。

- **问题描述**：`mod`浮点实现的外部依赖（`std.math.trunc`）未显式验证
- **所在位置**：§4.3 `%`运算的整数约束（第527行）
- **严重程度**：一般
- **改进建议**：在§10设计阶段验证要求表中新增一项，验证`Float32`浮点`mod`通过算术恒等式实现的正确性，覆盖边界值（`Float32.Max`、`Float32.MinPositive`、`0.0`、`±Inf`、`NaN`）下的行为。
