根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

### 发现 1（中等）— §11.5 与 §11.9 存在结构重叠，迁移工作量存在重复计算风险

§11.5 "`Inf == Inf` 容差比较路径行为差异" 与 §11.9 "`equalExact` 迁移模式" 描述的是同一组行为差异（容差路径下 `==` 的精度行为与 C++ GLM `==` 不同），但以两个独立子节呈现，各自定义了覆盖范围重叠的搜索替换规则和人工审查项。

两份工作量评估都包含了"人工审查浮点 Vec `==` 使用上下文"，未说明两份评估是累加还是互补，实施者如果按顺序执行可能对同一批代码进行两次审查。

**改进建议**：将 §11.5 合并为 §11.9 的一个子节（如 §11.9.1 "Inf/NaN 边界行为"），统一迁移指引和工作量评估。或在 §11.9 开头注明"§11.5 定义的 `Inf == Inf` 差异为本节所述行为差异的子集，本节迁移模式已覆盖 §11.5 场景，无需重复审查"。

### 发现 2（一般）— §4.3 残留 "const if" 术语，与 D34 约定不一致

§7 D34 明确要求全文将 `const if (...)` 统一改写为 `if (...)`，使用 "`const` 函数体内的 `if` 表达式"或"编译期 `if` 分支"表述。但 §4.3（第 646 行）仍使用 "`const if`" 作为单独术语："仓颉中 `const if` 要求所在函数必须为 `const` 函数"。

**改进建议**：将 "仓颉中 `const if` 要求" 改为 "仓颉中编译期分支 `if` 要求" 或 "仓颉中 `const` 函数体内的 `if` 表达式要求"，与 D34 一致。全文 grep 确认无其他残余。

### 发现 3（一般）— §11.4 引用了未在 §4 中定义的 `logicalNot` 函数

§11.4 迁移影响表（第 1529 行）给出了 `~bvec` 的仓颉替换选项："若需保持 C++ 行为，应替换为 `VecN<Bool, Q>(true)`（全 true）或按需使用 `logicalNot`"。但 §4.5 布尔逻辑运算中仅定义了 `logicalAnd`、`logicalOr` 和一元 `!` 运算符，未定义 `logicalNot` 具名函数。

**改进建议**：将 "按需使用 `logicalNot`" 改为 "直接使用 `!` 运算符（已重载）"，或同时在 §4.5 中增加 `logicalNot` 具名函数的定义以保持与 `logicalAnd`/`logicalOr` 的 API 对称性。

### 发现 4（一般）— D31 中 Vec1 跨类型 fill-from-Vec1 的豁免理由未明确

§7 D31 声明 "Vec2~Vec4 的 fill-from-Vec1 构造器仅提供同类型版本"，但未明确解释 Vec1 为什么可以保留跨类型版本（`public init<T2, Q2>(v: Vec1<T2, Q2>)`）。实施者需要自行推断原因。

**改进建议**：在 D31 末尾追加一句说明，例如："Vec1 的跨类型 fill-from-Vec1（`init<T2, Q2>(v: Vec1<T2, Q2>)`）不受此限制——Vec1 仅单个分量，跨类型版本实质是分量级类型转换而非广播填充，与 `const init(x: T)` 的参数列表（`T` vs `Vec1<T2, Q2>`）不构成重载冲突。"

### 发现 5（轻微）— §8.2 S3 偏离理由中 VArray 引用的相关性存疑

§8.2 范围可追溯性对照表 S3 行解释 `countof` 偏离的理由时提到将 VArray 作为替代方案引入解释。VArray 并非 countof 的替代品，而是仓颉中一种独立的数据结构。正确的偏离理由应更简洁。

**改进建议**：简化偏离理由为："Cangjie 无 C 数组类型——GLM 中 countof 宏基于 `sizeof(array)/sizeof(array[0])` 计算 C 数组元素个数，此模式在 Cangjie 中无对应映射。所有原使用场景在迁移时删除。" 移除 VArray 引用。

### 发现 6（提示）— 修订历史附录占文档总篇幅约 55%，影响可导航性

附录（约 1200 行）占全文 2092 行的 55% 以上。v18 的当前修订说明仅 3 条（约 30 行），其余内容均为 v2~v17 的历史修订记录。此问题曾在迭代 13 和迭代 15 中被标记，但应对措施仅限于添加分隔线和版本说明，未移除历史记录。

**改进建议**：将历次修订记录（v2~v17）移出主文档，存入独立的历史记录文件（如 `a_v18_revision_history.md`），仅保留 v18 的当前修订说明和一条指向历史文件的链接。或保留最近 3-5 个版本（v14~v17）的修订说明在附录中，更早的记录归档至独立文件。

## 历史迭代回顾

### 持续存在的问题
- **附录篇幅/可导航性问题**（发现 6）：迭代 13 问题 1（附录结构混乱）、迭代 15 问题 3（版本序列混乱）、本轮发现 6（篇幅占 55%）。问题持续多轮，v18 仅添加分隔线和版本关系图但未削减篇幅。本轮需实施实质性削减。
- **`const if` 术语残留**（发现 2）：迭代 11 问题 4、迭代 12 问题 3、本轮发现 2 均指出 `const if` 术语残留。此问题反复出现，需本轮彻底全文 grep 清除。
- **countof 偏离理由**（发现 5）：迭代 8 问题 3、迭代 9 问题 6、本轮发现 5 均涉及 countof/VArray 偏离理由表述不当。需本轮彻底修正。

### 已解决的问题
- 迭代 16 的 4 个问题（equalEpsilon 固定容差矛盾、equalEpsilon Bool 行为描述错误、equalEpsilon 代码示例未使用变量、equalEpsilon 验证计划缺失）—— 不再提及，已解决
- 迭代 17 的 3 个问题（equalEpsilon 回退依赖链未归档、fromBoolVec 不对称性说明、Display/ToString 策略说明）—— 不再提及，已解决
- 早期迭代（3~15）中大量问题不再出现，已逐轮修复

### 新发现的问题
- §11.5 与 §11.9 结构重叠（发现 1）—— 本轮首次识别
- §11.4 引用了未定义的 `logicalNot`（发现 3）—— 本轮首次识别
- D31 Vec1 跨类型 fill-from-Vec1 豁免理由不明确（发现 4）—— 本轮首次识别

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606190029_glm-migration-ood\a_v18_copy_from_v17.md

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606190029_glm-migration-ood\requirement.md
