# 质量审查诊断报告 — v21 OOD 设计文档

## 整体评价

该文档经过 20 轮迭代和审议，整体质量极高。结构清晰、内容详实、设计决策（36 项）记录完整，覆盖了从模块划分到验收策略的完整生命周期。以下发现侧重于前三者内部审议可能未充分覆盖的维度——**需求响应完备性、设计完整性、实施可操作性**。

---

## 发现 1（中等）— `@OverflowWrapping` 标注策略未覆盖 scalar-op-vec 方向包级独立函数

**所在位置**：§4.3 具名函数定义（第 631-646 行）、§4.6 `@OverflowWrapping` 标注分布表（第 736-748 行）

**问题描述**：
§4.6 的 `@OverflowWrapping` 标注分布表详细列出了二元算术运算符（20 处）、`<<`（4 处）、`increment`/`decrement`（8 处）和 **vec-op-scalar 方向扩展成员函数**（通过委托隐式继承，无需直接标注），但**完全未提及 scalar-op-vec 方向的包级独立函数**（`add(s,v)`、`sub(s,v)`、`mul(s,v)`、`div(s,v)`、`mod(s,v)`，共 5 运算 × 4 Vec = 20 个函数）。

这 20 个函数定义于 `scalar_vec_ops.cj`，使用 `T` 类型的原生算术运算符（如 `s + v.x`）。仓颉中泛型类型 `T` 的原生 `+`/`-`/`*`/`/` 默认采用 `@OverflowThrowing` 策略，而 **vec-op-scalar 方向对应函数**（`v.add(s)` 等）通过委托给已标注 `@OverflowWrapping` 的二元运算符而采用 wrapping 语义，两者溢出行为不一致。

**具体影响**：

| 调用形式 | 溢出行为 | 标注覆盖 |
|---------|---------|---------|
| `v + s`（二元运算符） | wrapping（`@OverflowWrapping`） | §4.6 覆盖 ✓ |
| `v.add(s)`（扩展成员函数） | wrapping（委托给 `v + s`） | §4.6 覆盖 ✓ |
| `add(s, v)`（包级独立函数） | **throwing（`@OverflowThrowing` 默认）** | **未覆盖 ✗** |

**严重程度**：中等 — 这是公共 API 面的行为不一致。下游消费者根据操作数顺序选择 `v.add(s)` 或 `add(s, v)`，预期行为应当一致，但实际溢出策略不同，可能导致迁移代码中的隐蔽 bug。

**改进建议**：
- 方案 A（推荐）：在 §4.6 标注分布表中新增条目，明确 scalar-op-vec 方向包级独立函数的 `@OverflowWrapping` 标注策略——在 `scalar_vec_ops.cj` 中的 20 个函数定义上直接标注 `@OverflowWrapping`，与二元运算符的溢出策略保持一致。
- 方案 B：若标注位置受限（包级独立函数能否在声明处标注需验证），则在 §4.3 中明确标注溢出行为约定——scalar-op-vec 方向函数的整数路径须通过委托或内联确保 wrapping 语义与 vec-op-scalar 方向一致。在 §4.6 分布表中新增"scalar-op-vec 方向包级独立函数"条目说明标注方式。

---

## 发现 2（一般）— §8.2 范围可追溯性表缺少 `scalar_vec_ops.cj` 的显式映射行

**所在位置**：§8.2 范围可追溯性对照表（第 1195-1211 行）

**问题描述**：
`scalar_vec_ops.cj` 在 §8.1 文件清单中列为序号 8a（新增文件，roadmap 子范围 2a，roadmap §3E 序号"新增"），但在 §8.2 范围可追溯性表中没有对应的显式行。`lib.cj`（同为新增文件）在 §8.2 末尾有一个 "—" 行并附偏离说明，而 `scalar_vec_ops.cj` 完全未出现在追溯范围中。

**严重程度**：一般 — 范围追溯是首轮交付的完整性保证措施，缺行使得实施者无法从 §8.2 直接确认 scalar_vec_ops.cj 的 roadmap 映射关系，需交叉查阅 §8.1 和 §8.2 两处才能确认。

**改进建议**：在 §8.2 末尾新增一行（可在 `lib.cj` 行之后），将 `scalar_vec_ops.cj` 的 roadmap 映射关系显式记录：

| Roadmap §3E 序号 | Roadmap 子范围 | GLM 源文件 | 本设计对应章节 | 本设计对应仓颉文件 | 偏离说明 |
|:--:|:--:|:--:|:--:|:--:|:--|
| 新增 | 2a（随 Vec 运算） | —（新增） | §4.3 算术运算（scalar-op-vec 方向）、§7 D16 | `scalar_vec_ops.cj` | 新增文件，对标 `scalar + vec` 方向因运算符重载不可实现而提供的具名函数替代方案（§7 D10）。函数命名（`add`/`sub`/`mul`/`div`/`mod`）通过 D35 约定独占所有权属 |

---

## 发现 3（一般）— Vec4 重载构造函数数量过多且重载歧义风险仅标记未消解

**所在位置**：§4.1 Vec4 构造函数清单（第 553-599 行），§4.1 跨 Q 构造函数重载解析风险段落（第 605-608 行）

**问题描述**：
Vec4 的 `init` 构造函数清单约 **30 个重载版本**（包括泛型和具象版本），涵盖 Vec3+标量、Vec2+Vec2、Vec2+Vec1+标量、Vec1+Vec1+Vec1+标量等 25+ 种组合。文档在第 605-608 行承认了重载解析风险："当调用方传递一个已部分具现化的 Vec 类型...仓颉编译器可能同时匹配多个泛型构造函数模板"，但唯一的应对策略是"若编码阶段发现重载歧义，策略为移除歧义分支或将模糊分支改为具名函数形式"。

此问题自第 7 轮内部审议（迭代第 7 轮，发现 5）即已识别，历经 14 个迭代轮次未得到具体消解方案——重载解析风险处于"已识别→编码阶段确认→若不行则砍掉"的未决状态，实施者无法从设计文档明确知晓每个构造函数在何种输入下会被选中。

**严重程度**：一般 — 此风险不阻碍编码启动，但实施者必须自行编写原型测试验证每个构造函数的重载解析行为，若发现冲突则需要自行决定砍掉哪些分支。此不确定性可能导致编码阶段的设计反复。

**改进建议**：
- 在 §4.1 末尾新增一个"重载解析优先级"子节或表，按参数结构的具体程度对 Vec4（最严重）的泛型构造函数分级（如：`Vec+Vec` 组合优先于 `Vec` 组合，`Vec+标量` 组合优先于 `Vec` 组合），给出编译器预期会选择哪个重载的判断依据。
- 或：在 §4.1 中为每个泛型构造函数标注其适用场景，避免实施者猜测。

---

## 发现 4（提示）— `equalEpsilon` 中 "内联固定容差" 回退策略的语义漂移未显式标注

**所在位置**：§3.5 `epsilonOf<T>()` 兼容性策略（第 328-329 行），§4.5 `equalEpsilon` 定义（第 701-714 行），§10 依赖④（第 1413 行）

**问题描述**：
§3.5 的兼容性回退策略描述为"将 epsilon 逻辑内联到 `ComputeEqual.callConst` 的浮点分支中...使用固定容差值 `T(1e-6)`"。但 §10 依赖④ 的描述将其理解为 "`equalEpsilon` 的实现从引用 `epsilonOf<T>()` 改为内联固定容差 `T(1e-6)`"。两个位置的描述存在两层歧义：

1. **内联位置歧义**：§3.5 说内联到 `ComputeEqual.callConst`，而 §10 依赖④ 说内联到 `equalEpsilon`。当回退被触发时，`equalEpsilon` 是否也应内联？若回退仅影响 `ComputeEqual.callConst`，`equalEpsilon` 保留独立引用 `epsilonOf<T>()`，则 `epsilonOf<T>()` 回退后的实现（内联固定值到 `callConst` 中但不修改 `epsilonOf<T>()` 函数本身）意味着 `equalEpsilon` 的行为在回退路径下仍然是 `epsilonOf<T>()` 所提供的 `NumericLimits<T>.epsilon()`，与回退后 `==` 使用的固定 `T(1e-6)` 不一致。两处回退后的容差行为不同。

2. **`epsilonOf<T>()` 函数是否保留**：回退后 `epsilonOf<T>()` 函数是修改实现（改为返回固定值）还是完全移除（代码内联到 `callConst` 中）？文档未明确。若移除，则 `equalEpsilon` 体内的引用失效；若保留并修改，则 `equalEpsilon` 的行为自动适应。

**严重程度**：提示 — 此歧义不影响当前设计框架（回退路径本身已定义），但实施者在编码阶段遇到回退时需自行解读歧义，可能选择与设计意图不同的实现方式。

**改进建议**：在 §3.5 兼容性策略中明确 `epsilonOf<T>()` 回退后的生命周期：
- 若函数保留：修改其实现为 `const func epsilonOf<T>(): T { T(1e-6) }`（移除 `Number<T>` 约束），则 `equalEpsilon` 无需修改。
- 若函数移除：在 §3.5 和 §4.5 中均标注联动修改要求，存明 `equalEpsilon` 须同步内联。

---

## 结论

该文档经过 21 轮迭代，已高度成熟，整体可直接指导编码实施。以上发现全部集中在近 20 轮内部审议（侧重于技术可行性验证）可能未充分关注的维度：**公共 API 面行为一致性**（发现 1）、**范围追溯完整性**（发现 2）、**未决风险的管理粒度**（发现 3）、**回退路径的语义一致性**（发现 4）。

建议在下一轮迭代中优先处理发现 1（影响公共 API 一致性）和发现 2（影响范围可追溯性），发现 3 和发现 4 可在编码前择机澄清。
