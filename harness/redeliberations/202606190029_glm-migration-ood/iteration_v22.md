# 再审议判定报告（v22）

## 判定结果

RETRY

## 判定理由

- **诊断报告**：包含 3 个"一般"等级问题（发现 1: `fwd.cj` 256 个别名缺少生成脚手架；发现 2: §4.5 `equalEpsilon` 代码示例未体现 §3.5 双路径备选；发现 3: `internal` 类型测试访问备选方案细化不足）和 1 个"提示"等级问题（发现 4: §11 迁移成本评估缺失）。
- **质询报告**：LOCATED，所有发现经质询确认，证据充分性、逻辑完整性、覆盖完备性三个维度均通过。
- **循环状态**：内部循环实际轮次（1）< 最大轮次（12），提前终止于 LOCATED。
- **判定依据**：诊断报告包含"一般"等级问题，不满足 PASS 条件（须不含严重或一般等级问题，或问题均为轻微等级），故判定 RETRY。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：`fwd.cj` 256 个别名缺乏可执行的生成脚手架，实施者面临命名规则理解偏差、脚本开发工时未纳入成本评估、生成后难以手动核对等实操风险。
  - **所在位置**：§3.8 向量类型别名（第 439-444 行）、完整 §8 迁移文件清单
  - **严重程度**：一般
  - **改进建议**：在 §3.8 末尾新增最小生成脚本示例（30-50 行 Python/Node.js 脚本模板）；在 §11 迁移成本评估中新增"脚本开发与调试"条目；建议生成脚本在 §2 项目初始目录结构中预先占位。

- **问题描述**：§4.5 `equalEpsilon` 代码示例仅展示了路径 A（调用 `epsilonOf<T>()`）的写法，未显式指向 §3.5 双路径备选策略，实施者可能仅按示例编写代码而忽略路径 B 的联动修改。
  - **所在位置**：§3.5 `epsilonOf<T>()` 双路径策略（第 328-335 行）vs §4.5 `equalEpsilon` 代码示例（第 708-718 行）
  - **严重程度**：一般
  - **改进建议**：在 §4.5 代码示例下方添加交叉引用标注，说明 `epsilonOf<T>()` 引用受 §3.5 双路径策略约束；或在 §3.5 末尾增加指向 §4.5 的交叉引用，形成双向可见。

- **问题描述**：`internal` 类型测试访问的备选方案仅简化为"将可见性从 `internal` 提升为 `public`"，缺少影响范围评估、条件判断逻辑和替代方案（如 `protected` 可见性或测试 helper 文件间接访问）。
  - **所在位置**：§12.3 测试工具（第 1886 行）
  - **严重程度**：一般
  - **改进建议**：在 §12.3 末尾新增"`internal` 类型测试访问回退方案"子节，明确首选方案（提升为 `public` 并同步更新 §3.1 和 §8.2）及替代方案评估；将此验证作为 §2.1 编码前验证检查清单的补充验证项。
