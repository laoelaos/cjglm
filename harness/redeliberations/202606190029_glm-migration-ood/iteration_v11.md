# 再审议判定报告（v11）

## 判定结果

RETRY

## 判定理由

诊断报告（b_v11_diag_v1）共识别出 5 个问题：P1（中等）、P2（中等）、P3（一般）、P4（一般）、P5（一般）。质询报告确认 LOCATED，所有问题证据充分、逻辑自洽。其中 P1-P2 为中等程度（影响导入路径和包目录结构的正确性），在判定框架中对应"一般"等级；P3-P5 为一般程度。根据判定标准，审查报告包含一般等级问题，应判定为 RETRY。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：§4.3 残留导入路径错误，`scalar-ops-vec` 方向函数仍表述为 `import glm.detail.scalar_vec_ops.*`，正确的导入路径应为 `import glm.detail.{ add, sub, ... }` 或 `import glm.detail.*`
- **所在位置**：§4.3，第 617 行
- **严重程度**：中等（对应判定框架"一般"）
- **改进建议**：将 `import glm.detail.scalar_vec_ops.*` 改为 `import glm.detail.{ add, sub, mul, div, mod }` 或 `import glm.detail.*`；全文搜索确认无其他残余的 `glm.detail.scalar_vec_ops` 路径引用

---

- **问题描述**：测试目录结构 `tests/glm.detail/` 与仓颉包路径解析规则不匹配（应用 `tests/glm/detail/`）；目录树形图缺少 `tests/glm/` 条目
- **所在位置**：§2 初始目录树形图（第 139 行）、§12.3 测试包组织结构（第 1714-1716 行）
- **严重程度**：中等（对应判定框架"一般"）
- **改进建议**：将 `tests/glm.detail/` 改为 `tests/glm/detail/`；在目录树形图中补充 `tests/glm/` 目录条目

---

- **问题描述**：§1 出现重复的 `### 设计目标` 标题，造成结构歧义
- **所在位置**：§1 概述，第 9-15 行
- **严重程度**：一般
- **改进建议**：将第一个标题改为 `### 范围可追溯性承诺`，或删除第一个标题并将段落移入第二个"设计目标"段落后或 §8

---

- **问题描述**：§3.5/§3.6 残留 `const if` 术语，未按 D34 设计决策统一改写
- **所在位置**：§3.5 第 272 行、§3.6 第 339 行
- **严重程度**：一般
- **改进建议**：将 `const if` 改写为"`const` 函数中的 `if` 表达式"或"编译期 `if` 分支"，与 D34 约定一致

---

- **问题描述**：§2 模块依赖表 `scalar_vec_ops` 声明"无包内其他依赖"，但其函数签名引用 `VecN<T, Q>` 类型，依赖描述不完整
- **所在位置**：§2 模块间依赖图，第 92 行
- **严重程度**：一般
- **改进建议**：将依赖修改为 `scalar_vec_ops → qualifier + type_vec1~4`，或补充说明同包内函数签名引用 Vec 类型但不构成独立编译依赖
