# 再审议判定报告（v29）

## 判定结果

RETRY

## 判定理由

1. 组件B质询报告判定为 **LOCATED**，即诊断报告中的问题已被确认有效。实际轮次（3）< 最大轮次（12），提前终止且审查结论被确认。
2. 诊断报告包含中等（等效于严重/一般等级）和一般等级的问题，不符合 PASS 的任一条件：
   - 发现1（const 真空缓解方案逻辑矛盾）属**逻辑矛盾**，根据判定标准应归为**严重**等级
   - 发现2（版本序列跳变）属**一般**等级
   - 发现3、4、5、8、10、11 均为**一般**等级
   - 发现9（接口定义充分性缺口）属**一般**等级
3. 根据判定标准，审查报告包含严重或一般等级的问题，应判定为 RETRY。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：const 真空缓解方案①（将 equalEpsilon 移至 struct 体）与 D29 回退触发条件存在逻辑矛盾——方案①不改变函数体内容，在 D29 已验证失败的条件下必然同样编译失败；§11.14 成本评估（0.5 人天）基于不可行前提
- **所在位置**：§4.5（第 881-884 行）、§11.14（第 2174-2204 行）、§7 D29（第 1257-1271 行）
- **严重程度**：严重
- **改进建议**：修正方案①为"理论路径"并标注前提条件；§11.14 可行性评级降级并标注前提条件依赖；补充第三条缓解方案（const 上下文使用精确比较）

- **问题描述**：修订说明版本序列存在非连续跳变（v29→v27 缺少 v28），影响文档可追溯性和版本审计能力
- **所在位置**：§附录 修订说明（第 2310-2395 行）
- **严重程度**：一般
- **改进建议**：确认 a_v28_copy_from_v27.md 中是否存在修订说明，将其移至 v29 附录正确位置；若 v28 为确认性审查则新增"版本 v28"条目

- **问题描述**：protected 回退方案声称"仅允许同包和子包访问"但仓颉规范中 protected 可见域为当前模块，跨模块可用性未在目标编译器上验证
- **所在位置**：§12.3（第 2297 行）
- **严重程度**：一般
- **改进建议**：标注前提条件（需验证测试包与生产包是否属同一模块）；调整回退层级顺序

- **问题描述**：cjpm.toml 骨架缺少 test-dir 或 [test] 相关配置，14 个测试文件的自动发现机制未说明，@Test 在 static 项目中的可用性未作为独立依赖项验证
- **所在位置**：§2（第 106-157 行）
- **严重程度**：一般
- **改进建议**：补充完整配置示例（含测试目录设置）；在 §10 新增测试可用性验证项

- **问题描述**：equalEpsilon 在整数 Vec 上的编译通过性声称（Int32 支持 - 和 abs）缺少对 epsilonOf<T>() 的 Number<Int32> 约束是否满足的显式验证追踪链
- **所在位置**：§4.5（第 871-873 行）、§10（第 1654 行）
- **严重程度**：一般
- **改进建议**：在 §4.5 注释中显式指向 §3.5 双路径策略；在 §10 依赖② 补充验证步骤与 §3.5 的联动关系

- **问题描述**：需求响应充分度补审结论整体良好，但 §8.4 策略 0 缺少验证失败后的具体回退操作步骤
- **所在位置**：§1, §8.2, §8.4
- **严重程度**：一般
- **改进建议**：在 §8.4 策略 0 中补充验证失败场景的具体执行路径（如 SIMD 路径非预期启用时的影响范围分析与依赖调整）

- **问题描述**：接口定义充分性方面，fromBoolVec 的 T 参数无约束但接口层面未说明合法取值范围；256 个别名缺少消费者快速选择指南
- **所在位置**：§2, §3.8, §4.8
- **严重程度**：一般
- **改进建议**：在 §4.8 补充 T 参数类型范围说明文本；在 §3.8 新增 2-3 行快速选择示例

- **问题描述**：异常场景与边界条件系统性评估发现两个缺口：A) mod 的 NaN/Infinity 边界未在 §9.3 显式声明；B) 有符号整数 @OverflowWrapping 与 C++ UB 的迁移注意事项缺少说明
- **所在位置**：§5.2, §9, §12.1
- **严重程度**：一般
- **改进建议**：在 §9.3 新增 mod 运算的 NaN/Infinity 说明行；在 §5.1 或 §11 补充有符号整数 @OverflowWrapping 迁移注意事项

- **问题描述**：编码直接指导性方面，阶段 5 粒度较粗（Vec1~Vec4 合并）、运算符签名使用自然语言描述缺少完整语法模板、equalEpsilon 跨 5 章节依赖缺少集中参考框、§10 依赖④ 缺少对 §12.2 对应验收项的引用
- **所在位置**：§4.3, §4.5, §8.3, §10
- **严重程度**：一般
- **改进建议**：拆分阶段 5 为 5a/5b；补充完整运算符语法模板；添加 equalEpsilon 集中参考框；补充 §10→§12 交叉引用
