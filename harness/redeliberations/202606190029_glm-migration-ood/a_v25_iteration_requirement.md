根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

### 严重 — C1：脚本模板命名错误，直接影响 48/256 个别名
- **位置**：§3.8 第 507 行，`gen_fwd_aliases.py` 最小脚本模板
- **问题**：别名名称生成格式 `f'{prec}{fam}Vec{dim}'` 在所有家族名后硬编码追加 "Vec"，但 `Vec`、`DVec`、`FVec` 三个家族的键名已包含 "Vec"，导致生成 `VecVec{dim}`/`DVecVec{dim}`/`FVecVec{dim}` 等错误名称（共 48/256 个别名错误，占 18.75%）。与 §3.8 命名约定表和 §11.7 映射表中的正确名称直接矛盾。
- **改进建议**：修复为条件格式——键名包含 "Vec" 的家族使用 `f'{prec}{fam}{dim}'`，其他保持 `f'{prec}{fam}Vec{dim}'`；同步更新 §3.8 预期示例；增加验证警告。

### 中等 — A1：迁移成本合计缺失
- **位置**：§11 迁移成本评估
- **问题**：§11 包含 11 个子节给出独立工作量评估，但缺少汇总合计，粒度不一，包含关系仅以注释标注。
- **改进建议**：在 §11 末尾新增 "§11.12 合计评估" 子节，按乐观/最可能/悲观三档汇总。

### 中等 — B2：const/extend 不对称的消费者体验影响未充分评估
- **位置**：§3.2 Vec 结构体系、§4.3 算术运算、§4.5 equalExact/equalEpsilon、§7 D32/D33
- **问题**：仓颉 extend 块不支持 const，11 个日常操作在 const 上下文中不可用（`v.add(s)`、`v.sub(s)`、`v.mul(s)`、`v.div(s)`、`v.mod(s)`、`v.equalExact(other)`、`v.logicalAnd(other)`、`v.logicalOr(other)`、`v.increment()`、`v.decrement()`、`v.bitwiseNot()`），易误导迁移开发者。
- **改进建议**：在 §3.2 末尾新增 "const 上下文中的 API 可用性速查表"；在 §11 中补充相关审查工作量。

### 一般 — A2：参考实现行为声明的可验证性不足
- **位置**：§7 D6、§8.4 参考实现使用策略
- **问题**：多处声明 C++ GLM 参考实现的特定行为（如 D6 中 `GLM_CONFIG_CLIP_CONTROL` 默认值推导），但未给出参考实现具体文件行号。
- **改进建议**：在 §8.4 策略 0 前置验证项中增加对应参考实现文件路径和行号范围。

### 一般 — A3：跨文件依赖未显式声明
- **位置**：§附录（第 2059 行）— `a_v18_revision_history.md` 引用
- **问题**：修订说明附录引用外部文件，但未声明跨文件依赖关系，单独获取设计文档将无法访问修订历史。
- **改进建议**：在 §附录首段增加跨文件依赖声明。

### 一般 — B3：编译器行为依赖的聚合风险高于单点评估之和
- **位置**：§10 验证依赖点 A~E、§7 D29+D30 组合回退分析、§2.1 编码前验证检查清单
- **问题**：设计文档依赖至少 9 项独立编译器行为假设，未做系统性多回退组合分析，多回退方案的编码实施工作量叠加成本未在 §11 评估。
- **改进建议**：在 §2.1 中新增 "编译器依赖统一验证测试"（原型程序覆盖所有依赖点）；在 §11 末尾新增 "编译器依赖验证回退成本" 子节。

### 提示 — A4：导航开销未纳入迁移成本评估（未被采纳）
- **位置**：全文跨章节引用网络
- **改进建议**：在 §2 或 §8 末尾新增 "实现速查索引表"。

### 提示 — A5：范围追溯链可加显式说明（非缺陷）
- **位置**：§1 范围可追溯性承诺、§8.2 对照表
- **改进建议**：在 §1 新增一行说明 "用户需求中的'首轮'对应 roadmap §3E + §3G 范围确认结论"。

### 提示 — B4：首轮 C++ 代码裁剪的完整性验证策略不够明确
- **位置**：§8.2 范围可追溯性对照表、§8.4 参考实现使用策略
- **改进建议**：在 §8.4 策略 0 前置验证项中新增：抽查 3-5 处被排除的 `#define`/`#if` 块，确认首轮其他文件中无间接引用。

### 提示 — C2：VecN<Bool, Q> 的模运算不可用性缺少集中声明
- **位置**：§3.2 第 246-252 行、§4.3 第 718 行、§4.5 第 780 行
- **问题**：Bool Vec 的模运算不可用性分散在三处，消费者需从不同规则推断。
- **改进建议**：在 §3.2 `VecN<Bool, Q>` 行为约定表中新增 "模运算（% / mod）" 行，标注 "所有形式均不可用"。

## 历史迭代回顾

### 已解决的问题（历史反馈中提及但当前反馈不再出现）
- **§4.3 导入路径错误**（v11 #1）— `glm.detail.scalar_vec_ops.*` 残留引用已修正
- **§2 目录树形图错误**（v9 #5、v11 #2）— 重复 `src/glm/` 条目和 `tests/glm.detail/` 路径已修正
- **§1 重复标题**（v11 #3）— 已修复
- **`const if` 术语残留**（v11 #4、v12 #3、v19 #1）— 全文 D34 术语统一已完成
- **修订历史附录混乱**（v13 #1、v15 #3）— 已整理
- **`type` 别名缺少 `public` 修饰符**（v23 #1/#2）— 已在 §3.8 脚本模板和示例中统一为 `public type`
- **§8.2 缺少 §3G 映射**（v23 #3）— 已新增 §3G 追溯段落
- **`@OverflowWrapping` 标注未覆盖 scalar-op-vec**（v21 #1）— 已补充
- **§8.2 缺少 `scalar_vec_ops.cj` 映射行**（v21 #2）— 已补充
- **equalEpsilon 与 epsilonOf 联动修改**（v17 #1、v20 #1）— 已覆盖
- **`componentAt` 纳入验证和验收**（v13 #3、v14 #1）— 已完成
- **`@Derive[Hashable]` 约束传递性验证**（v14 #3）— 已验证
- **`const init` 缺少 `public` 可见性**（v12 #1）— 已在 §4.1 中补充

### 持续存在的问题（多轮反馈中反复出现，需重点解决）
- **§11 迁移成本评估完整性**：从 v3 到 v24 持续有补充需求（别名重命名、跨 Q 模式、equalExact 迁移、scalar-left 运算、const/extend 消费者影响工作量、编译器依赖回退成本等），当前 A1 指出仍缺少汇总合计。需在本次迭代中完成合计评估，从根本上解决反复补充问题。
- **参考实现引用精度**：v9 #2（D6 描述错误）、v10 #4（参考实现路径验证）、v24 A2（引用行号缺失）——参考实现引用精度问题历经多轮仍未彻底解决，建议在 §8.4 中建立强制性的"引用必须带行号"约定。
- **编译器行为依赖管理**：v5 #4（D29/D30 组合回退）、v6 #3（const 兼容性）、v7 #6（const if 抑制）、v24 B3（聚合风险）——编译器依赖问题从 v5 至今一直以不同形式出现，本次需系统性解决。

### 新发现的问题（本轮首次识别）
- **C1（严重）— 脚本模板命名错误**：`gen_fwd_aliases.py` 中 3 个家族名包含 "Vec" 导致 48/256 个别名错误。虽然 v22 提出了需要生成脚手架，v23 提出了脚本验证，但具体命名逻辑 BUG 是 v24 首次识别。
- **C2（提示）— Bool Vec 模运算不可用性缺少集中声明**：分散在三处描述需集中。
- **A3（一般）— 跨文件依赖未显式声明**：附录引用外部修订历史文件未声明依赖。

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606190029_glm-migration-ood\a_v24_design_v2.md

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606190029_glm-migration-ood\requirement.md
