# 再审议判定报告（v8）

## 判定结果

RETRY

## 判定理由

组件B诊断报告确认1个严重问题（P1—扩展成员函数缺少@OverflowWrapping标注策略）和3个一般等级问题（P2—Vec1构造函数接口不对称性未加说明、P3—countof范围跟踪遗漏、P4—跨类型构造函数T2有效性范围未集中归档）。质询报告结论为LOCATED，确认审查质量合格、证据充分，实际轮次（1）未达最大轮次（12），提前终止。由于报告中包含严重和一般等级问题，符合RETRY条件。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：扩展成员函数 add/sub/mul/div/mod（vec-op-scalar 方向）缺少 @OverflowWrapping 标注策略，导致整数分量 Vec 在溢出时抛异常而非 wrapping，与二元运算符行为不一致
- **所在位置**：§4.3 具名函数定义段落（第 605-621 行）、§4.6 @OverflowWrapping 标注分布表（第 693-704 行）
- **严重程度**：严重
- **改进建议**：选择以下之一并在文档中明确：A. 在 §4.6 标注分布表中将20个扩展成员函数纳入 @OverflowWrapping 范围；B. 在 §4.3 中明确实现模式——委托给已标注 @OverflowWrapping 的二元运算符

- **问题描述**：Vec1 构造函数接口的不对称性缺少说明——Vec1 因 `const init(x: T)` 与 `init(scalar: T)` 参数列表相同而无法共存，但文档未解释此限制
- **所在位置**：§4.1 Vec1 构造函数清单（第 466-477 行）、§4.1 Vec2 构造函数清单（第 479-497 行）、§7 D11（第 856-860 行）
- **严重程度**：一般
- **改进建议**：在 §4.1 注释或 §7 D11 中补充 Vec1 构造接口不对称性的原因说明

- **问题描述**：`countof` 辅助函数的范围跟踪遗漏——roadmap 要求项在 §8.2 范围追溯表中未标注偏离
- **所在位置**：§8.2 范围可追溯性对照表第 1 行（第 1105 行）、§3.6 shim_cstddef.cj 描述（第 347 行）
- **严重程度**：一般
- **改进建议**：在 §8.2 序号 1 的偏离说明中补充 countof 的排除理由及替代方案

- **问题描述**：跨类型构造函数 `init<T2, Q2>(v: VecN<T2, Q2>)` 的 T2 有效性范围未集中归档，实施者需交叉查阅 §4.1 和 §9.4 才能了解各 T/T2 组合行为
- **所在位置**：§4.1 跨类型构造函数签名（第 472、486、506、536 行等）、§9.4 类型转换边界（第 1196 行）
- **严重程度**：一般
- **改进建议**：在 §4.1 跨类型构造函数描述末尾添加 T2 约束矩阵，明确各 T/T2 组合的编译时行为
