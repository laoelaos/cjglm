# 再审议判定报告（v10）

## 判定结果

RETRY

## 判定理由

组件B质询报告结果为 LOCATED（实际轮次 2 < 最大轮次 12，提前终止确认），诊断报告中 4 个问题经质询修订后全部被确认有效。其中 P1（中等）、P2（中等）、P3（中等）属于影响产出完整度的非致命问题，P4（一般）明确属于"一般"等级。根据判定标准，审查报告包含一般等级的问题，因此判定为 RETRY。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：`const if` 为文档简写而非语法关键字，`if` 在 `const` 函数中的分支抑制行为未经验证
- **所在位置**：§3.5 ComputeEqual.callConst、§3.6 isIec559Of 实现、§4.3 mod 双路径、§10 const if 抑制验证
- **严重程度**：中等
- **改进建议**：将 `const if (...)` 改写为 `if (...)`；在 §7 新增 D34 记录此依赖的验证状态；§10 原型测试中的代码与实际可编译代码保持一致

- **问题描述**：`public import glm.detail.{ add, sub, mul, div, mod }` 存在潜在的命名冲突——`glm.detail` 包的其他文件未来也可能导出同名函数
- **所在位置**：§2 lib.cj、§4.3 具名函数定义位置
- **严重程度**：中等
- **改进建议**：补充命名空间占用声明或在 §7 新增设计决策约定 `scalar_vec_ops.cj` 对 `add`/`sub`/`mul`/`div`/`mod` 名称的独占权

- **问题描述**：§11 迁移成本评估缺少 `equalExact` 迁移模式的系统性覆盖
- **所在位置**：§11（全文）
- **严重程度**：中等
- **改进建议**：在 §11 中新增子节"§11.9 `equalExact` 迁移模式"，覆盖 C++ GLM `==` 精确语义与仓颉容差 `==` 的行为差异、替换场景、搜索规则和工作量评估

- **问题描述**：对 `references/glm-1.0.3/` 的引用基于假设其存在，但未验证关键路径的实现准确性
- **所在位置**：§7 D6、§8.4
- **严重程度**：一般
- **改进建议**：在 §1 或 §8.4 补充前置验证步骤确认条件编译逻辑一致；增加兜底条款，若参考实现分支逻辑与描述不符则以参考实现为准更新 D6
