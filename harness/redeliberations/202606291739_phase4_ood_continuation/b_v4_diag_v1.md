# 质量审查报告 — 阶段四 OOD 设计文档（v4_copy_from_v3）

**审查范围**：需求响应充分度、事实错误与逻辑矛盾、深度与完整性（侧重内部审议未充分覆盖的维度）
**审查日期**：2026-06-29
**审查对象**：`a_v4_copy_from_v3.md`（第 4 轮迭代产出）

---

## 总体评价

产出整体质量较高。经过 15+ 轮内部审议迭代，事实错误和逻辑矛盾已基本清除，设计方案在架构视图、模块划分、核心类型与接口设计三个维度已达到可直接指导编码实现的深度。以下问题集中在内部审议未充分覆盖的维度——需求响应充分度的结构性缺陷、完整性的边缘遗漏，以及数据流/控制流分析的深度不足。

---

## 发现的问题

### P1（严重）— lib.cj 最终编译验证未纳入实施批次规划

- **所在位置**：§8 实施批次规划（第 998~1043 行）、§8 lib.cj 更新（第 1043~1116 行）
- **问题描述**：
  lib.cj 更新是本阶段最复杂的单文件变更——涉及修改现有第 23 行（删除 7 个符号的 gtc import）、追加约 50 行 public import（覆盖 core/ext/gtc 三层），包含跨包同名函数导入（`mix` 同时从 detail 和 ext 导入、`exp/log/pow/sqrt` 同时从 detail 和 ext 导入、`translate/rotate/scale/shear/lookAt/lookAtRH/lookAtLH` 从 ext 导入并与 gtc 原有导入冲突）。历史迭代第 8 轮 P3 明确要求"编写最小测试用例验证 CangJie 跨 public import 同名函数重载解析行为"，第 10 轮 P1 发现 noise 函数名引用错误（`{perlin, simplex}` 不存在），第 10 轮 P2 发现 packing 导出不全。

  然而 **§8 实施批次规划的 lib.cj 条目仅描述了修改内容，未包含任何编译验证步骤**。所有批次的"测试"列均针对单个模块的功能正确性，缺少对 lib.cj 最终的集成编译验证。此遗漏意味着编码团队完成全部 4 批次编码后，可能因 lib.cj 导入配置错误（重载决议歧义、符号重复、函数名拼写错误等）导致编译失败，而此时所有模块的单元测试已通过，排查成本极高。

- **严重程度**：严重
- **改进建议**：在 §8「lib.cj 更新」末尾新增验证步骤：
  1. **编译验证**：`cjpm build` 确认 lib.cj 全部 public import 可编译通过，无符号重复或歧义错误
  2. **重载决议验证**：为关键跨包同名函数编写最小调用用例——`mix(vec, vec, float)` 确认匹配 detail 版本、`mix(quat, quat, float)` 确认匹配 ext 版本；`exp(vec)` 确认匹配 detail 版本而非 ext 的 stub 版本；`translate(mat, vec)` / `perspective(args)` 确认 gtc 与 ext 统一入口处无歧义
  3. **归入「所有批次完成后」条目**，与 lib.cj 修改同级

---

### P2（一般）— ext/matrix_projection.cj unProject 系族隐式矩阵求逆的奇异场景未覆盖

- **所在位置**：§3.2 ext/matrix_projection.cj（第 528~544 行）、§5 错误处理策略表（第 937~948 行）
- **问题描述**：
  unProject 系族的实现路径描述为"视口逆变换 → 逆投影"（第 544 行），内部需计算 `inverse(proj * model)`。当 proj×model 为奇异矩阵（如投影矩阵为零矩阵或模型矩阵包含非可逆变换）时，此隐式求逆将产生 NaN/Inf 填充结果。但 §5 错误处理策略表的「奇异矩阵求逆」条目仅覆盖了 `matrix.cj` 的显式 `inverse()` 函数，未提及 unProject 内部隐式求逆的场景。

  此遗漏的影响：编码团队可能仅在 `matrix.cj.inverse()` 中处理奇异行为，而忽略 unProject 内部的求逆路径，导致 unProject 在奇异配置下的行为不确定。

- **严重程度**：一般
- **改进建议**：
  - 方案 A：在 §5 错误处理策略表中新增一行，说明 unProject 系族在 proj×model 为奇异矩阵时的行为与 matrix.cj inverse 一致（IEEE 754 自然传播 NaN/Inf，不抛异常），并注明此场景不重复设计决策
  - 方案 B：在 §3.2 ext/matrix_projection.cj 实现路径末尾补充脚注，引用 §5 的奇异矩阵行为描述

---

### P3（一般）— §4 关键数据流与控制流章节深度不足

- **所在位置**：§4 关键行为契约（第 842~931 行）
- **问题描述**：
  需求要求输出包含"关键数据流与控制流"的 OOD 文档。当前 §4 的 6 个核心场景以伪代码格式简要描述了输入→处理→输出流程，但存在以下不足：

  1. **缺乏跨模块调用链追踪**：slerp 的调用链涉及 `common.cj(clamp)` → `trigonometric.cj(acos/sin)` → `geometric.cj`（间接），但 §4.5 仅描述了 slerp 自身的公式，未展示数据在模块间的变换路径和每步的类型/约束转换
  2. **缺乏控制流分支分析**：normalize 在 Vec1 与 Vec2~Vec4 的分支路径差异、refract 的 k<0 全反射分支、slerp 的 sinOmega 退化分支等，在 §3 各模块职责中独立描述，但 §4 未在同一视图中集中呈现关键决策点
  3. **缺乏异常传播路径**：当奇异矩阵进入 inverse 或 acos 越界输入时，NaN/Inf 如何沿调用链传播至最终结果——此信息分散在 §3 和 §5，§4 未建立从"异常发生点"到"最终结果"的追踪路径

  相比之下，§3 各模块设计描述非常详尽（签名级别），§4 更像是 §3 的摘要重复而非独立的数据流/控制流分析。编码团队拿到设计文档后，需要自行反向工程才能理解跨模块协作的全貌。

- **严重程度**：一般
- **改进建议**：增强 §4 的深度——至少为以下 3 个关键函数补充完整数据流与控制流分析：
  1. **slerp**：调用链 `quaternion_common.slerp → detail.common.clamp → detail.trigonometric.acos/sin → detail.common(T(1))`，展示每步的类型约束和异常传播路径
  2. **lookAt**：`ext.matrix_transform.lookAt → detail.geometric.normalize/cross → detail.trigonometric(无)`，展示从 eye/center/up Vec3 到 Mat4x4 的完整变换路径
  3. **inverse (Mat4x4)**：展示 cofactor 展开的 4 次递归 + 3x3 子式分解 + 1/det 路径，以及 det=0 时 NaN/Inf 在结果矩阵中的填充传播路径

---

## 需求响应充分度评估

| 需求项 | 响应状态 | 说明 |
|--------|---------|------|
| 充分理解阶段四的范围和边界 | ✅ | §1.5 不覆盖范围 + 交叉验证映射表明确界定了范围边界 |
| 结合已有阶段成果，保证设计连续性 | ✅ | §2 依赖表 + §9 集成方式详细分析了与阶段一二三的衔接 |
| 充分考虑仓颉特性与限制 | ✅ | §1.4 表示约定 + H4~H6 确定性声明 + D27/D31 等仓颉限制应对 |
| 架构视图 | ✅ | §1.3 三层包架构 + §2 模块间依赖关系图 |
| 模块划分 | ✅ | §2 完整的包组织 + 文件级职责 |
| 核心类型与接口设计 | ✅ | §3 完整的函数签名 + 约束策略 + 实现路径 |
| **关键数据流与控制流** | ⚠️ **部分** | **P3：§4 深度不足，缺少跨模块调用链和分支分析** |
| 与已有阶段的集成方式 | ✅ | §9 详细分析了反向兼容性 |

## 事实错误与逻辑矛盾

经逐项排查，未发现显著事实错误或逻辑矛盾。此前 15+ 轮内部审议中发现的问题（frexp 指数公式错误、mirrorRepeat 实现公式错误、Simplex 返回类型错误、shear 签名错误等）均已在本版中得到修正，追溯记录在 §修订说明 中。

## 深度与完整性评估

| 维度 | 评估 | 说明 |
|------|------|------|
| 接口层定义粒度 | ✅ | 所有 25+ 个模块的函数签名均已定义，可直接指导编码 |
| 约束策略明确性 | ✅ | 每个函数族的泛型约束已明确标注 |
| 实现路径可操作性 | ✅ | 关键函数（modf/frexp/ldexp/ulp 等）均给出了仓颉中的具体实现公式 |
| 边界/异常场景覆盖 | ⚠️ 基本覆盖 | **P2：unProject 隐式求逆场景遗漏；其余场景已有覆盖** |
| 实施计划可执行性 | ✅ | 4 批次的拓扑排序 + 每文件测试项，颗粒度合理 |
| 跨模块集成验证 | ⚠️ 部分 | **P1：lib.cj 最终编译验证缺失；各模块独立测试但缺少集成级验证** |
| 关键数据流与控制流 | ⚠️ 不足 | **P3：§4 深度不足，需补充跨模块追踪与分支分析** |

---

## 总结

该产出在经过 15+ 轮内部审议后，在事实正确性、模块划分粒度、接口设计深度方面已达到较高水准。存在 1 个严重问题（lib.cj 编译验证缺失）和 2 个一般问题（unProject 奇异场景遗漏、§4 数据流深度不足），修复后可直接交付编码阶段。
