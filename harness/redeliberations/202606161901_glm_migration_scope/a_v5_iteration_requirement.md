根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

### 问题 1（严重）：定量依赖统计数据与当前代码库不一致
第 2.2 节和第 4.2 节中列出的"被引用文件数"与 GLM 1.0.3 源码的实际 `#include` 计数不一致。所有类型均被高估，差异明显且存在系统性模式：
- `vec<4,T,Q>`：声称 19，实际 18（+1）
- `vec<3,T,Q>`：声称 20，实际 19（+1）
- `vec<2,T,Q>`：声称 18，实际 17（+1）
- `vec<1,T,Q>`：声称 12，实际 11（+1）
- `mat<4,4,T,Q>`：声称 10，实际 8（+2）
- `mat<3,3,T,Q>`：声称 8，实际 7（+1）
- `type_quat`：声称 7，实际 4（+3）

建议：在实际 GLM 1.0.3 源码上重新运行统计并修正所有数值。

### 问题 2（中等）：源文件总数描述与实际不符
第 2.2 节末尾声称"`glm/` 目录下源文件总数约 284 个（不含测试和示例）"，但实际统计：`.hpp` 280 个，`.inl` 140 个，`.cpp` 1 个，合计 421 个。若只计 `.hpp`，280 也与 284 不符。

建议：用实际统计值替换，或明确标注所统计的文件类型范围和精确计数。

### 问题 3（中等）：`type_quat.hpp` 依赖描述不完整
第 1 节层次 3 表格将 `type_quat.hpp` 的依赖仅列为 4 个类型头文件（`type_mat3x3.hpp`、`type_mat4x4.hpp`、`type_vec3.hpp`、`type_vec4.hpp`），但实际 `type_quat.hpp` 中还包括以下 `#include`：
```cpp
#include "../ext/vector_relational.hpp"
#include "../ext/quaternion_relational.hpp"
#include "../gtc/constants.hpp"
#include "../gtc/matrix_transform.hpp"
```

建议：在依赖表中增补 ext/gtc 依赖，或添加说明区分"类型定义依赖"与"完整文件编译依赖"。类似地，矩阵类型的 `.inl` 实现中引入了 `../matrix.hpp`、`../common.hpp`、`../geometric.hpp` 等函数库头文件，应对此作提示。

### 问题 4（一般）：分层迁移建议不够充分
需求明确要求"给出分层的迁移建议（哪些必须第一批，哪些可以后续迁移）"。产出的第 3 节和第 5 节对首轮迁移的描述非常详细，但对后续轮次的规划仅以"首轮不包含的内容"一表概述，使用"可后续逐步迁移""可最后迁移"等模糊表述，没有给出具体分层方案。

建议：增补第 2-3 轮的具体迁移范围建议和依赖前提，例如第 2 轮：`ext/` 下的向量/矩阵别名文件 + 矩阵类型；第 3 轮：四元数 + 附带的 ext/gtc 依赖；第 4 轮：函数库迁移。

### 问题 5（轻微）：`type_mat4x4` 统计偏差超出 +1 模式
向量类型偏差均为恰好 +1（将文件自身计入），但 `type_mat4x4.hpp` 偏差为 +2（10 声称 vs 8 实际），`type_quat.hpp` 偏差为 +3（7 声称 vs 4 实际），说明统计不一致不仅是"计入自身"的统一问题。

建议：同问题 1，在实际源码上重新生成所有统计数据并统一统计口径。

## 历史迭代回顾

### 已解决的问题
- **文档标题版本号问题**（第 3 轮历史问题 1）：v2→v4 版本号不对应——已在 v4 轮次中将标题从"(v2)" 修正为 "(v4)"，与当前版本一致。
- **`fwd.hpp` 迁移范围界定不清**（第 3 轮历史问题 2）：已在 v4 轮次中明确首轮仅迁移 `fwd.hpp` 第 1-178 行标量部分，向量别名第 180-507 行需依赖 `vec<N,T,Q>` 完整定义。
- **`.inl` 文件额外依赖描述范围不完整**（第 3 轮历史问题 4）：已在 v4 轮次中将 `.inl` 依赖说明扩展至所有向量类型。
- **子步骤 2 依赖时序不清晰**（第 3 轮历史问题 6）：已在 v4 轮次中将子步骤 2 拆分为 2a（标量类型别名）和 2b（`vec<1,T,Q>` + 向量类型别名）。

### 持续存在的问题
- **定量依赖统计数据准确性**（第 4 轮问题 1）：v4 轮次新增了统计方法描述（grep 命令、直接引用口径、文件总数约 284 个），但数据本身与实际 GLM 1.0.3 源码不一致，所有类型的被引用数仍被高估。**本轮需重点解决。**
- **定量依赖统计方法可复现性**（第 3 轮问题 3 遗留）：统计方法已补充，但声明的"约 284 个"文件总数与事实不符，315 行 → 第 2.2 节末尾。**本轮需修正文件总数。**
- **`type_quat.hpp` 依赖描述不完整**（第 4 轮问题 3）：仍仅列出 4 个类型头文件，未补充 ext/gtc 依赖说明或分类标注。**本轮需修正。**
- **分层迁移建议粒度不足**（第 4 轮问题 4）：仍以"首轮不包含的内容"一表概述，缺乏第 2-3 轮的具体范围建议。**本轮需补充。**
- **源文件总数描述不准确**（第 4 轮问题 2）：仍保留"约 284 个"的表述。**本轮需修正。**

### 新发现的问题
- **`type_mat4x4` 和 `type_quat` 的统计偏差超出 +1 的系统性偏差模式**（问题 5）：表明统计方法执行存在额外的方法偏差，不仅是将自身计入的问题。**本轮可与问题 1 合并修正。**
- **矩阵 `.inl` 实现的函数库头文件依赖未被提及**（问题 3 附属建议）：`type_mat*.inl` 引入了 `../matrix.hpp`、`../common.hpp`、`../geometric.hpp` 等函数库头文件。**本轮可添加提示。**

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606161901_glm_migration_scope\a_v4_output_v1.md

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606161901_glm_migration_scope\requirement.md
