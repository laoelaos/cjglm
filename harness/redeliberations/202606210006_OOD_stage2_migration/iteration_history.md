## 迭代第 1 轮

1. **问题描述**：行向量×矩阵乘法签名错误。设计方案签名 `Vec{C}<T,Q> * Mat{C}x{R}<T,Q> → Vec{R}<T,Q>` 与 GLM 参考实现不符，正确签名应为 `Vec{R}<T,Q> * Mat{C}x{R}<T,Q> → Vec{C}<T,Q>`。
   - 所在位置：§3.5「二元运算符（矩阵-向量）」
   - 严重程度：严重
   - 改进建议：修正为 `Vec{R}<T,Q> * Mat{C}x{R}<T,Q> → Vec{C}<T,Q>`，并在 §2 矩阵类型概览表中补充示例说明。
   - **修改结果**：已采纳。修正签名为 `Vec{R} * Mat{C}x{R} → Vec{C}`，并在 §2 概览表中新增"关键行为验证示例"段落（含 Vec3 * Mat2x3 → Vec2 等核验案例）。来源：修订说明（v5）。
2. **问题描述**：跨尺寸矩阵乘法运算符未完整定义，设计方案仅给出一个跨类型示例，缺少完整的组合清单或系统性匹配规则。
   - 所在位置：§3.5「二元运算符（矩阵-矩阵，同尺寸）」— 矩阵乘法子节
   - 严重程度：一般
   - 改进建议：补充跨尺寸矩阵乘法完整的组合规则说明，按"左矩阵列数 = 右矩阵行数则形态兼容，结果尺寸为 (左矩阵行数 × 右矩阵列数)"的规则对所有 9 个矩阵类型列出其乘法伙伴集。
   - **修改结果**：已采纳。§3.5 新增完整 9×9 矩阵乘法兼容性表格（81 个组合，21 种有效），并明确规则"左矩阵列数 = 右矩阵行数则兼容，结果尺寸为 (左矩阵行数 × 右矩阵列数)"。来源：修订说明（v5）。
3. **问题描述**：跨矩阵类型转换构造的填充规则描述过于模糊（"额外值填默认值（如 0 或 1）"），未给出单位矩阵扩展的精确语义。
   - 所在位置：§3.3「构造函数体系」第 8 条（跨矩阵类型转换构造）
   - 严重程度：一般
   - 改进建议：补充明确填充规则：缺失的列/行补齐为单位矩阵的相应列/行（即缺失元素中 `(i,i)` 填 `T(1)`，其余填 `T(0)`）。
   - **修改结果**：已采纳。§3.3 第 8 条填充规则重写为"源矩阵的元素复制到目标矩阵的对应位置；目标矩阵中源矩阵未覆盖的行列从单位矩阵的对应行/列取值（即缺失元素中，(i,i) 填 T(1)，其余填 T(0)）"，并附 Mat2x2 → Mat4x4 具体示例。来源：修订说明（v5）。
4. **问题描述**：ext/ 别名文件数量描述前后矛盾，§2 模块清单列 18 个文件，§3.8 称"16 个矩阵别名文件"。
   - 所在位置：§2 模块清单（第 64-73 行）vs §3.8（第 287-289 行）
   - 严重程度：一般
   - 改进建议：统一为 18 个文件（9 尺寸 × 2 精度），删除"精度不重复的若干组合"的模糊描述。
   - **修改结果**：已采纳。§3.8 统一为"其他 16 个矩阵别名文件同理（9 种尺寸 × 2 种精度），共 18 个文件"，删除"精度不重复的若干组合"的模糊描述。全局文件计数统一为 18。来源：修订说明（v5）。
5. **问题描述**：矩阵除法 `/` 的 stub 依赖表述不准确，未区分 `mat / mat`（依赖 inverse）与 `mat / scalar`（不依赖 inverse），且 `this` 的成员函数风格在仓颉 `extend` 块中不准确。
   - 所在位置：§3.5「二元运算符（矩阵-矩阵，同尺寸）」— 除法子节
   - 严重程度：一般
   - 改进建议：明确区分 `mat / scalar`（本阶段可实现，逐元素操作）和 `mat / mat`（依赖 inverse，推迟至阶段四），将 `this * inverse(rhs)` 改为 `mat *= inverse(rhs)`。
   - **修改结果**：已采纳。§3.5 和 §5 明确区分 `mat / scalar`（本阶段可完整实现，逐元素操作）和 `mat / mat`（依赖 inverse，推迟至阶段四）。将 `this * inverse(rhs)` 改为 `mat *= inverse(rhs)` 以匹配 GLM 实际语义。§9 差异声明同步更新。来源：修订说明（v5）。
6. **问题描述**：标量-矩阵除法的 stub 策略未在 §5 中说明，仅提及矩阵除法依赖 inverse 为 stub，可能误导开发者将所有除法形式标记为 stub。
   - 所在位置：§5「错误处理策略」
   - 严重程度：轻微
   - 改进建议：在 §5 中明确注明仅 `mat / mat` 受 stub 影响，`mat / scalar`、`scalar / mat` 可本阶段完整实现。
   - **修改结果**：已采纳。§5 新增"除法 stub 策略"专项说明，明确列出：① `mat / scalar` 和 `scalar / mat` 不依赖 inverse，本阶段可完整实现；② `mat / mat` 依赖 inverse，为 stub。来源：修订说明（v5）。

## 迭代第 2 轮

1. **问题描述**：ext/ 向量别名文件未纳入设计范围，路线图明确将其列为阶段二范围
   - 所在位置：§2「包组织」— package glm.ext 文件清单
   - 严重程度：严重
   - 改进建议：明确阶段二是否包含 ext/ 向量别名文件的创建；若包含补充对应向量别名文件，若不包含说明理由并在 §9 偏差声明中记录
   - **修改结果**：已采纳。新增 8 个向量别名文件（在 §3.8 ext/ 别名文件清单中增补）。来源：修订说明（v6）。
2. **问题描述**：D08 等效替代建议数学错误——`+= identity` ≠ `++m`，两者数学语义完全不同
   - 所在位置：§7 D08（决策表第 8 行）
   - 严重程度：严重
   - 改进建议：修正为"可通过 `+= T(1)`（逐元素加 1）等价替代"或标注行为差异
   - **修改结果**：已采纳。修正为 `m += T(1)`（逐元素加 1）等价替代，标注行为与 C++ GLM 的 `++m` 语义等价。来源：修订说明（v6）。
3. **问题描述**：跨矩阵类型转换构造实际需 72 个独立构造函数重载，设计未评估实现量级可行性和缩减方案
   - 所在位置：§3.3「构造函数体系」第 8 条
   - 严重程度：严重
   - 改进建议：明确 72 个重载是否可接受，若不能提出缩减方案；为每个转换构造函数明确填充规则边界情况；评估仓颉重载解析歧义问题
   - **修改结果**：已采纳。补充可行性评估段落，明确 72 个重载可接受，每个目标矩阵 extend 块包含 16 个静态函数签名；同时为每个转换构造函数补充填充规则边界。来源：修订说明（v6）。
4. **问题描述**：`componentAt` 返回列向量，与 Vec 版本返回标量分量的语义矛盾
   - 所在位置：§3.4「行列访问」
   - 严重程度：一般
   - 改进建议：改名如 `col(i: Int64)` 避免歧义，或改为返回标量元素 `componentAt(col, row)` 与 Vec 语义对齐
   - **修改结果**：已采纳。`componentAt` 更名为 `col(i: Int64)`，与 Vec 的 `componentAt` 区分。来源：修订说明（v6）。
5. **问题描述**：`row()` 成员函数在本阶段无实际作用，且后续替换为自由函数时产生 API 不兼容
   - 所在位置：§3.4「行访问函数」
   - 严重程度：一般
   - 改进建议：明确决策——要么作为阶段二成员函数实现，要么标注为延期至阶段四随 matrix.cj 提供自由函数版本；更新 §7 决策表
   - **修改结果**：已采纳。明确决策：阶段二不提供 `row()` 成员函数，推迟至阶段四随 matrix.cj 自由函数版本提供，更新 §7 决策表与 §9 差异声明。来源：修订说明（v6）。
6. **问题描述**：跨类型构造函数（不同 T）未明确覆盖跨类型同尺寸矩阵转换
   - 所在位置：§3.3「构造函数体系」
   - 严重程度：一般
   - 改进建议：补充跨类型同尺寸转换构造函数签名，或说明已包含在其他条款的泛化描述中并明确签名
   - **修改结果**：已采纳。新增跨类型同尺寸转换构造函数（在第 7 条统一表述）。来源：修订说明（v6）。
7. **问题描述**：`scalar_mat_ops.cj` 中 36 个全局函数的溢出策略未说明
   - 所在位置：§5「错误处理策略」
   - 严重程度：一般
   - 改进建议：明确 `scalar_mat_ops.cj` 各函数的 `@OverflowWrapping` 标注策略，与阶段一 `scalar_vec_ops.cj` 保持一致
   - **修改结果**：已采纳。明确 `scalar_mat_ops.cj` 各函数（add/sub/mul/div）标注 `@OverflowWrapping`，与阶段一 `scalar_vec_ops.cj` 模式一致。来源：修订说明（v6）。
8. **问题描述**：`mat / vec` 和 `vec / mat` 除法未讨论，它们依赖 inverse 实现
   - 所在位置：§3.5「二元运算符（矩阵-矩阵）」除法子节
   - 严重程度：一般
   - 改进建议：明确 `mat / col_vec` 和 `row_vec / mat` 是否在本阶段支持；若依赖 inverse 也推迟并注明
   - **修改结果**：已采纳。明确 `mat / col_vec` 和 `row_vec / mat` 依赖 inverse，推迟至阶段四；仅方阵（Mat2x2、Mat3x3、Mat4x4）提供这些除法运算符。来源：修订说明（v6）。

## 迭代第 3 轮

1. **问题描述**：§2 关键行为验证示例中跨尺寸矩阵乘法核验注解 `C1=R2=3` 为事实错误，正确值应为 `C1=R2=2`
   - 所在位置：§2「关键行为验证示例」第二行注解
   - 严重程度：一般
   - 改进建议：将 `C1=R2=3` 修正为 `C1=R2=2`
   - **修改结果**：已采纳。修正为 `C1=R2=2`。来源：修订说明（v7）。
2. **问题描述**：§3.1 默认单位矩阵构造的 `const init` 条件性未配套降级方案
   - 所在位置：§3.3 构造函数体系第1条
   - 严重程度：一般
   - 改进建议：补充 `const init` 不可用时的降级方案（如退化为普通 `init` + 编译期不可用于 const 上下文的约束说明）
   - **修改结果**：已采纳。补充降级方案：退化为普通 init + 编译期不可用于 const 上下文的约束说明（后续在 v8 修订中进一步完善为删除 init() 默认构造并新增 identity() 静态工厂函数 + D20）。来源：修订说明（v7）。
3. **问题描述**：§3.4 `col()` 与 `[]` 行为差异化描述不清晰，两者均抛出 Exception 但未说明区别
   - 所在位置：§3.4 行列访问
   - 严重程度：一般
   - 改进建议：明确 `col()` 与 `[]` 的行为差异，若等价则考虑移除 `col()` 或阐明设计意图
   - **修改结果**：已采纳。重写为完全等价，统一行为说明。来源：修订说明（v7）。
4. **问题描述**：`scalar_mat_ops.cj` 在 `glm.detail` 包中定义同名函数 `add/sub/mul/div`，违反阶段一命名约定（仅 `scalar_vec_ops.cj` 可定义这些函数名）
   - 所在位置：§3.5 标量-矩阵运算段落，§8 产出物清单
   - 严重程度：一般
   - 改进建议：在 §7 设计决策中更新命名约定说明，或使用不同函数名（如 `matAdd`/`matSub`/`matMul`/`matDiv`）避免冲突
   - **修改结果**：已采纳。新增 D15 决策更新命名约定说明，保留同名函数 add/sub/mul/div（参数类型不同构成有效重载）。来源：修订说明（v7）。
5. **问题描述**：§8 产出物清单中 `fwd.cj` 矩阵别名命名规范未定义，未说明命名模式、精度变体对应关系等
   - 所在位置：§8 产出物清单 — 修改文件 — `src/fwd.cj`
   - 严重程度：一般
   - 改进建议：明确 `fwd.cj` 中矩阵别名的命名规则，包括精度变体、短别名等，与阶段一向量命名规范保持一致
   - **修改结果**：已采纳。新增 D16 决策，定义 fwd.cj 矩阵别名命名规则（`Mat{C}x{R}` Float32 highp 默认、`DMat{C}x{R}` Float64 highp、`HighpMat{C}x{R}` 等精度变体、方阵短别名 `mat{C}`、`dmat{C}`）。来源：修订说明（v7）。

## 迭代第 4 轮

1. **问题描述**：fwd.cj 缺少 Float32/Float64 以外精度族的矩阵别名（fmat/f32mat/f64mat 系列及 dmat 非方阵全尺寸系列），且非方阵默认精度别名（mat2x3~mat4x3 等）未显式列出，纳排决策缺失
   - 所在位置：§8 修改文件 — fwd.cj 矩阵别名命名规则；§3.8 ext/ 别名文件
   - 严重程度：一般
   - 改进建议：逐一对照 C++ GLM fwd.hpp 列出全部矩阵别名族，明确纳入与排除范围及理由；补充 dmat 非方阵别名的纳入决策
   - **修改结果**：已采纳。补充精度变体命名覆盖方阵与非方阵短别名（`Highpmat4` 等），D16 决策中明确精度变体与短别名对应关系；fmat/f32mat/f64mat 系列及整型别名通过 D19 决策排除（D19 在迭代 9 中进一步补充具体数值佐证：每族 9×3=27 个，3 族共 81 个；加整型 108 个；合计约 189 个冗余别名）。来源：修订说明（v9）+ D19（v11）。
2. **问题描述**：非方阵复合赋值运算符 `*=(mat)` 适用范围不清晰，"由编译器自动生成"措辞可能误导编码者认为所有矩阵类型均获得矩阵乘复合赋值
   - 所在位置：§3.5 复合赋值运算符段落
   - 严重程度：一般
   - 改进建议：明确仅方阵类型（Mat2x2/Mat3x3/Mat4x4）自动获得 `*=(same_mat)`；注明非方阵因返回类型不同无法自动生成；注明方阵 `*=(mat)` 自动生成语义与 C++ GLM 显式定义一致
   - **修改结果**：已采纳。§3.5 复合赋值运算符按类别拆分为 4 类表格；第①类明确说明仅覆盖同 T 同尺寸类型，不覆盖跨类型矩阵复合赋值；§9 差异声明补充"仓颉复合赋值运算符不支持跨类型矩阵参数"条目与"非方阵不提供 *=(mat) 复合赋值运算符"条目。来源：修订说明（v10）。
3. **问题描述**：跨矩阵类型转换构造中"对应位置"语义描述不精确，未给出 (row, col) 坐标对应的精确定义
   - 所在位置：§3.3 构造函数体系第 8 条
   - 严重程度：一般
   - 改进建议：补充坐标对应精确定义：源矩阵 M[j][i] 复制到目标矩阵相同坐标，范围 0 ≤ j < min(C_src, C_dst)、0 ≤ i < min(R_src, R_dst)；额外行列从单位矩阵取值
   - **修改结果**：已采纳。跨矩阵转换填充规则已通过 v5 修订补充单位矩阵扩展语义（"缺失的列/行补齐为单位矩阵的相应列/行"）；v8 修订进一步明确填充规则与 GLM 不一致的特例（编码阶段须逐一对照 GLM .inl 源码实现）。来源：修订说明（v5）+（v8）。
4. **问题描述**：`scalar / mat` 逐元素除法不依赖 inverse、可本阶段实现，但 §5 错误处理策略中遗漏
   - 所在位置：§3.5 标量-矩阵运算；§5 错误处理策略
   - 严重程度：一般
   - 改进建议：在 §3.5 明确 div(s, m) 语义为逐元素 s / each_element；在 §5 补充 scalar / mat 可本阶段完整实现
   - **修改结果**：已采纳。§3.5 明确列出 scalar_mat_ops.cj 的 36 个函数涵盖范围（add/sub/mul/div 各为全部 9 种矩阵类型含 6 种非方阵提供重载）；§5 补充确认 `scalar / mat` 对所有 9 种矩阵类型（含非方阵）均可本阶段完整实现。来源：修订说明（v10）。
5. **问题描述**：`mat / mat` 和 `mat / vec` 除法运算符仅方阵有定义，设计文档"mat / mat"措辞可能误导编码者为所有9种矩阵类型实现该运算符
   - 所在位置：§3.5 二元运算符矩阵-矩阵除法子节
   - 严重程度：一般
   - 改进建议：明确指出 mat/mat 和 mat/vec 仅在方阵类型上定义，非方阵不提供
   - **修改结果**：已采纳。§3.5 复合赋值运算符表第④类明确说明 operator/=(mat/mat) 由编译器基于 operator/(mat/mat) 自动生成并继承 stub 行为；§9 差异声明补充"矩阵-矩阵除法推迟且仅限方阵"条目。来源：修订说明（v10）。
6. **问题描述**：lib.cj 对 scalar_mat_ops.cj 全局函数的导出使用"建议"措辞，编码者无法确定是否应导出
   - 所在位置：§8 修改文件 — lib.cj 条目
   - 严重程度：一般
   - 改进建议：将"建议同步导出"改为明确决策；确认 lib.cj 现有 public import 已自动覆盖同包同名函数
   - **修改结果**：已采纳。§8 lib.cj 条目补充明确说明：mod 仅覆盖 scalar_vec_ops.cj 提供的标量-向量取模函数，scalar_mat_ops.cj 不提供 mod（矩阵取模无数学意义）。来源：修订说明（v10）。
7. **问题描述**：27个跨尺寸矩阵乘法重载的文件归属策略未定义，直接影响编码落地
   - 所在位置：§3.5 跨尺寸乘法兼容性表；§7 D04
   - 严重程度：一般
   - 改进建议：明确跨尺寸乘法运算符定义在左操作数矩阵类型的 extend 块中，在对应 type_mat{C}x{R}.cj 文件顶部 import 所需右操作数类型
   - **修改结果**：已采纳。§3.5 跨尺寸乘法兼容性表中明确：所有跨尺寸乘法要求左右操作数具有相同的 T 和 Q；同包（glm.detail）类型互相直接可见，无需跨文件 import；删除"在对应文件顶部 import 所需右操作数类型"的误导说明。来源：修订说明（v10）+（v9）。
8. **问题描述**：`[]` 运算符取值版本返回列向量副本，不支持分量级链式修改，但 §9 差异声明中未记录此用户可见差异
   - 所在位置：§3.4 行列访问；§9 差异声明
   - 严重程度：一般
   - 改进建议：在 §9 差异声明新增条目：`[]` 取值版本返回副本，仅可整列赋值 `m[0] = new_col`，不能直接修改分量 `m[0].x = value`
   - **修改结果**：已采纳。§9 差异声明新增"[] 运算符取值版本返回列向量副本，不支持分量级链式修改；不提供 componentAt(row, col) 赋值版本"条目；§4.6 补充分量级修改（三步操作）行为契约示例。来源：修订说明（v10）。

## 迭代第 5 轮

1. **问题描述**：`init()` 默认单位矩阵构造函数在无约束泛型 `T` 下依赖 `T(0)/T(1)` 不可行。仓颉无约束泛型不支持构造调用，且降级方案"const init 退化为普通 init"是对约束条件的误述，降级原因是 `T(0)/T(1)` 不可用而非 `const` 上下文限制
   - 所在位置：§3.3 构造函数体系第 1 条
   - 严重程度：严重
   - 改进建议：将默认单位矩阵构造推迟到 `Number<T>` 约束的 extend 块中作为 `public static func identity(): Mat4x4<T,Q>` 工厂函数；删除"const init 退化为普通 init"的描述改为对约束条件的明确声明；在 §9 差异声明中记录此偏差并显式引用 deviations.md DV-01
   - **修改结果**：已采纳。① 删除 `init()` 默认构造；② 新增 `identity()` 静态工厂函数 + D20 决策；③ §9 差异声明记录此偏差并引用 deviations.md DV-01。后续 v9 修订进一步完善：① `identity()` 签名改为 `identity(one: T): Mat{C}x{R}<T,Q>` 要求调用方显式传入 T(1)；② T(0) 改用 `one - one` 在 `Number<T>` 约束上下文中演算；③ 更新 D20 决策。来源：修订说明（v8）+（v9）。
2. **问题描述**：§3.3 第 2 条 `init<Q2>(m: Mat4x4<T, Q2>)` 与第 9 条 `init<U, P>(m: Mat4x4<U, P>)` 在 `U=T` 时参数类型完全相同，存在重载歧义
   - 所在位置：§3.3 构造函数体系第 2 条 vs 第 9 条
   - 严重程度：一般
   - 改进建议：删除第 2 条，其功能完全由第 9 条覆盖（当 `U=T` 时自然包含跨 Qualifier 复制）
   - **修改结果**：已采纳。删除原第 2 条构造函数，其功能完全由第 9 条（跨类型同尺寸）覆盖；新增 D21 决策记录此重命名与统一。来源：修订说明（v8）。
3. **问题描述**：ext/ 向量别名（camelCase）与 fwd.cj 向量别名（PascalCase）的定位差异和使用指引连续 3 轮未获回应，编码者无法判断应优先使用哪套别名
   - 所在位置：§3.8 ext/ 别名文件 — 向量别名文件
   - 严重程度：一般
   - 改进建议：在 §3.8 中补充说明两套别名的关系（对应 GLM ext/vector_float*.hpp vs fwd.hpp 的二元体系）及推荐用户优先使用 fwd.cj 中的别名
   - **修改结果**：已采纳。§3.8 ext/ 别名文件补充定位差异说明：ext/ 文件使用 `import glm.detail.{ Mat2x2, ... }` 命名导入，fwd.cj 使用限定访问 `detail.Mat4x4<...>`，两者命名风格差异的原因已说明；推荐用户优先使用 fwd.cj 中的别名（PascalCase）。来源：修订说明（v8）。
4. **问题描述**：跨矩阵类型转换构造的填充规则在 Mat4x4 ← Mat4x2 场景下与 GLM 行为不一致，且 GLM 在不同目标矩阵类型中行为因类型而异
   - 所在位置：§3.3 构造函数体系第 8 条
   - 严重程度：一般
   - 改进建议：在设计文档中添加注释说明当前规则为主规则描述但存在特例，编码阶段须逐一对照 GLM `.inl` 源码实现各转换构造函数；将此作为编码阶段风险项记录
   - **修改结果**：已采纳。补充编码阶段注意事项：当前填充规则为主规则描述但存在特例（Mat4x4 ← Mat4x2 与 GLM 不一致），编码阶段须逐一对照 GLM .inl 源码实现各转换构造函数；§9 差异声明记录此偏差。来源：修订说明（v8）。

## 迭代第 6 轮

1. **问题描述**：`identity()` 静态工厂函数中通过 `-(!(zero))` 演算 T(1) 依赖 `!` 运算符，但 `!` 属于 `Integer<T>` 接口而非 `Number<T>`，在 `Number<T>` 约束上下文中对浮点类型编译不通过
   - 所在位置：§3.3 第 8 条（第 222~228 行），D20 决策（第 464 行）
   - 严重程度：严重
   - 改进建议：`identity()` 需补充 T(1) 参数（如 `identity(one: T)`），或将约束改为 `Integer<T>`（但排除浮点矩阵），需在设计中明确决策并更新 D20
   - **修改结果**：已采纳。① 将 `identity()` 签名改为 `identity(one: T): Mat{C}x{R}<T,Q>`，要求调用方显式传入 T(1) 参数；② T(0) 改用 `one - one` 在 `Number<T>` 约束上下文中演算；③ 更新 D20 决策。来源：修订说明（v9）。
2. **问题描述**：`init(scalar: T)` 标量填充构造需要 T(0) 但构造函数必须在 struct 体内定义（无 `Number<T>` 约束），`scalar - scalar` 不可用
   - 所在位置：§3.3 第 1 条（第 191~196 行）
   - 严重程度：一般
   - 改进建议：增加 `zero: T` 参数（如 `init(scalar: T, zero: T)`），需考虑与问题 3 的统一解决策略
   - **修改结果**：已采纳。① 将标量填充构造从 `init(scalar: T)` 降级为 extend 块中的 `filled(scalar: T)` 静态工厂函数；② 在 `Number<T>` 约束的 extend 块中通过 `scalar - scalar` 获得 T(0)；③ 新增 D22 决策记录此变化。来源：修订说明（v9）。
3. **问题描述**：72 个跨尺寸转换构造函数需要 T(0)/T(1) 但作为 init 只能在 struct 体内定义，无约束泛型上下文中 T(0)/T(1) 不可获得
   - 所在位置：§3.3 第 6 条（第 211~216 行）
   - 严重程度：一般
   - 改进建议：改为 extend 块中的命名工厂函数（如 `fromMat2x2(m, zero, one)`），需考虑与问题 2 的统一解决策略（质询报告指出两者根因相同但改进策略不一致）
   - **修改结果**：已采纳。① 将 72 个跨尺寸转换构造统一降级为 `Number<T>` 约束 extend 块中的 `fromMat(m, one: T)` 静态工厂函数；② 新增 D23 决策记录此统一方案；③ 与问题 2 统一解决策略保持一致。来源：修订说明（v9）。
4. **问题描述**：`[]` 运算符赋值版本签名缺少 `mut` 修饰符
   - 所在位置：§3.4 行列访问（第 237~241 行）
   - 严重程度：一般
   - 改进建议：将签名补全为 `mut operator func [](i: Int64, value!: VecR<T,Q>): Unit`
   - **修改结果**：已采纳。签名补全为 `mut operator func [](i: Int64, value!: VecR<T,Q>): Unit`，与阶段一 Vec2 实际实现匹配（type_vec2.cj:37 已有 `mut operator func []`）。来源：设计修正（v8 阶段已确认签名风格与阶段一 Vec 一致）。
5. **问题描述**：跨类型逐分量构造（第 4 条）和跨类型列向量构造（第 5 条）的泛型类型转换在仓颉中实现可行性未分析，未参考阶段一 `castVecN` 模式
   - 所在位置：§3.3 第 4 条和第 5 条（第 206~210 行）
   - 严重程度：一般
   - 改进建议：分析跨类型构造在仓颉中的可行性约束，若不可行则降级为 `fromParts(v1, v2, ..., conv)` 工厂函数模式
   - **修改结果**：已采纳。跨类型逐分量构造降级为带 conv 闭包的 `fromParts(conv, ...)` 工厂函数；跨类型列向量构造降级为 `fromColumns(conv, ...)` 工厂函数。与阶段一 `castVecN` 模式一致。来源：修订说明（v9）。
6. **问题描述**：lib.cj 中 `public import glm.detail.{ add, sub, mul, div, mod }` 与阶段一向量版本的同名函数导出，下游消费者同时 `import glm.*` 和 `import glm.detail.*` 时是否存在歧义未评估
   - 所在位置：§8 修改文件 — lib.cj 条目（第 498 行）
   - 严重程度：一般
   - 改进建议：补充说明下游消费者使用不同导入路径时重载解析不冲突的依据，或确认会产生歧义并提供规避方案
   - **修改结果**：已采纳。确认 `scalar_mat_ops.cj` 与 `scalar_vec_ops.cj` 同包同名函数通过参数类型重载解析区分（仓颉函数重载规则确认参数类型不同即构成有效重载）。来源：设计审查（a_v10_review_v1.md）确认。
7. **问题描述**：`const init` 条件性描述未为各构造函数分类标注是否可 const init，与 D20 中"构造函数不能在 extend 块中定义"的约束存在遗留矛盾
   - 所在位置：§3.3 第 2 条（第 197~200 行）
   - 严重程度：一般
   - 改进建议：为每个构造函数标注 const init 可用性：纯赋值构造可 const、涉及类型转换或运算的不可 const
   - **修改结果**：已采纳。§3.3 构造函数/工厂函数体系的 const init 可用性汇总表（§3.3 末尾表格）完整覆盖全部 8 类构造方式的 const 可用性判断：纯赋值构造可 const（如逐分量构造、列向量构造）、涉及运算的工厂函数（identity/filled/fromMat）不可 const。来源：设计审查（a_v10_review_v1.md）确认。
8. **问题描述**：`identity()` 未分析 `const func` 可行性；若不可为 const func 则 `const val m = Mat4x4<Float32, PackedHighp>.identity()` 不可用，与 C++ GLM constexpr 行为差异未在 §9 差异声明中记录
   - 所在位置：§3.3 第 8 条，§9 差异声明，D20
   - 严重程度：一般
   - 改进建议：在 T(1) 获取方案确定后补充 const func 可行性分析；在 §9 差异声明中补充对应差异项
   - **修改结果**：已采纳。§9 差异声明补充"`identity(one)` 和 `filled(scalar)` 不可在 `const` 上下文使用"条目；说明泛型约束运算不满足 const 表达式规则（const 文档 §2 第 7 条仅允许数值类型算术表达式为 const 表达式）；提供逐分量 const init 构造的替代方案。来源：修订说明（v9）。

## 迭代第 7 轮

1. **问题描述**：`fromMat` 签名在 `SrcT ≠ T` 时缺少 `conv: (SrcT) -> T` 闭包参数，跨类型矩阵转换无法完成源元素类型转换，编码时将编译阻断
   - 所在位置：§3.3 第 6 条（第 235 行）
   - 严重程度：严重
   - 改进建议：补充 `conv: (SrcT) -> T` 闭包参数，或将同类型跨尺寸转换与跨类型转换拆分为两个独立工厂函数
   - **修改结果**：已采纳。拆分为 6a 同类型（`fromMat<SrcQ>(m: Mat2x2<T, SrcQ>, one: T)`）和 6b 跨类型（`fromMat<SrcT, SrcQ>(conv: (SrcT)->T, m: Mat2x2<SrcT, SrcQ>, one: T)`）两个独立工厂函数。新增 D27 决策记录此拆分设计。来源：修订说明（v9）。
2. **问题描述**：`fromMat` 填充规则描述 `dst[j][i] = src[j][i]` 在 `SrcT ≠ T` 时赋值不可行，类型不匹配
   - 所在位置：§3.3 第 6 条填充规则描述
   - 严重程度：严重
   - 改进建议：与问题1统一解决，补充conv闭包或限定fromMat仅用于同类型跨尺寸转换
   - **修改结果**：已采纳。与问题 1 统一解决：通过拆分 6a/6b 工厂函数，6a 版本（SrcT=T）的填充规则 `dst[j][i] = src[j][i]` 直接赋值可行；6b 版本（SrcT≠T）通过 `conv` 闭包完成元素转换。来源：修订说明（v9）。
3. **问题描述**：`fromColumns` 签名仅使用单一 `V1` 类型参数，与C++ GLM四列独立类型参数的能力不符，且未在差异声明中标注
   - 所在位置：§3.3 第 5 条（第 229 行）
   - 严重程度：一般
   - 改进建议：在文档中明确标注此为与C++ GLM的容量差异，在§9差异声明中记录
   - **修改结果**：已采纳。① §3.3 第 5 条补充"单一 V1 类型参数"说明；② 新增 D28 决策记录此设计取舍；③ §9 差异声明新增"fromColumns 仅支持单一列向量分量类型 V1"条目（与 C++ GLM 允许每列独立类型的差异）。来源：修订说明（v9）。
4. **问题描述**：`SrcQ` 类型参数声明但未使用，导致不必要的重载膨胀
   - 所在位置：§3.3 第 6 条（第 235 行）
   - 严重程度：一般
   - 改进建议：分析是否可移除 `SrcQ` 参数，或标注其不影响行为并说明重载策略
   - **修改结果**：已采纳。§3.3 第 6a 条补充 SrcQ 参数说明：SrcQ 仅出现在源矩阵参数类型声明中，使 fromMat 可接受不同 Qualifier 的源对象，不参与运算逻辑。SrcQ 使 fromMat 对每个 Qualifier 组合均构成重载，会增加编译器泛型实例化负担但不影响运行时行为。来源：修订说明（v9）。
5. **问题描述**：分量级修改操作流程（三步操作）未在关键行为契约中示例，且缺少此设计决策的显式记录
   - 所在位置：§3.4 行列访问；§4 关键行为契约
   - 严重程度：一般
   - 改进建议：在§4补充分量级修改代码示例，在§9差异声明中显式记录未提供componentAt赋值版本
   - **修改结果**：已采纳。① 新增 §4.6 分量级修改行为契约示例（取列→修改分量→赋回）；② §3.4 列访问补充"未提供 `componentAt(row, col)` 赋值版本"设计决策说明；③ §9 差异声明更新 `[]` 取值版本条目包含"不提供 componentAt(row, col) 赋值版本"说明。来源：修订说明（v9）+（v10）。
6. **问题描述**：三个stub文件的函数签名清单使用"等"字省略，编码者无法确定完整函数列表
   - 所在位置：§3.7 stub 函数库；§8 产出物清单
   - 严重程度：一般
   - 改进建议：为三个stub文件各提供完整的函数签名清单
   - **修改结果**：已采纳。提供完整的函数签名清单（common.cj / matrix.cj / geometric.cj 三文件全部展开具体签名），去除"等"字省略。来源：修订说明（v9）。
7. **问题描述**：cjpm子包发现机制未验证，`src/ext/` + `package glm.ext` 是否可编译通过未确认
   - 所在位置：§8 修改文件 — cjpm.toml 条目
   - 严重程度：一般
   - 改进建议：在§8补充编码启动前须验证子包构建的说明
   - **修改结果**：已采纳。① 新增 cjpm 子包构建预验证段落；② 新增编码启动前验证项说明（cjpm 不支持 `src/ext/` + `package glm.ext` 子包结构时需调整的备选方案）；③ §3.8 文件清单补充子包结构声明。来源：修订说明（v9）+ 设计审查（a_v10_review_v1.md）确认。

## 迭代第 8 轮

1. **问题描述**：`scalar / mat` 运算符对6种非方阵类型的支持被遗漏，§3.5 和 §5 未明确声明36个全局函数涵盖全部9种矩阵类型的标量除法，与C++ GLM参考实现不一致
   - 所在位置：§3.5「二元运算符（标量-矩阵）」；§5「错误处理策略」
   - 严重程度：严重
   - 改进建议：在§3.5明确列出 scalar_mat_ops.cj 的36个函数涵盖add/sub/mul/div各9种矩阵类型的重载（含6种非方阵）；在§5补充确认 `scalar / mat` 对所有9种矩阵类型可本阶段完整实现
   - **修改结果**：已采纳。① §3.5 明确列出 scalar_mat_ops.cj 的 36 个函数涵盖范围（add/sub/mul/div 各为全部 9 种矩阵类型含 6 种非方阵提供重载）；② §5 补充确认 `scalar / mat` 对所有 9 种矩阵类型（含非方阵）均可本阶段完整实现，与 C++ GLM 参考实现一致（如 type_mat2x3.hpp:146 定义了 `operator/(T, mat<2,3>)`）；③ 修正 §3.5 标量-矩阵段落，列出全部 9 种矩阵类型名称。来源：修订说明（v10）。
2. **问题描述**：`+=mat`/`-=mat` 复合赋值运算符的自动生成条件不精确，未说明仓颉中仅覆盖同T同尺寸矩阵、不覆盖C++ GLM的跨类型矩阵复合赋值差异
   - 所在位置：§3.5「复合赋值运算符」段落；§9 差异声明；D08
   - 严重程度：一般
   - 改进建议：在§3.5明确说明自动生成的 `+=(mat)` 仅覆盖同T同尺寸类型；在§9和D08补充跨类型矩阵复合赋值不可用的差异声明
   - **修改结果**：已采纳。① §3.5 复合赋值运算符按类别拆分为 4 类表格；② 每类标注适用矩阵范围；③ 第①类明确说明仅覆盖同 T 同尺寸类型，不覆盖跨类型矩阵复合赋值；④ §9 差异声明补充"仓颉复合赋值运算符不支持跨类型矩阵参数"条目；⑤ 更新 D08 决策补充此差异说明。来源：修订说明（v10）。
3. **问题描述**：geometric.cj stub 函数签名中 `Q <| Qualifier` 应为 `Q <: Qualifier`，`<|` 非仓颉合法语法
   - 所在位置：§3.7「geometric.cj」distance（Vec3版本）签名
   - 严重程度：一般
   - 改进建议：将 `Q <| Qualifier` 修正为 `Q <: Qualifier`
   - **修改结果**：已采纳。修正为 `Q <: Qualifier`（twice checked: Vec3 distance 签名已修正）。来源：修订说明（v10）。
4. **问题描述**：`matrix.cj` 中 `transpose` 使用了 `Mat{C}x{R}` 伪签名，非合法仓颉类型语法，编码者无法直接使用
   - 所在位置：§3.7「matrix.cj」完整函数签名清单
   - 严重程度：一般
   - 改进建议：将伪签名替换为9个具体重载签名列举，与 determinant/inverse 的具体签名风格一致
   - **修改结果**：已采纳。将 transpose 伪签名替换为 9 个具体重载签名的完整列举，与 determinant/inverse 签名风格一致。来源：修订说明（v10）。
5. **问题描述**：`mat / scalar` 在仓颉中为成员运算符而C++ GLM为全局自由函数，此API形态差异未在§9差异声明中记录
   - 所在位置：§3.5「二元运算符（矩阵-标量）」；§9 差异声明
   - 严重程度：一般
   - 改进建议：在§9差异声明中补充成员运算符vs自由函数的形态差异说明
   - **修改结果**：已采纳。① §3.5 二元运算符（矩阵-标量）段落补充说明 mat / scalar 为成员运算符函数的形态及与 C++ GLM 的差异；② §9 差异声明补充"mat / scalar 为成员运算符函数，C++ GLM 为全局自由函数"条目；③ 新增 D30 决策记录此设计取舍。来源：修订说明（v10）。
6. **问题描述**：方阵 `operator/=`（mat/mat）由编译器基于 `operator/` 自动生成并继承stub行为，但文档未明确说明
   - 所在位置：§3.5「二元运算符（矩阵-矩阵）」除法子节；§5「错误处理策略」
   - 严重程度：一般
   - 改进建议：在§3.5或§5中明确说明 `operator/=`（mat/mat）自动继承stub行为，无需单独声明
   - **修改结果**：已采纳。① §3.5 复合赋值运算符表第④类明确说明：operator/=(mat/mat) 由编译器基于 operator/(mat/mat) 自动生成，因此自动继承 stub 行为，无需单独声明 /= 的 stub 状态；② §9 差异声明补充"方阵 operator/=(mat/mat) 自动继承 stub 行为"条目；③ 注意方阵同时存在 /=scalar（可用）和 /=(mat/mat)（stub）两版重载，编译器按右操作数类型自动选择。来源：修订说明（v10）。
7. **问题描述**：`identity(one: T)` 在非方阵上的语义未明确（非方阵无单位矩阵定义），等价于 `filled(one)` 但未声明
   - 所在位置：§3.3 第8条 identity 工厂函数
   - 严重程度：一般
   - 改进建议：明确声明非方阵上 identity 语义等价于 filled(one)，或在差异声明中补充非方阵 identity 与C++ GLM不一致
   - **修改结果**：已采纳。① §3.3 第 8 条补充非方阵语义说明：identity 对所有 9 种矩阵类型均提供，非方阵上等价于 filled(one)，为有意设计（保持 API 统一性）；② 新增 D31 决策记录此取舍；③ §9 差异声明补充"identity 对非方阵的命名语义与 C++ GLM 不同"条目。来源：修订说明（v10）。
8. **问题描述**：矩阵-向量/矩阵-矩阵乘法的具体计算展开规则未提供，编码者手动实现27种乘法重载时缺乏参照
   - 所在位置：§3.5「二元运算符（矩阵-向量）」；§4.2 关键行为契约代码示例；D04
   - 严重程度：一般
   - 改进建议：在§3.5或§4中补充至少1-2个跨尺寸乘法具体展开示例（如Mat2x3 * Vec2 → Vec3）
   - **修改结果**：已采纳。§3.5 矩阵乘法部分新增"矩阵乘法展开规则"段落，提供通用公式 `result[j][i] = Σ(k=0..C1-1) lhs[k][i] * rhs[j][k]`，并附 2 个具体展开示例（Mat2x3*Mat3x2→Mat3x3 和 Mat2x3*Vec2→Vec3）。来源：修订说明（v10）。
9. **问题描述**：72个 fromMat 重载的编译可行性未评估（同名同参数个数的泛型重载数量是否在仓颉编译器中可承受）
   - 所在位置：§3.3 第6a条（质询报告覆盖遗漏）
   - 严重程度：一般
   - 改进建议：评估72个 fromMat 6a版本泛型重载在仓颉编译器中的可行性，或补充说明编译器重载解析的预期行为
   - **修改结果**：已采纳。§3.3 第 6 条末尾补充编译可行性评估：每个目标矩阵 extend 块包含 16 个静态函数签名，仓颉对单个 extend 块静态函数数量无上限限制，泛型按需实例化不导致编译膨胀；建议可拆分为多个 extend 块提升可读性。来源：修订说明（v10）"质询确认项 1"。
10. **问题描述**：复合赋值运算符的文档措辞未区分方阵和非方阵的不同自动生成行为，可能误导编码者
    - 所在位置：§3.5 复合赋值运算符段落
    - 严重程度：轻微
    - 改进建议：将复合赋值按类别拆分说明：逐元素±、标量运算、方阵乘法、方阵除法(stub)
    - **修改结果**：已采纳。§3.5 复合赋值运算符改为分类表格，按 4 类分别说明适用矩阵范围和自动生成条件（① 同尺寸矩阵 ±= ② 标量复合赋值 ③ 方阵 *= ④ 方阵 /=）。来源：修订说明（v10）。
11. **问题描述**：6a/6b fromMat 重载在 SrcT=T 时的调用优先级未给出指导
    - 所在位置：§3.3 第6a条 vs 第6b条
    - 严重程度：轻微
    - 改进建议：补充调用指导：SrcT=T时优先使用6a版本
    - **修改结果**：已采纳。§3.3 第 6a 条末尾补充调用指导：当 SrcT=T 时应优先使用 6a 版本（无需 conv 闭包），仅在 SrcT≠T 时使用 6b 版本。来源：修订说明（v10）。
12. **问题描述**：cjpm 子包 `glm.ext` 的构建可行性未经验证确认
    - 所在位置：§8 产出物清单（质询报告覆盖遗漏）
    - 严重程度：轻微
    - 改进建议：在阶段二编码启动前验证 cjpm 子包发现机制是否可用
    - **修改结果**：已采纳。已有预验证段落和编码首日验证步骤，无需额外修改（设计文档已包含 cjpm 子包构建预验证段落和编码启动前验证项）。来源：修订说明（v10）"质询确认项 2"。

## 迭代第 9 轮

1. **问题描述**：跨尺寸乘法兼容性表遗漏 Mat3x3*Mat2x3 和 Mat4x3*Mat2x4 两个合法乘法组合，跨尺寸乘法总数应为26而非24，总有效重载应为29而非27
   - 所在位置：§3.5 跨尺寸乘法兼容性表；D04；§8 产出物清单中相关数量描述
   - 严重程度：严重
   - 改进建议：补充遗漏的2个乘法组合，同步更新跨尺寸乘法总数和总有效重载数量
   - **修改结果**：已采纳。① §3.5 跨尺寸乘法兼容性表中 Mat3x3 条目补充 Mat2x3 右操作数（Mat3x3*Mat2x3→Mat2x3），同步标注同尺寸 Mat3x3*Mat3x3→Mat3x3；② Mat4x4 条目同步标注同尺寸；③ 跨尺寸计数从 24 修正为 26（加同尺寸 3 = 总计 29），补充穷举验证段落（按左操作数 C1 遍历 R2=C1 的右操作数，逐项列举跨尺寸数：2+3+3+3+2+3+3+3+2=26）；④ D04 补充乘法重载总数 29；⑤ §8 产出物清单补充乘法运算符总数。来源：修订说明（v11）。
2. **问题描述**：非方阵 operator/=(scalar) 复合赋值运算符的存在性在§3.5表格中未明确，与方阵 /=(mat/mat) stub状态区分模糊
   - 所在位置：§3.5 复合赋值运算符表格第②类和第④类
   - 严重程度：一般
   - 改进建议：在第②类中明确 /=(scalar) 对所有9种矩阵类型均可用，补充说明方阵同时存在两版 /= 重载按右操作数类型选择
   - **修改结果**：已采纳。§3.5 复合赋值运算符表第②类明确 //=(scalar) 对所有 9 种矩阵类型均可用（含非方阵）；补充方阵同时存在 /=(scalar)（可用）和 /=(mat/mat)（stub）两版重载的说明，编译器按右操作数类型自动选择重载。来源：修订说明（v11）。
3. **问题描述**：/=(scalar) 与 /=(mat/mat) 的 stub 差异在表格中未显式标注，可能导致编码者误将 /=(scalar) 也标记为 stub
   - 所在位置：§3.5 复合赋值运算符表格；§5 错误处理策略
   - 严重程度：轻微
   - 改进建议：在第②类中注明 /=(scalar) 逐元素操作不依赖 inverse 为完整实现（非stub），与第④类方阵 /=(mat/mat) stub 形成显式区分
   - **修改结果**：已采纳。① §3.5 第②类备注中明确"//=scalar 逐元素操作，不依赖 inverse，本阶段完整实现（非 stub）"；② §5 错误处理策略中补充"mat/scalar 不受 stub 影响"的显式说明；③ §9 差异声明中 /= 自动继承 stub 条目补充 /=scalar 与 /=(mat/mat) 两版重载的区分说明。来源：修订说明（v11）。
4. **问题描述**：fromMat 6a/6b 中 one 参数在目标尺寸小于源尺寸时冗余但不可省略，未分析此场景
   - 所在位置：§3.3 第 6a/6b 条
   - 严重程度：一般
   - 改进建议：明确说明 one 参数在尺寸缩减方向上的冗余性，评估是否可额外提供无 one 参数的缩减方向工厂函数重载
   - **修改结果**：已采纳。① §3.3 第 6a 条补充 `one: T` 参数在 C_dst ≤ C_src 且 R_dst ≤ R_src 时的冗余性分析；② 评估了额外提供 fromMatTruncate 无 one 参数重载的方案，结论为不采纳（72→144 签名膨胀，成本与收益不匹配）；③ 保留统一签名并在文档中标注此冗余的简化路径。来源：修订说明（v11）。
5. **问题描述**：第7条 fromMat<U,P> 与第6a条在源矩阵与目标矩阵元素类型相同且尺寸相同时功能完全重叠，未说明调用优先级
   - 所在位置：§3.3 第7条 vs 第6a条
   - 严重程度：一般
   - 改进建议：明确说明第7条在同类型同尺寸场景下冗余，推荐调用方优先使用第6a条
   - **修改结果**：已采纳。① §3.3 第 7 条后补充功能重叠说明：当 U=T 且尺寸相同时，第 7 条与第 6a 条功能完全重叠；② 明确推荐规则——同类型同尺寸时优先使用第 6a 条（更简洁，无需 conv 闭包），第 7 条仅用于 U≠T 的跨类型场景。来源：修订说明（v11）。
6. **问题描述**：outerProduct 参数 c 被命名为"列向量"可能误导编码者认为 c 分量数=列数，实际 c 分量数=行数
   - 所在位置：§3.7 matrix.cj — outerProduct 签名列表
   - 严重程度：一般
   - 改进建议：明确 outerProduct 参数语义：第一个参数 c 分量数=结果矩阵行数R，第二个参数 r 分量数=结果矩阵列数C；补充说明 GLM 中 c 指"用于构建列"的向量
   - **修改结果**：已采纳。① §3.7 outerProduct 段落补充参数语义说明：第一个参数 c 分量数=结果矩阵行数 R，第二个参数 r 分量数=结果矩阵列数 C；② 补充结果公式 M[j][i]=c[i]*r[j]；③ 补充 c 来自 GLM outerProduct(col, row) 命名中 col-like 指"用于构建列"而非"列向量"的解释，避免分量数=列数的误解。来源：修订说明（v11）。
7. **问题描述**：common.cj stub 仅包含标量版本函数，向量版本的 mix/step/smoothstep 推迟决策未说明
   - 所在位置：§3.7 common.cj 完整函数签名清单
   - 严重程度：一般
   - 改进建议：明确说明 common.cj stub 仅含标量版本，向量版本推迟至阶段四；或验证方阵 .inl 是否依赖向量版函数后补充对应 stub 签名
   - **修改结果**：已采纳。① §3.7 common.cj 条目补充阶段二 stub 范围说明：仅包含标量版本函数签名，向量版本推迟至阶段四；② 补充当前方阵 .inl 仅引用标量版本的依赖闭合分析。来源：修订说明（v11）。
8. **问题描述**：geometric.cj stub 中 dot 函数缺少 Vec1 版本
   - 所在位置：§3.7 geometric.cj — dot 函数签名清单
   - 严重程度：轻微
   - 改进建议：补充 Vec1 版本 dot 签名，或显式说明阶段二 stub 仅覆盖方阵 .inl 实际引用的签名
   - **修改结果**：已采纳。① §3.7 geometric.cj 签名清单补充 Vec1 版本 dot 签名（为保持签名字母序完整性一并提供）；② 补充阶段二 stub 范围说明：当前方阵 .inl 未直接引用 Vec1 dot，但一并提供以保持完整性；③ §8 产出物清单更新 geometric.cj 说明。来源：修订说明（v11）。
9. **问题描述**：差异声明中 ++/-- 运算符排除未显式说明影响全部9种矩阵类型（含非方阵）
   - 所在位置：§9 差异声明"无 ++/-- 运算符"条目；D08
   - 严重程度：轻微
   - 改进建议：明确标注全部9种矩阵类型在 C++ GLM 中均提供 ++/-- 运算符，仓颉中均不可用
   - **修改结果**：已采纳。① §9 差异声明中 ++/-- 条目更新为"全部 9 种矩阵类型（含 6 种非方阵）均提供 ++/-- 运算符，仓颉中全部 9 种均不可用"；② D08 决策理由补充"全部 9 种矩阵类型（含 6 种非方阵）均提供 ++/--"的显式说明。来源：修订说明（v11）。
10. **问题描述**：D19 排除 fmat/f32mat/f64mat 系列非方阵精度变体的具体数量未说明，排除理由数值佐证不足
    - 所在位置：D19；§8 fwd.cj 排除决策说明
    - 严重程度：轻微
    - 改进建议：在 D19 中补充 fmat/f32mat/f64mat 系列非方阵精度变体数量（每族9×3=27个，3族共81个），强化排除理由
    - **修改结果**：已采纳。D19 补充具体数值：每族 9 个矩阵尺寸 × 3 种精度前缀 = 27 个别名/族，3 族共 81 个；加上 i8mat~u64mat 等 12 种整型前缀 × 9 种矩阵尺寸 = 108 个；合计约 189 个冗余别名，与纳入的约 60 个形成显著缩减对比。来源：修订说明（v11）。

## 迭代第 10 轮

> **判定结果**：PASS（全部 10 个问题均为轻微等级，无严重或一般问题；满足设计可直接指导编码实现的条件）
> **来源**：b_v10_diag_v1.md + iteration_v10.md + a_v10_review_v1.md
> **修改结果说明**：迭代 10 判定为 PASS，所有问题均为轻微等级（涉及文档表述精度、参数冗余性分析一致性、特定场景的示例补充、语义局限的说明等），不影响设计方案的总体可行性。审查结论指出前序迭代中的所有严重问题均已在本次版本中修正，当前设计可直接指导编码实现。

1. **问题描述**：`fromMat` 6a 与 6b 在 `SrcT=T` 且同尺寸时存在重载歧义，文档未给出编译器层面的消歧方案（使用"调用指导"措辞而非"编译器自动选择"，可能误导编码者认为编译器无法区分两者）
   - 所在位置：§3.3 第 6a 条 vs 第 6b 条（第 264 行附近）
   - 严重程度：轻微
   - 改进建议：将"调用指导"重写为"编译器消歧说明"——明确指出 6a 和 6b 参数个数不同，编译器按参数数量自动选择，无需调用方显式指定优先级
   - **修改结果**：本轮 PASS，文档已在迭代 9 修订中补充 6a/6b 调用指导（SrcT=T 时优先 6a）。编译器按参数数量自动消歧的依据可在后续版本中显式补充；当前设计已明确推荐规则，编码者有清晰指导。
2. **问题描述**：`fwd.cj` 中 `Mat{C}x{R}` 与 `detail.Mat{C}x{R}<T, Q>` 别名遮蔽问题未评估——矩阵别名将使 fwd.cj 膨胀至约 500+ 行（阶段一 fwd.cj 已有 324 行纯向量别名），大规模别名文件的维护性和可读性值得注意
   - 所在位置：§8 修改文件 — fwd.cj 条目（第 765~772 行）
   - 严重程度：轻微
   - 改进建议：注明矩阵别名将使 fwd.cj 膨胀至约 500+ 行，建议编码阶段考虑别名生成脚本自动生成（阶段一已有此注释），和/或评估是否需要拆分 fwd.cj 为多个文件
   - **修改结果**：本轮 PASS。设计文档 fwd.cj 条目已说明别名命名规则与阶段一向量命名规范保持一致；fwd.cj 文件大小膨胀问题为编码实现阶段风险项，可在阶段二编码启动时评估是否需要别名生成脚本。
3. **问题描述**：`fromMat` 6b 中 `conv` 闭包与 `one: T` 参数在跨类型同尺寸转换中的冗余共存未分析（当 `C_dst = C_src, R_dst = R_src` 时 `one` 参数不再需要，但 6b 签名仍保留 `one`）
   - 所在位置：§3.3 第 6b 条 vs 第 7 条（第 264 行附近 vs 第 287 行附近）
   - 严重程度：轻微
   - 改进建议：在第 6b 条补充 `one` 参数冗余性分析：与 6a 相同——当 C_dst ≤ C_src 且 R_dst ≤ R_src 时 `one` 冗余（仅涉及 conv 转换源元素，无扩展填充），设计选择同样保留统一签名
   - **修改结果**：本轮 PASS。设计文档迭代 9 修订说明（v11）已对 6a 中 `one` 参数冗余性进行了分析；6b 中相同模式的冗余性分析可在后续版本中补充，编码者可类比 6a 的冗余场景理解。
4. **问题描述**：`identity(one: T)` 的泛型推断可行性未充分验证——别名类型 `mat4.identity(1.0f)` 在仓颉中的类型别名透明度行为未显式说明
   - 所在位置：§3.3 第 8 条（第 295~303 行）
   - 严重程度：轻微
   - 改进建议：在 §3.3 第 8 条的完整泛型参数展开后补充一句：别名类型（如 mat4 = Mat4x4<Float32, PackedHighp>）上的 `identity(1.0f)` 调用推断行为与原始泛型类型一致（仓颉类型别名为透明别名）
   - **修改结果**：本轮 PASS。设计审查（a_v10_review_v1.md）确认 extend 块中静态函数的 Q 推断模式与阶段一 Vec 的 `fromBoolVec` 中 Q 的推断方式相同（已成功编译先例），别名透明性已在审查中得到确认。
5. **问题描述**：`@Derive[Hashable]` 对矩阵类型的可行性未验证——Float32/Float64 矩阵的 Hashable 实现对 NaN 分量的语义局限未记录（C++ GLM 矩阵 `operator==` 对 NaN 返回 false，但哈希可能将不同 NaN 矩阵映射到同一桶）
   - 所在位置：§3.1 矩阵结构体（第 181 行）；D17
   - 严重程度：轻微
   - 改进建议：在 D17 中补充说明：Float32/Float64 矩阵的 Hashable 实现对 NaN 分量的行为与 Vec 的 Hashable 一致——NaN 在哈希中可能产生冲突。若下游消费者需区分不同 NaN 模式的矩阵，应使用 `equalEpsilon` 而非 HashSet/HashMap
   - **修改结果**：本轮 PASS。设计审查（a_v10_review_v1.md）确认 `@Derive[Hashable]` 语法上可行（Vec 已可哈希）；NaN 哈希语义局限为浮点哈希的通用问题，可在后续版本 D17 中补充语义说明，当前设计不影响编码实现。
6. **问题描述**：`scalar_mat_ops.cj` 中 `div` 函数对整型矩阵的除零行为未充分说明——仓颉整型除零会抛出运行时异常，C++ GLM 中整型除零为未定义行为，此差异未在 §9 差异声明中记录
   - 所在位置：§3.5「二元运算符（标量-矩阵）」（第 368~375 行）；§5 错误处理策略
   - 严重程度：轻微
   - 改进建议：在 §9 差异声明中补充：仓颉整型矩阵的标量-矩阵除法（`div(s, m)`）在除零时抛出运行时异常，而 C++ GLM 中整型除零为未定义行为。这是仓颉运行时安全性与 C++ 未定义行为的规范化差异
   - **修改结果**：本轮 PASS。设计文档 §5 已提及"整型矩阵除零风险"与阶段一 Vec 类型一致；具体到 §9 差异声明的整型除零异常差异可在后续版本中补充，当前设计满足编码实现需求。
7. **问题描述**：`fromColumns` 签名中 `Q2` 参数未被使用，与 6a 中 `SrcQ` 的分析不一致（迭代 7 已分析 6a 中 SrcQ 的作用，第 5 条的 Q2 未做同等分析）
   - 所在位置：§3.3 第 5 条（第 243 行）
   - 严重程度：轻微
   - 改进建议：补充 Q2 参数的作用说明（与 6a 中 SrcQ 一致：允许接受不同 Qualifier 的列向量），并说明 Q2 不影响运行时行为，仅影响编译器泛型实例化
   - **修改结果**：本轮 PASS。设计文档迭代 7 修订说明（v9）已对 fromColumns 的单一 V1 类型参数进行了说明；Q2 参数与 SrcQ 同类（仅出现在参数类型声明中，不参与运算逻辑），可在后续版本中显式说明以保持文档一致性。
8. **问题描述**：非方阵的 `filled(scalar: T)` 行为示例仅描述了 Mat2x3 一种情况，其他 5 种非方阵未给出示例（特别是 C > R 时，如 Mat3x2 的主对角线长度 min(3,2)=2，第 2 列对角元素超出行范围）
   - 所在位置：§3.3 第 3 条（第 224~233 行）；§4.1 行为契约
   - 严重程度：轻微
   - 改进建议：在 §4.1 补充一个 C > R 的非方阵 `filled` 示例（如 Mat3x2），与现有的 Mat2x3（C < R）示例形成互补覆盖
   - **修改结果**：本轮 PASS。主对角线规则（min(C,R)）已足够推导；Mat2x3 示例（C < R）已说明主对角线规则，编码者可类推其他非方阵。Mat3x2 示例（C > R）可在后续 §4.1 中补充。
9. **问题描述**：跨尺寸矩阵乘法编码时 `operator func *(rhs: Mat{C2}x{R2})` 的返回类型推断可行性未分析——文档未显式说明"跨尺寸乘法仅支持同 Q 操作数"
   - 所在位置：§3.5 跨尺寸乘法兼容性表及归属策略（第 380~395 行）
   - 严重程度：轻微
   - 改进建议：在 §3.5 跨尺寸乘法兼容性表中补充说明：所有跨尺寸乘法要求左右操作数具有相同的 T 和 Q——不同 T 或不同 Q 的矩阵不可直接相乘（与 C++ GLM 中 `mat<C,R,T,Q>` 的 T 和 Q 必须匹配一致）
   - **修改结果**：本轮 PASS。设计审查（a_v10_review_v1.md）确认跨尺寸乘法定义在左操作数矩阵类型的 extend 块中，同包（glm.detail）类型互相直接可见。跨尺寸乘法的 T/Q 一致性约束在跨尺寸乘法兼容性表的示例中隐含体现（所有示例 Q 一致），可在后续版本中显式声明。
10. **问题描述**：`outerProduct` 签名中参数 `c` 和 `r` 的 Qualifier 与结果矩阵的 Qualifier 关系未说明——设计统一要求 c 和 r 的 Q 必须相同，与 C++ GLM 允许跨 Q 的能力有微小差异
    - 所在位置：§3.7 matrix.cj — outerProduct 签名清单（第 496~508 行）
    - 严重程度：轻微
    - 改进建议：明确声明 outerProduct 要求两个向量参数具有相同 Q，与 C++ GLM 允许跨 Q 的能力有微小差异。若此差异对用户有影响，可考虑提供一个 Q2 版本 `outerProduct<T, Q, Q2>(c: VecR<T,Q>, r: VecC<T,Q2>): Mat{C}x{R}<T,Q>`
    - **修改结果**：本轮 PASS。设计文档迭代 9 修订说明（v11）已对 outerProduct 的参数语义（c 分量数=行数 R，r 分量数=列数 C）进行了详细说明；统一 Q 的设计与 C++ GLM 跨 Q 能力的差异可在后续版本中显式记录，当前设计满足核心需求。
