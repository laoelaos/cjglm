根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

1. **问题描述**：fwd.cj 矩阵默认精度别名使用 PackedHighp，与 GLM 参考实现的 defaultp 语义不一致——设计文档未说明选择 PackedHighp 而非 Defaultp 别名的理由，编码者可能困惑于 fwd.cj 和 ext/ 文件的不一致引用风格
   - 所在位置：§8 修改文件 — fwd.cj 矩阵别名定义；§3.8 ext/ 别名文件
   - 严重程度：轻微
   - 改进建议：① fwd.cj 矩阵别名可考虑直接使用 Defaultp（`detail.Mat4x4<Float32, detail.Defaultp>`），与 GLM 的 defaultp 语义完全对齐；② 或在文档中明确注明虽然均使用 PackedHighp 而非 Defaultp，但两者语义等效（Defaultp = PackedHighp），且选择 PackedHighp 与阶段一 fwd.cj 现状一致

2. **问题描述**：fwd.cj 缺少 Float32/Float64 以外精度族的矩阵别名（如 fmat/f32mat/f64mat 系列及 dmat 全尺寸系列），且非方阵默认精度别名（mat2x3~mat4x3 等）未显式列出，纳排决策缺失
   - 所在位置：§8 修改文件 — fwd.cj 矩阵别名命名规则
   - 严重程度：一般
   - 改进建议：① 明确列出 fwd.cj 中将定义的全部矩阵别名族，逐一对照 C++ GLM fwd.hpp 中的定义；② 明确排除不纳入的别名族（如 fmat/f32mat/f64mat 等）并给出理由；③ 确认非方阵默认精度别名（如 mat2x3、mat3x2 等）是否纳入 fwd.cj；④ 补充明确 dmat 系列非方阵别名（dmat2x3~dmat4x3）的纳入决策

3. **问题描述**：非方阵类型缺少复合赋值矩阵乘法运算符（*= mat），设计方案未讨论其影响——"由仓颉编译器自动生成"措辞可能误导编码者认为所有类型均获得 *= 矩阵乘版本
   - 所在位置：§3.5 复合赋值运算符段落
   - 严重程度：一般
   - 改进建议：① 明确标注仅方阵类型（Mat2x2/Mat3x3/Mat4x4）自动获得 *=（same_mat）复合赋值运算符；② 说明非方阵类型因矩阵乘法结果类型不同而无法自动生成矩阵复合赋值；③ 在文档中注明方阵 *=（mat）由编译器自动生成，语义与 C++ GLM 的显式定义一致（a *= b ≡ a = a * b）

4. **问题描述**：跨矩阵类型转换构造函数中对"源矩阵元素复制到目标矩阵对应位置"的语义描述不够精确——"对应位置"存在歧义
   - 所在位置：§3.3 构造函数体系第 8 条
   - 严重程度：一般
   - 改进建议：补充精确说明：源矩阵第 j 列第 i 行的元素（M[j][i]）复制到目标矩阵的相同坐标位置，其中 0 ≤ j < min(C_src, C_dst)、0 ≤ i < min(R_src, R_dst)。当 C_dst > C_src 时，额外列的对应位置从单位矩阵取值；当 R_dst > R_src 时，额外行的对应位置从单位矩阵取值

5. **问题描述**：Mat4x4 的 `init(scalar: T)` 语义描述仅提供方阵示例，未讨论非方阵标量填充的"主对角线"长度
   - 所在位置：§3.3 第 3 条；§4.1 行为契约
   - 严重程度：轻微
   - 改进建议：在 §3.3 第 3 条或 §4.1 行为契约中补充一个非方阵示例（如 `Mat2x3(1.0f)` 产生 2×3 对角矩阵：`[[1,0,0],[0,1,0]]`），明确非方阵标量填充的"主对角线"长度为 min(C,R)

6. **问题描述**：scalar / mat（标量除以矩阵）运算在本阶段的 stub 策略未明确——其行为是逐元素 scalar / each_element，不依赖 inverse，应可本阶段完整实现，但 §5 未提及
   - 所在位置：§3.5 标量-矩阵运算（scalar_mat_ops.cj）；§5 错误处理策略
   - 严重程度：一般
   - 改进建议：① 在 §3.5 标量-矩阵运算段落中明确 div(s, m) 的语义为逐元素 s / each_element；② 在 §5 错误处理策略中补充"标量-矩阵除法（scalar / mat）为逐元素操作，不依赖 inverse，可本阶段完整实现"

7. **问题描述**：非方阵类型不提供 mat / mat 和 mat / vec 除法运算符，但设计文档"mat / mat"措辞可能误导编码者为所有 9 种矩阵类型实现该运算符
   - 所在位置：§3.5 二元运算符（矩阵-矩阵）除法子节
   - 严重程度：一般
   - 改进建议：明确指出 mat / mat 和 mat / vec 除法运算符仅在方阵类型（Mat2x2、Mat3x3、Mat4x4）上定义，非方阵类型不提供这两种除法运算符。与 GLM 参考实现一致

8. **问题描述**：ext/ 别名文件使用命名导入与 fwd.cj 的限定访问风格不同，设计文档未说明此差异及原因
   - 所在位置：§3.8 ext/ 别名文件
   - 严重程度：轻微
   - 改进建议：在 §3.8 中补充说明 ext/ 别名文件使用命名导入 `import glm.detail.{ Mat2x2, ... }` 而非 fwd.cj 的限定访问 `import glm.detail`，原因：ext/ 文件声明 package glm.ext，其中定义的别名（如 mat4）不会与 detail 包中的类型名（如 Mat4x4）冲突，因此命名导入安全可用

9. **问题描述**：lib.cj 对 scalar_mat_ops.cj 全局函数的导出使用"建议"措辞，编码者无法确定是否应导出
   - 所在位置：§8 修改文件 — lib.cj 条目
   - 严重程度：一般
   - 改进建议：① 将"建议同步导出"改为明确决策（导出），并说明理由；② 确认 lib.cj 中现有 `public import glm.detail.{ add, sub, mul, div, mod }` 已自动覆盖 scalar_mat_ops.cj 中的同名函数（因两者同属 glm.detail 包），无需额外 import 语句；③ 确认 mod 函数的矩阵版本不存在（矩阵取模无数学意义），lib.cj 的 mod 导出仅涉及向量版，不受影响

10. **问题描述**：跨尺寸矩阵乘法运算符（27 个重载）的文件归属策略未定义，直接影响编码落地
    - 所在位置：§3.5 跨尺寸乘法兼容性表；§7 D04
    - 严重程度：一般
    - 改进建议：明确跨尺寸矩阵乘法运算符定义在左操作数矩阵类型的 extend 块中（如 Mat2x3 的 extend 块中定义 operator func *(rhs: Mat2x2) 等重载），在对应 type_mat{C}x{R}.cj 文件顶部 import 所需右操作数矩阵类型。说明此设计选择与在左操作数上定义运算符的模式一致

11. **问题描述**：矩阵-标量除法和标量-矩阵除法对整型矩阵的除零风险未讨论
    - 所在位置：§3.5；§5 错误处理策略
    - 严重程度：轻微
    - 改进建议：在 §5 中注明"矩阵-标量除法（mat / scalar）和标量-矩阵除法（scalar / mat）对整型矩阵的除零行为与阶段一 Vec 类型一致——仓颉整数除法由编译器/运行时处理，本设计不额外添加除零保护"

12. **问题描述**：`[]` 下标运算符取值版本返回列向量副本而非引用，不支持分量级链式修改（如 m[0].x = value），但 §9 差异声明中未记录此用户可见差异
    - 所在位置：§3.4 行列访问；§9 对齐 GLM 参考实现的差异声明
    - 严重程度：一般
    - 改进建议：在 §9 差异声明中新增条目：`[]` 运算符取值版本返回列向量副本，不支持分量级链式修改——C++ GLM 中 m[i] 返回引用，可直接修改 m[0].x = value；仓颉版本中 m[0] 返回副本，仅可整列赋值 m[0] = new_col，不能直接修改分量。如需修改单个分量，需先取列、修改、赋回

13. **问题描述**：`equalEpsilon` 的实现方式未明确是逐列委托 Vec 的 equalEpsilon 还是逐标量比较
    - 所在位置：§3.6 比较运算符
    - 严重程度：轻微
    - 改进建议：明确 equalEpsilon 的实现方式为逐列委托 VecR<T,Q>.equalEpsilon，这与 == 逐列委托 VecR<T,Q>.== 的模式一致。明确约束传递关系：矩阵 equalEpsilon 要求列向量类型 T 满足 Number<T> & Equatable<T> & Comparable<T>，与阶段一 Vec 的约束条件一致

## 历史迭代回顾

### 已解决的问题（出现在历史反馈但当前反馈中不再提及的问题）

- **迭代第 1 轮问题 1**：行向量×矩阵乘法签名错误（严重）——已在 v5 修订中采纳修正，当前反馈不再提及
- **迭代第 1 轮问题 2**：跨尺寸矩阵乘法运算符未完整定义——已在 v5 修订中补充完整兼容性表，当前反馈不再提及
- **迭代第 1 轮问题 4**：ext/ 别名文件数量描述前后矛盾——已在 v5 修订中统一为 18 个文件，当前反馈不再提及
- **迭代第 1 轮问题 5/6**：矩阵除法 stub 依赖表述不准确、标量-矩阵除法策略未说明——已在 v5 修订中明确区分 mat/mat 和 mat/scalar，当前反馈问题 6 是对 §5 中 scalar/mat 遗漏的进一步细化
- **迭代第 2 轮问题 1**：ext/ 向量别名文件未纳入设计范围（严重）——已在 v6 修订中新增 8 个向量别名文件，当前反馈不再提及
- **迭代第 2 轮问题 2**：D08 等效替代数学错误（严重）——已在 v6 修订中修正为 m += T(1)，当前反馈不再提及
- **迭代第 2 轮问题 3**：72 个跨矩阵类型转换构造函数可行性未分析（严重）——已在 v6 修订中补充可行性评估，当前反馈不再提及
- **迭代第 2 轮问题 4**：componentAt 返回类型与 Vec 版本不一致——已在 v6 修订中更名为 col()，当前反馈不再提及
- **迭代第 2 轮问题 5**：row() 成员函数在本阶段无实际用途——已在 v6 修订中明确推迟至阶段四，当前反馈不再提及
- **迭代第 2 轮问题 6**：跨类型构造函数未覆盖同尺寸转换——已在 v6 修订中新增第 9 条构造函数，当前反馈不再提及
- **迭代第 2 轮问题 7**：scalar_mat_ops.cj 溢出策略未说明——已在 v6 修订中补充 @OverflowWrapping 标注，当前反馈不再提及
- **迭代第 2 轮问题 8**：mat/vec 和 vec/mat 除法未讨论——已在 v6 修订中补充推迟至阶段四的说明，当前反馈不再提及
- **迭代第 3 轮问题 1-5**：C1=R2 注解错误、const init 降级方案、col()与[]行为差异化、命名约定冲突、fwd.cj 命名规范未定义——已在 v7 修订中全部采纳修正，当前反馈不再提及

### 持续存在的问题（在多轮反馈中反复出现，需重点解决）

- **fwd.cj 矩阵别名覆盖完整性**：迭代第 1 轮未提及 fwd.cj 别名规范；迭代第 3 轮问题 5 要求定义 fwd.cj 命名规则（已在 v7 修订中采纳 D16）；迭代第 4 轮问题 1/2 仍指出 fwd.cj 缺少 fmat/f32mat/f64mat 系列别名、非方阵默认精度别名未显式列出——需在本轮彻底解决，逐一对照 C++ GLM fwd.hpp 列出全部别名族并明确纳排决策
- **跨矩阵转换"对应位置"语义不精确**：迭代第 1 轮问题 3 提出填充规则模糊（已在 v5 修订中补充"未覆盖行列从单位矩阵取值"）；迭代第 4 轮问题 3 仍指出"对应位置"定义不够精确——需在本轮补充 (row, col) 坐标对应的精确定义
- **除法运算符的完整性/精确性**：迭代第 1 轮问题 5/6 要求区分 mat/mat 和 mat/scalar（已修正）；迭代第 2 轮问题 8 补充 mat/vec 推迟说明（已修正）；迭代第 4 轮问题 6/7 进一步指出 scalar/mat 遗漏和非方阵除法限定——需在本轮确保 §5 覆盖全部除法场景（mat/mat、mat/scalar、scalar/mat、mat/vec、vec/mat），并明确各方阵/非方阵适用性

### 新发现的问题（本轮新识别的问题）

- **问题 1**：fwd.cj 使用 PackedHighp 而非 Defaultp 的风格说明缺失
- **问题 5**：非方阵标量填充构造缺少示例
- **问题 8**：ext/ 与 fwd.cj 导入风格差异未说明
- **问题 10**：27 个跨尺寸乘法重载的文件归属未定义
- **问题 11**：整型矩阵除零风险未讨论
- **问题 12**：[] 运算符引用语义偏差未在差异声明中记录
- **问题 13**：equalEpsilon 实现方式（逐列委托 vs 逐标量比较）未明确

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606210006_OOD_stage2_migration\a_v4_design_v1.md

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606210006_OOD_stage2_migration\requirement.md
