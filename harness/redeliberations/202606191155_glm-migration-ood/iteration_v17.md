# 再审议判定报告（v17）

## 判定结果

RETRY

## 判定理由

诊断报告识别出 Q1（严重）— D30 回退方案中循环委托遗漏未被修复，以及 Q2/Q3（一般）— 扩展成员函数可见性依赖未纳入验收前置条件、`fromBoolVecQ2` 显式标注模板不完整。质询报告确认 LOCATED，诊断结论成立。根据判定标准，审查报告包含严重及一般等级的问题，应重新运行组件A。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：D30 回退方案中循环委托遗漏未被修复——扩展成员函数委托给运算符，运算符又委托回扩展成员函数，形成循环调用。自 v1 审查以来持续未被修复。
- **所在位置**：§4.3 第 1015 行、§7 D30 回退方案（第 1609 行）、§4.6 第 1226 行、§11.6 第 2255-2261 行
- **严重程度**：严重
- **改进建议**：在 D30 回退方案中增加扩展成员函数实现从"委托给二元运算符"改为"直接逐分量运算"的改造步骤；同步修正 §11.6 变更成本表和 §11.13 设计快照

- **问题描述**：扩展成员函数在 `public import` 重导出后的可见性依赖未经验证的编译器行为（D37 A20），回退方案未被纳入 §12.2 验收前置条件
- **所在位置**：§2 第 210-214 行、§7 D37 A20（第 1785 行）、§12.2 第 2777-2812 行、§8.5 排除清单
- **严重程度**：一般
- **改进建议**：在 §12.2 中新增"扩展成员函数 `public import` 可见性验证"作为非阻塞验收前置项；在 §8.5 排除清单中记录此风险

- **问题描述**：`fromBoolVecQ2` 显式标注 `<T, Q>` 后的完整函数体模板仅覆盖 Vec2/Vec4，缺少 Vec1/Vec3 对应模板，与脚注声明"所有 4 个重载版本"不一致
- **所在位置**：§4.8 第 1307-1319 行
- **严重程度**：一般
- **改进建议**：补充 Vec1 和 Vec3 的完整函数体模板，或提供统一的泛型函数体模板
