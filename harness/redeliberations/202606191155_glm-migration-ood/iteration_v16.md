# 再审议判定报告（v16）

## 判定结果

RETRY

## 判定理由

组件B诊断报告识别出 5 项问题，其中 1 项严重（问题1：P37 "已解决（v16）"状态声明与实际解决粒度存在实质性差距）、2 项一般（问题2：decrement() const 替代方案语义错误；问题3：@OverflowWrapping 浮点标注缺乏验证覆盖）、2 项轻微（问题4/5）。质询报告确认（LOCATED）全部问题证据充分、逻辑完整、覆盖完备。根据判定标准，审查报告包含严重及一般等级问题，需 RETRY 重新运行组件A。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：P37 "已解决（v16）"状态声明与实际解决程度存在实质性差距——7 个非 Vec 文件仅提供核查清单级扫描，未达到逐 API 级对照
- **所在位置**：§1 第 95 行问题状态追踪表 P37、附录 A 第 2877 行验证范围声明
- **严重程度**：严重
- **改进建议**：将 P37 状态修正为 "✅ 部分解决——4/11 文件已完成逐 API 级对照；7 个非 Vec 文件已提供核查清单，逐 API 级对照推迟至编码阶段"，或拆分为 P37a（已解决）/ P37b（推迟至编码阶段）

- **问题描述**：§3.2 const 上下文 API 可用性速查表中 `decrement()` 的 const 替代方案 `sub(T(1), v)` 语义错误——`sub(s: T, v: VecN<T,Q>)` 定义为标量减向量（`s - v`），计算结果为 `1 - v_i` 而非 `v_i - 1`
- **所在位置**：§3.2 const 上下文 API 可用性速查表（第 477 行）
- **严重程度**：一般
- **改进建议**：将 const 替代方案修正为 `v - T(1)`（若二元运算符 `-` 可声明为 const）或 `v + (-T(1))`

- **问题描述**：§4.6 承认 `@OverflowWrapping` 可能抑制 NaN/Infinity 检测优化路径，但 §2.1 编码前验证清单的 25 项验证中无任何一项针对此风险设计验证方法
- **所在位置**：§4.6（第 1229 行）浮点 `@OverflowWrapping` 说明段、§2.1 编码前验证清单
- **严重程度**：一般
- **改进建议**：在 §2.1 中新增验证项，编写原型测试对比标注与不标注 `@OverflowWrapping` 时浮点运算结果差异，视结果制定条件性标注策略
