根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

以下为组件B诊断报告提取的质量问题（质询结论 LOCATED，诊断可信度高）：

1. **[中等]** `diagonal(scalar: T)` 仅对方阵提供，非方阵不提供。但 GLM 1.0.3 对所有 9 个矩阵类型均提供 `mat(T scalar)` 构造函数（含非方阵），API 存在对称性缺陷。改进建议：对所有 9 个矩阵类型均提供 `diagonal(scalar)`，或在 §9 差异声明中明确声明此为仓颉设计与 GLM `mat(T s)` 的行为偏差。
   - 所在位置：§3.3 item 3（L231-236）；§7 D34（L753）；§9 差异声明 diagonal 条目（L868）

2. **[中等]** 矩阵-矩阵除法 `Mat / Mat` 在 `rhs` 不可逆（奇异矩阵）时的行为未定义，`inverse` 的奇异矩阵行为策略缺失。改进建议：在 §5 错误处理策略中新增"矩阵除法奇异矩阵行为"条目，明确 `inverse` 对奇异矩阵的行为选择（抛异常 / 返回未定义值），并在差异声明中相应标注。
   - 所在位置：§3.5 矩阵-矩阵除法签名（L515-521）；§5 错误处理策略（L713）

3. **[一般]** Vec2 `cross`（2D 叉积，属 gtx/exterior_product）未在 §10 追踪，可能导致阶段六实施者遗漏此 API。改进建议：(1) 在 `geometric.cj` 签名清单附近注明 Vec2 `cross` 属 gtx 范围不在本阶段；(2) 在 §10 中为 `cross` 追加备注"Vec2 `cross` 属 gtx/exterior_product，后续阶段六覆盖"。
   - 所在位置：§3.7 geometric.cj 签名清单（L649）；§10 geometric 函数覆盖矩阵

4. **[中等]** `common.cj` 中 `mod` 函数签名标注为"暂定"（`Number<T>` 约束对 Float32/Float64 为确定性编译期错误），但未在 §8 编码启动前验证项中列为阻断性风险项。改进建议：在 §8 新增阻断项——验证 `mod<T>` 对 Float32/Float64 是否可编译，若不能则拆分签名，引用 deviations.md DV-02 作为已验证证据。
   - 所在位置：§3.7 common.cj mod 签名（L578-579）；§8 编码启动前验证项（L771）

5. **[一般]** 编码模板示例 1 使用 `m.c0[0]` 下标访问而非 `m.c0.x` 属性访问，将带来不必要的 assert 开销。改进建议：将模板示例中 `[0]`/`[1]` 改为 `.x`/`.y` 属性访问，注明优先使用属性访问的理由。
   - 所在位置：§3.3 编码模板示例 1（L406-423）

6. **[一般]** `scalar_mat_ops.cj` 新增的 36 个全局函数与 `scalar_vec_ops.cj` 同名函数重载解析未纳入 §8 编码启动前验证项。改进建议：在 §8 验证项中增加——验证 `add(s: T, m: Mat2x2<T,Q>)` 与 `add(s: T, v: Vec2<T,Q>)` 重载无歧义。
   - 所在位置：§8 编码启动前验证项（L771）

7. **[一般]** `geometric.cj` stub 中缺少 Vec1 版本的 `length`/`normalize`/`distance`/`reflect`/`refract`，与 GLM 之间存在潜在覆盖差异（待查验确认）。改进建议：查验 GLM `func_geometric.inl` 确认 Vec1 版本是否实际提供特化；若确认遗漏则补齐，若为有意省略则在文档中说明理由。质询提示：在查验完成前应使用"潜在覆盖差异"而非"遗漏"。
   - 所在位置：§3.7 geometric.cj 签名清单（L650-664）；§10 geometric 函数覆盖矩阵

8. **[一般]** `fromMat` 6a/6b 纯收缩方向 `one` 参数被忽略的行为未在 §9 差异声明中显式声明。改进建议：在 §9 差异声明中新增条目，声明此为仓颉泛型限制导致的 API 设计偏差。
   - 所在位置：§3.3 item 6 纯收缩方向 `one` 参数行为说明（L286 附近）；§9 差异声明

9. **[提示]** §10 fwd.hpp 别名族统计使用"4 精度 × 9 尺寸 = 36"，但仓颉版 defaultp = PackedHighp，实际仅 3 独立精度族。改进建议：加注说明 GLM 含 defaultp 共 4 精度级别，仓颉版实际 3 独立精度族。
   - 所在位置：§10 fwd.hpp 别名覆盖矩阵（L947-948）

10. **[提示]** 路线图阶段 2 产出物列出了"矩阵测试文件"，但 OOD §8 产出物清单未提及测试文件的设计/规划。改进建议：在 §8 产出物清单中新增"矩阵测试文件"条目，说明测试文件组织和命名约定（沿用阶段一模式），并引用路线图验证标准。
    - 所在位置：§8 产出物清单（L760-764）

11. **[提示]** 阶段一到阶段二迁移连续性确认：fwd.cj 新增矩阵别名的 import 声明和命名冲突确认未显式说明。改进建议：在 §8 别名清单前新增说明——阶段一 `fwd.cj` 已包含 `import glm.detail`，矩阵别名可直接通过 `detail.MatNxN<...>` 限定访问，无需额外 import；确认矩阵别名命名与现有向量别名无冲突。
    - 所在位置：§8 fwd.cj 别名清单（L798-843）

## 历史迭代回顾

### 已解决的问题（出现在历史反馈但当前反馈中不再提及的问题）

- **迭代第 1 轮 #1**：`[]` 赋值版本签名双重语法违规 — v35 修订中已修正为 `public mut operator func [](i: Int64, value!: VecR<T,Q>): Unit`
- **迭代第 1 轮 #2**：复合赋值运算符自动生成被标注为"待验证假设" — v35 修订中已明确为语言规范保证行为，手动回退方案移至附录 A
- **迭代第 1 轮 #3**：type_float.cj / type_half.cj 未提及 — v35 修订中新增 D35
- **迭代第 1 轮 #4**：mod 与矩阵标量运算导出交互未说明 — v35 修订中显式说明 mod 不在 scalar_mat_ops.cj 中提供
- **迭代第 1 轮 #5**：abs/sign 泛型约束 Equatable→Comparable — v35 修订中已修正
- **迭代第 1 轮 #6**：Mat/Mat 除法签名缺失 — v35 修订中已补充 3 个签名（但奇异矩阵行为策略仍缺失，见下方持续问题）

### 持续存在的问题（在多轮反馈中反复出现的问题，需重点解决）

以下 8 个问题在第 2 轮和第 3 轮反馈中连续出现，必须在本轮迭代中彻底解决：

1. **diagonal(scalar) 对非方阵的排除**（迭代第 2 轮 #1 → 当前 #1）：连续 2 轮提出，v2 诊断提供了更完整的 GLM 逐文件证据。需在本轮做出决策：要么对所有 9 个矩阵类型提供 `diagonal(scalar)`，要么在 §9 差异声明中显式声明此为与 GLM 的行为偏差。
2. **Mat/Mat 除法奇异矩阵行为未定义**（迭代第 2 轮 #2 → 当前 #2）：连续 2 轮提出。需在 §5 错误处理策略中新增奇异矩阵行为条目。
3. **Vec2 cross 未在 §10 追踪**（迭代第 2 轮 #3 → 当前 #3）：连续 2 轮提出。需在 §10 和 §3.7 中补充备注。
4. **mod 签名暂定性未升级为阻断项**（迭代第 2 轮 #4 → 当前 #4）：连续 2 轮提出，严重程度从中等加重。需在 §8 新增阻断项。
5. **编码模板示例使用 [] 访问**（迭代第 2 轮 #5 → 当前 #5）：连续 2 轮提出。需修改模板示例代码。
6. **scalar_mat_ops 与 scalar_vec_ops 重载歧义**（迭代第 2 轮 #6 → 当前 #6）：连续 2 轮提出。需在 §8 新增验证项。
7. **geometric.cj Vec1 版本潜在覆盖差异**（迭代第 2 轮 #7 → 当前 #7）：连续 2 轮提出。需查验 GLM 源码确认，若确认遗漏则补齐，否则说明省略理由。
8. **fromMat 纯收缩方向 one 参数未在 §9 声明**（迭代第 2 轮 #8 → 当前 #8）：连续 2 轮提出。需在 §9 差异声明中新增条目。

### 新发现的问题（本轮新识别的问题）

9. **fwd.hpp 精度族统计误导**（当前 #9）：本轮新发现，需在 §10 加注说明。
10. **矩阵测试文件规划缺失**（当前 #10）：本轮新发现，需在 §8 产出物清单中补充。
11. **fwd.cj 别名导入连续性确认**（当前 #11）：本轮新发现，需在 §8 别名清单前补充说明。

## 上一轮产出路径

C:\Develop\Software\cjglm_wp\harness\redeliberations\202606231014_OOD_stage2_migration_v3\a_v2_copy_from_v1.md

## 用户需求

C:\Develop\Software\cjglm_wp\harness\redeliberations\202606231014_OOD_stage2_migration_v3\requirement.md
