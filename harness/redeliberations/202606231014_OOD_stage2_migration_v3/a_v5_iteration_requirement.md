根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

### 问题 1（严重）：§3.3 第 338 行声明的"非 B 类方向对照分析"完全缺失，63 个混合方向仅基于理论推导、未经 GLM 源码逐项验证
- 所在位置：§3.3 第 338 行（声明"非 B 类方向对照分析"后续应存在），§3.3 第 340-354 行（仅提供组合规则而无对照分析），§3.3 9×9 转换矩阵表（第 372-382 行，63 个混合方向）
- 改进建议：在 B 类对照分析表后补充 3-5 个代表性混合方向的 GLM 源码逐项对照（建议：Mat2x2→Mat3x3 即 colExt(3)→rowExt、Mat3x2→Mat2x3 即 colSh→rowExt、Mat4x2→Mat2x4 即 colSh→rowExt、Mat2x3→Mat3x2 即 colExt(3)→rowSh、Mat3x3→Mat2x2 即 colSh→rowSh），覆盖"双扩展""列缩行扩""列扩行缩""双收缩"四类组合；或删除第 338 行对"非 B 类方向对照分析"的虚假承诺，明确声明混合方向仅基于理论推导，编码阶段须逐方向构造非平凡测试用例与 GLM 结果对照
- 质询确认：LOCATED——经实际读取确认第 338 行确实包含该声明但全文无任何非 B 类方向对照分析，"完全缺失"判定正确

### 问题 2（中等）：别名总量统计 108 与对照汇总表的阶段实现数 54 直接矛盾
- 所在位置：§8 第 886 行（108 统计）；§8 第 894 行（PascalCase 本阶段实现 18）；§8 第 900 行（合计 54）
- 改进建议：将第 886 行的统计改为按"本阶段实现"口径计算："PascalCase 9×2=18 个（2 默认精度族）+ 方阵短别名 8×3=24 个 + 非方阵 camelCase 2×6=12 个 = **54 个新增别名**（与 §10 对照汇总表一致）"。原 108 数字改为"GLM 全量 108 个"标注，与 54 区分
- 质询确认：LOCATED——第 886 行 108 与汇总表 54 口径不一致，报告重新计算 18+24+12=54 与汇总表一致

### 问题 3（中等）：geometric.cj 和 matrix.cj 的 outerProduct/matrixCompMult stub 签名使用 Number<T> 约束过于宽松——GLM 对这些函数仅允许浮点类型，当前约束允许整数实例化
- 所在位置：§3.7 geometric.cj 签名清单（第 697-717 行）；§3.7 matrix.cj outerProduct/matrixCompMult 签名（第 671-679、660-669 行）
- 改进建议：(1) 在 geometric.cj 签名清单和 matrix.cj outerProduct/matrixCompMult 签名旁添加注释，标注"GLM 仅允许浮点类型（is_iec559 断言），当前 Number<T> 约束为仓颉泛型机制限制下的最紧约束，允许整数实例化但行为与 GLM 不一致。阶段四实现时若仓颉提供浮点专用接口则收紧约束，否则须在实现中添加运行时断言"；(2) 补充说明 transpose 在 GLM 中也有 is_iec559 断言但可通过 UNRESTRICTED_GENTYPE 绕过，当前放松约束的设计决策应在 D01-D35 决策表中显式记录
- 质询确认：LOCATED——报告未简单要求修正约束，而是识别出"约束差异应在 stub 阶段就标注"这一实际问题

### 问题 4（中等）：equalEpsilon 约束 Number<T> & Equatable<T> & Comparable<T> 中 Equatable<T> 为冗余声明——Comparable<T> 已继承 Equatable<T>
- 所在位置：§3.6 比较运算符（第 609 行附近）
- 改进建议：将 Number<T> & Equatable<T> & Comparable<T> 简化为 Number<T> & Comparable<T>，与 v35 对 abs/sign 的约束修正一致。同步修正阶段一 Vec 类型实现中的相同冗余约束（type_vec1.cj:162、type_vec2.cj:143、type_vec3.cj:152、type_vec4.cj:161、compute_vector_relational.cj:11），以及在 OOD 文档中声明此简化为项目统一风格
- 质询确认：LOCATED——Comparable<T> 确实继承 Equatable<T>，冗余事实成立

### 问题 5（一般）：diagonal(scalar: T) 与 GLM mat(T s) 的接口形态偏差未在 §9 差异声明中声明
- 所在位置：§9 差异声明表 diagonal 条目（第 927 行）
- 改进建议：在 §9 diagonal 差异声明中补充：GLM 中 mat(T s) 为构造函数语法（如 mat2x3(1.0f)），仓颉中为 Number<T> 约束的 extend 块静态工厂函数（如 Mat2x3.diagonal(1.0f)），不可通过构造函数语法调用。此为仓颉值类型限制导致的接口形态偏差
- 质询确认：LOCATED——与迭代 2 问题 1（覆盖范围问题）是不同维度的差异

### 问题 6（一般）：附录 A 中方阵 *=(Mat) 和 /=(Mat) 的状态表述不精确——未明确"编译器自动生成"与"手动定义回退"的边界
- 所在位置：附录 A 表格（第 1179-1194 行）；§3.5 复合赋值段落（第 576 行）；附录 A 前注释和 † 图例（第 1191 行）
- 改进建议：在附录 A 前注释中显式声明：(1) 方阵 *=(Mat 同尺寸) 和 /=(Mat 同尺寸) 已由编译器自动生成（返回类型匹配），其函数体为 this = this * rhs 即 this * inverse(rhs)，因此自动生成版在 stub 阶段执行时抛出 inverse 的 stub 异常——无需手动定义；(2) 附录 A 中方阵行的 *=(Mat) 和 /=(Mat) 标注仅表示"若编译器版本回归导致自动生成失效的防御路径"，正常编码路径中不编写此两行代码
- 质询确认：LOCATED——核心困惑点在"编码者是否需要为方阵手动编写代码"的操作指导层面矛盾

### 问题 7（一般）：[] 取值版本"返回列向量副本"的表述可能误导编码者对性能的预期
- 所在位置：§3.4 [] 取值版本描述（第 474 行）；§9 差异声明"[] 返回副本而非引用"（第 921 行）
- 改进建议：将"返回列向量副本"修正为"返回列向量（struct 值语义下为副本，无额外拷贝开销）"，消除性能误导
- 质询确认：LOCATED

### 问题 8（一般）：identity(one) 缺少 C > R 非方阵编码模板示例——编码者对"整列全 T(0)"的边界场景缺乏直观参考
- 所在位置：§3.3 item 8 identity 编码路径说明和示例（第 296-307 行）
- 改进建议：在 identity 编码路径后补充 1 个 C > R 非方阵模板示例（如 Mat3x2<Float32, PackedHighp>.identity(1.0f) → c0=Vec2(1.0, 0.0), c1=Vec2(0.0, 1.0), c2=Vec2(0.0, 0.0)），确认编码者正确处理"列索引 i ≥ R 时整列全 T(0)"的边界
- 质询确认：LOCATED

### 问题 9（一般）：scalar_mat_ops.cj 与 scalar_vec_ops.cj 同名重载歧义验证范围不足——仅提到 add，未覆盖 sub/mul/div 三组
- 所在位置：§8 编码启动前验证项（第 828 行）
- 改进建议：将验证范围从"add 等同名全局函数"扩展为"add/sub/mul/div 四组同名全局函数"，并补充关键风险场景标注：div(s: T, m) 中当 T = Int32 时，整数标量除以矩阵各分量的语义与 GLM mat / scalar 不一致（GLM 为逐分量除法，标量在右侧为主流设计），当前 scalar_mat_ops.cj 中 div(s, m) 的实际展开为 m / s 的等价变换——需验证此语义等价性
- 质询确认：LOCATED

## 历史迭代回顾

### 已解决的问题（出现在历史反馈但当前反馈中不再提及的问题）
- 迭代 1 问题 1：[] 赋值版本签名双重语法违规——已在 v37 中修正
- 迭代 1 问题 2：复合赋值运算符自动生成标注为"待验证假设"——已在 v37 中明确声明为语言规范保证
- 迭代 1 问题 3：type_float.cj / type_half.cj 需求覆盖遗漏——已在 v37 §7 新增 D35 决策
- 迭代 1 问题 4：lib.cj 中 mod 函数与矩阵标量运算导出交互——已在 v37 中显式说明
- 迭代 1 问题 5：abs/sign 的泛型约束 Equatable<T> 应为 Comparable<T>——已在 v35 修正
- 迭代 1 问题 6：矩阵-矩阵除法签名缺失——已在 v37 中补充 3 个方阵除法签名
- 迭代 2 问题 1：diagonal 仅对方阵提供——已在 v37 中扩展为对所有 9 个矩阵类型均提供
- 迭代 2 问题 2：矩阵除法奇异矩阵行为未定义——已在 v37 中明确
- 迭代 2 问题 3：Vec2 cross 未在 §10 追踪——已在 v37 中追加备注
- 迭代 2 问题 4：common.cj mod 签名暂定性验证项——已纳入 §8
- 迭代 2 问题 5：编码模板使用下标访问而非属性访问——已在 v37 中修正为属性访问
- 迭代 2 问题 6：scalar_mat_ops.cj 与 scalar_vec_ops.cj 重载歧义验证——已部分纳入 §8（但验证范围不足，见当前问题 9）
- 迭代 2 问题 7：geometric.cj 缺少 Vec1 版本——已查验并处理
- 迭代 2 问题 8：fromMat 纯收缩方向 one 参数差异声明——已在 v37 中处理
- 迭代 3 问题 1：colExtend 定义中"已包含行扩展/收缩"与先列后行组合规则矛盾——已在 v37 中修正
- 迭代 3 问题 2：R_dst 获取方式描述前后矛盾——已在 v37 中修正
- 迭代 3 问题 3：27 个跨尺寸乘法编码模板缺失——已在 v37 §3.5 新增跨尺寸矩阵乘法定义和 2 个编码模板
- 迭代 3 问题 4：同包前向类型引用验证未列入 §8 阻断项——已提升
- 迭代 3 问题 5：colExtend 中 VecR 未标注是 R_src 还是 R_dst——已修正
- 迭代 3 问题 6：matrix.cj 分类为"部分实现文件"但 §8 归入纯 stub 批次——已明确标注为"混合型文件"

### 持续存在的问题（在多轮反馈中反复出现的问题，需重点解决）
- **非 B 类方向对照分析缺失**（迭代 4 问题 1，当前仍为严重）：第 338 行声明"非 B 类方向对照分析"但从未兑现，经迭代 4 质询确认完全缺失。本轮必须解决——补充代表性混合方向对照或删除虚假承诺
- **别名总量统计矛盾**（迭代 4 问题 2，当前升级为中等）：108 vs 54 矛盾。虽前期未以统计矛盾形式出现，但根源问题一直存在
- **约束差异未标注**（迭代 4 问题 3，当前升级为中等）：Number<T> 约束与 GLM is_iec559 断言的差异在 v37 中仍未标注，阶段四实现时将导致签名 breaking change
- **Equatable 冗余约束**（迭代 4 问题 4，当前升级为中等）：v35 已修正 abs/sign 的同类冗余，但 equalEpsilon 处仍未简化
- **scalar_mat_ops/vec_ops 重载验证范围不足**（迭代 2 问题 6 首次提出，迭代 4 问题 9 再次指出）：仅验证 add 未覆盖 sub/mul/div

### 新发现的问题（本轮新识别的问题）
- 问题 5（diagonal 接口形态偏差未声明）：此前仅识别了 diagonal 覆盖范围问题（迭代 2），未识别接口形态偏差
- 问题 6（附录 A 方阵 *=(Mat)/=(Mat) 操作指导矛盾）：迭代 1 问题 2 确认了自动生成的确定性，但未解决操作指导层面的矛盾
- 问题 7（"返回副本"表述的性能误导）：此前未关注
- 问题 8（identity 缺少 C > R 非方阵示例）：此前仅提供 R > C 示例

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606231014_OOD_stage2_migration_v3\a_v4_copy_from_v3.md

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606231014_OOD_stage2_migration_v3\requirement.md
