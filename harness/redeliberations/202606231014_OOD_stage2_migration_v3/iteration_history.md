## 迭代第 1 轮

1. **问题描述**：`[]` 赋值版本签名存在双重语法违规——参数名未使用 `value!` 命名参数，且缺少 `mut` 修饰符，将直接导致编译失败。此为历史迭代已修正问题的回归
   - 所在位置：§3.4 行列访问（第 474 行）
   - 严重程度：严重
   - 改进建议：将签名修正为 `public mut operator func [](i: Int64, value!: VecR<T,Q>): Unit`，全文检查所有 `[]` 赋值签名统一修正

2. **问题描述**：复合赋值运算符自动生成被标注为"待验证假设"，但仓颉语言规范已明确声明为确定性保证，导致文档包含不必要的手动回退方案且可能误导编码者
   - 所在位置：§3.5 复合赋值运算符段落；§8 编码启动前验证项
   - 严重程度：一般
   - 改进建议：明确声明自动生成为语言规范保证行为，将手动回退方案移至独立附录标注为编译器 bug 防御方案，从 §8 验证项中删除或降级为非阻断性可选测试

3. **问题描述**：路线图可选的 `type_float.cj` / `type_half.cj` 在设计文档中完全未提及，需求覆盖存在遗漏
   - 所在位置：全文——§1 概述、§2 模块划分、§7 设计决策表、§8 产出物清单
   - 严重程度：一般
   - 改进建议：在 §7 设计决策表中新增决策，明确 `type_float.cj` / `type_half.cj` 的阶段归属及取舍理由

4. **问题描述**：`lib.cj` 新增导出清单中 `mod` 函数与矩阵标量运算导出的交互未显式说明
   - 所在位置：§2 scalar_mat_ops.cj 条目；§8 lib.cj 新增导出清单
   - 严重程度：一般
   - 改进建议：在 §8 lib.cj 导出清单中显式说明 `mod` 不在矩阵标量运算中提供，现有 `mod` 导出仅覆盖向量版且无需修改

5. **问题描述**：`abs` 和 `sign` 的泛型约束选用 `Equatable<T>` 而非 `Comparable<T>`，无法满足实现所需的 `<` 比较运算接口
   - 所在位置：§3.7 common.cj — `abs` 和 `sign` 签名（第 585-586 行）
   - 严重程度：一般
   - 改进建议：将约束从 `where T <: Number<T> & Equatable<T>` 修正为 `where T <: Number<T> & Comparable<T>`

6. **问题描述**：矩阵-矩阵除法 `Mat / Mat` 的运算符签名完全缺失，编码者无法确定方阵除法运算符的归属和签名
   - 所在位置：§3.5 算术运算符；§10 API 覆盖矩阵
   - 严重程度：一般
   - 改进建议：在 §3.5 补充方阵矩阵-矩阵除法的 3 个签名，标注内部实现为 `this * inverse(rhs)`（依赖 inverse stub），同步更新 §10
## 迭代第 2 轮

1. **问题描述**：`diagonal(scalar: T)` 仅对方阵提供，非方阵不提供，但 GLM 对所有 9 个矩阵类型均提供 `mat(T scalar)` 构造函数（含非方阵），API 存在对称性缺陷
   - 所在位置：§3.3 item 3 diagonal 构造函数说明（L231-236）；§7 D34（L753）；§9 差异声明 diagonal 条目（L868）
   - 严重程度：一般
   - 改进建议：对所有 9 个矩阵类型均提供 `diagonal(scalar)`，与 `identity(one)` 形成完整配对；若保留当前设计，在 §9 差异声明中明确声明此为仓颉设计与 GLM `mat(T s)` 的行为偏差

2. **问题描述**：矩阵-矩阵除法 `Mat / Mat` 在 `rhs` 不可逆（奇异矩阵）时的行为未定义，`inverse` 的奇异矩阵行为策略缺失
   - 所在位置：§3.5 矩阵-矩阵除法签名（L515-521）；§5 错误处理策略（L713）
   - 严重程度：一般
   - 改进建议：在 §5 错误处理策略中新增"矩阵除法奇异矩阵行为"条目，明确 `inverse` 对奇异矩阵的行为选择（抛异常 / 返回未定义值），并在差异声明中相应标注

3. **问题描述**：Vec2 `cross`（2D 叉积，属 gtx/exterior_product）未在 §10 追踪，可能导致阶段六实施者遗漏此 API
   - 所在位置：§3.7 geometric.cj 完整函数签名清单（L649）；§10 geometric 函数覆盖矩阵
   - 严重程度：一般
   - 改进建议：在 §10 中为 `cross` 追加备注"Vec2 `cross` 属 gtx/exterior_product，后续阶段六覆盖"

4. **问题描述**：`common.cj` 的 `mod` 签名暂定性未在 §8 编码启动前验证项中列为阻断性风险项，`Number<T>` 约束的 `mod` 对 Float32/Float64 为确定性编译期错误
   - 所在位置：§3.7 common.cj mod 签名（L578-579）；§8 编码启动前验证项（L771）
   - 严重程度：一般
   - 改进建议：在 §8 编码启动前验证项中新增阻断项：验证 `mod<T>` 对 Float32/Float64 是否可编译，若不能则拆分签名

5. **问题描述**：编码模板示例使用 `m.c0[0]` 下标访问而非 `m.c0.x` 属性访问，将带来不必要的 assert 开销
   - 所在位置：§3.3 编码模板示例 1（L406-423）
   - 严重程度：一般
   - 改进建议：将模板示例中 `[0]`/`[1]` 改为 `.x`/`.y` 属性访问，注明优先使用属性访问的理由

6. **问题描述**：`scalar_mat_ops.cj` 新增的 36 个全局函数与 `scalar_vec_ops.cj` 同名函数重载解析未纳入 §8 编码启动前验证项
   - 所在位置：§8 编码启动前验证项（L771）
   - 严重程度：一般
   - 改进建议：在 §8 验证项中增加：验证 `add(s: T, m: Mat2x2<T,Q>)` 与 `add(s: T, v: Vec2<T,Q>)` 重载无歧义

7. **问题描述**：`geometric.cj` stub 中缺少 Vec1 版本的 `length`/`normalize`/`distance`/`reflect`/`refract`，与 GLM 之间存在潜在覆盖差异（待查验确认）
   - 所在位置：§3.7 geometric.cj 完整函数签名清单（L650-664）；§10 geometric 函数覆盖矩阵
   - 严重程度：一般
   - 改进建议：查验 GLM `func_geometric.inl` 确认 Vec1 版本是否实际提供特化；若确认遗漏则补齐，若为有意省略则在文档中说明理由

8. **问题描述**：`fromMat` 6a/6b 纯收缩方向 `one` 参数被忽略的行为未在 §9 差异声明中显式声明
   - 所在位置：§3.3 item 6 纯收缩方向 `one` 参数行为说明（L286 附近）；§9 差异声明
   - 严重程度：一般
   - 改进建议：在 §9 差异声明中新增条目，声明此为仓颉泛型限制导致的 API 设计偏差


## 迭代第 3 轮

1. **问题描述**：`colExtend` 定义中"已包含行扩展/收缩"的注解与先列后行组合规则直接矛盾，可能导致编码者将行扩展逻辑错误混入 colExtend 步骤
   - 所在位置：§3.3 fromMat 四项基本操作定义 — `colExtend` 条目（第 343-344 行）
   - 严重程度：中等
   - 改进建议：将"列向量复制（已包含行扩展/收缩）"修正为"列向量复制（行维度不变，保持源行数 R_src）"，明确 colExtend 结果矩阵行数等于 R_src

2. **问题描述**：fromMat 6a/6b 编码策略中 R_dst 获取方式描述先暗示可通过函数调用获取、后又否定，前后矛盾导致编码者困惑
   - 所在位置：§3.3 "规则化的编码策略"段落 — "维度信息的获取方式"子段（第 389-392 行）
   - 严重程度：中等
   - 改进建议：删除暗示函数调用获取的前半句，直接陈述"R_src 和 R_dst 无法通过仓颉静态查询函数获取，在每个 fromMat 重载的函数体中硬编码为字面量"

3. **问题描述**：27 个 Mat×Mat 跨尺寸乘法运算符仅提供签名表和结果类型推导规则，缺少编码模板或实现说明，编码者面对 24 个方向的高工作量且易出错的展开逻辑缺乏指导
   - 所在位置：§3.5 算术运算符 — 27 个乘法重载签名表（第 485-513 行）
   - 严重程度：中等
   - 改进建议：补充 1~2 个代表性方向的编码模板示例，注明统一展开定义 `result[j][i] = sum_{k=0}^{C_left-1} left[k][i] * right[j][k]`

4. **问题描述**：同包前向类型引用延迟解析被标记为待验证但未列入 §8 编码启动前阻断性验证项，若验证失败且 fallback 均不可行则 Vec×Mat 运算符 API 形态需重新决策，属设计层面风险
   - 所在位置：§8 编码启动前验证项（第 771 行附近），§8 compile order 说明（第 783 行）
   - 严重程度：一般
   - 改进建议：将该原型验证提升为 §8 编码启动前阻断性验证项，明确阻断条件及 API 形态退化时的决策路径

5. **问题描述**：`colExtend` 定义中"VecR(...)"的 R 未明确标注是 R_src 还是 R_dst，编码者可能误解
   - 所在位置：§3.3 fromMat 四项基本操作定义 — `colExtend` 条目（第 344 行）
   - 严重程度：一般
   - 改进建议：将"VecR(...)"改为"VecRdst(...)"或标注"VecR 此处 R = R_dst"

6. **问题描述**：`matrix.cj` 包含 27 个可直接实现函数和 6 个 stub，被归类为"部分实现文件"但在 §8 编码顺序中被归入纯 stub 批次，可能误导编码优先级判断
   - 所在位置：§1 核心抽象表（第 19 行）及 §2/§8 相关位置
   - 严重程度：一般
   - 改进建议：将 matrix.cj 分类明确标注为"混合型文件"，在 §8 编码顺序中将其从纯 stub 批次独立或添加注释说明
## 迭代第 4 轮

1. **问题描述**：§3.3 第 338 行声明的"非 B 类方向对照分析"完全缺失，63 个混合方向仅基于理论推导、未经 GLM 源码逐项验证
   - 所在位置：§3.3 第 338 行（声明"非 B 类方向对照分析"后续应存在），§3.3 第 340-354 行（仅提供组合规则而无对照分析），§3.3 9×9 转换矩阵表（第 372-382 行，63 个混合方向）
   - 严重程度：严重
   - 改进建议：在 B 类对照分析表后补充 3-5 个代表性混合方向的 GLM 源码逐项对照（覆盖"双扩展""列缩行扩""列扩行缩""双收缩"四类组合），或删除第 338 行对"非 B 类方向对照分析"的虚假承诺，明确声明混合方向仅基于理论推导

2. **问题描述**：别名总量统计 108 与对照汇总表的阶段实现数 54 直接矛盾
   - 所在位置：§8 第 886 行（108 统计）；§8 第 894 行（PascalCase 本阶段实现 18）；§8 第 900 行（合计 54）
   - 严重程度：一般
   - 改进建议：将第 886 行统计改为按"本阶段实现"口径计算 54 个，原 108 数字改为"GLM 全量 108 个"标注

3. **问题描述**：outerProduct/matrixCompMult stub 签名使用 Number<T> 约束过于宽松，GLM 仅允许浮点类型
   - 所在位置：§3.7 geometric.cj 签名清单（第 697-717 行）；§3.7 matrix.cj outerProduct/matrixCompMult 签名（第 671-679、660-669 行）
   - 严重程度：一般
   - 改进建议：在签名旁添加约束差异注释，标注 Number<T> 为仓颉泛型机制限制下的最紧约束，阶段四实现时可能需收紧

4. **问题描述**：equalEpsilon 约束中 Equatable<T> 为冗余声明——Comparable<T> 已继承 Equatable<T>
   - 所在位置：§3.6 比较运算符（第 609 行附近）
   - 严重程度：一般
   - 改进建议：将 Number<T> & Equatable<T> & Comparable<T> 简化为 Number<T> & Comparable<T>，与 v35 同类修正一致

5. **问题描述**：diagonal(scalar) 与 GLM mat(T s) 的接口形态偏差（构造函数 vs 工厂函数）未在 §9 差异声明中声明
   - 所在位置：§9 差异声明表 diagonal 条目（第 927 行）
   - 严重程度：一般
   - 改进建议：在 §9 diagonal 差异声明中补充接口形态偏差说明

6. **问题描述**：附录 A 中方阵 *=(Mat) 和 /=(Mat) 的状态表述不精确——编码者不确定是否需要手动编写代码
   - 所在位置：附录 A 表格（第 1179-1194 行）；§3.5 复合赋值段落（第 576 行）
   - 严重程度：一般
   - 改进建议：在附录 A 前注释中显式声明方阵 *=(Mat)/=(Mat) 已由编译器自动生成，正常编码路径中不编写此两行代码

7. **问题描述**：[] 取值版本"返回列向量副本"的表述可能误导编码者对性能的预期
   - 所在位置：§3.4 [] 取值版本描述（第 474 行）；§9 差异声明（第 921 行）
   - 严重程度：一般
   - 改进建议：将"返回列向量副本"修正为"返回列向量（struct 值语义下为副本，无额外拷贝开销）"

8. **问题描述**：identity(one) 缺少 C > R 非方阵编码模板示例
   - 所在位置：§3.3 item 8 identity 编码路径说明和示例（第 296-307 行）
   - 严重程度：一般
   - 改进建议：补充 1 个 C > R 非方阵模板示例（如 Mat3x2）

9. **问题描述**：scalar_mat_ops.cj 与 scalar_vec_ops.cj 同名重载歧义验证范围不足——仅提到 add，未覆盖 sub/mul/div 三组
    - 所在位置：§8 编码启动前验证项（第 828 行）
    - 严重程度：一般
    - 改进建议：将验证范围从 add 扩展为 add/sub/mul/div 四组，并补充 div 整数语义风险标注

## 迭代第 5 轮

1. **问题描述**：fromMat 6b T(0) 演算路径存在类型系统缺陷。6b 中 `m.c0.x` 类型为 U（源元素类型），`m.c0.x - m.c0.x` 计算 U(0) 而非 T(0)，且 6b 约束未声明 `U <: Number<U>`，直接沿用 6a 模板中的零值演算路径将引发编译错误或类型不匹配。
    - 所在位置：§3.3 编码模板说明（第467-472行）、§3.3 item 6b 模板示例区（第280-282行附近）
    - 严重程度：一般
    - 改进建议：将 6b 的 T(0) 演算路径从 `m.c0.x - m.c0.x` 修正为 `one - one`；为 6b 单独提供编码模板示例；若 U 需参与运算，增加 `U <: Number<U>` 约束。

2. **问题描述**：div(s, m) 语义歧义仍未在设计层面锁定。`add`/`sub`/`mul` 语义一致但 `div(s, m)` 若实现为 `m / s` 则与参数顺序暗示的 `s / m` 语义相反，将不确定性推迟至编码阶段可能导致连锁修改。
    - 所在位置：§8 编码启动前验证项（第844行末尾）、§3.5 scalar_mat_ops.cj（第591-613行）
    - 严重程度：一般
    - 改进建议：在 §7 设计决策表中新增决策项锁定 `div(s, m)` 最终语义，选择 `s / m` 或 `m / s` 并给出展开公式，移除"待验证"标注。

3. **问题描述**：§10 API 覆盖矩阵缺少 fromParts/fromColumns/fromMat 系列工厂函数追踪条目。fromParts（item 4）、fromColumns（item 5）、fromMat 6a/6b（item 6）、fromMat 7（item 7）共 5 个函数组完全未在覆盖矩阵中追踪。
    - 所在位置：§10 mat2x2~mat4x4.hpp 覆盖矩阵（第1004-1025行）
    - 严重程度：一般
    - 改进建议：在 §10 覆盖矩阵中新增上述 5 个函数组的行级追踪条目，标注实现阶段归属。

4. **问题描述**：fromMat 6a/6b 的 `one` 参数 NaN 值行为未在设计层面定义。`one` 完全由调用方传入，若误传 NaN 则对角填充结果全为 NaN，identity(one) 也存在同样问题。
    - 所在位置：§3.3 item 6a/6b one 参数语义说明（第276-282行）、§3.3 item 8 identity(one) 行为定义（第293-318行）
    - 严重程度：一般
    - 改进建议：在 one 参数语义说明和 identity 行为定义中补充 NaN 值行为约束："调用方应确保 one 为非 NaN 值，否则行为未定义"；或定义为"当 one 为 NaN 时对角填充自动退化为全 NaN 填充"。

## 迭代第 6 轮

1. **问题描述**：§8 矩阵别名命名规则说明声称"仓颉不支持下划线别名命名"，与 `src/fwd.cj` 第 19 行已大量使用下划线别名（如 `highp_int8`、`highp_float`）的事实直接矛盾
   - 所在位置：§8 fwd.cj 矩阵别名清单 → 方阵短别名命名规则说明（第 915 行）
   - 严重程度：严重
   - 改进建议：删除错误陈述，修正为与向量别名风格一致的驼峰拼接决策理由

2. **问题描述**：§8 验证项中"36 标量-向量"实际应为 16（4 运算 × 4 Vec 类型），"144 对函数签名消歧"缺乏精确数学推导，可能误导编码者关于验证范围的判断
   - 所在位置：§8 编码启动前验证项 → scalar_mat_ops.cj/scalar_vec_ops.cj 同名函数重载解析验证（第 869 行）
   - 严重程度：一般
   - 改进建议：修正数字为精确描述，将"36 标量-向量"改为"16 标量-向量"并调整相应计算
