根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

### P1（严重）：GLM_CONFIG_SWIZZLE 用途说明列 mapping 与 GLM 1.0.3 源码事实不符
第 303 行 GLM_CONFIG_SWIZZLE 的用途说明列写"0=operator, 1=function, 2=disabled"。经核查 GLM 1.0.3 实际定义为：`GLM_SWIZZLE_DISABLED=0, GLM_SWIZZLE_OPERATOR=1, GLM_SWIZZLE_FUNCTION=2`。正确映射应为"0=disabled, 1=operator, 2=function"。此错误在第 9 轮曾部分标记（值=2→=0 已更正），但说明列 mapping 仍完全颠倒。改进建议：将用途说明修正为"控制 swizzle 操作：0=disabled, 1=operator, 2=function。设为 disabled"

### P2（严重）：修订说明（v10）仍嵌入交付物正文，阻塞直接交付
第 699-707 行的"修订说明（v10）"章节记录了本轮迭代的处理方式。该问题在第 3、5、7 轮已被反复标记且严重程度为"严重"，v10 仍未解决。改进建议：删除"修订说明（v10）"章节

### P3（一般）：§4.6 判定标准与验证结论仍使用"预期"措辞残留
§4.6 技术挑战表中"对应 §4.4 范围定义标准"列（第 597-602 行）共 6 处使用"预期：全部通过"/"预期：通过"语言；验证结论段落（第 604 行）也包含"处于设计预期"措辞。虽然 §4.4 已重构，但 §4.6 引用仍在使用"预期"语言。改进建议：将"预期：全部通过"改为"判定条件：全部通过"或直接使用事实性陈述；将"处于设计预期"改为"已定义且满足设计要求"

### P4（轻微）：§0 引言包含元注释性升级表述
第 9 行"将此前版本中的'假设'升级为明确结论"暗示文档经过版本迭代，与"基线分析"定位不协调。改进建议：删除该句或改写为"已将各项迁移所需语言能力的评估从推测性判断明确为确定性结论"

## 历史迭代回顾

### 已解决的问题
- §4.1 标题残存"首轮"字样（第 1 轮）→ 已修复
- 附录 A "首轮不包含"措辞（第 1 轮）→ 已修复
- §4.2 定量依赖佐证与 §2 数据重复（第 1 轮）→ 已修复
- §1 swizzle 规划性措辞残留（第 2 轮）→ 已修复
- 文档定位声明与附录 A 规划性内容矛盾（第 2 轮）→ 已修复
- Vec1 纳入理由中"共享模板机制"论证不当（第 2 轮）→ 已修复
- §2 多视角重叠子节合并不充分（第 2 轮）→ 已修复
- §3 标题"已实现的迁移范围"语义张力（第 3 轮）→ 已修复
- §0.1 未被采纳方案② 仍描述为并列选项（第 3 轮）→ 已修复
- §0.5.1 推测性措辞（第 3 轮）→ 已修复
- §4.6 迁移风险评估系统性未来时态（第 3 轮）→ 已修复
- @OverflowWrapping 标注数量事实验证依据（第 5 轮）→ 已修复
- 附录 A 信息密度过低（第 5 轮）→ 已修复
- §4.4 验收准则验证主张缺乏可追溯证据（第 6 轮）→ 通过 §4.4 重构为范围定义标准解决
- §0.5.1 "后续优化方向"等规划性语言（第 6 轮）→ 已修复
- "迁移顺序"执行计划措辞（第 6 轮）→ 已修复
- 附录 A "当前状态"列规划性措辞（第 6 轮）→ 已修复
- §4.4 验收准则验证元信息不足（第 7 轮）→ 通过 §4.4 重构解决
- §4.6 "可管理"缺乏客观判定标准（第 7 轮）→ 已增加独立判定标准
- 排除组件对 Vec 公共 API 影响分析缺失（第 7 轮）→ 已补充排除影响分析
- §0.7.2 "v9 升级"标记（第 8 轮）→ 已修复
- 运算符重载委托模式与自动推导方向相反（第 8 轮）→ 已修复
- §4.6 "可管理"循环论证（第 8 轮）→ 已修复
- 定位声明边界模糊（第 8 轮）→ 已修复
- §0.5.1 "标注遗漏风险说明"（第 8 轮）→ 已修复
- §3F 缺少 `package` 声明映射、`import` 依赖矩阵、shim 归属（第 9 轮）→ 已补充
- 附录 A 文件数统计来源（第 9 轮）→ 已补充
- GLM_CONFIG_SWIZZLE 枚举值标注错误（=2→=0）（第 9 轮）→ 已更正
- §4.4 "预期"措辞（第 9 轮）→ 通过 §4.4 重构解决

### 持续存在的问题
- **修订说明嵌入交付物正文**（第 3、5、7、10 轮持续出现）：已反复标记 4 轮仍未根除，每次仅将旧版修订说明替换为新版，未将修订说明彻底从交付物中移除。需从根本上解决——彻底删除"修订说明"章节，而非更新其内容
- **GLM_CONFIG_SWIZZLE mapping 事实错误**（第 9 轮值标注错误已更正，但第 10 轮发现说明列 mapping 完全颠倒）：值=2→=0 已更正，但用途说明列仍写"0=operator, 1=function, 2=disabled"，实际应为"0=disabled, 1=operator, 2=function"。同一行存在两个维度的问题，第二个维度被遗漏，需彻底修正
- **"预期"措辞残留**（第 6、7、8、9 轮涉及 §4.4 的"预期"措辞已通过重构解决，但同类型措辞在 §4.6 仍存在）：体现为同一类型问题在不同章节间"迁移"的趋势，需全局排查所有章节中的"预期""建议""将"等未来/推测性措辞，而非仅修补被标记的特定行

### 新发现的问题
- §0 引言第 9 行"将此前版本中的'假设'升级为明确结论"暗示文档版本迭代历史，与"基线分析"定位不协调（P4，轻微）

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606202257_roadmap_downgrade\a_v10_output_v1.md

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606202257_roadmap_downgrade\requirement.md
