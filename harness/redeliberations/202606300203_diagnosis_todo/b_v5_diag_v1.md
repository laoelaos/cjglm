# 质量审查报告：a_v5_diag_v1.md（第 5 轮）

## 审查范围

- **审查对象**：`a_v5_diag_v1.md` — Phase 4 Review Issues 诊断报告（v5 修订版）
- **审查维度**：需求响应充分度、事实错误与逻辑矛盾、深度与完整性、可操作性
- **审查方式**：逐项核查 + 交叉验证（与 `todo.md`、`deviations.md` 对照）

---

## 发现的问题

### 问题 1：[严重] 汇总表中 G24 根因分类与正文矛盾

- **问题描述**：修改流程建议的「按根因分类汇总」表（第468行）将 G24 归入"**测试覆盖遗漏 | G13-G37（全部测试不足 issue）**"组。但正文第 343-344 行明确 G24 的**真实性**为"设计争议"、**根因**为"设计争议（未明确零轴行为）"，修改方向为"需明确设计决策"。G24 不是测试覆盖问题，不能与纯测试不足的 issue 混为一谈。
- **所在位置**：第 468 行（汇总表）vs 第 342-347 行（正文诊断）
- **严重程度**：严重
- **改进建议**：将 G24 从"测试覆盖遗漏"行中排除，在汇总表中新增"设计待决"类别单独列出 G24，或将其移至"OOD 设计决策（非问题）"对应的行。修改方向需保留"需先完成 OOD 决策"的特殊性。

---

### 问题 2：[一般] G15 与 G24 中引用的 `deviations.md` 内容不存在

- **问题描述**：两处诊断声称参考了 `docs/deviations.md` 中的特定内容，但经全文检索确认这些内容在 deviations.md 中**不存在**：
  1. **G15 第 285 行**："根据 `docs/deviations.md`，Cangjie 的 `std.math` 对 Float16 的支持通过 `T(Float64(n))` 字面量替代路径实现（见 `docs/05_ood_phase3.md` 中 H1 验证）"——deviations.md 中仅在 DV-02 的 `mod(s,v)` 函数中提及 Float16，无任何关于 Float16 通用支持路径的表述，更未提及 `T(Float64(n))` 字面量替代路径。该信息实际来源于 OOD 设计文档，而非 deviations.md。
  2. **G24 第 345 行**："根据 `docs/deviations.md`，Cangjie 的 `cross(零向量, v)` 行为取决于实现。该问题涉及……这是一个已知的库行为边界问题，非 Cangjie 特有偏差"——deviations.md 中无任何关于 `cross(零向量, v)` 行为或零轴旋转的偏差记录，该问题并非 deviations.md 中"已知的库行为边界问题"。
- **所在位置**：G15 第 285 行、G24 第 345 行
- **严重程度**：一般
- **改进建议**：
  - G15：将"根据 `docs/deviations.md`"改为"根据设计文档 `docs/05_ood_phase3.md` 中 H1 验证结果"或直接删除，保留 `T(Float64(n))` 的引用来源为 OOD 文档。
  - G24：删除或修正为"该问题涉及 `ext/quaternion_transform.cj` 中 `rotate` 函数的零轴保护边界，是一个库行为边界问题"。不应声称 deviations.md 已有记录。

---

### 问题 3：[一般] 修改流程建议优先级编号不连续

- **问题描述**：修改流程建议的「第二优先」部分（第 498-504 行）以编号 7-11 结尾，紧接着「第三优先」（第 507-510 行）以编号 13 开头。编号 12 缺失，致使 11→13 直接跳跃。追溯历史记录可见：G7 原为第 12 项，v3 迭代中将 G7 合并至 S2（第 2 项）后，后续项编号未重新排序。
- **所在位置**：第 504 行（11. G9）→ 第 508 行（13. G3）
- **严重程度**：一般
- **改进建议**：将第三优先的编号从 13 改为 12，并顺延后续编号（若无后续编号则仅改 13→12）。

---

### 问题 4：[轻微] 文档标题版本号与修订说明不一致

- **问题描述**：文档标题（第 1 行）标注为"（v3 修订版）"，但修订说明包含 v2、v3、v4、v5 共四个轮次的修订记录，其中 v5 为最新。标题版本号落后于实际内容版本。
- **所在位置**：第 1 行
- **严重程度**：轻微
- **改进建议**：将标题中的"v3 修订版"更新为"v5 修订版"，与最新修订轮次一致。

---

### 问题 5：[轻微] G10（slerp 修复）缺少副作用评估

- **问题描述**：G10 修改了现有生产代码的 slerp 核心公式（从 `sin((1-a)*ω/k)/sin(ω/k)` 改为 `sin(angle - a*phi)/sin(angle)`，`phi = angle + k*pi`）并补充 cosTheta<0 最短路径分支。该修改改变了两参数和四参数 slerp 的行为，属于**行为变更型修复**，但未提供副作用评估。当前文档中 S1/S2/G5/G8 已按 v1 迭代要求补充副作用评估，G10 的修改影响面不亚于 S1（同样影响数学计算结果），应一并评估。
- **所在位置：G10 诊断段落（第 220-232 行），缺少类似 S1 第 29-33 行的副作用评估结构
- **严重程度**：轻微
- **改进建议**：补充 G10 修复的副作用评估，至少覆盖：
  1. 受影响测试文件及期望值需重新计算的测试清单
  2. 向后兼容性说明（行为改变对依赖 slerp 的现有代码的影响）
  3. 是否需要同步更新 deviations.md（若 cosTheta<0 最短路径分支为 GLM 行为的补充而非修正）

---

## 整体质量评价

产出覆盖了 todo.md 中所有 37 个 issue（S1-S4、G1-G37）的诊断，对每个 issue 回答了要求的三个问题（真实性、根因类型、修改建议），结构清晰，证据引用充分（含源代码行号、GLM 参考源码对照）。优先级分组合理，关键修复方案（S1/S2/G5/G8）有详细的副作用评估。修订记录完整显示了迭代改进的过程。

仍存在上述 5 个问题未修复。其中**问题 1**（G24 分类矛盾）和**问题 3**（编号不连续）为前序轮次已引入但未被前置审查捕获的新增/残留矛盾。**问题 2**（deviations.md 引用错误）涉及事实准确性，逐轮迭代中未被纠正。
