# 质量审查报告：a_v5_diag_v1.md（第 5 轮）

## 审查范围

- **审查对象**：`a_v5_diag_v1.md` — Phase 4 Review Issues 诊断报告（v5 修订版）
- **审查维度**：需求响应充分度、事实错误与逻辑矛盾、深度与完整性、可操作性
- **审查方式**：逐项核查 + 交叉验证（与 `todo.md`、`deviations.md` 对照）

---

## 发现的问题

### 问题 1：[严重] 汇总表中 G24 仍被归入"测试覆盖遗漏"组（v5 修订未闭合）

- **问题描述**：v5 修订说明声称已修复 G24 分类矛盾（第 589 行），但仅修正了诊断正文中 G24 的真实性标注（从"真实问题"改为"设计争议"）。**汇总表「按根因分类汇总」（第 468 行）仍将 G24 隐含地归入 `G13-G37（全部测试不足 issue）` 的"测试覆盖遗漏"行**。这导致正文诊断（G24 为"设计争议"，修改方向为"需先完成 OOD 决策"）与汇总表分类（G24 为"测试覆盖遗漏"）之间仍然矛盾。
- **所在位置**：第 468 行（汇总表）vs 第 342-347 行（正文诊断）——与上一轮 `b_v5_diag_v1.md` 问题 1 相同
- **严重程度**：严重
- **改进建议**：此问题已跨越 3 轮迭代（v3→v5）未被完全修复。应在汇总表中将 G24 从"测试覆盖遗漏"行排除，新增"设计待决"类别行单独列出。参考与汇总表中 G3（OOD 文档修正）、G7（实现编码遗漏）类似的处理方式。

---

### 问题 2：[一般] G15 与 G24 仍错误引用 deviations.md 中不存在的内容（两次修订均未修复）

- **问题描述**：两处诊断声称参考了 `docs/deviations.md` 中的特定内容，但经全文检索确认这些内容在 deviations.md 中**不存在**，且该问题在 v5 修订说明中未被提及：
  1. **G15 第 285 行**："根据 `docs/deviations.md`，Cangjie 的 `std.math` 对 Float16 的支持通过 `T(Float64(n))` 字面量替代路径实现（见 `docs/05_ood_phase3.md` 中 H1 验证）"——deviations.md 中仅在 DV-02 的 `mod(s,v)` 函数中提及 Float16，无任何关于 Float16 通用支持路径或 `T(Float64(n))` 字面量替代路径的表述。该信息实际来源于 OOD 设计文档，而非 deviations.md。
  2. **G24 第 345 行**："根据 `docs/deviations.md`，Cangjie 的 `cross(零向量, v)` 行为取决于实现。该问题涉及……这是一个已知的库行为边界问题，非 Cangjie 特有偏差"——deviations.md 中无任何关于 `cross(零向量, v)` 行为或零轴旋转的偏差记录，该问题并非 deviations.md 中"已知的库行为边界问题"。
- **所在位置**：G15 第 285 行、G24 第 345 行——与上一轮 `b_v5_diag_v1.md` 问题 2 完全相同
- **严重程度**：一般
- **改进建议**：
  - G15：将"根据 `docs/deviations.md`"改为"根据设计文档 `docs/05_ood_phase3.md` 中 H1 验证结果"，或直接删除对 deviations.md 的引用，保留 `T(Float64(n))` 的引用来源为 OOD 文档。
  - G24：删除"根据 `docs/deviations.md`"前缀，直接说明"该问题涉及 `ext/quaternion_transform.cj` 中 `rotate` 函数的零轴保护边界，属于库行为边界问题"。不应声称 deviations.md 已有此记录。

---

### 问题 3：[一般] 修改流程建议优先级编号 12 缺失仍未修复

- **问题描述**：修改流程建议的「第二优先」部分以编号 7-11 结尾（第 498-504 行），紧接着「第三优先」以编号 13 开头（第 508 行）。编号 12 缺失。该问题在 v3 迭代中 G7 被合并至 S2（第 2 项）后引入，v5 修订仍未重排编号。
- **所在位置**：第 504 行（11. G9）→ 第 508 行（13. G3）
- **严重程度**：一般
- **改进建议**：将「第三优先」的编号从 13 改为 12。

---

### 问题 4：[轻微] G10（slerp 修复）缺少副作用评估

- **问题描述**：G10 修改了 `ext/quaternion_common.cj` 中 slerp 核心公式（四参数版本从 `sin((1-a)*ω/k)/sin(ω/k)` 改为 `sin(angle - a*phi)/sin(angle)`，`phi = angle + k*pi`）并补充两参数及四参数 slerp 的 cosTheta<0 最短路径分支。该修改改变了两参数和四参数 slerp 的数学行为，属于行为变更型修复。但 G10 诊断段（第 220-232 行）完全缺少副作用评估，而同类行为变更型修复（S1/S2/G5/G8）均已按前序迭代要求补充。该问题在 v5 修订说明中未被提及。
- **所在位置**：G10 诊断段落（第 220-232 行），缺少类似 S1（第 29-33 行）的副作用评估结构
- **严重程度**：轻微
- **改进建议**：补充 G10 修复的副作用评估，至少覆盖：
  1. 受影响测试文件及期望值需重新计算的测试清单（`quaternion_common_test.cj` 中涉及 slerp 的用例）
  2. 向后兼容性说明（公式语义从"频率缩放"改为"spin count 累加"，对依赖 slerp(k) 精确保留行为的现有代码的影响）
  3. cosTheta<0 最短路径分支属于 GLM 行为的正确性补全，非扩展功能——修复后是否需同步更新 deviations.md 记录
  4. 两参数 slerp 和四参数 slerp(k) 的受影响范围分别评估

---

## 修订说明（v2）

| 质询意见 | 回应 |
|---------|------|
| **Problem 4（文档标题版本号不一致）属于无关紧要的细节问题，不应纳入审查报告** | 已采纳。删除 Problem 4。标题版本号属于文档维护问题，与任务要求的"诊断 issue 真实性、根因、修改方向"关系不大。本轮审查已移除该条目。 |
| **Problem 5（G10 缺少副作用评估）中"影响面不亚于 S1"的断言缺乏证据支撑** | 已修正。删除"不亚于 S1"的无证据比较，精简为客观描述："G10 修改了 slerp 核心公式并补充最短路径分支，属于行为变更型修复，但完全缺少副作用评估"。改进建议不再引述 S1 作为参照，仅列出 G10 自身应覆盖的评估项。 |
