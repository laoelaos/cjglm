根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

1. **严重**：S1 副作用评估事实错误——rotate_slow/shear_slow 并非委托 ext（第31行）。代码确认两者为独立实现，完全依赖 mat4Mul 而非 ext.rotate/ext.shear。需修正描述。
2. **中等**：G14 修改方向与证据自相矛盾（第276-277行）。要求补充 inversesqrt(0) 断言，但证据已确认该测试存在。需删除 inversesqrt(0) 项，仅保留 log(0)/log(-1)。
3. **中等**：G31 诊断信息严重不足，无法操作（第387-391行）。无证据引用、无函数列表、无测试文件路径。需补充 gtc 重导出 ext 的函数清单，并区分两类测试：gtc 自有实现与 ext 重导出函数。
4. **中等**：S2（第一优先）与 G7（第二优先）优先级矛盾——位于同一函数，诊断自身要求"应同时修复"但分入不同优先级组。需将 G7 提升至第一优先与 S2 合并。
5. **轻微**：G10 分类为"待验证"过于保守，证据已充分证明公式不等价。应直接确认为真实问题并归入第一优先，同时补充缺少的 cosTheta<0 分支问题。
6. **轻微**：G3 关于 dot(v,v) 生成 -0.0 的论证不成立（第120行）。IEEE 754 中负×负=正，dot(v,v)不可能产生 -0.0。需删除此论据，改用更严谨理由。

## 历史迭代回顾

分析历史反馈与当前反馈的关系：

- **已解决的问题**：
  - 迭代1 #1（G10分类矛盾：正文与汇总表不一致）→ 已解决，v2 已统一归类
  - 迭代1 #2（G14-G37缺乏逐条诊断）→ 已解决，v2 已逐条补充
  - 迭代1 #3（未引用 deviations.md）→ 已解决，v2 已新增对照
  - 迭代1 #5（G2阶段责任未区分）→ 已解决，v2 已关联 phase3 文档
  - 迭代1 #6（S1/S2缺少测试回归影响）→ 已解决，v2 已补充副作用评估
  - 迭代1 #7（G12诊断不完整）→ 已解决，v2 已完成诊断
  - 迭代1 #9（优先级缺少合理性论证）→ 已解决，v2 已重组优先级分组
  - 迭代2 #1（S1事实错误）→ 已解决，本轮修订中需修正

- **持续存在的问题**：
  - 迭代1 #4（G3修改建议问题）→ 持续存在：v2 选定单一方向（修改 OOD 文档），但本轮发现其论证细节（dot(v,v)生成-0.0）不成立，需同时修复论据
  - 迭代1 #8（缺乏副作用评估）→ 持续存在：v2 补充了副作用评估框架，但 S1 评估中存在事实错误，需修正评估内容
  - 迭代2 #2（G14自相矛盾）→ 持续存在，需在本次修订中修正
  - 迭代2 #3（G31信息不足）→ 持续存在，需在本次修订中补充
  - 迭代2 #4（S2/G7优先级矛盾）→ 持续存在，需在本次修订中调整

- **新发现的问题**：
  - G10 分类"待验证"过于保守（证据已充足），应直接确认为真实问题
  - G3 论证中 dot(v,v) 可产生 -0.0 的数学理由不成立，需更换论据

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606300203_diagnosis_todo\a_v2_diag_v1.md

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606300203_diagnosis_todo\requirement.md
