# 再审议判定报告（v10）

## 判定结果

RETRY

## 判定理由

组件B诊断报告识别出 6 个问题（1 严重 + 3 一般 + 2 轻微）。质询报告结论为 LOCATED，所有维度审查通过，确认诊断结论成立。内部循环实际轮次（1）< 最大轮次（12），提前终止且审查被确认。诊断报告中包含严重和一般等级的问题，满足 RETRY 条件。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：编译期条件包含 `_swizzle.hpp` 替代方案存在事实盲区——"const 配置常量 + if 分支"被表述为首选策略，但该策略无法处理条件包含独立文件的场景，且 CangJie 中不存在与 C++ `#if` + `#include` 等价的机制
- **所在位置**：0.4 节"推荐首轮策略"段落（第 46 行）
- **严重程度**：严重
- **改进建议**：将"const 配置常量 + if 分支"降级为适用于内联条件代码的有限替代方案；明确标注不适用于条件包含独立文件的场景；补充 CangJie 宏在条件代码生成方面的约束说明

- **问题描述**：3D 节"合计可实例化类型 = 256"表述未按 v9 修订计划修正——应为"合计类型别名定义数"，实际泛型结构体实例化数约 44 个
- **所在位置**：第 3D 节首轮范围规模总结表（第 402 行）
- **严重程度**：一般
- **改进建议**：按 v9 修订方案将表述改为"合计类型别名定义数"，同步补充"泛型结构体实际实例化数：≤44 个"

- **问题描述**：3E → 3G → 3F 章节顺序逻辑颠倒——3G 节超前使用了 `package glm.detail` 等概念，但读者此时尚未读到 3F 节对这些映射规则的定义
- **所在位置**：第 3 节内子章节（3G 始于第 454 行，3F 始于第 522 行）
- **严重程度**：一般
- **改进建议**：将 3F 节前移至 3E 节之后、3G 节之前，或在 3G 节首次出现相关概念处添加交叉引用说明

- **问题描述**：3E 文件清单中 `<functional>` 适配的 Shim 覆盖说明缺失——清单第 4 行 C++ 依赖列标注了 `<functional>`，但 Shim 依赖列未作任何标注
- **所在位置**：3E 节文件清单第 4 行（第 414 行）
- **严重程度**：一般
- **改进建议**：在 Shim 依赖列增加注释说明"`<functional>` → 内联替代，无独立 shim 文件"，或添加脚注
