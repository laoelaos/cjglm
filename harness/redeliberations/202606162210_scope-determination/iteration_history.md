## 迭代第 1 轮

1. **问题描述**：缺少可直接执行的「核心文件清单」
   - 所在位置：第 3 节（尤其是 3D "首轮范围规模总结"）
   - 严重程度：严重
   - 改进建议：在第 3 节补充表格形式的「首轮迁移文件清单」，包含源文件路径、类型说明、依赖关系、子范围编号
2. **问题描述**：CangJie 语言特性适应性分析缺失
   - 所在位置：第 0 节 "目标语言能力假设" 表
   - 严重程度：严重
   - 改进建议：查阅 CangJie 文档对泛型特化、运算符重载、类型别名、编译期条件、固定位宽整数等关键能力给出明确结论，或标注"需语言专家确认"
3. **问题描述**：需求首项目标的度量标准偏差
   - 所在位置：第 2.2 节
   - 严重程度：一般
   - 改进建议：补充类型级依赖关系分析，与现有文件级数据互为印证
4. **问题描述**：验证标准过于抽象，缺乏可操作性
   - 所在位置：第 4.4 节
   - 严重程度：一般
   - 改进建议：为每条验收准则补充可执行验证步骤和编译验证命令
5. **问题描述**：`_swizzle.hpp` 的 CangJie 迁移可行性未评估
   - 所在位置：第 5 节图注及第 3B 节
   - 严重程度：一般
   - 改进建议：明确标注迁移风险并提供备选方案（如默认关闭 swizzle 功能）

## 迭代第 2 轮

1. **问题描述**：子范围节号引用"4.5"应为"4.6"
   - 所在位置：第 3E 节，文件清单表后的注释行
   - 严重程度：中
   - 改进建议：将"4.5 节"改为"4.6 节"
2. **问题描述**：后续轮次规划内容依赖外部文件，文档自包含性不足
   - 所在位置：第 3 节末尾
   - 严重程度：中
   - 改进建议：将后续轮次规划的摘要或关键里程碑以内联形式纳入本文件，或提供 `a_v1_imported.md` 的完整绝对路径
3. **问题描述**：缺少 C++ 命名空间 → 仓颉包模块的映射策略
   - 所在位置：贯穿第 3 节（首轮迁移范围）
   - 严重程度：中
   - 改进建议：在第 3 节或新增小节中补充"命名空间/包映射表"，明确每个迁移文件对应的仓颉包声明、跨包引用策略

## 迭代第 3 轮

1. **问题描述**：3D节辅助文件数量与3E节清单状态矛盾——3D节将`_swizzle.hpp`列为4个辅助文件之一并得出合计核心文件=10，但3E节将其标注为排除，4.5节推荐方案A明确首轮关闭swizzle
   - 所在位置：3D节第283-284行、3E节第302行、4.5节第508行
   - 严重程度：严重
   - 改进建议：将3D节辅助文件行中的`_swizzle.hpp`移除，改为3个辅助文件；合计核心文件改为2+4+3=9个文件
2. **问题描述**：`compute_vector_decl.hpp`的`<functional>`和`<limits>`依赖未在文件清单中声明——该文件实际使用`std::plus<T>`等函数对象和`std::numeric_limits<T>::is_iec559`，但在3E节依赖列仅标注`setup.hpp`+`qualifier.hpp`
   - 所在位置：3E节第296行、0.7节第75-77行
   - 严重程度：一般
   - 改进建议：在文件清单`compute_vector_decl.hpp`的依赖列补充`<functional>`和`<limits>`（需CangJie等效替代）；在0.7节同步更新
3. **问题描述**：文件清单缺少CangJie shim层对应条目——首轮范围内`setup.hpp`依赖`<cassert>`和`<cstddef>`、`compute_vector_relational.hpp`依赖`<limits>`均需CangJie等效替代，但3E节清单未列出对应shim文件
   - 所在位置：3E节第293-305行文件清单表（无shim层条目）、0.7节第73-75行
   - 严重程度：一般
    - 改进建议：在文件清单表中新增shim层文件条目（如`shim_assert.cj`、`shim_numeric_limits.cj`），或在3E节补充注释说明shim层处理策略及推荐目录位置

## 迭代第 4 轮

1. **问题描述**：`type_vec1.hpp` 至 `type_vec4.hpp` 的依赖列遗漏了 `<cstddef>` 直接依赖
   - 所在位置：第 1 节层次 1 依赖表、第 3E 节文件清单第 6-9 行
   - 严重程度：一般
   - 改进建议：在文件清单第 6-9 行的依赖列补充 `+ <cstddef>`，或增加注释说明通过 `qualifier.hpp → setup.hpp` 传递包含已引入 `<cstddef>`

2. **问题描述**：`setup.hpp`（1188 行高度平台相关代码）的规模复杂度未被定量评估，首轮工作量可能被低估
   - 所在位置：第 3A 节第 1 项、第 3D 节"合计核心文件 = 9"计数
   - 严重程度：一般
   - 改进建议：在第 3A 节或第 3D 节补充：(1) 仅约 50-100 行逻辑需要移植；(2) 约 1000+ 行平台检测代码整体标记为"首轮无需直接迁移"；(3) 提供保留功能具体清单

## 迭代第 5 轮

1. **问题描述**：`_vectorize.hpp` 中 C++ 模板模板参数（template template parameter）的 CangJie 迁移方案未被覆盖，文档未分析其对迁移方案设计的影响
   - 所在位置：0.1 节泛型偏特化说明段落、3E 节文件清单第 5 行
   - 严重程度：一般（映射为中等）
   - 改进建议：补充分析模板模板参数差异，给出应对策略（如为 Vec1~Vec4 分别定义独立 functor 结构体或使用宏生成），并评估对工作量的影响

2. **问题描述**：`compute_vector_decl.hpp` 的 `std::size_t` 依赖在文件清单中遗漏，未记录 `<cstddef>` 依赖
   - 所在位置：3E 节文件清单第 4 行
   - 严重程度：一般（映射为中等）
   - 改进建议：在 3E 节文件清单补充 `<cstddef>` 依赖，在 0.7 节 `<cstddef>` 行补充用途说明

3. **问题描述**：`compute_vector_relational.hpp` 的 `<limits>` 依赖描述存在误导，未说明调用方代码活跃使用 `std::numeric_limits`
   - 所在位置：第 1 节层次 1 描述（第 123 行）、3E 节文件清单第 3 行、4.6 节、5 节图注
   - 严重程度：一般
   - 改进建议：修正相关描述，明确说明调用方通过 `compute_equal` 模板实参活跃使用 `std::numeric_limits<T>::is_iec559`，在文件清单依赖列统一标注

4. **问题描述**：0.5 节固定位宽整数映射与 0.7 节整数溢出处理策略之间未衔接，未说明全局默认溢出策略选择
    - 所在位置：0.5 节（第 48-64 行）、0.7 节末行（第 80 行）
    - 严重程度：一般
    - 改进建议：在 0.5 节末补充溢出策略说明（建议全局 wrapping 或使用 `std.overflow`），或在 0.8 节增加相应行

## 迭代第 6 轮

1. **问题描述**：`setup.hpp` 的剩余 1000+ 行平台/编译器/架构检测代码，声称"其功能由目标语言的运行时特性隐式提供"，但 CangJie 不存在与 C++ 预处理器等价的平台检测机制，该断言缺少事实支撑
   - 所在位置：第 3A.1 节（第 244 行）
   - 严重程度：严重
   - 改进建议：将断言改为待验证假设并标注需 CangJie 语言专家确认；或提供具体的 CangJie 等效映射方案；或将此检测功能整体列入后续轮次

2. **问题描述**：文档标题写为"(v4)"但文档实际已迭代至 v6，版本号更新遗漏
   - 所在位置：第 1 行
   - 严重程度：一般
   - 改进建议：将标题中的"(v4)"修改为"(v6)"

3. **问题描述**：`storage` 结构体的 SIMD 平台特化以条件句"若提供"处理，但 CangJie 当前版本确无 SIMD 基础类型，条件分支留下不确定状态
   - 所在位置：第 3A.2 节（第 247 行）
   - 严重程度：一般
   - 改进建议：将条件句改为明确结论——CangJie 当前版本不提供 SIMD 基础类型，首轮始终使用非 SIMD 通用路径

4. **问题描述**：称降级 `vec1` 可"减少约 16 个别名"但与文档自身计量体系（16×4=64 或 256）口径不一致
   - 所在位置：第 3D 节末尾（第 320 行）
   - 严重程度：一般
   - 改进建议：统一计量口径并注明统计范围

5. **问题描述**：整数溢出策略建议"全局采用 wrapping 算术"但 CangJie 不支持全局溢出策略设置，两种互斥机制未说明推荐方案
   - 所在位置：第 0.5 节（第 65 行）、第 0.8 节（第 98 行）
    - 严重程度：一般
    - 改进建议：明确推荐在每个向量算术运算符函数上标注 `@OverflowWrapping`，并补充验证步骤

## 迭代第 7 轮

1. **问题描述**：Section 1 中 `_swizzle.hpp` 范围状态与排除结论矛盾，Section 1 保留"纳入范围"的描述，而 3D/3E/4.5 节已将其排除，构成逻辑矛盾
   - 所在位置：第 1 节，层次 1 描述末尾注文（第 127 行）
   - 严重程度：严重
   - 改进建议：将 Section 1 注文中"纳入范围"表述修改，确保全文对 `_swizzle.hpp` 范围状态表述一致
2. **问题描述**：`type_vec3.inl` 和 `type_vec4.inl` 底部 `GLM_CONFIG_SIMD` 条件包含 `type_vec_simd.inl` 未被覆盖，该文件不在首轮范围内，若保留默认 `GLM_ENABLE` 则构建必然失败
   - 所在位置：第 3A 节（首轮基础设施层）、第 3E 节文件清单第 8-9 行依赖列
   - 严重程度：严重
   - 改进建议：在 3A.1 节 `GLM_CONFIG_*` 清单中明确 `GLM_CONFIG_SIMD` 首轮设为 `GLM_DISABLE`；在 3E 节文件清单补充条件性注释；在 4.4 节验证标准增加此项配置检查
3. **问题描述**：`compute_vector_decl.hpp` 的 `<limits>` 依赖被标注为直接依赖，实为通过 `compute_vector_relational.hpp` 传递引入，影响 shim 层 import 语句设计
   - 所在位置：3E 节文件清单第 4 行（`compute_vector_decl.hpp`）依赖列
   - 严重程度：一般
   - 改进建议：将依赖列中的 `<limits>` 改为注释形式说明传递包含关系；同步检查 0.7 节 `<limits>` 行的描述
4. **问题描述**：`@OverflowWrapping` 标注范围缺乏运算符类别区分、量化指导和委托模式利用，浮点类型溢出语义未讨论
   - 所在位置：第 0.5 节（第 65 行）、第 0.8 节（第 98 行）
   - 严重程度：一般
   - 改进建议：补充运算符分类表明确标注策略；利用非成员运算符委托 `operator+=` 的特性仅标注复合赋值运算符；补充浮点类型分量不受溢出注解影响的可忽略性说明；量化标注工作量

## ������ 8 ��

1. **��������**��size_t���޷��ţ��� Int64���з��ţ�������Ӱ��δ����
   - ����λ�ã��� 0.7 �� <cstddef> �У��� 108 �У����ļ��嵥 S3���� 344 �У���3E �ļ��嵥�� 4 �������У��� 333 �У�
   - ���س̶ȣ�һ��
   - �Ľ����飺�� 0.7 �� <cstddef> �и���ȷ�����ۣ���ȷѡ�� Int64 �� UInt64���������з���/�޷������������������豣���޷������壬�������� shim_cstddef.cj Ϊǿ����

2. **��������**��3D ��"�ϼƿ�ʵ�������� = 256"�������������ì��
   - ����λ�ã�3D ��"�ϼƿ�ʵ��������"�У��� 322 �У�
   - ���س̶ȣ�һ��
   - �Ľ����飺��"�ϼƿ�ʵ��������"��Ϊ"�ϼ����ͱ���������"��"�ϼƾ�������"��˵��ʵ�ʷ��ͽṹ��ʵ��������

3. **��������**��GLM_CONFIG_* ��������ö��ȱʧ
   - ����λ�ã�3A.1 �ڣ��� 274 �У���0.4 �ڱ��񣨵� 45 �У�
   - ���س̶ȣ�һ��
   - �Ľ����飺�� 3A.1 �ڸ��������嵥���񣬰���������CangJie ��Ч�������������Ƽ�ֵ����;˵��

4. **��������**�����ַ�������"��"ȱ��������������
   - ����λ�ã�"�ֲ�Ǩ������"���� 1 �У��� 465 �У�
   - ���س̶ȣ�һ��
   - �Ľ����飺��������������Ϊ"��"��"�е�"�����ڱ���� 3A ��ĩβ��������嵥�����ʹ�ʩ

5. **��������**������һ�����������ʶ�𣩵Ľ���δ�������֣��������ַ�Χ����
   - ����λ�ã��� 2 �������������� 213-243 �У����� 4.1 ��ѡ�����ɣ��� 475-483 �У�
   - ���س̶ȣ�һ��
   - �Ľ����飺����"���������ʶ�����"С�ڣ��������Ƚ������к������ͣ������ַ�Χ�γ�"ʶ�������"���������



## ������ 9 ��

1. **��������**��Task 3"��Χȷ�Ͻ���"ȱ��ͳһ���֣�����Ҫ�أ��ļ��嵥��������������֤��׼����ɢ��3D��3E��4.4���ڣ�ʵʩ������½�ƴ�����ܻ�ȡ����������ͼ
   - ����λ�ã���3�ڣ�3D��3E������4.4��
   - ���س̶ȣ�һ��
   - �Ľ����飺��3E��֮������"��Χȷ�Ͻ���"С�ڣ��Ծ����嵥���ܺ����ļ��嵥���ļ���������11����������������4��Vec�ṹ��+256������+10����������+bool������֤��׼ժҪ������ȷ�����÷�Χ���������պ��ԺͶ����ɱ�����

2. **��������**���ļ��嵥(3E)������ʹ��C++ͷ�ļ����ţ�δ����shim�㽨����ʽ������ʵʩ�����ֶ���������0.7.2�ڲ���ȷ�����import����
   - ����λ�ã�3E���ļ��嵥����0.7.2��Shim�����ܱ�
   - ���س̶ȣ�һ��
   - �Ľ����飺��3E���ļ��嵥��������Ϊÿ��C++ͷ�ļ�������ע��Ӧ���shim�ļ�����������"shim������"ֱ���г�ÿ���ļ���shim�����б�

3. **��������**��compute_vector_relational.hpp��<limits>���������ڲ��importģ���´��������󵼣��ĵ��ദ��"����δֱ��ʹ��std::numeric_limits"�������ļ�ģ�嶨����ȷʵ������std::numeric_limits<T>::is_iec559�����ʵ����ֱ��import shim_limits
   - ����λ�ã���1�ڲ��1������3E���ļ��嵥��3�С�4.6���ӷ�Χ2a˵������5��ͼע
   - ���س̶ȣ�һ��
   - �Ľ����飺ͳһ���������������"����δֱ��ʹ��"��Ϊ��ȷ˵�����ļ���Ҫֱ��import shim_limits������3E���ļ��嵥�н����ļ���<limits>������ע����������Ϊ��׼������

## 迭代第 10 轮

1. **问题描述**：编译期条件包含 _swizzle.hpp 替代方案存在事实盲区——"const 配置常量 + if 分支"被表述为首选策略，但该策略无法处理条件包含独立文件的场景，且 CangJie 中不存在与 C++ #if + #include 等价的机制
   - 所在位置：0.4 节"推荐首轮策略"段落（第 46 行）
   - 严重程度：严重
   - 改进建议：将"const 配置常量 + if 分支"降级为适用于内联条件代码的有限替代方案；明确标注不适用于条件包含独立文件的场景；补充 CangJie 宏在条件代码生成方面的约束说明
2. **问题描述**：3D 节"合计可实例化类型 = 256"表述未按 v9 修订计划修正——应为"合计类型别名定义数"，实际泛型结构体实例化数约 44 个
   - 所在位置：第 3D 节首轮范围规模总结表（第 402 行）
   - 严重程度：一般
   - 改进建议：按 v9 修订方案将表述改为"合计类型别名定义数"，同步补充"泛型结构体实际实例化数：≤44 个"
3. **问题描述**：3E → 3G → 3F 章节顺序逻辑颠倒——3G 节超前使用了 package glm.detail 等概念，但读者此时尚未读到 3F 节对这些映射规则的定义
   - 所在位置：第 3 节内子章节（3G 始于第 454 行，3F 始于第 522 行）
   - 严重程度：一般
   - 改进建议：将 3F 节前移至 3E 节之后、3G 节之前，或在 3G 节首次出现相关概念处添加交叉引用说明
4. **问题描述**：3E 文件清单中 <functional> 适配的 Shim 覆盖说明缺失——清单第 4 行 C++ 依赖列标注了 <functional>，但 Shim 依赖列未作任何标注
   - 所在位置：3E 节文件清单第 4 行（第 414 行）
   - 严重程度：一般
    - 改进建议：在 Shim 依赖列增加注释说明"<functional> → 内联替代，无独立 shim 文件"，或添加脚注

## 迭代第 11 轮

1. **问题描述**：3A 节 `setup.hpp` 描述中 "约 10 个宏" 与完整清单 18 个宏存在事实矛盾
   - 所在位置：第 3A 节第 1 项，第 328 行（`a_v11_output_v1.md`）
   - 严重程度：一般
   - 改进建议：将 "约 10 个宏" 修改为 "18 个宏（完整清单见下表）"，与 v9 轮次已更新的 18 项完整表格保持一致
2. **问题描述**：4.4 节验证步骤中的文件总数与 3D/3G 节正式统计口径不一致
   - 所在位置：第 4.4 节，依赖闭合性验证步骤描述（第 697 行）
   - 严重程度：一般
    - 改进建议：统一按 3G 节正式口径表述——"将首轮迁移的 11 个 GLM 源文件 + 3 个 shim 文件共 14 个文件 按依赖拓扑放入验证项目"

## 迭代第 12 轮

1. **问题描述**：`Vec<T, Q>` 的泛型参数 Q 设计存在根本性语法错误，CangJie 泛型类型参数仅接受类型作为实参，不接受枚举值
   - 所在位置：0.1节第15行、0.3节第36行、3F节第478行、4.4节第699行及全文涉及 `Vec<T, Q>` 所有位置
   - 严重程度：严重
   - 改进建议：重新设计 qualifier 映射方案。方案A：将 qualifier 变体定义为独立类型 + `where Q <: Qualifier` 约束；方案B：去掉 Q 类型参数，将 qualifier 改为运行时枚举字段
2. **问题描述**：文档标题版本号写为 v11，实际应为 v12，且此问题在迭代6中曾出现过同类错误
   - 所在位置：第1行标题
   - 严重程度：一般
   - 改进建议：将标题中的 `(v11)` 更新为 `(v12)`
3. **问题描述**：`_vectorize.hpp` 文件清单条目未反映模板模板参数的适配影响，实现者无法感知设计复杂度
   - 所在位置：3E节文件清单第5行（第417行）
   - 严重程度：一般
   - 改进建议：补充注释标注模板模板参数适配影响，提示 CangJie 需拆分为4组 functor（共16个定义）

## 迭代第 13 轮

1. **问题描述**：`compute_vector_relational.hpp`的`<limits>`依赖描述存在事实错误，4处声称该文件"自身模板代码引用`std::numeric_limits<T>::is_iec559`"，但实际源文件中活跃代码仅执行`return a == b`，未引用`std::numeric_limits`；唯一涉及`std::numeric_limits`的代码位于被注释掉的特化版本中
   - 所在位置：3E节第3行（第423行）、0.7节第119行、1节注文（第189行）、5节图注（第801行），共4处
   - 严重程度：严重
   - 改进建议：将4处错误描述统一修正为："`compute_vector_relational.hpp`包含但不主动使用`<limits>`；实际引用`std::numeric_limits<T>::is_iec559`的是`compute_vector_decl.hpp`第163行（通过`compute_equal`模板实参）。由于该行依赖`<limits>`经`compute_vector_relational.hpp`的**翻译单元级包含顺序**隐式提供（`.inl`文件先包含`compute_vector_relational.hpp`后包含`compute_vector_decl.hpp`），CangJie import设计中需注意此跨文件依赖需显式处理。"

2. **问题描述**：3个新增shim文件（`shim_assert.cj`、`shim_limits.cj`、`shim_cstddef.cj`）在文件清单中仅标注"新增"和路径，未指定其所属`package`声明
   - 所在位置：3E节文件清单S1-S3行（第433-435行）、3F节包映射策略（第466-488行）
   - 严重程度：一般
   - 改进建议：在3E节文件清单的"子范围"列或3F节包映射表中为S1-S3补充`package`声明。推荐方案：将S1-S3纳入`package glm.detail`，或单独创建`package glm.detail.shim`子包

3. **问题描述**：需求说明书列出的参考材料`docs/01_roadmap.md`未被产出任何位置引用，未说明v13版本与该文档中此前范围分析结论的关系（supersede/merge/独立发展）
   - 所在位置：全文
   - 严重程度：一般
   - 改进建议：在引言或第0节补充对`01_roadmap.md`的引用说明

4. **问题描述**：`compute_vector_decl.hpp`第163行调用`compute_equal`模板，但该模板定义位于`compute_vector_relational.hpp`；在C++中此依赖通过`.inl`包含顺序隐式满足，但在CangJie显式import模型中需相应处理
   - 所在位置：3E节第4行（第424行），`compute_vector_decl.hpp`的依赖列
   - 严重程度：一般
   - 改进建议：在3E节第4行的依赖列补充`compute_equal`模板定义的跨文件依赖说明，标注`compute_vector_decl.cj`中需显式`import glm.detail.compute_vector_relational`
