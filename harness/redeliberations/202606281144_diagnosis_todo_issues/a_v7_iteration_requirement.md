根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

组件B对 `a_v6_diag_v1.md` 的质量审查共发现 10 个问题，诊断结论已被质询组件确认（LOCATED）。问题按修复优先级分层如下：

**优先级1（影响主结论，必须修复）：**

1. **G5.6 分类与需求定义框架不一致（中）** — G5.6（slerp 4 参数重载无测试）按 requirement.md 第 4 类「其他类型的问题」定义（"如测试覆盖不足、代码可优化等"），应仅归类为「其他类型（测试覆盖不足）」。当前错误同时归为「真实存在（无测试覆盖）」，但其实现行为符合 stub 状态设计意图，不存在实现偏差。汇总表真实存在 11.0 应减至 10.5，其他类型 24.0 应增至 24.5，章节映射表同步修正。

2. **S3 非身份 round-trip 捕获能力的证据链不完整（中）** — 报告正确识别非身份四元数 round-trip 可捕获 S1 因子 2 bug，但论据"mat3Cast 重建矩阵涉及分量二次型"的前提（mat3Cast 不对输入四元数做归一化）未在报告中显式引用代码实现位置。需在 §S3 证据段补充：`type_quat_cast.cj:5-25` 的 `mat3Cast` 直接使用 q.x/q.y/q.z/q.w 计算矩阵元素未做归一化。优先级理由中"非身份 round-trip 虽可捕获但未针对性覆盖身份盲区"应改为"非身份 round-trip 可捕获（mat3Cast 不归一化已验证），身份 round-trip 仍存在捕获盲区"。

**优先级2（影响可操作性，建议修复）：**

3. **G2.2 修复方向未明确"约束收紧后下游影响"（中）** — 建议在阶段四前收紧约束至 `FloatingPoint<T>`，但未分析下游影响：现有调用方若存在整数类型实例化，收紧约束将导致编译失败，需同步识别并修改下游代码。需在 G2.2 修复方案中追加下游整数实例化调用点的扫描建议（如 `grep -rn "mix<.*Int64\|lerp<.*Int64" cjglm/`）。

**优先级3（改善文档质量，可选修复）：**

4. **S2 cjpm test 输出数字与 todo.md 不一致未做说明（低）** — todo.md 称"Summary: TOTAL: 422"，报告 S2 证据段给出"TOTAL: 425 ... PASSED: 422, FAILED: 3"。需在证据段明确说明 todo.md 数字 422 仅反映通过数，实际 TOTAL 为 425 含 3 失败用例。

5. **G3.5 行号引用与 todo.md 不一致未说明校正来源（低）** — todo.md G3.5 标称 `fwd.cj.bak:330-332`，报告引用 `:331/:334/:337`。需在 G3.5 证据段追加校正说明（行号漂移因后续编辑插入所致）。

6. **G5.9 误报分类的判定标准不清（低）** — G5.9 子项分类应明确为「其他类型（测试断言精度不足）」，"testInverseZero 描述不完全准确"作为分析说明，不另设「误报」分类；或在汇总表脚注明确说明本项「误报」分类的特殊性。

7. **type_quat_cast.cj 修复管线未明确"G2.3 重构必须在 S1 修复后合并提交"的副作用（低）** — 在 G2.3 重构说明中追加风险提示：若 S1 修复后 G2.3 重构引入 sqrt 路径变更，可能因浮点路径变化导致现有 passing 测试产生新差异。建议分两次提交（先 S1 单独修复并验证，再提交 G2.3 重构）。

8. **G3.5 修复建议未说明 "git rm --cached" vs "rm + git add" 的差异（低）** — 在 G3.5 修复建议中明确推荐 `git rm cjglm/src/fwd.cj.bak`（从索引与工作区同时删除），G3.6 修复建议明确推荐脚本修改为"检测已存在文件的行尾格式并保留"。

9. **G6.1 优先级未单独列出（低）** — 在修复优先级表中单独列出 G6.1 为 Medium 优先级，理由为"直接验证 OOD §2 lib.cj 20 个 import 契约，阶段四新增 import 时需同步补充测试"。

10. **G5.1/G5.2 缺口数量与 todo.md 表述不一致未说明（低）** — todo.md 称"27 函数 / OOD 要求 42 / 文件实际 30 / 缺口 -12"；报告 G5.1 仅称"实际仅 30 个用例，距 ≥40 下限短缺 10+"。需在 G5.1 分析段追加说明 27 与 42 的差异需 OOD §8.2 与 type_quat.cj 函数列表交叉确认。

## 历史迭代回顾

**已解决的问题（出现在历史反馈但当前反馈中不再提及）：**

- 第 1 轮问题 1-5（计数准确性、G1.6 sqrtT 矛盾、deviations.md 使用、汇总表口径、修复优先级缺失）— 均已在 v6 中修复或显著改善，未在本轮反馈中复发
- 第 2 轮问题 1-4（S3 优先级矛盾、G3.5/G3.6 分类调整、S1/S2 Blocker 矛盾、S1 修复后 S3 降级）— 均已修复
- 第 3 轮问题 1-3（round-trip 测试数量 7 vs 6、ULP stub 计数 8 vs 16、S3/S4 分类框架）— 均已修复
- 第 4 轮问题 1-4（S3 错误断言、deviations.md 使用方向、S2 修复方案副作用、type_quat_cast.cj 联合修复策略）— 均已修复
- 第 5 轮问题 1-3（S2 工具链行为实证、deviations.md 交叉验证深度、S2 与测试类问题依赖关系）— 均已修复

**持续存在的问题（多轮反馈中反复出现）：**

- 本轮问题 2（S3 证据链不完整）— S3 在第 3、4 轮均被反馈过不同维度的问题（分类框架、错误断言），本轮聚焦证据链完整性。S3 是历史持续高问题密度条目，需重点关注。
- 本轮问题 3（G2.2 修复方向）— 涉及"约束收紧后下游影响"评估，是首次直接反馈该问题；但 G2.2 的可操作性建议维度在第 4 轮已有所铺垫。
- 本轮问题 7（type_quat_cast.cj 修复管线）— 与第 4 轮问题 4（联合修复策略）形成闭环呼应；本轮补充合并提交副作用评估。

**新发现的问题（本轮新识别）：**

- 本轮问题 1（G5.6 分类）— 首次发现分类框架与定义的不一致
- 本轮问题 4（G3.5 行号差异校正来源）— 首次明确需要显式说明校正依据
- 本轮问题 5（G5.9 误报分类）— 首次发现误报分类标准不清
- 本轮问题 6（S2 cjpm test 数字差异）— 首次发现 todo.md 与实测数字差异需说明
- 本轮问题 8（git rm 方式选择）— 首次明确删除方式选择
- 本轮问题 9（G6.1 优先级单列）— 首次建议优先级调整
- 本轮问题 10（G5.1/G5.2 缺口数量差异）— 首次发现与 todo.md 表述差异需说明

**重要历史信号：** 第 4-6 轮反馈均聚焦"可操作性"维度（第 4 轮 S2 修复方案副作用、第 4 轮 type_quat_cast.cj 联合策略、第 5 轮 S2 工具链实证、第 6 轮 G2.2 下游影响、第 6 轮合并提交风险）。本轮的问题 6、7、8 延续该趋势，反映"诊断的可执行性"是本任务当前阶段的核心质量瓶颈。

## 上一轮产出路径

`harness/redeliberations/202606281144_diagnosis_todo_issues/a_v6_diag_v1.md`

## 用户需求

`harness/redeliberations/202606281144_diagnosis_todo_issues/requirement.md`