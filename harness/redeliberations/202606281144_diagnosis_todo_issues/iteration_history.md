## 迭代第 1 轮

1. **问题描述**：严重问题/一般问题数量与实际不符，报告声称 6 项严重 + 42 项一般 = 48 项，但实际仅列出 S1~S4 共 4 项严重问题，一般问题各组合计 38 项，实际总计 42 项
   - 所在位置：`a_v1_diag_v1.md:5`、`a_v1_diag_v1.md:15`
   - 严重程度：严重
   - 改进建议：修正严重问题计数为 4 项、一般问题计数为 38 项，并在概述中说明 todo.md 原文标称计数与实际列表不一致的情况

2. **问题描述**：G1.6 段落存在自相矛盾描述，先称"sqrtT 未在 type_quat_cast.cj 中被调用"，后称"quatCast 函数体内确实调用了 sqrtT"
   - 所在位置：`a_v1_diag_v1.md:137`
   - 严重程度：一般
   - 改进建议：删除第一句错误断言，仅保留正确表述

3. **问题描述**：未充分使用 `docs/deviations.md` 进行交叉验证，仅在 G4.4 一处提及，其余 40+ 项分析中均未对照 deviations.md 验证；G1.5、G2.3、G4.4 等关键偏差未检查 deviations.md 的登记状态
   - 所在位置：全文各处
   - 严重程度：严重
   - 改进建议：对每项分类为"真实存在（实现偏差）"的问题增加 deviations.md 对照说明；对未登记但应登记的偏差标注"建议追加登记至 deviations.md"

4. **问题描述**：分类汇总表中"真实存在（实现偏差）"计数 11 项但实际列举 13 个子项签名；"其他类型"计数 29 项但实际仅 23 项；未提供各分类覆盖的具体 todo.md 条目编号映射
   - 所在位置：`a_v1_diag_v1.md:435-441`
   - 严重程度：一般
   - 改进建议：统一计数口径，明确"部分（partial）"的加减规则；增加 todo.md 条目编号到报告章节的完整映射表

5. **问题描述**：缺乏修复优先级排序与依赖关系分析，修复者无法判断"先改什么、后改什么、什么可以一起改"
   - 所在位置：`a_v1_diag_v1.md:442-447`
   - 严重程度：一般
   - 改进建议：增加"修复优先级建议"小节，按 blocker / high / medium / low 分级；标注问题间的文件级依赖关系；特别指出 S2 作为测试基础设施问题应列为最高优先级

## 迭代第 2 轮

1. **问题描述**：S3优先级表中"理由"与"依赖关系"两列自相矛盾——理由写"需先修复测试才能验证S1修复"，依赖关系却写"测试依赖S1修复结果"。实际工程中S1可独立修复，S1修复后S3主诉求自然消失。
   - 所在位置：§修复优先级建议（第532行）
   - 严重程度：严重
   - 改进建议：删除S3矛盾理由，改为"round-trip测试方法论薄弱，应补充等价性测试"；在S1修复上下文中标注S1修复后S3优先级降为Low
2. **问题描述**：G3.5（备份文件误纳入）和G3.6（构建脚本CRLF/LF幂等性）被分类为"真实存在"（实现偏差），但二者均不属于实现与设计/参考实现的偏差，更适配"其他类型"分类。报告自身在deviations.md对照中也承认非语义偏差。
   - 所在位置：§G3.5（分类：真实存在）、§G3.6（分类：真实存在）
   - 严重程度：一般
   - 改进建议：将G3.5、G3.6的分类调整为"其他类型（版本控制/构建兼容性问题）"
3. **问题描述**：优先级表将S1和S2均列为"Blocker"，特别说明却写"S2应为最高优先级"。表格层级与说明矛盾，修复者需阅读两个分散位置才能理解真实优先级。
   - 所在位置：§修复优先级建议（第529-530行+第544行特别说明）
   - 严重程度：一般
   - 改进建议：方案A：S2单列Critical/Blocking，S1降为High；方案B：保留双Blocker但增加"修复顺序"列标注Step 1/Step 2
4. **问题描述**：未讨论S1修复对S3紧迫性的影响。S3核心诉求是"round-trip测试无法捕获S1 bug"，若S1被修复则该诉求自动消失，S3优先级应从High降至Low。报告将S3独立列为High，可能导致修复者并行修复S1和S3产生无效工作量。
    - 所在位置：§S3分析 + §修复优先级建议
    - 严重程度：一般
    - 改进建议：在S3分析末尾增加S1修复后条件性降级注；优先级表中S3标注条件性降级标注

## 迭代第 3 轮

1. **问题描述**：S3 round-trip测试数量表述与代码实际不符，将6个round-trip测试错误记为7个
    - 所在位置：§S3（第68-69行、第71行）
    - 严重程度：严重
    - 改进建议：将「7个」修正为「6个」，将「仅1个」修正为「2个」；证据段行号引用调整为`test_type_quat_cast.cj:18-93`时注明非round-trip用例并分别计数
2. **问题描述**：S4 ULP stub函数签名计数错误，将16个`public func`声明误计为8个stub函数导致覆盖率数据失实（3.125%应为6.25%）
    - 所在位置：§S4（第81行、第83行）
    - 严重程度：严重
    - 改进建议：将stub声明数修正为16，覆盖率修正为1/16 ≈ 6.25%；统一计数口径
3. **问题描述**：S3/S4分类与需求定义的分类框架不一致，测试覆盖问题应归入「其他类型」而非「真实存在」
    - 所在位置：§S3（第67行）、§S4（第79行）、§汇总（第471行）
    - 严重程度：一般
    - 改进建议：将S3、S4的分类改为「其他类型（测试覆盖不足）」；汇总表同步调整

## 迭代第 4 轮

1. **问题描述**：S3 分析中错误断言「round-trip 测试无法捕获 quatCast 因子 2 bug（缩放因子在往返中自抵消）」，未区分身份与非身份四元数 round-trip 的不同捕获能力
   - 所在位置：§S3 分析段，第 68-69 行
   - 严重程度：严重
   - 改进建议：修正 S3 分析，区分身份与非身份 round-trip；删除错误表述；移除 S1 修复后 S3 自动降级的假设
2. **问题描述**：deviations.md 使用方向偏离需求意图——需求要求用于交叉验证（核对实现是否与偏差记载一致），实际用于检查注册状态
   - 所在位置：§S1~§G6 各节的「deviations.md 对照」段落
   - 严重程度：一般
   - 改进建议：对所有涉及 deviations.md 已记载偏差的现有问题，补充一轮基于 deviations.md 的交叉验证
3. **问题描述**：S2 修复方案（测试文件重命名）潜在副作用未评估，包括 git 历史追溯、测试文件间依赖、CI 配置影响、替代方案
   - 所在位置：§S2 分析段及优先级表
   - 严重程度：一般
   - 改进建议：补充 S2 修复方案的风险评估，提供 2-3 种修复选项及 trade-off 对比
4. **问题描述**：type_quat_cast.cj 多问题缺少联合修复策略——5 个问题（S1、G1.5、G1.6、G1.7、G2.3）影响同一文件，修复顺序和合并策略未明确
   - 所在位置：§修复优先级建议表第 2 行（S1）及涉及 type_quat_cast.cj 的各项
   - 严重程度：一般
   - 改进建议：为 type_quat_cast.cj 制定统一修复管线，明确修复顺序和合并策略（如优先修复 S1 算法正确性，再分批修复 G 类问题）

## 迭代第 5 轮

1. **问题描述**：S2 核心工具链行为主张缺乏实证支撑——报告声称「cjpm 1.1.0 工具链仅识别 `src/**/*_test.cj` 的测试文件」但未提供任何实证
   - 所在位置：§S2 分析段（line 54）
   - 严重程度：严重
   - 改进建议：补充至少一项实证：执行 `cjpm test` 输出、引用 cjpm 官方文档、或提供最小复现用例
2. **问题描述**：deviations.md 交叉验证深度不足——绝大多数条目仅标注「不适用」但未探讨间接关联、相似模式或是否需要更新已有条目
   - 所在位置：§S1~§G6 各节的「deviations.md 交叉验证」段落
   - 严重程度：一般
   - 改进建议：对交叉验证段落做分层处理：说明为何不适用、评估是否需要登记、评估是否影响已有条目
3. **问题描述**：S2 与测试类问题（S3/S4/G5/G6）的依赖关系未显式阐明
   - 所在位置：§S2 分析段与优先级表（line 592-609）
   - 严重程度：一般
   - 改进建议：在 §S2 段末或优先级表上方增加显式依赖声明，说明测试类问题在 S2 修复前无法被验证

## 迭代第 6 轮

1. **问题描述**：G5.6 分类与需求定义框架不一致。按 requirement.md 第 4 类「其他类型的问题」的定义（"如测试覆盖不足、代码可优化等"），G5.6（slerp 4 参数重载无测试）应仅归类为「其他类型（测试覆盖不足）」，不应同时归类为「真实存在」——其实现行为符合当前 stub 状态的设计意图，不存在实现偏差。
   - 所在位置：a_v6_diag_v1.md §G5.6（"分类"行）；汇总表第 587 行（真实存在 11.0 含 G5.6(0.5)）；章节映射表 G5.6 行
   - 严重程度：一般
   - 改进建议：将 G5.6 分类修改为「其他类型（测试覆盖不足）」；汇总表真实存在从 11.0 减至 10.5，其他类型从 24.0 增至 24.5；章节映射表 G5.6 行同步修正。

2. **问题描述**：S3 非身份 round-trip 捕获能力的证据链不完整。报告正确识别非身份四元数 round-trip 可捕获 S1 因子 2 bug，但论据"mat3Cast 重建矩阵涉及分量二次型"的成立前提（mat3Cast 不对输入四元数做归一化）未在报告中显式引用代码实现位置。
   - 所在位置：a_v6_diag_v1.md §S3 分析段（行 104-110）；S3 优先级理由（行 644）
   - 严重程度：一般
   - 改进建议：在 §S3 证据段补充：「已验证 mat3Cast（type_quat_cast.cj:5-25）直接使用 q.x/q.y/q.z/q.w 计算矩阵元素，未对输入四元数做归一化处理——故 2 倍缩放分量经 mat3Cast 重建后矩阵元素必然偏离原矩阵」。优先级理由中"非身份 round-trip 虽可捕获但未针对性覆盖身份盲区"应改为"非身份 round-trip 可捕获（mat3Cast 不归一化已验证），身份 round-trip 仍存在捕获盲区"。

3. **问题描述**：G2.2 修复方向未明确"约束收紧后下游影响"。建议在阶段四前收紧约束至 `FloatingPoint<T>`，但未分析下游影响：现有调用方若存在整数类型实例化，收紧约束将导致编译失败，需同步识别并修改下游代码。
   - 所在位置：a_v6_diag_v1.md §G2.2 分析段（行 277-289）
   - 严重程度：一般
   - 改进建议：在 G2.2 修复方案中追加：「建议在收紧约束前先执行 `grep -rn "mix<.*Int64\|lerp<.*Int64" cjglm/` 扫描现有调用点，确认无整数类型实例化使用，避免收紧约束引入级联编译错误」。或建议在阶段四首次调用 `mix`/`lerp` 时添加编译期断言以识别整数实例化调用点。

## 迭代第 7 轮

1. **问题描述**：§G2.1 准确识别 `slerp(x, y, a, spin: Bool)` 签名与 OOD §3.11、D22、§11.5 三处承诺的 `k: Int64` 不一致，但修复建议未提供：(1) 现有调用点扫描手段（是否存在 `slerp(x, y, a, spin)` 调用方需要同步修改）；(2) `spin` → `k` 的语义对齐验证（GLM 原版 `k` 允许任意整数控制旋转圈数，`Bool` 仅 true/false，两者语义不完全等价）；(3) stub 状态（`throw Exception("stub")`）修复后的运行时验证用例（G5.6 已记录测试缺失）。执行者需自行评估迁移成本与测试补齐策略。
   - 所在位置：a_v7_diag_v1.md §G2.1 分析段（行 259-274）
   - 严重程度：一般
   - 改进建议：在 G2.1 修复建议中追加：(1) "执行 `grep -rn 'slerp.*spin\|slerp(.*,.*,.*,.*)' cjglm/` 扫描现有 4 参数调用点"；(2) "明确语义对齐：保留 GLM 原版 `phi = angle + k * pi<T>()` 公式（`k=0` 即 3 参数 lerp 等价，`k=1` 旋转一周，`k=-1` 反向旋转）"；(3) "修复签名后新增测试用例 `testSlerp4ArgsK0/K1/KMinus1` 验证 `k` 参数语义对齐 GLM"。

## 迭代第 8 轮

1. **问题描述**：§S2 归因分析段引用了不存在的文件 `src/detail/type_quat_cast_test.cj`，并将其测试名指向位于 `tests/` 目录下、不被 `cjpm test` 执行的 `tests/glm/detail/test_type_quat_cast.cj` 中的 `testQuatCastNonIdentityMat3RoundTrip` 等用例。但根据 §S2 自身证据段（全部 425 个用例均来自 `src/detail/*_test.cj`），当前 `cjpm test` 输出的 3 个 FAILED 用例唯一来源应是 `src/detail/type_quat_cast_s1_test.cj` 的 3 个 `@Test`（`testS1QuatCastScalingXBranch`、`testS1QuatCastScalingWBranch`、`testS1QuatCastNonUnitRoundTrip`），而非所引用的不存在文件与未执行文件。该错误归因直接影响 S1 bug 实际暴露面的定位、修复后「3 FAILED → PASS」预测的可信度，并导致与 §S1 修复验证后备方案第 4 项已正确引用 `type_quat_cast_s1_test.cj` 形成内部矛盾。
   - 所在位置：a_v8_diag_v1.md §S2「3 个 FAILED 用例的归因分析」段（约第 69 行）
   - 严重程度：严重
   - 改进建议：将 §S2 归因分析改为"`cjpm test` 输出的 3 个 FAILED 用例预期均来自 `cjglm/src/detail/type_quat_cast_s1_test.cj`（S1 独立修复验证文件）——该文件含 3 个 `@Test`：`testS1QuatCastScalingXBranch`（q=(0.8,0.1,0.1,0.1)，X 分支）、`testS1QuatCastScalingWBranch`（q=(0.1,0.1,0.1,0.8)，W 分支）、`testS1QuatCastNonUnitRoundTrip`（q=(0.2,0.3,0.4,0.8)，非身份 round-trip），每个 `@Test` 函数体内多个 `@Expect` 断言在 S1 bug 影响下均不成立（如 `testS1QuatCastScalingXBranch` 中 `@Expect(q1.y, 0.1)` 在 bug 下实际值约为 0.2）。归因核实手段：① 运行 `cjpm test --verbose` 确认 FAILED 用例名集中在 `testS1Quat*` 前缀；② 归因一致时 S1 修复后预期 3 FAILED → PASS。"

2. **问题描述**：§S2 证据段（行 60）描述"全部 425 个测试用例均来自 `src/detail/*_test.cj`（阶段一/二残留的 13 个测试文件）"，而修复优先级建议表后的特别说明（行 726）描述"`src/detail/*_test.cj` 14 个文件共 425 个测试用例仍正常执行"。两处"13 个"与"14 个"不一致，且未明确两个数字的语义区分，可能误导执行者对 cjpm 测试基础设施的统计认知。
   - 所在位置：a_v8_diag_v1.md §S2 行 60 vs 行 726
   - 严重程度：一般
   - 改进建议：明确"13 个"与"14 个"的语义：13 个指阶段一/二遗留测试文件（13 个 `_test.cj`，合计 422 个 `@Test`），14 个指上述 13 个 + 阶段三 S1 独立修复验证文件 `type_quat_cast_s1_test.cj`（含 3 个 `@Test`），合计 425 个。建议在 §S2 证据段明确表述"src/detail/ 下 14 个测试文件（含 13 个阶段一/二残留 + 1 个阶段三 S1 独立修复验证文件 `type_quat_cast_s1_test.cj`），共 425 个 `@Test`"。

3. **问题描述**：§S2 归因段"待执行者核实手段"②指出"单文件 `cjc` 编译 `test_type_quat_cast.cj`"——但该文件位于 `tests/` 目录下、不在 `src/detail/`，且其包声明为 `glm.detail`；此外此文件名与归因分析的 `src/detail/type_quat_cast_test.cj`（即问题 1 中不存在的文件）不一致，执行者按此命令执行时会找不到文件。
   - 所在位置：a_v8_diag_v1.md §S2 行 69「待执行者核实手段」②
   - 严重程度：轻微
   - 改进建议：将核实手段②改为"单文件 `cjc` 编译 `cjglm/src/detail/type_quat_cast_s1_test.cj`（含必要的 `import glm.unittest.*` 与 `glm.unittest.testmacro.*`）后独立运行，确认 3 个 `@Test` 函数因 S1 bug 全部 FAIL"。

4. **问题描述**：§S1 分析段描述"以 `Quat(0.2, 0.3, 0.4, 0.8)` round-trip 验证：返回约 (0.38, 0.57, 0.76, 0.84)，约为原 q 的 1.9 倍"。实际推导：v ≈ 1.6852；x ≈ 0.3798；y ≈ 0.5697；z ≈ 0.7596；w ≈ 0.8426。非 w 分量实测比例约 1.899（非精确 2.0），w 分量实测比例约 1.053，呈现浮点累积误差所致的不对称性，报告未明确标注"约"的精确度。
   - 所在位置：a_v8_diag_v1.md §S1 行 38
   - 严重程度：轻微
   - 改进建议：在 §S1 行 38 末尾追加："具体数值校核：`v = sqrt(2.84) ≈ 1.6852`（缺 *0.5 因子）；x/y/z 各分量约为 0.3798/0.5697/0.7596（实测比例 1.899）；w = 0.8426（实测比例 1.053）。浮点累积误差致非 w 分量实测比例非精确 2.0，但数学上确为 2.0 倍——`0.64/(sqrt(2.84))` 与正确路径 `0.64*0.5/sqrt(2.84)` 的比值精确为 2.0。"

5. **问题描述**：§G1.3 修复方向推荐段论据 ② 描述"GLM `quatCast` 内部同样依赖 `T > T` 比较"，但未限定 GLM `quatCast` 的具体行号范围（GLM `references/glm-1.0.3/glm/glm/gtc/quaternion.inl` 的 `>` 比较位于第 95-104 行），执行者查阅 GLM 源码时需自行定位。
   - 所在位置：a_v8_diag_v1.md §G1.3 行 190 论据 ②
   - 严重程度：轻微
   - 改进建议：在论据 ② 追加具体行号引用："GLM `references/glm-1.0.3/glm/glm/gtc/quaternion.inl:95-104` 使用 3 个 `if (fourX/Y/ZSSquaredMinus1 > fourBiggest...)` 串行比较"。

## 迭代第 9 轮

1. **问题描述**：§S2 证据段（`a_v9_diag_v1.md:54-69`）声称 "实测 `cjpm test` 输出：`Summary: TOTAL: 425, PASSED: 422, FAILED: 3`"，但未提供实际执行命令、时间戳、输出日志或文件路径引用。3 个 FAILED 用例归因到 `type_quat_cast_s1_test.cj` 的 3 个 `@Test` 函数也仅为逻辑推断，未明确标注假设前提。该事实是 §S1 修复验证后备方案第 4 项、§S3 捕获能力论证、§G5.6 测试覆盖结论等下游分析的共同基线。
   - 所在位置：`a_v9_diag_v1.md:54-69`（S2 证据段 + 归因分析）
   - 严重程度：严重
   - 改进建议：(1) 在 S2 证据段补注执行命令 `cjpm test 2>&1 | tee /tmp/cjpm_test_$(date +%Y%m%d_%H%M%S).log`，并附输出片段或日志路径；(2) 将"3 FAILED 用例归因"明确标注为"假设性归因（待 `cjpm test --verbose` 验证）"；(3) 补充反事实论证：若 3 FAILED 不全来自 `type_quat_cast_s1_test.cj`，S1 影响范围需扩展分析的可能性。

2. **问题描述**：G6.1 分析段文字表述 "实际仅测试 18 个用例覆盖 5 个 import 组中的 3 个（Quat: 1 个、cast: 2 个、trigonometric: 15 个）"，但同一报告的可执行矩阵表显示 6 行（trig / detail.Quat / detail.mat3Cast / detail.quatCast / detail.mat4Cast / 合并的 ext+gtc 长串），其中 4 行标记为"是"——即实际覆盖 4 个独立 import 组而非 3 个。文本将 cast 视为 1 个 import 组（部分覆盖），表格将 mat3Cast/quatCast 视为 2 个独立组（都已覆盖），导致"已覆盖组数"统计不一致。
   - 所在位置：`a_v9_diag_v1.md:617`（文本）与 `a_v9_diag_v1.md:619-630`（可执行矩阵表）
   - 严重程度：一般
   - 改进建议：推荐选项 A——统一采用表格口径，将"3 个"改为"4 个"，并显式列出未覆盖组（仅 detail.mat4Cast）；补充说明"cast 在 lib.cj 是 1 个 public import 语句但 OOD §2 列出 3 个独立 cast 函数（mat3Cast/mat4Cast/quatCast），故按函数粒度计算为 3 个 import 组，其中 2 个被覆盖、1 个（mat4Cast）未被覆盖"。选项 A/B 均需说明表格第 6 行（合并 ext+gtc 长串）的归类处理。

3. **问题描述**：§G1.3「修复方向推荐」论据 ② 引用 GLM `references/glm-1.0.3/glm/glm/gtc/quaternion.inl:95-104` 包含 3 个 `if` 串行比较，但经实际核查该范围仅覆盖后两个 `if`（行 95-99 Y 分支、行 100-104 Z 分支），遗漏首个 `if`（行 90-94 X 分支）。报告中"3 个 `if`"的陈述与"`:95-104`"的行号范围自相矛盾。
   - 所在位置：`a_v9_diag_v1.md:189`（G1.3 修复方向推荐 ② 段）
   - 严重程度：一般
   - 改进建议：将行号引用改为 `quaternion.inl:90-104`，覆盖全部 3 个 `if` 串行比较。

4. **问题描述**：G3.5 修复建议段（行 415-419）推荐 `git rm cjglm/src/fwd.cj.bak`（不带 `--cached` 标志）以从索引与工作区同时删除。此命令的适用前提是文件已被 git 跟踪，但报告仅引用 todo.md 的"`git status` 显示 `new file:`"，未独立验证 fwd.cj.bak 的当前 git 跟踪状态。若文件处于未跟踪状态，`git rm` 将报 "fatal: pathspec ... did not match any files" 错误。
   - 所在位置：`a_v9_diag_v1.md:415-419`
   - 严重程度：一般
   - 改进建议：在修复建议前增加前置验证步骤：(1) `git ls-files --error-unmatch cjglm/src/fwd.cj.bak` 确认文件已被跟踪；若返回 0 则执行 `git rm cjglm/src/fwd.cj.bak`；(2) 若返回非 0 则执行 `rm cjglm/src/fwd.cj.bak`；(3) 或统一推荐 `rm cjglm/src/fwd.cj.bak` + `git add -A` 同步索引（不依赖文件当前状态）。

5. **问题描述**：§G3.8 分析段（行 472-479）声称 "`cjpm build` 输出 17 条 `unused import` 警告"，该数字逻辑上自洽但报告未提供实际 `cjpm build` 输出作为证据。
   - 所在位置：`a_v9_diag_v1.md:472-479`
   - 严重程度：一般
   - 改进建议：补充 `cjpm build 2>&1 | grep -c 'unused import'` 的实际执行结果，或标注"逻辑推断（基于 lib.cj:12/14/16 import 在 lib.cj 文件内无引用事实），未经 `cjpm build` 实证"。

## 迭代第 10 轮

1. **问题描述**：需求响应不充分——requirement.md 指定的 `docs/02_ood_phase0.md` 缺失影响未分析
   - 所在位置：`a_v10_diag_v1.md:13`
   - 严重程度：严重
   - 改进建议：确认 `02_roadmap.md` 是否即为所指文档；若确认无 OOD phase 0 文档，应在报告中说明缺失对诊断结论置信度的影响

2. **问题描述**：证据链的实证基线声明与报告分析主体之间存在结构矛盾
   - 所在位置：全文档，尤其 §S2、概述段、优先级表、关键发现汇总
   - 严重程度：严重
   - 改进建议：在文档头部添加全局未实证声明；在优先级表 S2 行添加假设性前提标注；将基线声明提升为文档级前置声明

3. **问题描述**：S3 标题与核心结论不匹配（"无法捕获"夸大问题严重性，剩余价值论证不足）
   - 所在位置：§S3（行 114-136）
   - 严重程度：严重
   - 改进建议：标题改为"身份四元数 round-trip 存在捕获盲区"；重新评估 S3 在 S1 修复后的独立修复价值

4. **问题描述**：G4.2 分类与客观事实不符（不可达路径标注为"OOD 文档问题"）
   - 所在位置：§G4.2（行 520-526）
   - 严重程度：中等
   - 改进建议：重新分类为"其他类型（设计决策惰性）"或给出具体修改指向

5. **问题描述**：G2.2 替代方案推荐与同节引用的 OOD §1 告诫自相矛盾
   - 所在位置：§G2.2（行 303-327）
   - 严重程度：中等
   - 改进建议：明确 OOD §1 告诫是否适用本场景，在修复方向段先驳斥不适用性再给出推荐方案

6. **问题描述**：G6.1 与 G6.2 高度重叠但未交叉引用
   - 所在位置：§G6.1（行 632-656）、§G6.2（行 658-664）
   - 严重程度：中等
   - 改进建议：合并为单条问题下设两个子视角，或将 G6.2 降级为 G6.1 的说明段

7. **问题描述**：S2 方案 A 缺少完整的待重命名文件清单
   - 所在位置：§S2 替代方案对比表（行 102-108）
   - 严重程度：中等
   - 改进建议：提供完整的待重命名文件清单（按子目录分组）或给出可生成清单的命令

8. **问题描述**：G2.1 迁移成本评估的第 3 项（新增测试用例建议）与 S2 类型关系未交代
   - 所在位置：§G2.1（行 285-291）
   - 严重程度：中等
   - 改进建议：在第 3 项后添加 S2 依赖关系说明，或建议测试添加在 `src/` 目录下

## 迭代第 11 轮

1. **问题描述**：S1修复管线"可合并"与"应分开"指引矛盾
   - 所在位置：a_v11_diag_v1.md 行809-822
   - 严重程度：严重
   - 改进建议：统一为决策树：① 若维护者接受混合偏差的诊断难度→一次提交；② 若追求精确定位→分两次提交。必须二选一而非两存。

2. **问题描述**：S1管线中"不改变数值结果"与"产生数值差异"矛盾
   - 所在位置：a_v11_diag_v1.md 行811 vs 行816
   - 严重程度：严重
   - 改进建议：统一表述为"G2.3重构会改变浮点精度行为但不改变算法正确性，可能使精确比较的测试出现微小偏差"。

3. **问题描述**：S2后备方案2（复制测试文件）可行性未经验证
   - 所在位置：a_v11_diag_v1.md 行803-804
   - 严重程度：一般
   - 改进建议：在发布后备方案前验证至少一种编译可行性方案（符号链接、直接编译、确认无依赖）。如不可行，删除该推荐。

4. **问题描述**：G6.1/G6.2权重数据在分类汇总表、权重明细、优先级表之间不一致
   - 所在位置：a_v11_diag_v1.md 行710-711/724/794
   - 严重程度：一般
   - 改进建议：统一数据结构——若G6.2已合并则完全移除G6.2条目并调整G6.1权重；若未合并则删除"已合并"声明并恢复G6.2独立章节。

5. **问题描述**：分析结构依赖未实证数字的方法论循环
   - 所在位置：a_v11_diag_v1.md 全文多处（S2/S3/后备方案5）
   - 严重程度：一般
   - 改进建议：在报告开头新增假设依赖摘要框，集中列出所有依赖未实证数字的结论及有效条件。

6. **问题描述**：G2.3优先级High #5偏高，与同类G1.x Low建议不一致
   - 所在位置：a_v11_diag_v1.md 行782
   - 严重程度：一般
   - 改进建议：降级为Medium，理由改为"type_quat_cast.cj管线中的前置步骤"而非"项目级一致性"。

7. **问题描述**：G3.8优先级Medium-High #8a偏高，与同类G3.6/G3.5不一致
   - 所在位置：a_v11_diag_v1.md 行785
   - 严重程度：一般
   - 改进建议：降级为Medium或Medium-Low，与G3.6/G3.5对齐。

8. **问题描述**：未独立验证todo.md条目完整性（是否存在标称的S5/S6）
   - 所在位置：a_v11_diag_v1.md 行15
   - 严重程度：一般
    - 改进建议：逐项grep确认todo.md是否存在报告中未列出的条目，确保分类分析无遗漏。

## 迭代第 12 轮

1. **问题描述**：汇总表 OOD 行条目列表仍包含 G4.2(1)，其他类型行条目列表未包含 G4.2，与权重明细标注（G4.2=其他:1.0）及正文分类不一致
   - 所在位置：`a_v12_diag_v1.md` 第 714 行（OOD 行条目列表）、第 715 行（其他类型行条目列表）、第 728 行（权重明细）
   - 严重程度：一般
    - 改进建议：将汇总表 OOD 行条目列表中的 `G4.2(1)` 移除，添加至其他类型行条目列表中；同时将 OOD 行加权计数修正为 5.5

## 迭代第 13 轮

1. **问题描述**：§概述 计数说明段声称执行了 `grep -n '^\*\*S[0-9]'` 和 `grep -n '^\*\*G[0-9]\.[0-9]'` 并得到匹配结果，但 todo.md 实际使用 `### S` 和 `#### G` 格式，上述 grep 命令应退出码 1 零匹配。证据手段描述与事实不符。
   - 所在位置：a_v13_diag_v1.md 第 25 行
   - 严重程度：一般
   - 改进建议：将 grep 命令更正为正确的标题模式 `^### S[0-9]` 和 `^#### G[0-9]\.[0-9]`，或改为手动确认陈述，移除不准确的命令引用。
