# 诊断质询报告（v1）

## 质询结果

LOCATED

## 逐维度审查

### 1. 证据充分性

**[通过]** S1 quatCast 因子 2 bug 的代码证据（`type_quat_cast.cj:84-106` 各分支使用 `sqrtT(four?SquaredMinus1 + one)` 后直接 `matrixElement / v`，缺少 GLM 的 `* 0.5` 和 `0.25 / biggestVal` 分步）已通过实际代码核查确认。

**[通过]** S2 tests/ 不被 cjpm 发现的证据（`cjpm.toml:8 [test] src-dir = "tests"`、测试文件命名 `test_*.cj` vs cjpm 要求的 `*_test.cj`）已通过实际配置和文件列表核查确认。

**[通过]** S3 中 `mat3Cast`/`mat4Cast` 不归一化的核心论断已通过 `type_quat_cast.cj:5-49` 代码核查确认——函数体直接消费 `q.x`/`q.y`/`q.z`/`q.w` 计算矩阵元素，未调用 `length`/`normalize`。

**[通过]** S4 ULP stub 16 个 public func 声明已通过 `vector_relational.cj:199-251` 实际代码确认（Vec1×4 + Vec2×4 + Vec3×4 + Vec4×4）。

**[通过]** 全局实证基线声明已提升为文档级前置声明（行 1-3），所有依赖 `cjpm test` 数字的结论均标注了假设性前提。

**[通过]** 02_ood_phase0.md 缺失影响声明已明确：确认 `02_roadmap.md` 非 `02_ood_phase0.md` 的替代文档，并说明了缺失对诊断置信度的影响。

**[问题-轻微]** S4 证据段（行 181）等号测试覆盖行号范围标注为 `199-250`，实际 ULP stub 最后一个函数声明在行 251（`notEqual<T, Q>(x: Vec4, y: Vec4, ULPs: Vec4<Int64, Q>)`），正确范围应为 `199-251`。该差异不影响覆盖率结论（1/16 ≈ 6.25% 不变）。

### 2. 逻辑完整性

**[通过]** 从问题现象到根因的因果链完整：S1 从 round-trip 数值偏差追溯到 `mat4Cast` 缺少 `*0.5` 因子；S2 从 `cjpm test` 输出 425（待实证）追溯到测试命名约定与 cjpm 发现规则不匹配；S3 区分身份/非身份 round-trip 的捕获能力差异，逻辑自洽。

**[通过]** G2.2 的 OOD §1 告诫适用性分析已明确说明本场景是 OOD §1 告诫的例外，并给出了理由（stub 状态无生产调用方依赖整数实例化），消除了与同节引用的 OOD §1 告诫之间的自相矛盾。

**[通过]** G4.2 分类已按审查建议调整为「其他类型（设计决策惰性）」，并给出具体修改指向（方向 A：删除 OOD 中不可达的异常约定）。

**[通过]** type_quat_cast.cj 统一修复管线（S1→G2.3→G1.5→G1.6→G1.7 分步策略 + 合并提交风险提示）逻辑合理。

### 3. 覆盖完备性

**[通过]** 任务描述中 todo.md 全部 42 项（S1~S4 + G1~G6）均有分析，提供编号→章节映射表。

**[通过]** G6.1 和 G6.2 已合并为单条问题的两个子视角（视角 A = 测试覆盖缺口，视角 B = import 结构偏差），消除重叠。

**[通过]** G1.6 与 G2.3 在 `type_quat_cast.cj` 部分重叠处已添加显式交叉引用（行 284："本问题与 G2.3「项目级 sqrtT 一致性」在 `type_quat_cast.cj` 部分重叠，参见 G2.3 的修复方向分析"）。

**[通过]** S2 待重命名文件清单已按子目录分组完整提供（共 41 个文件跨越 tests/glm/detail/ 30 个、tests/glm/ext/ 5 个、tests/glm/gtc/ 2 个、tests/glm/ 4 个），并提供生成该清单的命令。

**[通过]** 分类汇总表已添加逐项权重分配明细（行 718-724），确保加权计数可复现。

## 质询要点

（无——仅含轻微问题，评定为 LOCATED）
