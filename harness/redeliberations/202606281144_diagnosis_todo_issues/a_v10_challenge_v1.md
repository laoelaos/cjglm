# 诊断质询报告（v10）

## 质询结果

LOCATED

## 逐维度审查

### 1. 证据充分性

**[通过]** S1 bug 的根因定位（quatCast 缺少 `*0.5` 因子）有充分的代码级证据支撑：`type_quat_cast.cj:84-106` 各分支 `v = sqrtT(four?SquaredMinus1 + one)` 缺少 GLM `quaternion.inl:106-107` 的 `*0.5` 分步，数值校核（q=(0.2,0.3,0.4,0.8) 经 mat3Cast → quatCast 得 ≈(0.38,0.57,0.76,0.84)）与代码行为一致，z 分量符号（+0.7596）经 `type_quat_cast.cj:105`（z = (m.c0.y - m.c1.x) / v）核算正确。

**[通过]** G6.1 修复后口径统一：实测 `test_lib.cj` 确含 18 个阶段三用例（trig×15 + Quat×1 + mat3Cast×1 + quatCast×1），mat4Cast 确实未被覆盖（grep 无匹配），矩阵表 6 行与文本"4 个覆盖 + mat4Cast 遗漏 + 合并长串遗漏"一致。

**[通过]** G1.3 行号修正验证：`quaternion.inl` 中 3 个 `if` 串行比较确在行 90-94（X）、95-99（Y）、100-104（Z），`:90-104` 引用正确。

**[通过]** S2 实证缺失已通过显著标注和反事实论证妥善处理——实证基线声明、"(待实证)"标注、"假设性归因"标题、反事实论证四重措辞确保读者不会误读为已验证事实。

**[通过]** G3.5 `fwd.cj.bak` 行号 `:331,334,337` 经核查确认包含三个被禁止的 `*pFQuat` 别名，`git ls-files --error-unmatch` 前置验证建议正确。

### 2. 逻辑完整性

**[通过]** 从问题现象到根因的因果链完整：S1（代码对比 GLM → 缺 *0.5 因子 → 非最大分量 2x 缩放）→ S2（cjpm.toml 配置 + 命名规则 → tests/ 目录 300+ @Test 静默跳过）→ S3（身份 round-trip 捕获盲区因非最大分量为 0，缩放后仍为 0；非身份 round-trip 因 mat3Cast 不归一化可捕获）→ 各 G 项均形成完整因果链。

**[通过]** S2 依赖关系声明清晰：明确 S2 阻塞 S3/S4/G5/G6 的 `cjpm test` 验证，S1 修复验证后备方案（5 项替代手段）已覆盖 S2 被阻塞时的应对策略。

**[通过]** 修复优先级逻辑自洽：S2 列为唯一 Critical（测试基础设施问题，影响所有下游验证），S1 为 High（最严重功能缺陷但有后备方案），各项依赖关系图中无循环依赖。

**[通过]** 反事实论证（S2 3 FAILED 不全来自 `type_quat_cast_s1_test.cj` 时的三种影响路径）覆盖了关键不确定性。

### 3. 覆盖完备性

**[通过]** 42 项 todo.md 条目（4 严重 + 38 一般）全部有对应分析章节，映射表（行 684-727）验证无遗漏。

**[通过]** requirement.md 要求的 4 类分类（真实存在/误报/OOD 文档问题/其他类型）已全部应用，多分类项采用 0.5 权重拆分确保合计 42.0。

**[通过]** 已识别 requirement.md 指定的 `docs/02_ood_phase0.md` 不存在，改用实际存在的 `docs/05_ood_phase3.md`。

**[通过]** 已处理 todo.md 标题标称计数与实际列表不符的差异（42 vs 48 项），归因为"文档编辑遗留"并附追溯说明。

**[通过]** 历次修订说明（v3~v10）完整记录了质询意见的响应状态，持续存在的问题（问题 1~5）均在新版中得到修正。

## 质询要点

无（LOCATED，无须补充质询要点）。
