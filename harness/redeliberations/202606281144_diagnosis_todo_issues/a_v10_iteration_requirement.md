根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

本轮（v9 v2）质量审查共发现 7 个质量问题，质询报告（b_v9_challenge_v2.md）已通过 LOCATED 验证，确认问题成立。质询 v2 撤回原 v1 的问题 1（S1 z 分量符号错误）——该问题的事实错误判定本身存在严重事实错误，原始报告 `a_v9_diag_v1.md:38` 的 z = +0.7596 数值正确无误（基于 `type_quat_cast.cj:21-22, 105` 重新核算 m.c0.y = +0.76, m.c1.x = -0.52, z = (0.76-(-0.52))/1.6852 = +0.7596）。问题 1 撤回后，原 v1 问题 2-8 重新编号为问题 1-7。其中问题 1、2（原 v1 问题 2、3）升为最高"必须修正"优先级，原 v1 问题 3（cjpm test 实证缺失）的下游依赖最广（§S1 修复验证后备方案第 4 项、§S3 捕获能力论证、§G5.6 测试覆盖结论等均依赖此事实）。

### 问题 1（高，必须修正）：G6.1 文本与可执行矩阵表内部矛盾
- **位置：** `a_v9_diag_v1.md:617` 与 `a_v9_diag_v1.md:619-630`（可执行矩阵表）
- **现状：** G6.1 分析段文字表述 "实际仅测试 18 个用例覆盖 5 个 import 组中的 3 个（Quat: 1 个、cast: 2 个、trigonometric: 15 个）"，但同一报告稍后提供的"可执行矩阵"表显示 6 行（trig / detail.Quat / detail.mat3Cast / detail.quatCast / detail.mat4Cast / 合并的 ext+gtc 长串），其中 4 行标记为"是"（trig 15、Quat 1、mat3Cast 1、quatCast 1），仅 detail.mat4Cast 与合并长串标记为"否"——即实际覆盖 **4 个独立 import 组**而非 3 个。文本将 cast 视为 1 个 import 组（部分覆盖），而表格将 mat3Cast/quatCast 视为 2 个独立组（都已覆盖），导致"已覆盖组数"统计不一致。
- **影响：** 影响执行者识别 test_lib.cj 测试覆盖率缺口数量与未覆盖 import 组的具体清单（mat4Cast 已发现未被测试但被文本遗漏）。
- **修复建议（推荐选项 A）：** 统一采用表格口径，将"3 个"改为"4 个"，并显式列出未覆盖组（仅 detail.mat4Cast）；补充说明"cast 在 lib.cj 是 1 个 public import 语句但 OOD §2 列出 3 个独立 cast 函数（mat3Cast/mat4Cast/quatCast），故按函数粒度计算为 3 个 import 组，其中 2 个被覆盖、1 个（mat4Cast）未被覆盖"。**补充说明：** 选项 A/B 均需明确处理表格第 6 行（合并 ext+gtc 长串）的归类——按"独立未覆盖行"保留，并入"未覆盖组"清单，或作为通配符导入组合行单独标注。

### 问题 2（高，必须修正，必须升为最高优先级）：cjpm test 输出数字与 3 FAILED 归因未提供实际执行证据
- **位置：** `a_v9_diag_v1.md:54-69`（S2 证据段 + 归因分析）
- **现状：** §S2 证据段（行 54-69）声称 "实测 `cjpm test` 输出：`Summary: TOTAL: 425, PASSED: 422, FAILED: 3`"，但未提供实际执行命令、时间戳、输出日志或文件路径引用。同样，§S2「3 个 FAILED 用例的归因分析」段将 3 个 FAILED 归因到 `type_quat_cast_s1_test.cj` 的 3 个 `@Test` 函数（`testS1QuatCastScalingXBranch`/`testS1QuatCastScalingWBranch`/`testS1QuatCastNonUnitRoundTrip`），使用"预期"一词承认这是逻辑推断，但行文整体以事实口吻呈现，未明确标注假设前提。修复优先级表、S1 修复验证后备方案、S3 证据链等下游多处依赖此事实。若实际 cjpm 输出并非 TOTAL=425/FAILED=3，或 3 FAILED 来自其他文件，则 S2 分析链、S3 捕获能力论证、type_quat_cast.cj 修复管线均需调整。todo.md 描述的 TOTAL 是 422，与 v9 报告的 425 存在差异（报告已说明 422 = PASSED 数，425 = TOTAL 含 3 FAILED），但若 cjpm test 实际输出与报告描述不同，则 S2 证据基础动摇。
- **影响：** 多章节推论依赖此事实；本问题在原 v1 问题 1 撤回后升为最高优先级"必须修正"，是诊断报告多处下游分析的唯一事实基线，缺乏实证将使 S2 证据链、归因分析、S3 论证链、S1 修复验证后备方案等所有依赖该数据的段落失去支撑。
- **修复建议：**
  1. 在 S2 证据段补注执行命令：`cjpm test 2>&1 | tee /tmp/cjpm_test_$(date +%Y%m%d_%H%M%S).log`，并在报告中附上输出片段或日志路径。
  2. 将"3 FAILED 用例归因"明确标注为"假设性归因（待 `cjpm test --verbose` 验证）"，避免误读为已验证结论。
  3. 补充反事实论证：若 3 FAILED 不全来自 `type_quat_cast_s1_test.cj`，S1 影响范围需要扩展分析的可能性。

### 问题 3（中，建议修正）：G1.3 GLM quatCast `if` 比较行号范围偏差
- **位置：** `a_v9_diag_v1.md:189`（G1.3 修复方向推荐 ② 段）
- **现状：** §G1.3「修复方向推荐」论据 ② 引用 "GLM `references/glm-1.0.3/glm/glm/gtc/quaternion.inl:95-104` 使用 3 个 `if (fourX/Y/ZSSquaredMinus1 > fourBiggest...)` 串行比较"，但经实际核查 GLM 源文件：3 个 `if` 串行比较实际分布在 **行 90-104**：
  - 行 90-94：第一个 `if (fourXSquaredMinus1 > fourBiggestSquaredMinus1)`（X 分支判定）
  - 行 95-99：第二个 `if (fourYSquaredMinus1 > fourBiggestSquaredMinus1)`（Y 分支判定）
  - 行 100-104：第三个 `if (fourZSquaredMinus1 > fourBiggestSquaredMinus1)`（Z 分支判定）
  
  报告引用的 `:95-104` 仅覆盖后两个 `if`，遗漏首个 `if`（行 90-94）。报告中"3 个 `if`"的陈述与"`:95-104`"的行号范围自相矛盾。
- **影响：** 行号偏差可能误导执行者定位 GLM 参考实现；不影响 G1.3 主结论。
- **修复建议：** 将行号引用改为 `quaternion.inl:90-104`，覆盖全部 3 个 `if` 串行比较。如需保留 `:95-104` 可改述为"3 个 `if` 中后两个（X 分支对应行 90-94，Y 分支对应 95-99，Z 分支对应 100-104）"。

### 问题 4（中，建议修正）：G3.5 fwd.cj.bak 的 git 跟踪状态未验证
- **位置：** `a_v9_diag_v1.md:415-419`
- **现状：** G3.5 修复建议段（行 415-419）推荐 `git rm cjglm/src/fwd.cj.bak`（不带 `--cached` 标志）以"从索引与工作区同时删除"。此命令的适用前提是文件已被 git 跟踪（在索引中存在）。报告仅引用 todo.md 的"`git status` 显示 `new file:`"，但未独立验证 fwd.cj.bak 的当前 git 跟踪状态。若文件当前处于未跟踪（untracked）状态，`git rm` 会报 "fatal: pathspec ... did not match any files" 错误，需先 `git add` 或改用普通 `rm`。
- **影响：** 修复命令可能直接执行失败。
- **修复建议：** 在修复建议前增加前置验证步骤：
  1. `git ls-files --error-unmatch cjglm/src/fwd.cj.bak` 确认文件已被跟踪；若返回 0（已跟踪），执行 `git rm cjglm/src/fwd.cj.bak`。
  2. 若返回非 0（未跟踪），执行 `rm cjglm/src/fwd.cj.bak`。
  3. 或统一推荐 `rm cjglm/src/fwd.cj.bak` + `git add -A` 同步索引（不依赖文件当前状态）。

### 问题 5（中，建议修正）：G3.8 "17 个 unused import 编译警告" 未经实际构建验证
- **位置：** `a_v9_diag_v1.md:472-479`
- **现状：** §G3.8 分析段（行 472-479）声称 "`cjpm build` 输出 17 条 `unused import` 警告：15 个 trigonometric + 1 个 glm.ext.* + 1 个 glm.gtc.* = 17"。该数字在逻辑上自洽（lib.cj 第 12/14/16 行的 import 在 lib.cj 内部均未被引用），但报告未提供实际 `cjpm build` 输出作为证据。v9 修订说明未涉及此项核验。
- **影响：** 推论合理但未经实证；不同 cjpm 版本对 unused import 的检测粒度可能不同。
- **修复建议：** 补充 `cjpm build 2>&1 | grep -c 'unused import'` 的实际执行结果，或标注"逻辑推断（基于 lib.cj:12/14/16 import 在 lib.cj 文件内无引用事实），未经 `cjpm build` 实证"。

### 问题 6（低，可选优化）：§S1 "1.899 比例" vs "2.0 倍" 比值基线易混淆
- **位置：** `a_v9_diag_v1.md:38`
- **现状：** §S1 行 38 末尾 v9 修订说明补充 "实测比例 1.899" 与 "数学上确为 2.0 倍" 两个比值，但两个比值的分母不同：1.899 = buggy 输出（0.3798）/ 原始 q.x（0.2）；2.0 = buggy 输出（0.3798）/ 正确输出（0.1899）。当前修订已通过数学等式说明 2.0 比值，但未明确标注 "实测 1.899 = buggy/qx_original" 的对比基线，读者可能混淆两个比值指代。
- **影响：** v9 修订已补充数学等式，但对比基线可更明确。
- **修复建议：** 将 "实测比例 1.899" 改为 "实测 buggy / 原 q.x = 0.3798/0.2 ≈ 1.899（基线为原 q.x）"，并明确 "数学比值 2.0 = buggy / 正确输出（基线为正确输出 0.1899）"。两个比值的分母差异是浮点累积误差的来源，标注清楚有助于执行者理解。

### 问题 7（低，可选优化，与问题 1 强相关）：G6.1 "5 个 import 组"指代粒度不一致
- **位置：** `a_v9_diag_v1.md:617`（文本）与 `a_v9_diag_v1.md:619-630`（表格）
- **现状：** G6.1 同时存在两种"5 个 import 组"的口径：
  - 文本口径：5 = trig + cast 合并 + ext + gtc + Quat（共 5 个组，3 个覆盖：Quat/cast合并/trig）。
  - 表格口径：6 行（trig + detail.Quat + detail.mat3Cast + detail.quatCast + detail.mat4Cast + 合并长串），其中 4 行标记"是"（trig/Quat/mat3Cast/quatCast）。
  
  两种口径将 cast 视为 1 个 vs 3 个 import 组，且 Quat 的归属也不同（文本未明确 Quat 是来自 lib.cj 还是 fwd.cj）。两种口径对应的"5"含义不同，但报告未明确说明采用哪种分组粒度。
- **影响：** 影响解读清晰度，不影响主结论；问题 1 修复后本问题自动消解。
- **修复建议：** 在 G6.1 段首明确分组粒度，例如："本节按 OOD §2 中的 import 项计 5 个 OOD 阶段三引入的 import 组（trig、cast 整体、Quat 别名），其中 cast 在 OOD §2 中虽为单条 import 语句但实质包含 mat3Cast/mat4Cast/quatCast 3 个独立转换函数"。或与问题 1 一并采用"选项 A"修复。

### 修复优先级
1. **关键修复（必须修正，blocking）：** 问题 2（cjpm test 输出实证缺失）—— 本问题升为最高优先级"必须修正"，是诊断报告多处下游分析的唯一事实基线。
2. **关键修复（必须修正）：** 问题 1（G6.1 文本/表矛盾）—— 影响执行者解读测试覆盖缺口；与问题 7 强相关，修复本问题可同步消解问题 7。
3. **次优修复（建议修正）：** 问题 3（GLM 行号范围 95-104 → 90-104）；问题 4（fwd.cj.bak git 状态前置检查）；问题 5（cjpm build 输出实证）。
4. **可选优化：** 问题 6（1.899 vs 2.0 比值基线明确标注）；问题 7（5 import 组指代粒度，问题 1 修复后自动消解）。

## 历史迭代回顾

历史迭代共 9 轮，本轮（v9 v2 审查）为第 10 轮迭代的输入。本轮 7 个问题中：
- 问题 1（G6.1 文本/表矛盾）= 迭代第 9 轮问题 2，**持续存在**，需重点修复
- 问题 2（cjpm test 实证缺失）= 迭代第 9 轮问题 1（原 v1 编号为 3，因 v1 问题 1 撤回后重新编号），**持续存在**，需重点修复
- 问题 3（GLM `if` 行号范围）= 迭代第 9 轮问题 3（注：v8 时行号引用为 `quaternion.inl:95-104`，v9 未修正反被核查发现实际应为 90-104），**持续存在**，需修复
- 问题 4（fwd.cj.bak git 跟踪状态）= 迭代第 9 轮问题 4，**持续存在**，需修复
- 问题 5（17 个 unused import 实证）= 迭代第 9 轮问题 5，**持续存在**，需修复
- 问题 6（1.899 vs 2.0 比值基线）= **本轮新发现**
- 问题 7（5 import 组指代粒度）= **本轮新发现**（与问题 1 强相关，问题 1 修复后自动消解）

- **已解决的问题（历史反馈中已修复，本轮不再提及）：**
  - 迭代第 1 轮的 5 个问题（计数偏差、G1.6 自相矛盾、deviations.md 使用不足、汇总表计数偏差、缺乏优先级排序）— v9 报告中均已妥善解决
  - 迭代第 2 轮的 4 个问题（S3 优先级理由矛盾、G3.5/G3.6 分类、Blocker 优先级矛盾、S1 修复后 S3 降级）— v9 报告中均已妥善解决
  - 迭代第 3 轮的 3 个问题（S3 round-trip 计数、S4 stub 计数、S3/S4 分类）— v9 报告中均已妥善解决
  - 迭代第 4 轮的 4 个问题（S3 区分身份/非身份、deviations.md 方向、S2 副作用评估、type_quat_cast.cj 联合修复）— v9 报告中均已妥善解决
  - 迭代第 5 轮的 3 个问题（S2 实证支撑、deviations.md 交叉验证深度、S2 依赖关系显式阐明）— v9 报告中均已妥善解决
  - 迭代第 6 轮的 3 个问题（G5.6 分类、S3 证据链完整、G2.2 下游影响）— v9 报告中均已妥善解决
  - 迭代第 7 轮的 1 个问题（G2.1 迁移成本三项诉求）— v9 报告中均已妥善解决
  - 迭代第 8 轮的 5 个问题（§S2 归因引用不存在文件、§S2 计数矛盾、S2 归因段核实手段②错误、S1 数值精度说明缺失、G1.3 GLM 行号引用）— 前 4 项已在 v9 修订说明（v9）中标记为已修正；第 5 项（GLM 行号）部分修正但仍存在偏差（持续到本轮）

- **持续存在的问题（重点解决）：**
  - 迭代第 9 轮问题 1 / 本轮问题 2（cjpm test 输出实证缺失）— **严重，必须修正**
  - 迭代第 9 轮问题 2 / 本轮问题 1（G6.1 文本/表 3 vs 4 矛盾）— **高，必须修正**
  - 迭代第 9 轮问题 3 / 本轮问题 3（GLM 行号引用偏差 95-104 vs 实际 90-104）— **中等**
  - 迭代第 9 轮问题 4 / 本轮问题 4（fwd.cj.bak git 跟踪状态前置检查缺失）— **中等**
  - 迭代第 9 轮问题 5 / 本轮问题 5（17 个 unused import 编译警告未经实证）— **中等**

- **新发现的问题（本轮 v9 v2 审查新识别）：**
  - 问题 6（§S1 "1.899 比例" vs "2.0 倍" 比值基线易混淆）— 低，可选优化
  - 问题 7（G6.1 "5 个 import 组"指代粒度不一致，与问题 1 强相关）— 低，可选优化

**优先级提示：** 持续存在的 5 个问题已在 v9 报告中部分触及（如 v9 修订说明涉及 GLM 行号引用），但未实质性完成修复。v10 必须重点解决这两个"必须修正"问题（问题 1 G6.1 矛盾、问题 2 cjpm 实证），建议同时完成 3 个"建议修正"问题，问题 6/7 作为可选优化。

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606281144_diagnosis_todo_issues\a_v9_diag_v1.md（836 行，少于 1000 行，无需 COPY_AND_EDIT 模式）

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606281144_diagnosis_todo_issues\requirement.md