根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

### P1（严重）— 矛盾声明："全部 stub 替换完成" vs. `quaternion_exponential.cj` 保留为 stub
**位置**：§1 概述（第 11 行）、§5 错误处理策略（第 557 行）、§2 包组织（第 107 行）
**描述**：文档声称"全部 stub 在本阶段替换"但 §2 明确标注 `quaternion_exponential.cj` 仍为 stub，且 `docs/02_roadmap.md` 第 219 行将"全部 stub 已替换"列为阶段四集成验证标准。
**改进建议**：若阶段四不包括 `quaternion_exponential.cj`，则删除"全部 stub 均已替换"的绝对表述，新增"本阶段不覆盖范围"章节；若包括，则在 §3 中新增该模块设计描述。

### P2（严重）— 错误处理策略与具体设计自相矛盾：acos 输入是否 clamp
**位置**：§5 错误处理策略（第 554 行）vs. §3.2 `ext/quaternion_common.cj` slerp 设计（第 340 行）
**描述**：§5 声明 acos 输入不做 clamp，但 slerp 设计显式执行 `clamp(dot(x, y), -T(1), T(1))` 后调用 acos。
**改进建议**：在 §5 acos 条目中增加例外说明，或在 §7 设计决策中新增策略例外条目。

### P3（严重）— `gtc/type_precision.cj` 未定义具体别名范围
**位置**：§3.3 `gtc/type_precision.cj`（第 422-429 行）
**描述**：仅描述概念未给出具体别名列表或预估数量，实现者无法判断完成标准。
**改进建议**：在 §3.3 中给出完整的别名清单，或引用 GLM 1.0.3 对应头文件的精确行号范围。

### P4（一般）— 引用未定义的 `Common<T>` 约束
**位置**：§3.1 `common.cj` 协作关系（第 226 行）
**描述**：`Common<T>` 在全文中无定义，也不是现有代码库中存在的类型约束。
**改进建议**：将 "`Common<T>` 约束" 改为 "`common.cj` 函数族" 或明确定义。

### P5（一般）— `T(0)/T(1)` 字面量写法与 §1.4 确立的 `T(Float64(n))` 约定不一致
**位置**：全文各处函数公式（§3.1 §3.2 §4 等，典型：第 237 行、第 268 行、第 555 行）
**描述**：§1.4 约定使用 `T(Float64(n))`，但函数公式中大量使用 `T(1)`、`T(0)`、`T(2)` 简写。
**改进建议**：统一替换为 `T(Float64(n))`，或在 §1.4 中增加约定声明说明 `T(n)` 为简写。

### P6（一般）— `mod` 函数约束策略与现有 stub 及 GLSL 行为不一致
**位置**：§3.1 `common.cj`（第 219 行）
**描述**：设计文档将 `mod` 约束设为 `FloatingPoint<T>`，但现有 stub 使用 `Integer<T>`，GLSL 同时支持浮点和整数。
**改进建议**：在 §7 设计决策中记录约束选择理由，或补充整数版本的 `mod` 重载。

### P7（一般）— `geometric.cj` 约束收紧未显式标记为向后不兼容变更
**位置**：§3.1 `geometric.cj` 约束策略（第 264 行）vs. 现有 stub 代码
**描述**：现有 stub 使用 `Number<T>`，设计文档改为 `FloatingPoint<T>`，属约束收紧但未显式记录。
**改进建议**：在 §7 中新增设计决策条目记录约束收紧理由。

### P8（轻微）— `floatBitsToInt/uintBitsToFloat` 等位运算函数的实现路径未确认
**位置**：§3.1 `common.cj`（第 220 行）、§7 D12（第 584 行）
**描述**：文档建议 "CFFI 或原生位操作 API"，但仓颉标准库已提供原生 `toBits(): UInt32` 和 `fromBits(bits: UInt32): Float32` 方法。
**改进建议**：引用 `Float32.toBits()`/`Float32.fromBits()` 原生 API，新增验证项确认可用性。

## 历史迭代回顾

### 已解决的问题
本轮无已解决问题——历史反馈的 7 个问题在当前诊断中全部复现。

### 持续存在的问题
- **P1（矛盾声明）**：第 1 轮已提出，本轮仍然存在，需重点解决
- **P2（acos clamp 矛盾）**：第 1 轮已提出，本轮仍然存在，需重点解决
- **P3（type_precision 别名范围）**：第 1 轮已提出，本轮仍然存在，需重点解决
- **P4（Common<T> 未定义）**：第 1 轮已提出，本轮仍然存在
- **P5（T(n) 写法不一致）**：第 1 轮已提出，本轮仍然存在
- **P6（mod 约束策略）**：第 1 轮已提出，本轮仍然存在
- **P7（geometric 约束收紧）**：第 1 轮已提出，本轮仍然存在

### 新发现的问题
- **P8（位运算函数实现路径）**：本轮新识别的问题

## 上一轮产出路径
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606291301_phase4_ood\a_v1_design_v1.md

## 用户需求
C:\Develop\Software\cjglm_wp\harness\redeliberations\202606291301_phase4_ood\requirement.md
