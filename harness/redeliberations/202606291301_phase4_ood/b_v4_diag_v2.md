# 阶段四 OOD 设计方案 质量审查报告

## 整体评价

经过前 3 轮迭代修正，设计方案在核心架构、模块划分、依赖关系、接口约束等方面已达到较高成熟度。但仍存在若干显著问题需要修复。

---

## P1（严重）— `modf`/`frexp`/`ldexp` 签名设计完全缺失

**所在位置**：§3.1 `common.cj` 职责（第85-88行）

**问题描述**：设计文档列出了 `modf`、`frexp`、`ldexp` 三个函数，但全文未给出任何一处在仓颉中的签名设计方案。C/GLM 1.0.3 中 `modf(x, &i)` 和 `frexp(x, &exp)` 均使用引用/指针参数输出第二个值（分别返回整数部分和指数），仓颉没有引用参数。现行阶段三 stub 也未包含这三个函数（`common.cj` 仅有 11 个 stub）。设计文档未讨论元组返回、封装类型或其他替代方案。

**影响**：编码阶段无法直接实现——实现者不清楚签名形态。

**改进建议**：在 §3.1 `common.cj` 中为 `modf`/`frexp` 补充仓颉签名设计。建议：
- `modf(x: T): (T, T)` 返回 `(fractional, integer)` 元组
- `frexp(x: T): (T, Int64)` 返回 `(mantissa, exponent)` 元组
- `ldexp(x: T, exp: Int64): T` 直接委托 `std.math.ldexp`（相对简单，但签名也需明确）

---

## P2（严重）— 奇异矩阵求逆行为在三处自相矛盾

**所在位置**：
- §3.1 `matrix.cj` 职责（第310行）— "返回零矩阵或全NaN"
- §4.3 核心场景：矩阵行列式与逆（第621行）— "返回零矩阵"
- §5 错误处理策略表（第679行）— "返回零矩阵或保留未定义值"

**问题描述**：三处对同一行为的描述不一致：（a）"零矩阵或全NaN"、（b）"零矩阵"、（c）"零矩阵或保留未定义值"。此外，GLM 1.0.3 的余子式展开在 `det=0` 时实际产生 NaN 填充矩阵（因 `1/0 → Inf`，然后 `Inf × cofactor` 中的零分量产生 `Inf × 0 = NaN`，经 IEEE 754 NaN 传播），而非"零矩阵"。

**影响**：编码阶段实现者将无法确定奇异矩阵的正确行为。

**改进建议**：统一为与 GLM 1.0.3 一致的行为描述。建议表述：
"奇异矩阵求逆的结果由 IEEE 754 浮点运算自然决定（`1/det → Inf`，`Inf × 0 → NaN`），最终可能产生 NaN 或 Inf 填充的矩阵。函数不抛出异常。调用方需自行通过行列式检查矩阵的奇异性。"

---

## P3（严重）— `mod` 约束的当前状态描述与代码事实不符

**所在位置**：§3.1 `common.cj` 约束策略（第239行）、D15（第717行）

**问题描述**：设计文档声称 `mod` 函数"当前约束为 `FloatingPoint<T>`"，但现有 `common.cj` 第12行的实际约束是 `Integer<T>`（`func mod<T>(a: T, b: T): T where T <: Integer<T>`）。设计文档错误描述了当前状态。此外，D15 中"编码阶段可根据实际需求补充整数版本的重载"也需对应修正。

**影响**：从 `Integer<T>` 改为 `FloatingPoint<T>` 是向后不兼容变更——现有使用整数类型调用 `mod` 的代码将在阶段四编译报错。该变更未被标注。

**改进建议**：
1. 修正 D15 的"当前约束"描述为 `Integer<T>`（而非 `FloatingPoint<T>`）
2. 在 D15 或 §1 中明确标注此变更为向后不兼容（与 D16 对 geometric.cj 的处理一致）
3. 评估现有代码是否在 `Integer<T>` 约束下调用了 `mod`，确认退回影响

---

## P4（一般）— `trigonometric.cj` 依赖图中遗漏 `scalar_constants.cj`

**所在位置**：§2 模块间依赖表（第171行）

**问题描述**：`trigonometric.cj` 的 `radians(deg)` 和 `degrees(rad)` 使用 `pi<T>()`（来自同包 `scalar_constants.cj`），但依赖表中仅列出 `std.math + qualifier`，缺少 `scalar_constants`。虽然同包内自动可见，但依赖图的不完整可能误导实现者的批处理顺序判断。

**改进建议**：在第171行补充 `scalar_constants（pi<T>() 函数）`。

---

## P5（一般）— `gtc/random.cj` 种子初始化竞态风险评估不足

**所在位置**：§3.3 `random.cj` 随机数引擎管理（第523-524行）

**问题描述**：设计文档认为"可能因并发得到相同种子值，但后续随机数序列的推进过程天然隔离"。若两个线程在相同毫秒时间戳内同时首次调用 `linearRand`/`gaussRand`，二者将获得相同种子，生成完全相同的随机数序列。这在图形渲染的多线程场景（如并行粒子发射器）中将产生视觉上可感知的模式重复。

**影响**：尽管与 GLM 1.0.3 的行为一致（GLM 也不提供种子控制），但仓颉实现有机会做得更好。

**改进建议**：在 D19 中补充种子生成策略的增强说明，例如使用 `(DateTime.now().toUnixMillisecond() ^ threadId)` 作为种子组合，以降低碰撞概率。如确定为已知风险且接受，在 D19 中明确标注"已知的种子碰撞风险，与 GLM 1.0.3 行为一致，当前设计接受此风险"。

---

## 补充维度一：需求响应充分度评估

**评估对象**：设计文档是否完整覆盖 `requirement.md` 各项要求。

### requirement.md 要求的五项产出要素

| 要求项 | 设计文档覆盖情况 | 状态 |
|--------|----------------|------|
| 架构视图 | §1.3 整体架构思路（第41-43行）：三层包结构体系（detail/ext/gtc），自由函数模式 | ✅ 已覆盖 |
| 模块划分 | §2 包组织（第82-163行）：完整列出各包文件、★标记、函数清单 | ✅ 已覆盖 |
| 核心类型与接口设计 | §3 核心抽象（第224-579行）：各模块的角色/职责/约束/协作关系/签名 | ✅ 已覆盖 |
| 关键数据流与控制流 | §4 关键行为契约（第584-669行）：6 个核心场景的输入/流程/约束 | ✅ 已覆盖 |
| 与已有阶段的集成方式 | §9 与已有阶段的集成方式（第820-845行）：阶段三反馈影响、阶段二/一反向兼容 | ✅ 已覆盖 |

### 总体评价

设计文档完整覆盖了 `requirement.md` 的全部五项输出要求。阶段四的范围和边界在 §1.5（不覆盖范围）和 §2（★标记）中有明确定义。与阶段一二三的集成方式在 §9 中有详细描述。**本维度未发现缺项。**

保留的观察（非问题）：
- "架构视图"以纯文本形式描述，缺少形式化架构图或分层模型图。当前文档详实度足以指导实现，但对新成员的知识传递效率不如图示。
- requirement.md 要求"充分理解阶段四的范围和边界"，设计文档有明确的 in-scope/out-of-scope 划分，但在 §3 的函数签名中未逐一标注哪些函数在 GLM 1.0.3 中的对应位置（仅部分模块标注了行号范围），实现者需自行交叉比对。

---

## 补充维度二：下游消费者接口定义评估

**评估视角**：从 API 消费者的角度判断接口签名和行为契约是否足以正确使用。

### 逐模块评估

| 模块 | 接口清晰度 | 行为契约 | 消费者风险 |
|------|-----------|---------|-----------|
| common.cj | ✅ 约束与签名明确 | ⚠️ `modf`/`frexp`/`ldexp` 签名未定（P1） | 高（P1 阻塞） |
| exponential.cj | ✅ 签名明确 | ✅ 边界行为（`inversesqrt(0)=+Inf`）已文档化 | 低 |
| trigonometric.cj | ✅ 签名明确 | ✅ 委托 std.math，行为由库定义 | 低 |
| geometric.cj | ✅ 签名与 Vec1/Vec2~4 差异明确 | ✅ 零向量行为了两个分支均已定义 | 中（Vec1 NaN vs Vec2~4 零向量的差异可能意外） |
| matrix.cj | ✅ 公式与约束明确 | ⚠️ 奇异矩阵行为三处不一致（P2） | 高（P2 阻塞） |
| ext/matrix_* | ✅ 签名系族完整 | ✅ 数学公式引用 GLM | 低 |
| ext/quaternion_* | ✅ 签名与退化条件明确 | ✅ slerp 退化、clamp 的职责分离明确 | 低 |
| gtc/packing.cj | ✅ 完整签名清单 | ✅ 实现路径（toBits/fromBits）明确 | 低 |
| gtc/noise.cj | ⚠️ 仅列出函数名 | ✅ 算法路径已描述 | 中（实现者需从 GLM 源码查参数类型） |
| gtc/random.cj | ✅ 签名明确 | ✅ 引擎管理/线程安全策略完整 | 中（种子碰撞风险，P5） |
| gtc/type_precision.cj | ✅ 完整别名清单 | ✅ 引用 GLM 行号 | 低 |
| gtc/ulp.cj | ✗ 泛型签名但无实现路径 | ✗ 未讨论仓颉中位操作能力的限制（见 P6） | **高（阻塞）** |
| gtc/round.cj | ⚠️ 签名明确但约束不明确 | ⚠️ `T <: FloatingPoint<T>`/`T <: Number<T>` 二选一未定 | 中 |

### 总体评价

多数模块的接口定义足以支持下游消费者。P1（modf/frexp/ldexp 签名缺失）和 P6（ulp.cj 实现路径缺失）是直接阻塞消费者使用的严重问题。P2（奇异矩阵行为不一致）使消费者无法预判异常输入的行为。其余模块的接口清晰度与行为契约充分性均可接受。

---

## 补充维度三：整体深度与完整性评估

### 各模块深度矩阵

| 模块 | 深度评价 | 判断依据 |
|------|---------|---------|
| common.cj | ✅ 充分 | 约束、实现路径、协作关系完整，**但 modf/frexp/ldexp 签名缺失（P1）** |
| exponential.cj | ✅ 充分 | 含边界行为（inversesqrt(0)）与实现细节 |
| trigonometric.cj | ✅ 充分 | 实现委托路径清晰 |
| geometric.cj | ✅ 充分 | Vec1 vs Vec2~4 零值行为差异化文档化 |
| matrix.cj | ✅ 充分 | 公式明确，**但奇异矩阵行为不一致（P2）** |
| ext/scalar_common.cj | ✅ 充分 | 委托 core common，简洁明确 |
| ext/vector_common.cj | ⚠️ 偏少 | 仅说明"向量级公共运算"，无具体函数范围说明 |
| ext/matrix_transform.cj | ✅ 充分 | 完整函数列表 + GLM 行号 |
| ext/matrix_projection.cj | ✅ 充分 | 完整函数列表 + GLM 行号 |
| ext/matrix_clip_space.cj | ✅ 充分 | 按系族分类的完整函数清单 |
| ext/quaternion_*.cj | ✅ 充分 | 含退化条件、公式 |
| gtc/matrix_transform.cj | ⚠️ 偏少 | 仅列系族名称，未给出每个系族内函数的完整签名 |
| gtc/matrix_inverse.cj | ✅ 充分 | 简洁明确 |
| gtc/matrix_access.cj | ✅ 充分 | 简洁明确 |
| gtc/packing.cj | ✅ 充分 | 完整签名清单（按八组分类） |
| gtc/noise.cj | ✅ 充分 | 算法路径、存储机制、工作量评估均覆盖 |
| gtc/random.cj | ✅ 充分 | 含 ThreadLocal 策略、竞态分析、备选方案 |
| gtc/type_precision.cj | ✅ 充分 | 完整别名清单（~100 个） |
| gtc/ulp.cj | ✗ **不足以指导编码** | 泛型签名但无仓颉可行实现路径（见 P6） |
| gtc/round.cj | ⚠️ 偏少 | 约束不明确，实现路径未指定 |

### 总体评价

设计文档的整体深度较好——21 个模块中 15 个达到"充分"级别、3 个"偏少"、1 个"不足以指导编码"（ulp.cj）。P1 和 P2 的影响面最广（阻塞编码 + 消费者使用）。P6（ulp.cj）是唯一完全不可编码的模块。

**是否可进入编码阶段？** 建议先解决 P1（modf/frexp/ldexp 签名）、P2（奇异矩阵行为统一）、P6（ulp.cj 实现方案）三个阻塞项，以及 P3（mod 约束事实错误）修正后，其余模块可直接进入编码。

---

## P6（严重）— `gtc/ulp.cj` 泛型函数在仓颉中缺少实现路径

**所在位置**：§3.3 `gtc/ulp.cj`（第557-566行）、§4（无对应场景）、§7（无对应设计决策）

**问题描述**：设计文档为 `gtc/ulp.cj` 的四个函数定义了 `T <: FloatingPoint<T>` 泛型约束的签名：
```
func next_float<T>(x: T): T where T <: FloatingPoint<T>
func prev_float<T>(x: T): T where T <: FloatingPoint<T>
func float_distance<T>(x: T, y: T): T where T <: FloatingPoint<T>
func ulp<T>(x: T): T where T <: FloatingPoint<T>
```
这些函数需要操作浮点数的位表示（如将尾数位加 1 以获得下一个可表示浮点数）。在 GLM 1.0.3 中，通过 C++ 的 `reinterpret_cast` 或 `memcpy` 进行浮点位模式与整数的互转实现。

仓颉标准库的 `FloatingPoint<T>` 接口仅提供 6 个静态方法（`getE`/`getInf`/`getPI`/`getMinDenormal`/`getMinNormal`/`getNaN`）和 3 个实例方法（`isInf`/`isNaN`/`isNormal`），**不提供位级操作能力**。`Float32.toBits(): UInt32` 和 `Float32.fromBits(bits: UInt32): Float32` 等位操作方法仅存在于具体类型 `Float32`/`Float64` 上，不在 `FloatingPoint<T>` 接口中。因此，以 `T <: FloatingPoint<T>` 泛型约束声明的 ULP 函数在仓颉中无法通过泛型代码实现。

**影响**：编码阶段无法按当前签名实现——实现者面临两个选择：要么放弃泛型改用具体类型重载（Float32/Float64 各一份），要么重新设计实现策略。该设计回退未被文档讨论。

**改进建议**：
1. 在 §3.3 `ulp.cj` 中补充实现路径分析：承认 `FloatingPoint<T>` 接口不提供位操作能力，提出替代方案
2. 方案 A（推荐）：改为具体类型重载——`next_float(x: Float32): Float32` + `next_float(x: Float64): Float64`，在内部使用 `toBits()`/`fromBits()` 操作位模式
3. 方案 B：尝试通过 `getInf()`/`getMinDenormal()`/`getMinNormal()` 和 `isInf()`/`isNaN()` 等接口方法组合实现有限版本，但此方案无法实现完整的 ULP 功能
4. 新增设计决策条目记录选择理由

---

## 总结

| 编号 | 严重程度 | 类别 | 摘要 |
|------|---------|------|------|
| P1 | 严重 | 关键遗漏 | `modf`/`frexp`/`ldexp` 无仓颉签名设计，编码阶段不可行动 |
| P2 | 严重 | 逻辑矛盾 | 奇异矩阵行为三处描述不一致，且偏离 GLM 实际行为 |
| P3 | 严重 | 事实错误 | `mod` 当前约束描述为 `FloatingPoint`，实际代码为 `Integer` |
| P4 | 一般 | 完整性 | `trigonometric.cj` 依赖图漏列 `scalar_constants` |
| P5 | 一般 | 深度不足 | `random.cj` 种子碰撞风险评估不充分 |
| P6 | 严重 | 深度不足/事实错误 | `ulp.cj` 泛型函数在 `FloatingPoint<T>` 接口下无位操作实现路径，当前签名在仓颉中不可编码 |

### 各补充维度结论

**【需求响应充分度】** ✅ 设计文档完整覆盖 `requirement.md` 的全部五项输出要素。阶段四范围边界清晰，集成方式明确。

**【下游消费者接口定义】** ⚠️ 多数模块接口可支持消费者使用，但 P1（modf/frexp/ldexp）和 P6（ulp.cj）导致对应模块完全不可用，P2 导致消费者无法预判异常行为。

**【整体深度与完整性】** ⚠️ 21 个模块中 15 个深度充分、3 个偏少、1 个不可编码（ulp.cj）。建议先解决 P1/P2/P6 三个阻塞项和 P3 事实错误后进入编码。

---

## 修订说明（v2）

| 质询意见 | 回应 |
|---------|------|
| **Gap 1 — 诊断维度"是否充分响应了用户需求"未覆盖** | 在「补充维度一：需求响应充分度评估」中逐项比对 requirement.md 五项要素，确认设计文档全覆盖。额外补充了两项保留观察 |
| **Gap 2 — 接口定义是否足以支持下游消费者未系统性评估** | 在「补充维度二：下游消费者接口定义评估」中按模块逐一评估接口清晰度、行为契约、消费者风险，给出分级评价。新增 P6 发现 ulp.cj 泛型实现路径缺失，该问题属"接口定义不足以支持下游消费者"维度的具体例证 |
| **Gap 3 — 整体深度和完整性评估碎片化** | 在「补充维度三：整体深度与完整性评估」中给出模块深度矩阵（21 模块逐一评定）、总体评价、编码就绪建议。补充了三个维度的定性结论 |
| **P1-P5 的个案发现的正确性** | 所有 P1-P5 保留不变。P6 为新发现，不属于对已有审查意见的修改，不与其他 P 项冲突 |
