# 测试报告（v8）

## 测试文件

`tests/glm/detail/test_vec_mat_mul.cj`

## 用例清单

| 类别 | 用例数 | 说明 |
|------|--------|------|
| 正常路径 | 9 | Vec2/3/4 × Mat{2,3,4}x{N} 各 1 个 Int64/Defaultp 用例 |
| 零向量边界 | 9 | 全零向量 × 各矩阵，预期结果为全零 |
| 负值边界 | 3 | 含负分量的向量 × 矩阵（Vec2/3/4 各挑一个代表性重载） |
| 不可变性 | 3 | 运算后原向量和矩阵数据不变 |
| Float64 类型 | 3 | Vec2/3/4 × 方阵使用 Float64 |
| 不同 Qualifier | 3 | 使用 PackedHighp 替代 Defaultp |
| **合计** | **30** | |

## 覆盖维度

- 正常路径：9/9 重载全覆盖 ✅
- 边界条件：零向量（9 个全零）、负值（3 个代表性）✅
- 状态交互：不可变性（3 个代表性）✅
- 类型变化：Float64（3 个方阵用例）✅
- Qualifier 变化：PackedHighp（3 个方阵用例）✅

## 测试规范遵循

- 基于行为契约编写，不测实现细节 ✅
- 每个重载至少一个正向用例 ✅
- 用例独立，不依赖执行顺序 ✅
- 不修改源码文件 ✅
- 与项目已有测试风格一致（`@Test`/`@Expect`，无多余 import）✅
