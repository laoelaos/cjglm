# 计划审查报告（v2 r1）

## 审查结果
REJECTED

## 发现

- **[严重]** T2（type_mat2x2.cj）与 T3/T4 之间的类型依赖未解决。任务规范要求 T2 实现跨类型引用（fromMat 6a/6b 共 17 个重载分别引用 Mat2x2/Mat2x3/Mat2x4/Mat3x2/Mat3x3/Mat3x4/Mat4x2/Mat4x3/Mat4x4；Mat2x2×Mat3x2 和 Mat2x2×Mat4x2 跨尺寸乘法返回 Mat3x2/Mat4x2），但这些目标类型在 T3/T4 之前不存在于包中。仓颉以包为最小编译单元，所有引用的类型必须在编译时存在于包内某文件中。计划未处理此依赖关系，按当前顺序实施将导致编译失败。

## 修改要求（仅 REJECTED 时）

1. **[严重]** 横跨类型的引用依赖必须解决。建议方向：
   a. 将 T3（6 个非方阵）和 T4（Mat3x3/Mat4x4 方阵）提前至 T2 之前，先创建所有矩阵类型的文件骨架（结构体定义 + length + 基础构造函数），再为 T2 实现完整的运算符/工厂函数；或
   b. 将 T2 的跨类型引用（fromMat 6a/6b、Mat2x2×Mat3x2、Mat2x2×Mat4x2）拆分到单独的 extend 块，延迟到 T3/T4 完成后追加在 separate 文件中；或
   c. 在当前 round 先创建全部 9 个矩阵类型的 stub 结构体定义文件，再逐个完善。
