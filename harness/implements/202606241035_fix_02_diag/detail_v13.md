# 详细设计（v13）

## 概述

在 `test_vec_mat_mul.cj` 中 ZeroVec 测试区域上方添加注释，记录诊断评估结论：保留全部 9 个 ZeroVec 测试。该任务仅涉及注释追加，无代码逻辑变更。

## 文件规划

| 文件路径 | 操作 | 职责 |
|---------|------|------|
| `tests/glm/detail/test_vec_mat_mul.cj` | 编辑 | 在 `// === Boundary: Zero Vector ===` 上方插入 ZeroVec 评估结论注释 |

## 类型定义

无新增类型。

## 错误处理

不涉及。

## 行为契约

### 注释位置
在 `// === Boundary: Zero Vector ===` 注释（当前第 93 行）上方插入多行块注释，与被注释代码之间留一空行。

### 注释内容
```
/**
 * ZeroVec 测试评估结论（T18）：
 * 保留全部 9 个 ZeroVec 测试，理由：
 * 1. 维护成本极低（与基础测试共享展开公式，仅输入向量值不同）
 * 2. 每个矩阵类型（2×2～4×4）均有独立验证价值
 * 3. 零向量×任意矩阵=零矩阵，该恒等式需逐矩阵类型验证
 */
```

### 注释格式
- 使用 `/** */` Javadoc 风格块注释
- 内容以中文书写（与现有注释语言一致）

## 依赖关系

- 源文件：`tests/glm/detail/test_vec_mat_mul.cj`
- 依赖诊断报告 §4 T18（02_diag.md `:520-537`）的评估结论
